import type { CatalogModule } from "../types";

const xoauth2: CatalogModule = {
	source: "XOAUTH2",
	extractionNote: "XOAUTH2 (Google vendor doc): pending Phase 3 extraction.",
	requirements: [],
};

export default xoauth2;
