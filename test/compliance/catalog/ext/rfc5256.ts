import type { CatalogModule } from "../types";

const rfc5256: CatalogModule = {
	source: "RFC5256",
	extractionNote: "RFC5256 (SORT/THREAD): pending Phase 5 extraction.",
	requirements: [],
};

export default rfc5256;
