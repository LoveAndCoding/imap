import type { CatalogModule } from "../types";

const rfc2221: CatalogModule = {
	source: "RFC2221",
	extractionNote: "RFC2221 (LOGIN-REFERRALS): pending Phase 6 extraction.",
	requirements: [],
};

export default rfc2221;
