/**
 * Placeholder for the full spec §5.5 typed registry. The open fallback
 * shape is stable; known-code variants are added incrementally as the
 * router/parser work (the task that owns §5.5) lands them.
 */
export type TypedResponseCode = { name: string; args: string | null };
