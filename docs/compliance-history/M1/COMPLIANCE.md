# IMAP Client Spec Compliance

Generated by the compliance suite reporter. Do not edit by hand.

## Summary

| Source | Profile | Level | Score | Pass | Violation | Unimplemented | Untested | Untestable |
|---|---|---|---|---|---|---|---|---|
| RFC3501 | rev1 | MUST | 62% | 34 | 0 | 20 | 0 | 6 |
| RFC3501 | rev1 | MUST NOT | 40% | 6 | 0 | 9 | 0 | 3 |
| RFC3501 | rev1 | SHOULD | 58% | 7 | 0 | 5 | 0 | 6 |
| RFC3501 | rev1 | SHOULD NOT | 33% | 1 | 0 | 2 | 0 | 3 |
| RFC3501 | rev1 | MAY | 50% | 4 | 0 | 4 | 0 | 1 |
| RFC2971 | rev1 | MUST NOT | 100% | 3 | 0 | 0 | 0 | 1 |
| RFC2971 | rev2 | MUST NOT | 100% | 3 | 0 | 0 | 0 | 1 |
| RFC9525 | rev1 | MUST | 100% | 5 | 0 | 0 | 0 | 11 |
| RFC9525 | rev1 | MUST NOT | 100% | 1 | 0 | 0 | 0 | 2 |
| RFC9525 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 4 |
| RFC9525 | rev1 | SHOULD NOT | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC9525 | rev1 | MAY | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC9525 | rev2 | MUST | 100% | 5 | 0 | 0 | 0 | 11 |
| RFC9525 | rev2 | MUST NOT | 100% | 1 | 0 | 0 | 0 | 2 |
| RFC9525 | rev2 | SHOULD | n/a | 0 | 0 | 0 | 0 | 4 |
| RFC9525 | rev2 | SHOULD NOT | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC9525 | rev2 | MAY | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC9051 | rev2 | MUST | 51% | 45 | 1 | 42 | 0 | 15 |
| RFC9051 | rev2 | MUST NOT | 33% | 6 | 0 | 12 | 0 | 4 |
| RFC9051 | rev2 | SHOULD | 44% | 8 | 1 | 9 | 0 | 12 |
| RFC9051 | rev2 | SHOULD NOT | 20% | 1 | 0 | 4 | 0 | 3 |
| RFC9051 | rev2 | MAY | 37% | 3 | 0 | 5 | 0 | 2 |
| RFC8314 | rev1 | MUST | 100% | 3 | 0 | 0 | 0 | 4 |
| RFC8314 | rev1 | MUST NOT | 100% | 2 | 0 | 0 | 0 | 8 |
| RFC8314 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 10 |
| RFC8314 | rev1 | SHOULD NOT | n/a | 0 | 0 | 0 | 0 | 2 |
| RFC8314 | rev1 | MAY | n/a | 0 | 0 | 0 | 0 | 3 |
| RFC8314 | rev2 | MUST | 100% | 3 | 0 | 0 | 0 | 4 |
| RFC8314 | rev2 | MUST NOT | 100% | 2 | 0 | 0 | 0 | 8 |
| RFC8314 | rev2 | SHOULD | n/a | 0 | 0 | 0 | 0 | 10 |
| RFC8314 | rev2 | SHOULD NOT | n/a | 0 | 0 | 0 | 0 | 2 |
| RFC8314 | rev2 | MAY | n/a | 0 | 0 | 0 | 0 | 3 |
| RFC2595 | rev1 | MUST | 100% | 6 | 0 | 0 | 0 | 3 |
| RFC2595 | rev1 | MUST NOT | 100% | 2 | 0 | 0 | 0 | 1 |
| RFC2595 | rev1 | SHOULD | 100% | 4 | 0 | 0 | 0 | 2 |
| RFC2595 | rev1 | MAY | n/a | 0 | 0 | 0 | 0 | 2 |
| RFC2595 | rev2 | MUST | 100% | 6 | 0 | 0 | 0 | 3 |
| RFC2595 | rev2 | MUST NOT | 100% | 2 | 0 | 0 | 0 | 1 |
| RFC2595 | rev2 | SHOULD | 100% | 4 | 0 | 0 | 0 | 2 |
| RFC2595 | rev2 | MAY | n/a | 0 | 0 | 0 | 0 | 2 |
| RFC7817 | rev1 | MUST | 100% | 2 | 0 | 0 | 0 | 4 |
| RFC7817 | rev1 | MUST NOT | 100% | 2 | 0 | 0 | 0 | 0 |
| RFC7817 | rev2 | MUST | 100% | 2 | 0 | 0 | 0 | 4 |
| RFC7817 | rev2 | MUST NOT | 100% | 2 | 0 | 0 | 0 | 0 |
| RFC4422 | rev1 | MUST | 62% | 5 | 0 | 3 | 0 | 2 |
| RFC4422 | rev1 | MUST NOT | 100% | 2 | 0 | 0 | 0 | 1 |
| RFC4422 | rev1 | SHOULD | 0% | 0 | 0 | 3 | 0 | 0 |
| RFC4422 | rev1 | SHOULD NOT | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC4422 | rev1 | MAY | 100% | 2 | 0 | 0 | 0 | 0 |
| RFC4422 | rev2 | MUST | 62% | 5 | 0 | 3 | 0 | 2 |
| RFC4422 | rev2 | MUST NOT | 100% | 2 | 0 | 0 | 0 | 1 |
| RFC4422 | rev2 | SHOULD | 0% | 0 | 0 | 3 | 0 | 0 |
| RFC4422 | rev2 | SHOULD NOT | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC4422 | rev2 | MAY | 100% | 2 | 0 | 0 | 0 | 0 |
| RFC4616 | rev1 | MUST | 100% | 2 | 0 | 0 | 0 | 0 |
| RFC4616 | rev1 | MUST NOT | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC4616 | rev1 | SHOULD | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC4616 | rev1 | SHOULD NOT | 100% | 1 | 0 | 0 | 0 | 1 |
| RFC4616 | rev1 | MAY | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC4616 | rev2 | MUST | 100% | 2 | 0 | 0 | 0 | 0 |
| RFC4616 | rev2 | MUST NOT | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC4616 | rev2 | SHOULD | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC4616 | rev2 | SHOULD NOT | 100% | 1 | 0 | 0 | 0 | 1 |
| RFC4616 | rev2 | MAY | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC2195 | rev1 | MUST | 100% | 4 | 0 | 0 | 0 | 0 |
| RFC2195 | rev2 | MUST | 100% | 4 | 0 | 0 | 0 | 0 |
| RFC7628 | rev1 | MUST | 100% | 6 | 0 | 0 | 0 | 0 |
| RFC7628 | rev2 | MUST | 100% | 6 | 0 | 0 | 0 | 0 |
| XOAUTH2 | rev1 | MUST | 100% | 3 | 0 | 0 | 0 | 2 |
| XOAUTH2 | rev1 | MAY | 100% | 1 | 0 | 0 | 0 | 0 |
| XOAUTH2 | rev2 | MUST | 100% | 3 | 0 | 0 | 0 | 2 |
| XOAUTH2 | rev2 | MAY | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC4959 | rev1 | MUST | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC4959 | rev1 | MUST NOT | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC4959 | rev1 | MAY | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC5161 | rev1 | MUST | 100% | 1 | 0 | 0 | 0 | 1 |
| RFC5161 | rev1 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5161 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC5161 | rev2 | MUST | 100% | 1 | 0 | 0 | 0 | 1 |
| RFC4978 | rev1 | MUST | 0% | 0 | 0 | 1 | 0 | 3 |
| RFC4978 | rev1 | MUST NOT | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC4978 | rev1 | MAY | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC4978 | rev2 | MUST | 0% | 0 | 0 | 1 | 0 | 3 |
| RFC4978 | rev2 | MUST NOT | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC4978 | rev2 | MAY | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC8437 | rev1 | MUST | 0% | 0 | 0 | 4 | 0 | 2 |
| RFC8437 | rev1 | SHOULD | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC8437 | rev1 | MAY | 0% | 0 | 0 | 3 | 0 | 0 |
| RFC8437 | rev2 | MUST | 0% | 0 | 0 | 4 | 0 | 2 |
| RFC8437 | rev2 | SHOULD | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC8437 | rev2 | MAY | 0% | 0 | 0 | 3 | 0 | 0 |
| RFC7888 | rev1 | MUST | 0% | 0 | 0 | 3 | 0 | 0 |
| RFC7888 | rev1 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC7888 | rev1 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC7888 | rev2 | MUST | 0% | 0 | 0 | 3 | 0 | 0 |
| RFC7888 | rev2 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC7888 | rev2 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC6855 | rev1 | MUST | 80% | 4 | 0 | 1 | 0 | 1 |
| RFC6855 | rev1 | MUST NOT | 66% | 2 | 0 | 1 | 0 | 0 |
| RFC6855 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 2 |
| RFC6855 | rev1 | MAY | 50% | 1 | 0 | 1 | 0 | 0 |
| RFC4315 | rev1 | MUST | 75% | 3 | 0 | 1 | 0 | 0 |
| RFC4315 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC4315 | rev1 | MAY | n/a | 0 | 0 | 0 | 0 | 2 |
| RFC4315 | rev2 | MUST | 75% | 3 | 0 | 1 | 0 | 0 |
| RFC4315 | rev2 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC4315 | rev2 | MAY | n/a | 0 | 0 | 0 | 0 | 2 |
| RFC6851 | rev1 | MUST | 20% | 1 | 0 | 4 | 0 | 1 |
| RFC6851 | rev1 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 1 |
| RFC6851 | rev1 | SHOULD | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC6851 | rev1 | MAY | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC6851 | rev2 | MUST | 33% | 1 | 0 | 2 | 0 | 1 |
| RFC6851 | rev2 | MAY | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC2342 | rev1 | MUST | 100% | 1 | 0 | 0 | 0 | 1 |
| RFC2342 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 2 |
| RFC2342 | rev1 | MAY | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC2342 | rev2 | MUST | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC2342 | rev2 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC2342 | rev2 | MAY | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC5258 | rev1 | MUST | 100% | 2 | 0 | 0 | 0 | 2 |
| RFC5258 | rev1 | MUST NOT | 100% | 2 | 0 | 0 | 0 | 0 |
| RFC5258 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 2 |
| RFC5258 | rev1 | SHOULD NOT | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC5258 | rev2 | MUST NOT | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC5819 | rev1 | MUST | 100% | 3 | 0 | 0 | 0 | 1 |
| RFC5819 | rev2 | MUST | 100% | 3 | 0 | 0 | 0 | 1 |
| RFC6154 | rev1 | MUST | 100% | 3 | 0 | 0 | 0 | 0 |
| RFC6154 | rev1 | MUST NOT | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC6154 | rev1 | MAY | 100% | 3 | 0 | 0 | 0 | 0 |
| RFC6154 | rev2 | MUST | 100% | 3 | 0 | 0 | 0 | 0 |
| RFC6154 | rev2 | MUST NOT | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC6154 | rev2 | MAY | 100% | 3 | 0 | 0 | 0 | 0 |
| RFC4314 | rev1 | MUST | 0% | 0 | 0 | 4 | 0 | 2 |
| RFC4314 | rev1 | MUST NOT | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC4314 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC4314 | rev2 | MUST | 0% | 0 | 0 | 4 | 0 | 2 |
| RFC4314 | rev2 | MUST NOT | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC4314 | rev2 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC9208 | rev1 | MUST | 50% | 2 | 0 | 2 | 0 | 0 |
| RFC9208 | rev1 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 4 |
| RFC9208 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC9208 | rev2 | MUST | 50% | 2 | 0 | 2 | 0 | 0 |
| RFC9208 | rev2 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 4 |
| RFC9208 | rev2 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC5464 | rev1 | MUST | 12% | 1 | 0 | 7 | 0 | 4 |
| RFC5464 | rev1 | MUST NOT | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC5464 | rev1 | MAY | 0% | 0 | 0 | 3 | 0 | 0 |
| RFC5464 | rev2 | MUST | 12% | 1 | 0 | 7 | 0 | 4 |
| RFC5464 | rev2 | MUST NOT | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC5464 | rev2 | MAY | 0% | 0 | 0 | 3 | 0 | 0 |
| RFC8514 | rev1 | MUST | 0% | 0 | 0 | 6 | 0 | 0 |
| RFC8514 | rev2 | MUST | 0% | 0 | 0 | 6 | 0 | 0 |
| RFC8474 | rev1 | MUST | 10% | 1 | 0 | 9 | 0 | 3 |
| RFC8474 | rev1 | SHOULD | 0% | 0 | 0 | 1 | 0 | 2 |
| RFC8474 | rev1 | MAY | 0% | 0 | 0 | 4 | 0 | 0 |
| RFC8474 | rev2 | MUST | 10% | 1 | 0 | 9 | 0 | 3 |
| RFC8474 | rev2 | SHOULD | 0% | 0 | 0 | 1 | 0 | 2 |
| RFC8474 | rev2 | MAY | 0% | 0 | 0 | 4 | 0 | 0 |
| RFC3502 | rev1 | MUST | 0% | 0 | 0 | 3 | 0 | 0 |
| RFC3502 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC3502 | rev1 | MAY | 0% | 0 | 0 | 3 | 0 | 0 |
| RFC3502 | rev2 | MUST | 0% | 0 | 0 | 3 | 0 | 0 |
| RFC3502 | rev2 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC3502 | rev2 | MAY | 0% | 0 | 0 | 3 | 0 | 0 |
| RFC4469 | rev1 | MUST | 33% | 2 | 0 | 4 | 0 | 3 |
| RFC4469 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC4469 | rev2 | MUST | 33% | 2 | 0 | 4 | 0 | 3 |
| RFC4469 | rev2 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC3516 | rev1 | MUST | 33% | 1 | 0 | 2 | 0 | 0 |
| RFC3516 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 2 |
| RFC3516 | rev1 | MAY | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC3516 | rev2 | MUST | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC3516 | rev2 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC8508 | rev1 | MUST | 0% | 0 | 0 | 4 | 0 | 0 |
| RFC8508 | rev1 | MUST NOT | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC8508 | rev1 | SHOULD | 0% | 0 | 0 | 1 | 0 | 1 |
| RFC8508 | rev1 | MAY | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC8508 | rev2 | MUST | 0% | 0 | 0 | 4 | 0 | 0 |
| RFC8508 | rev2 | MUST NOT | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC8508 | rev2 | SHOULD | 0% | 0 | 0 | 1 | 0 | 1 |
| RFC8508 | rev2 | MAY | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC7162 | rev1 | MUST | 83% | 15 | 0 | 3 | 0 | 7 |
| RFC7162 | rev1 | MUST NOT | 50% | 2 | 0 | 2 | 0 | 2 |
| RFC7162 | rev1 | SHOULD | 0% | 0 | 0 | 2 | 0 | 2 |
| RFC7162 | rev1 | MAY | 0% | 0 | 0 | 7 | 0 | 1 |
| RFC7162 | rev2 | MUST | 81% | 13 | 0 | 3 | 0 | 7 |
| RFC7162 | rev2 | MUST NOT | 50% | 2 | 0 | 2 | 0 | 2 |
| RFC7162 | rev2 | SHOULD | 0% | 0 | 0 | 2 | 0 | 2 |
| RFC7162 | rev2 | MAY | 0% | 0 | 0 | 7 | 0 | 1 |
| RFC5256 | rev1 | MUST | 18% | 3 | 0 | 13 | 0 | 2 |
| RFC5256 | rev1 | SHOULD | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5256 | rev2 | MUST | 18% | 3 | 0 | 13 | 0 | 2 |
| RFC5256 | rev2 | SHOULD | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5957 | rev1 | MUST | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5957 | rev1 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5957 | rev1 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5957 | rev2 | MUST | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5957 | rev2 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5957 | rev2 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC4731 | rev1 | MUST | 83% | 5 | 0 | 1 | 0 | 0 |
| RFC4731 | rev1 | MUST NOT | 50% | 1 | 0 | 1 | 0 | 0 |
| RFC4731 | rev1 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC4731 | rev2 | MUST | 66% | 2 | 0 | 1 | 0 | 0 |
| RFC4731 | rev2 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5267 | rev1 | MUST | 90% | 10 | 0 | 1 | 0 | 4 |
| RFC5267 | rev1 | MUST NOT | 0% | 0 | 0 | 5 | 0 | 0 |
| RFC5267 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC5267 | rev1 | MAY | 0% | 0 | 0 | 2 | 0 | 1 |
| RFC5267 | rev2 | MUST | 90% | 10 | 0 | 1 | 0 | 4 |
| RFC5267 | rev2 | MUST NOT | 0% | 0 | 0 | 5 | 0 | 0 |
| RFC5267 | rev2 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC5267 | rev2 | MAY | 0% | 0 | 0 | 2 | 0 | 1 |
| RFC5182 | rev1 | MUST | 33% | 1 | 0 | 2 | 0 | 0 |
| RFC5182 | rev1 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 1 |
| RFC5182 | rev1 | MAY | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC5182 | rev2 | MUST | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5182 | rev2 | MUST NOT | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC5182 | rev2 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC6203 | rev1 | MUST | 50% | 1 | 0 | 1 | 0 | 1 |
| RFC6203 | rev1 | MUST NOT | 0% | 0 | 0 | 6 | 0 | 1 |
| RFC6203 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC6203 | rev1 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC6203 | rev2 | MUST | 50% | 1 | 0 | 1 | 0 | 1 |
| RFC6203 | rev2 | MUST NOT | 0% | 0 | 0 | 6 | 0 | 1 |
| RFC6203 | rev2 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC6203 | rev2 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC9394 | rev1 | MUST | 50% | 2 | 0 | 2 | 0 | 2 |
| RFC9394 | rev1 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC9394 | rev1 | MAY | 0% | 0 | 0 | 2 | 0 | 1 |
| RFC9394 | rev2 | MUST | 50% | 2 | 0 | 2 | 0 | 2 |
| RFC9394 | rev2 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC9394 | rev2 | MAY | 0% | 0 | 0 | 2 | 0 | 1 |
| RFC2177 | rev1 | MUST | 33% | 1 | 0 | 2 | 0 | 0 |
| RFC2177 | rev1 | MUST NOT | 50% | 1 | 0 | 1 | 0 | 0 |
| RFC2177 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC2177 | rev1 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC2177 | rev2 | MUST | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC2177 | rev2 | MUST NOT | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC2177 | rev2 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5465 | rev1 | MUST | 73% | 11 | 0 | 4 | 0 | 1 |
| RFC5465 | rev1 | MUST NOT | 60% | 3 | 0 | 2 | 0 | 0 |
| RFC5465 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC5465 | rev1 | SHOULD NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5465 | rev1 | MAY | 33% | 1 | 0 | 2 | 0 | 1 |
| RFC5465 | rev2 | MUST | 73% | 11 | 0 | 4 | 0 | 1 |
| RFC5465 | rev2 | MUST NOT | 60% | 3 | 0 | 2 | 0 | 0 |
| RFC5465 | rev2 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC5465 | rev2 | SHOULD NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5465 | rev2 | MAY | 33% | 1 | 0 | 2 | 0 | 1 |
| RFC5466 | rev1 | MUST | 20% | 1 | 0 | 4 | 0 | 3 |
| RFC5466 | rev1 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5466 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 4 |
| RFC5466 | rev1 | MAY | n/a | 0 | 0 | 0 | 0 | 2 |
| RFC5466 | rev2 | MUST | 20% | 1 | 0 | 4 | 0 | 3 |
| RFC5466 | rev2 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5466 | rev2 | SHOULD | n/a | 0 | 0 | 0 | 0 | 4 |
| RFC5466 | rev2 | MAY | n/a | 0 | 0 | 0 | 0 | 2 |
| RFC5032 | rev1 | MUST | 0% | 0 | 0 | 1 | 0 | 1 |
| RFC5032 | rev1 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5032 | rev1 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC5032 | rev2 | MUST | 0% | 0 | 0 | 1 | 0 | 1 |
| RFC5032 | rev2 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5032 | rev2 | SHOULD | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC5255 | rev1 | MUST | 50% | 9 | 0 | 9 | 0 | 1 |
| RFC5255 | rev1 | SHOULD | 50% | 1 | 0 | 1 | 0 | 0 |
| RFC5255 | rev2 | MUST | 50% | 9 | 0 | 9 | 0 | 1 |
| RFC5255 | rev2 | SHOULD | 50% | 1 | 0 | 1 | 0 | 0 |
| RFC5259 | rev1 | MUST | 23% | 4 | 0 | 13 | 0 | 1 |
| RFC5259 | rev1 | MUST NOT | 0% | 0 | 0 | 2 | 0 | 1 |
| RFC5259 | rev1 | SHOULD | 0% | 0 | 0 | 1 | 0 | 3 |
| RFC5259 | rev1 | MAY | 33% | 1 | 0 | 2 | 0 | 0 |
| RFC5259 | rev2 | MUST | 23% | 4 | 0 | 13 | 0 | 1 |
| RFC5259 | rev2 | MUST NOT | 0% | 0 | 0 | 2 | 0 | 1 |
| RFC5259 | rev2 | SHOULD | 0% | 0 | 0 | 1 | 0 | 3 |
| RFC5259 | rev2 | MAY | 33% | 1 | 0 | 2 | 0 | 0 |
| RFC4467 | rev1 | MUST | 33% | 4 | 0 | 8 | 0 | 0 |
| RFC4467 | rev1 | MUST NOT | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC4467 | rev1 | MAY | 0% | 0 | 0 | 3 | 0 | 1 |
| RFC4467 | rev2 | MUST | 33% | 4 | 0 | 8 | 0 | 0 |
| RFC4467 | rev2 | MUST NOT | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC4467 | rev2 | MAY | 0% | 0 | 0 | 3 | 0 | 1 |
| RFC5524 | rev1 | MUST | 75% | 3 | 0 | 1 | 0 | 0 |
| RFC5524 | rev1 | MUST NOT | 100% | 2 | 0 | 0 | 0 | 0 |
| RFC5524 | rev1 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5524 | rev2 | MUST | 75% | 3 | 0 | 1 | 0 | 0 |
| RFC5524 | rev2 | MUST NOT | 100% | 2 | 0 | 0 | 0 | 0 |
| RFC5524 | rev2 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5802 | rev1 | MUST | 0% | 0 | 0 | 25 | 0 | 0 |
| RFC5802 | rev1 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5802 | rev1 | SHOULD | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC5802 | rev1 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5802 | rev2 | MUST | 0% | 0 | 0 | 25 | 0 | 0 |
| RFC5802 | rev2 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC5802 | rev2 | SHOULD | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC5802 | rev2 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC7677 | rev1 | MUST | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC7677 | rev2 | MUST | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC4505 | rev1 | MUST | 0% | 0 | 0 | 3 | 0 | 1 |
| RFC4505 | rev1 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 2 |
| RFC4505 | rev1 | SHOULD | 0% | 0 | 0 | 1 | 0 | 1 |
| RFC4505 | rev1 | SHOULD NOT | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC4505 | rev1 | MAY | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC4505 | rev2 | MUST | 0% | 0 | 0 | 3 | 0 | 1 |
| RFC4505 | rev2 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 2 |
| RFC4505 | rev2 | SHOULD | 0% | 0 | 0 | 1 | 0 | 1 |
| RFC4505 | rev2 | SHOULD NOT | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC4505 | rev2 | MAY | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC2221 | rev1 | MUST | 100% | 4 | 0 | 0 | 0 | 3 |
| RFC2221 | rev1 | MUST NOT | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC2221 | rev2 | MUST | 100% | 4 | 0 | 0 | 0 | 3 |
| RFC2221 | rev2 | MUST NOT | 100% | 1 | 0 | 0 | 0 | 0 |
| RFC2193 | rev1 | MUST | 77% | 7 | 0 | 2 | 0 | 1 |
| RFC2193 | rev1 | MAY | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC2193 | rev2 | MUST | 77% | 7 | 0 | 2 | 0 | 1 |
| RFC2193 | rev2 | MAY | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC3348 | rev1 | MUST | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC3348 | rev1 | MUST NOT | 100% | 1 | 0 | 0 | 0 | 1 |
| X-GM-EXT-1 | rev1 | MUST | 20% | 2 | 0 | 8 | 0 | 7 |
| X-GM-EXT-1 | rev1 | MAY | 0% | 0 | 0 | 5 | 0 | 2 |
| X-GM-EXT-1 | rev2 | MUST | 20% | 2 | 0 | 8 | 0 | 7 |
| X-GM-EXT-1 | rev2 | MAY | 0% | 0 | 0 | 5 | 0 | 2 |
| RFC7889 | rev1 | MUST | 25% | 1 | 0 | 3 | 0 | 0 |
| RFC7889 | rev1 | SHOULD | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC7889 | rev2 | MUST | 25% | 1 | 0 | 3 | 0 | 0 |
| RFC7889 | rev2 | SHOULD | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC8438 | rev1 | MUST | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC8438 | rev1 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC8438 | rev2 | MUST | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC8438 | rev2 | MAY | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC8440 | rev1 | MUST | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC8440 | rev1 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC8440 | rev1 | SHOULD | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC8440 | rev2 | MUST | 0% | 0 | 0 | 2 | 0 | 0 |
| RFC8440 | rev2 | MUST NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC8440 | rev2 | SHOULD | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC8970 | rev1 | MUST | 0% | 0 | 0 | 5 | 0 | 0 |
| RFC8970 | rev1 | SHOULD NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC8970 | rev2 | MUST | 0% | 0 | 0 | 5 | 0 | 0 |
| RFC8970 | rev2 | SHOULD NOT | 0% | 0 | 0 | 1 | 0 | 0 |
| RFC9585 | rev1 | MUST | 100% | 2 | 0 | 0 | 0 | 3 |
| RFC9585 | rev1 | MAY | n/a | 0 | 0 | 0 | 0 | 1 |
| RFC9585 | rev2 | MUST | 100% | 2 | 0 | 0 | 0 | 3 |
| RFC9585 | rev2 | MAY | n/a | 0 | 0 | 0 | 0 | 1 |

## Requirements

| Requirement | Level | Profile | Status | Detail |
|---|---|---|---|---|
| RFC3501-2.2-1 — Client sends CRLF-terminated lines; reads lines or octet-counted sequences | MUST | rev1 | pass |  |
| RFC3501-2.2.1-1 — Client prefixes each command with a distinct tag | MUST | rev1 | pass |  |
| RFC3501-2.2.1-2 — Client follows command syntax strictly | MUST | rev1 | pass |  |
| RFC3501-2.2.1-3 — Client completes a command (all continuations) before initiating a new one | MUST | rev1 | pass |  |
| RFC3501-2.2.2-2 — Client dispatches responses by first token (tag, *, or +) | MUST | rev1 | untestable | This describes internal client dispatch logic. The correctness of how the client routes a response to the right handler is not directly observable at the protocol layer from a black-box perspective; only downstream effects (e.g., incorrect command completion handling) are observable. |
| RFC3501-2.2.2-1 — Client accepts any server response at all times | MUST | rev1 | pass |  |
| RFC3501-2.2.2-3 — Client SHOULD record server data to avoid redundant requests | SHOULD | rev1 | untestable | Whether the client caches server data internally and avoids redundant re-fetch commands is an implementation quality matter. A black-box test could observe whether the client issues unnecessary commands, but the RFC permits such commands and does not make their absence a MUST, so there is no definitive pass/fail boundary at the protocol layer. |
| RFC3501-2.2.2-4 — Client MUST record certain categories of server data | MUST | rev1 | untestable | The RFC does not enumerate in §2.2.2 which specific server data items are subject to this MUST; those are identified in the Server Responses section. This entry captures the general obligation. Whether the client has correctly recorded a specific data item can sometimes be inferred from subsequent client behavior (e.g., it does not re-fetch when it should not need to), but the general requirement is not directly verifiable at the black-box protocol layer without exercising every applicable data type. |
| RFC3501-2.3.2-1 — Client cannot alter the \Recent flag | MUST NOT | rev1 | fail | unimplemented: This flag can not be altered by the client. |
| RFC3501-2.3.2-2 — Client cannot use \Recent as argument in STORE or APPEND | MUST NOT | rev1 | fail | unimplemented: \Recent can not be used as an argument in a STORE or APPEND command, and thus can not be changed at all. |
| RFC3501-3-1 — Client must not attempt a command while the connection is in an inappropriate state | MUST NOT | rev1 | fail | unimplemented: It is a protocol error for the client to attempt a command while the connection is in an inappropriate state, and the server will respond with a BAD or NO (depe… |
| RFC3501-3.1-1 — Client must supply authentication credentials in Not Authenticated state | MUST | rev1 | pass |  |
| RFC3501-3.2-1 — Client must select a mailbox before commands that affect messages will be permitted | MUST | rev1 | fail | unimplemented: In the authenticated state, the client is authenticated and MUST select a mailbox to access before commands that affect messages will be permitted. |
| RFC3501-3.4-1 — Client reads tagged OK before closing after LOGOUT | MUST | rev1 | pass |  |
| RFC3501-3.4-2 — Client SHOULD NOT unilaterally close the connection | SHOULD NOT | rev1 | pass |  |
| RFC3501-4.1-1 — Atom must consist of one or more non-special characters | MUST | rev1 | pass |  |
| RFC3501-4.2-1 — Number must consist of one or more digit characters | MUST | rev1 | fail | unimplemented: A number consists of one or more digit characters, and represents a numeric value. |
| RFC3501-4.3-1 — Client must wait for continuation request before sending literal octet data | MUST | rev1 | fail | unimplemented: In the case of literals transmitted from client to server, the client MUST wait to receive a command continuation request (described later in this document) bef… |
| RFC3501-4.3-2 — Client must wait for continuation request even for zero-octet literals | MUST | rev1 | fail | unimplemented: Even if the octet count is 0, a client transmitting a literal MUST wait to receive a command continuation request. |
| RFC3501-4.3-3 — Quoted string must contain only 7-bit characters excluding CR and LF | MUST | rev1 | pass |  |
| RFC3501-4.3.1-1 — Implementations MAY transmit 8-bit or multi-octet characters in literals but SHOULD only do so when CHARSET is identified | SHOULD | rev1 | fail | unimplemented: IMAP4rev1 implementations MAY transmit 8-bit or multi-octet characters in literals, but SHOULD do so only when the [CHARSET] is identified. |
| RFC3501-4.3.1-2 — Implementations must encode binary data into a textual form before transmitting | MUST | rev1 | fail | unimplemented: Implementations MUST encode binary data into a textual form, such as BASE64, before transmitting the data. |
| RFC3501-4.3.1-3 — String with excessive CTL characters MAY be treated as binary | MAY | rev1 | fail | unimplemented: A string with an excessive amount of CTL characters MAY also be considered to be binary. |
| RFC3501-5.1-1 — Client MUST NOT create 8-bit mailbox names | MUST NOT | rev1 | fail | unimplemented: Client implementations MUST NOT attempt to create 8-bit mailbox names, and SHOULD interpret any 8-bit mailbox names returned by LIST or LSUB as UTF-8. |
| RFC3501-5.1-2 — Client SHOULD interpret 8-bit mailbox names returned by LIST or LSUB as UTF-8 | SHOULD | rev1 | pass |  |
| RFC3501-5.1-3 — Client MUST interact with any server case-sensitivity model | MUST | rev1 | untestable | The sentence is a blanket obligation to interoperate with all three server case-sensitivity models (fully case-sensitive, case-insensitive with case-preservation, case-insensitive with case-coercion). There is no single observable protocol interaction that proves general compliance; a client can pass any particular test case yet still fail with an untested server variant. Full coverage would require testing against all three server behaviours, which is an integration property rather than a single black-box observable. |
| RFC3501-5.1.3-1 — Clients MUST NOT depend on server validation of modified UTF-7 mailbox names | MUST NOT | rev1 | untestable | 'Depending on the server' is an internal client design choice not directly observable at the protocol boundary. A client that silently relies on the server to reject invalid modified UTF-7 names is indistinguishable (at the wire level) from one that validates locally, unless the client sends an invalid name — at which point RFC3501-5.1.3-2 would be violated instead. |
| RFC3501-5.1.3-2 — Client SHOULD NOT create mailbox names with bare '&' unless modified UTF-7 compliant | SHOULD NOT | rev1 | fail | unimplemented: ...SHOULD NOT attempt to create a mailbox name with an embedded "&" character unless it complies with the modified UTF-7 syntax. |
| RFC3501-5.1.3-3 — Modified BASE64 MUST NOT encode printable US-ASCII that can represent itself | MUST NOT | rev1 | fail | unimplemented: Modified BASE64 MUST NOT be used to represent any printing US-ASCII character which can represent itself. |
| RFC3501-5.1.3-4 — International mailbox names MUST end in US-ASCII | MUST | rev1 | fail | unimplemented: However, all names start in US-ASCII, and MUST end in US-ASCII; that is, a name that ends with a non-ASCII ISO-10646 character MUST end with a "-"). |
| RFC3501-5.1.3-5 — No implicit shift from BASE64 to US-ASCII; null shifts are not permitted | MUST NOT | rev1 | fail | unimplemented: There is no implicit shift from BASE64 to US-ASCII, and null shifts ("-&" while in BASE64; note that "&-" while in US-ASCII means "&") are not permitted. |
| RFC3501-5.2-1 — Client MUST record mailbox size updates | MUST | rev1 | pass |  |
| RFC3501-5.2-2 — Client MUST NOT assume subsequent commands return mailbox size | MUST NOT | rev1 | pass |  |
| RFC3501-5.5-1 — Client MAY pipeline commands without waiting for completion | MAY | rev1 | pass |  |
| RFC3501-5.5-2 — Command continuation request and response MUST be negotiated before next command | MUST | rev1 | fail | unimplemented: However, any command continuation request responses and command continuations MUST be negotiated before any subsequent command is initiated. |
| RFC3501-5.5-3 — Client MUST NOT send multiple commands without waiting if an ambiguity would result | MUST NOT | rev1 | fail | unimplemented: Clients MUST NOT send multiple commands without waiting if an ambiguity would result. |
| RFC3501-5.5-4 — Client MUST wait for completion before sending sequence-number command after non-FETCH/STORE/SEARCH | MUST | rev1 | fail | unimplemented: Therefore, if the client sends any command other than FETCH, STORE, or SEARCH, it MUST wait for the completion result response before sending a command with mes… |
| RFC3501-5.5-5 — Client must wait for completion after a UID command before sending a sequence-number command | MUST | rev1 | fail | unimplemented: Note: UID FETCH, UID STORE, and UID SEARCH are different commands from FETCH, STORE, and SEARCH. If the client sends a UID command, it must wait for a completio… |
| RFC3501-6.1.1-1 — Client MUST implement STARTTLS, LOGINDISABLED, and AUTH=PLAIN capabilities | MUST | rev1 | pass |  |
| RFC3501-6.1.2-1 — NOOP usable as a periodic poll | MAY | rev1 | pass |  |
| RFC3501-6.2.1-1 — Client discards cached capabilities after STARTTLS | MUST | rev1 | pass |  |
| RFC3501-6.2.1-2 — Client re-issues CAPABILITY after STARTTLS | SHOULD | rev1 | pass |  |
| RFC3501-6.2.1-3 — Client MUST NOT send further commands until TLS negotiation is complete | MUST NOT | rev1 | pass |  |
| RFC3501-6.2.2-1 — Client MUST implement the AUTHENTICATE command | MUST | rev1 | pass |  |
| RFC3501-6.2.2-2 — Client cancels AUTHENTICATE exchange by sending a single '*' line | MUST | rev1 | pass |  |
| RFC3501-6.2.2-3 — Client SHOULD implement additional SASL mechanisms beyond PLAIN | SHOULD | rev1 | untestable | Which specific SASL mechanisms are supported is a deployment/configuration choice; black-box testing cannot determine whether the client has implemented 'additional' non-plaintext mechanisms or merely chosen not to advertise them in a given test environment. |
| RFC3501-6.2.2-4 — Client MUST re-issue CAPABILITY after AUTHENTICATE when security layer was negotiated | MUST | rev1 | pass |  |
| RFC3501-6.2.2-5 — Client MAY retry authentication after AUTHENTICATE NO response | MAY | rev1 | pass |  |
| RFC3501-6.2.3-1 — Client MUST NOT send LOGIN when LOGINDISABLED is advertised | MUST NOT | rev1 | pass |  |
| RFC3501-6.2.3-2 — Client SHOULD NOT use LOGIN except as a last resort | SHOULD NOT | rev1 | untestable | 'Last resort' is a policy judgment about the client's authentication preference ordering which cannot be observed in a single black-box exchange — a client that always offers LOGIN immediately might still be compliant if no other mechanisms were available, and a test harness cannot distinguish intentional last-resort use from indiscriminate use. |
| RFC3501-6.2.3-3 — Client implementations SHOULD have a means to disable automatic LOGIN | SHOULD | rev1 | untestable | This is a requirement about the client's configuration/UI surface ('have a means to disable'), not about observable wire behavior. A behavioral test could only exercise a public configuration affordance that actually exists (set the disable option, observe no LOGIN on the wire); the requirement instead asserts the EXISTENCE of such an affordance, which is an API-inventory fact a black-box protocol exchange cannot establish. |
| RFC3501-6.3.1-1 — Client SHOULD implement defaults for missing SELECT untagged data | SHOULD | rev1 | fail | unimplemented: Note that earlier versions of this protocol only required the FLAGS, EXISTS, and RECENT untagged data; consequently, client implementations SHOULD implement def… |
| RFC3501-6.3.1-2 — Client must track that a failed SELECT leaves no mailbox selected | MUST | rev1 | fail | unimplemented: The SELECT command automatically deselects any currently selected mailbox before attempting the new selection. Consequently, if a mailbox is selected and a SELE… |
| RFC3501-6.3.2-1 — Client must recognise [READ-ONLY] in tagged OK response to EXAMINE | MUST | rev1 | fail | unimplemented: The text of the tagged OK response to the EXAMINE command MUST begin with the "[READ-ONLY]" response code. |
| RFC3501-6.3.8-1 — Client SHOULD NOT use non-standard reference argument except at user request | SHOULD NOT | rev1 | untestable | 'At the explicit request of the user' is a behavioral/UI policy that cannot be observed over the wire. A black-box test cannot determine whether a reference argument was sent in response to a user request or autonomously by the client. |
| RFC3501-6.3.8-2 — Hierarchical browser MUST NOT assume server reference interpretation | MUST NOT | rev1 | untestable | 'Making assumptions' is an internal client implementation decision that is not directly observable on the wire. A test can verify that the client sends syntactically correct LIST commands but cannot observe whether it is making assumptions about how the server interprets the reference component. |
| RFC3501-6.3.9-1 — Client must treat LIST flags as more authoritative than LSUB flags when they differ | MUST | rev1 | fail | unimplemented: The returned untagged LSUB response MAY contain different mailbox flags from a LIST untagged response. If this should happen, the flags in the untagged LIST are… |
| RFC3501-6.3.10-1 — Client SHOULD NOT use STATUS on the currently selected mailbox | SHOULD NOT | rev1 | fail | unimplemented: Because the STATUS command can cause the mailbox to be opened internally, and because this information is available by other means on the selected mailbox, the … |
| RFC3501-6.3.10-2 — Client MUST NOT use STATUS as a check for new messages in the selected mailbox | MUST NOT | rev1 | fail | unimplemented: The STATUS command MUST NOT be used as a "check for new messages in the selected mailbox" operation (refer to sections 7, 7.3.1, and 7.3.2 for more information … |
| RFC3501-6.3.10-3 — Client SHOULD NOT expect reasonable performance from many consecutive STATUS commands | SHOULD NOT | rev1 | untestable | This binds the client's performance expectations, not its wire behavior. A client that issues many consecutive STATUS commands is not violating the sentence — only a client that *expects* reasonable performance from doing so is, and an expectation is an internal design assumption that is not observable at the protocol layer. |
| RFC3501-6.3.11-1 — APPEND literal argument SHOULD be in RFC-2822 message format | SHOULD | rev1 | fail | unimplemented: The APPEND command appends the literal argument as a new message to the end of the specified destination mailbox. This argument SHOULD be in the format of an [R… |
| RFC3501-6.3.11-2 — Client MAY issue NOOP (or CHECK) after APPEND if server sends no untagged EXISTS | MAY | rev1 | fail | unimplemented: Specifically, the server SHOULD notify the client immediately via an untagged EXISTS response. If the server does not do so, the client MAY issue a NOOP command… |
| RFC3501-6.4.1-1 — Client SHOULD use NOOP, not CHECK, for new message polling | SHOULD | rev1 | fail | unimplemented: There is no guarantee that an EXISTS untagged response will happen as a result of CHECK. NOOP, not CHECK, SHOULD be used for new message polling. |
| RFC3501-6.4.2-1 — Client MAY issue SELECT, EXAMINE, or LOGOUT without a prior CLOSE | MAY | rev1 | fail | unimplemented: Even if a mailbox is selected, a SELECT, EXAMINE, or LOGOUT command MAY be issued without previously issuing a CLOSE command. |
| RFC3501-6.4.4-1 — CHARSET specification is the word CHARSET followed by a registered charset | MUST | rev1 | fail | unimplemented: The OPTIONAL [CHARSET] specification consists of the word "CHARSET" followed by a registered [CHARSET]. It indicates the [CHARSET] of the strings that appear in… |
| RFC3501-6.4.4-2 — Client must treat tagged NO (not BAD) as the unsupported-CHARSET outcome | MUST | rev1 | fail | unimplemented: If the server does not support the specified [CHARSET], it MUST return a tagged NO response (not a BAD). |
| RFC3501-6.4.5-1 — BODY[<section>] implicitly sets \Seen; BODY.PEEK is the non-setting alternative | MUST | rev1 | fail | unimplemented: The \Seen flag is implicitly set; if this causes the flags to change, they SHOULD be included as part of the FETCH responses. ... An alternate form of BODY[<sec… |
| RFC3501-6.4.5-2 — FETCH macros (ALL/FAST/FULL) must be used by themselves | MUST | rev1 | fail | unimplemented: A macro must be used by itself, and not in conjunction with other macros or data items. |
| RFC3501-6.4.5-6 — MIME part specifier MUST be prefixed by numeric part specifiers | MUST | rev1 | fail | unimplemented: The MIME part specifier MUST be prefixed by one or more numeric part specifiers. |
| RFC3501-6.4.6-1 — Untagged FETCH may arrive for external flag changes even with .SILENT | SHOULD | rev1 | fail | unimplemented: Regardless of whether or not the ".SILENT" suffix was used, the server SHOULD send an untagged FETCH response if a change to a message's flags from an external … |
| RFC3501-6.4.8-5 — Number after '*' in untagged FETCH is a sequence number, even for UID commands | MUST | rev1 | fail | unimplemented: The number after the "*" in an untagged FETCH response is always a message sequence number, not a unique identifier, even for a UID command response. |
| RFC3501-6.4.8-6 — FETCH responses caused by UID commands implicitly include the UID data item | MUST | rev1 | fail | unimplemented: server implementations MUST implicitly include the UID message data item as part of any FETCH response caused by a UID command, regardless of whether a UID was … |
| RFC3501-6.5.1-1 — Non-standard commands MUST use the X prefix | MUST | rev1 | pass |  |
| RFC3501-7-1 — Client must be prepared to accept any server response at all times | MUST | rev1 | pass |  |
| RFC3501-7-2 — Client must record certain critical server data when received | MUST | rev1 | pass |  |
| RFC3501-7-3 — Client should record other server data for later reference | SHOULD | rev1 | untestable | Whether a client retains or discards server data for later reference is an internal state-management decision not directly observable via the wire protocol in a black-box test. |
| RFC3501-7.1-1 — Client must present ALERT response-code text to the user | MUST | rev1 | pass |  |
| RFC3501-7.1-2 — Client interprets PERMANENTFLAGS to know which flags it can set permanently | MUST | rev1 | fail | unimplemented: Followed by a parenthesized list of flags, indicates which of the known flags the client can change permanently. Any flags that are in the FLAGS untagged respon… |
| RFC3501-7.1-3 — Client may retry APPEND/COPY with CREATE after TRYCREATE hint | MAY | rev1 | fail | unimplemented: An APPEND or COPY attempt is failing because the target mailbox does not exist (as opposed to some other reason). This is a hint to the client that the operatio… |
| RFC3501-7.1-4 — Client should ignore unrecognized response codes | SHOULD | rev1 | pass |  |
| RFC3501-7.1.1-1 — Client accepts the untagged OK greeting | MUST | rev1 | pass |  |
| RFC3501-7.1.1-2 — Client may present OK human-readable text to user as information | MAY | rev1 | untestable | Whether a client surfaces OK human-readable text to the user is an internal UI decision not observable via the wire protocol in a black-box test. Unlike the ALERT duty (RFC3501-7.1-1), this stays untestable even with logger-capture observability: the level is MAY with no constraining envelope, so surfacing and not surfacing are both compliant — no observation can distinguish a violation. |
| RFC3501-7.1.4-1 — Client treats PREAUTH greeting as already authenticated | MUST | rev1 | pass |  |
| RFC3501-7.1.5-1 — Client recognizes BYE greeting as connection rejection | MUST | rev1 | pass |  |
| RFC3501-7.1.5-2 — Client should continue reading responses after BYE until connection closes | SHOULD | rev1 | pass |  |
| RFC3501-7.2.1-1 — Client must not issue LOGIN when LOGINDISABLED capability is advertised | MUST NOT | rev1 | pass |  |
| RFC3501-7.2.1-2 — Client should not require capabilities beyond IMAP4rev1 and must ignore unknown ones | MUST | rev1 | pass |  |
| RFC3501-7.2.6-1 — Client must record the update from the FLAGS response | MUST | rev1 | pass |  |
| RFC3501-7.3.1-1 — Client must record the update from the EXISTS response | MUST | rev1 | pass |  |
| RFC3501-7.3.2-1 — Client must record the update from the RECENT response | MUST | rev1 | pass |  |
| RFC3501-7.4.1-1 — Client must record the update from the EXPUNGE response | MUST | rev1 | pass |  |
| RFC3501-7.4.2-1 — Client should interpret BODY section string per content transfer encoding, type, and subtype | SHOULD | rev1 | untestable | How a client internally interprets a BODY section string (decoding, content-type handling) is not directly observable through the IMAP wire protocol in a black-box test. Nor does the library's public API boundary help: interpretation per content transfer encoding/type/subtype is a rendering-layer duty that the library legitimately delegates to the consuming application by handing over the section data as received, so no API-output observation has a mandated pass/fail boundary at this layer. |
| RFC3501-7.4.2-2 — Client must decode transfer-encoded binary data to derive original binary | MUST | rev1 | untestable | Whether the client correctly decodes transfer-encoded binary data internally is not directly observable via the IMAP protocol in a black-box test; it is a processing obligation on received data. The duty binds whichever component derives the original binary; a protocol library that hands the raw transfer-encoded string to the consuming application (which then decodes) is compliant, so an API-output assertion (decoded vs raw) would encode an API design choice rather than the RFC duty. |
| RFC3501-7.4.2-3 — Client doing BODYSTRUCTURE fetch must be prepared to accept unknown extension data | MUST | rev1 | pass |  |
| RFC3501-7.4.2-4 — Client should treat NIL and empty string as identical in ENVELOPE members | SHOULD | rev1 | pass |  |
| RFC3501-7.5-1 — Client must not send literal octets until server signals readiness | MUST NOT | rev1 | fail | unimplemented: The client is not permitted to send the octets of the literal unless the server indicates that it is expected. This permits the server to process commands and r… |
| RFC3501-9-1 — ABNF alternative-rule priority: earlier rule takes priority | MUST | rev1 | pass |  |
| RFC3501-9-2 — Case-insensitivity of alphabetic token strings | MUST | rev1 | pass |  |
| RFC3501-9-3 — SP is exactly one space; TAB and LWSP substitution prohibited | MUST NOT | rev1 | pass |  |
| RFC3501-9-4 — ASCII NUL character (%x00) must never be used | MUST NOT | rev1 | pass |  |
| RFC3501-9-5 — Client MUST accept flag-extension flags | MUST | rev1 | pass |  |
| RFC3501-9-6 — All case variants of INBOX MUST be interpreted as INBOX | MUST | rev1 | pass |  |
| RFC3501-11.1-1 — Client MUST implement TLS_RSA_WITH_RC4_128_MD5 cipher suite | MUST | rev1 | untestable | RC4 suites are removed from Node's OpenSSL (and prohibited by RFC 7465); 3DES suites are disabled at OpenSSL security level 2 in Node 20 (RFC 8996); the mandated suites cannot be negotiated by any modern stack, so the requirement is unverifiable black-box (and obsolete in practice). |
| RFC3501-11.1-2 — Client SHOULD implement TLS_DHE_DSS_WITH_3DES_EDE_CBC_SHA cipher suite | SHOULD | rev1 | untestable | RC4 suites are removed from Node's OpenSSL (and prohibited by RFC 7465); 3DES suites are disabled at OpenSSL security level 2 in Node 20 (RFC 8996); the mandated suites cannot be negotiated by any modern stack, so the requirement is unverifiable black-box (and obsolete in practice). |
| RFC3501-11.1-3 — Client MUST verify server hostname against server certificate during TLS | MUST | rev1 | pass |  |
| RFC3501-11.1-4 — On hostname/certificate mismatch, client SHOULD ask user or terminate | SHOULD | rev1 | pass |  |
| RFC3501-11.1-5 — Client MUST use original server hostname (not insecure DNS) for certificate comparison | MUST | rev1 | untestable | Whether the client internally uses an insecure DNS-derived hostname for comparison cannot be observed from the outside via black-box testing of the IMAP protocol exchange; it requires white-box inspection of the TLS implementation. |
| RFC3501-11.1-6 — Client MUST NOT use server hostname from insecure remote source for certificate comparison | MUST NOT | rev1 | untestable | The source of the hostname used internally by the client for certificate verification is not observable via black-box IMAP protocol testing. |
| RFC3501-11.1-7 — Client SHOULD use subjectAltName dNSName as source of server identity when present | SHOULD | rev1 | pass |  |
| RFC3501-11.1-8 — Client MUST check result of STARTTLS command and TLS negotiation for acceptable security | MUST | rev1 | pass |  |
| RFC3501-11.1-9 — Certificate matching: case-insensitive, wildcard MAY, any-of-multiple-names acceptable | MAY | rev1 | pass |  |
| RFC2971-3.3-1 — At most 30 field-value pairs in ID | MUST NOT | rev1 | pass |  |
| RFC2971-3.3-1 — At most 30 field-value pairs in ID | MUST NOT | rev2 | pass |  |
| RFC2971-3.3-2 — ID field/value length limits | MUST NOT | rev1 | pass |  |
| RFC2971-3.3-2 — ID field/value length limits | MUST NOT | rev2 | pass |  |
| RFC2971-3.3-3 — No duplicate field names in ID | MUST NOT | rev1 | pass |  |
| RFC2971-3.3-3 — No duplicate field names in ID | MUST NOT | rev2 | pass |  |
| RFC2971-3.3-4 — MUST NOT use contact information for automatic bug reports | MUST NOT | rev1 | untestable | This requirement constrains out-of-band client behavior (whether the client software silently submits bug reports using contact details from the server ID response). No protocol-layer observation can confirm or deny compliance; it requires code audit or vendor attestation. |
| RFC2971-3.3-4 — MUST NOT use contact information for automatic bug reports | MUST NOT | rev2 | untestable | This requirement constrains out-of-band client behavior (whether the client software silently submits bug reports using contact details from the server ID response). No protocol-layer observation can confirm or deny compliance; it requires code audit or vendor attestation. |
| RFC9525-6.6-1 — Client rejects certificates that fail identity verification | MUST NOT | rev1 | pass |  |
| RFC9525-6.6-1 — Client rejects certificates that fail identity verification | MUST NOT | rev2 | pass |  |
| RFC9525-6.1.1-1 — Client MUST construct reference identifiers independently of presented identifiers | MUST | rev1 | untestable | Whether the client's reference-identifier list was derived solely from its own configured hostname input, or was contaminated by data read from the server's certificate, is an internal construction-order property. A compliant and a non-compliant implementation produce identical wire traces: both connect to the same address and both either accept or reject the same certificate whenever the server's presented identifier happens to equal the client's configured hostname (the only case this harness's loopback fixtures can arrange). The harness cannot induce the client to derive a reference identifier from the certificate without already knowing the certificate's content, at which point independence versus coincidence become indistinguishable from outside. Observable downstream consequences of a wrong reference identifier (mismatched-cert rejection or wildcard matching) are separately catalogued as testable. |
| RFC9525-6.1.1-1 — Client MUST construct reference identifiers independently of presented identifiers | MUST | rev2 | untestable | Whether the client's reference-identifier list was derived solely from its own configured hostname input, or was contaminated by data read from the server's certificate, is an internal construction-order property. A compliant and a non-compliant implementation produce identical wire traces: both connect to the same address and both either accept or reject the same certificate whenever the server's presented identifier happens to equal the client's configured hostname (the only case this harness's loopback fixtures can arrange). The harness cannot induce the client to derive a reference identifier from the certificate without already knowing the certificate's content, at which point independence versus coincidence become indistinguishable from outside. Observable downstream consequences of a wrong reference identifier (mismatched-cert rejection or wildcard matching) are separately catalogued as testable. |
| RFC9525-6.1.1-2 — Client MUST NOT treat intermediate resolution values as reference identifiers | MUST NOT | rev1 | untestable | This client connects by a hostname/address supplied directly by the caller and performs no protocol-internal indirection (e.g., DNS SRV or NAPTR-driven redirection to a different target host) whose intermediate names could tempt an implementation into misuse as a reference identifier. There is no such indirection step to arm in the harness, so a compliant and a non-compliant implementation are wire-indistinguishable here: neither ever produces an intermediate identifier to begin with. |
| RFC9525-6.1.1-2 — Client MUST NOT treat intermediate resolution values as reference identifiers | MUST NOT | rev2 | untestable | This client connects by a hostname/address supplied directly by the caller and performs no protocol-internal indirection (e.g., DNS SRV or NAPTR-driven redirection to a different target host) whose intermediate names could tempt an implementation into misuse as a reference identifier. There is no such indirection step to arm in the harness, so a compliant and a non-compliant implementation are wire-indistinguishable here: neither ever produces an intermediate identifier to begin with. |
| RFC9525-6.1.1-3 — Client MUST construct reference identifiers per the URI-ID/SRV-ID/IP-ID/DNS-ID priority rules | MUST | rev1 | untestable | This is a priority-ordered decision procedure for choosing *which identifier type* to construct, not a wire-observable outcome. IMAP has no formal-protocol-document mandate for URI-IDs, and this client does not resolve DNS SRV records, so both the URI-ID and SRV-ID branches are permanently inapplicable for this implementation and the DNS-ID (default/fallback) branch is the only one ever reachable — meaning the 'priority' aspect of the rule has no observable branch point here. What remains observable — that the client in fact compares the server certificate against the caller-supplied hostname as a DNS-ID — is the separately catalogued and testable outcome of RFC9525-6.3-1/-2 and RFC9525-6.6-1/-2. |
| RFC9525-6.1.1-3 — Client MUST construct reference identifiers per the URI-ID/SRV-ID/IP-ID/DNS-ID priority rules | MUST | rev2 | untestable | This is a priority-ordered decision procedure for choosing *which identifier type* to construct, not a wire-observable outcome. IMAP has no formal-protocol-document mandate for URI-IDs, and this client does not resolve DNS SRV records, so both the URI-ID and SRV-ID branches are permanently inapplicable for this implementation and the DNS-ID (default/fallback) branch is the only one ever reachable — meaning the 'priority' aspect of the rule has no observable branch point here. What remains observable — that the client in fact compares the server certificate against the caller-supplied hostname as a DNS-ID — is the separately catalogued and testable outcome of RFC9525-6.3-1/-2 and RFC9525-6.6-1/-2. |
| RFC9525-6.2-1 — Client SHOULD stop searching once a presented identifier matches | SHOULD | rev1 | untestable | Whether the client's matching loop terminates early on the first match or continues scanning the remainder of its (single-element, for this client) reference-identifier list is an internal loop-control detail with no wire signature: the connection outcome (accept the certificate) is identical either way, since finishing an exhausted scan of an already-matched list changes nothing observable. |
| RFC9525-6.2-1 — Client SHOULD stop searching once a presented identifier matches | SHOULD | rev2 | untestable | Whether the client's matching loop terminates early on the first match or continues scanning the remainder of its (single-element, for this client) reference-identifier list is an internal loop-control detail with no wire signature: the connection outcome (accept the certificate) is identical either way, since finishing an exhausted scan of an already-matched list changes nothing observable. |
| RFC9525-6.2-2 — DNS-ID reference identifier MUST be used directly as the DNS domain name | MUST | rev1 | pass |  |
| RFC9525-6.2-2 — DNS-ID reference identifier MUST be used directly as the DNS domain name | MUST | rev2 | pass |  |
| RFC9525-6.2-3 — IP-ID reference identifier MUST exactly match, no partial matching | MUST | rev1 | pass |  |
| RFC9525-6.2-3 — IP-ID reference identifier MUST exactly match, no partial matching | MUST | rev2 | pass |  |
| RFC9525-6.2-4 — Client MUST match DNS name, IP address, or service type per the identifier's produced components | MUST | rev1 | pass |  |
| RFC9525-6.2-4 — Client MUST match DNS name, IP address, or service type per the identifier's produced components | MUST | rev2 | pass |  |
| RFC9525-6.3-1 — Non-IDN DNS domain name matching MUST be case-insensitive ASCII, label-by-label | MUST | rev1 | pass |  |
| RFC9525-6.3-1 — Non-IDN DNS domain name matching MUST be case-insensitive ASCII, label-by-label | MUST | rev2 | pass |  |
| RFC9525-6.3-2 — IDN DNS domain name matching MUST convert U-labels to A-labels and compare case-insensitively | MUST | rev1 | untestable | Exercising this rule requires a hostname that is genuinely an internationalized domain name (containing a U-label such as a non-ASCII label), routed through the harness's loopback TLS fixtures with a certificate carrying the corresponding A-label DNS-ID. Whether this client implements IDNA U-label-to-A-label conversion at all is an existence-of-affordance question a single black-box connection attempt cannot establish or refute across the implementation's whole input space (a failure to connect with a U-label hostname could mean either 'not IDNA-aware' or 'the fixture's DNS/hostname plumbing rejected the non-ASCII label before TLS was reached', which this harness cannot distinguish). |
| RFC9525-6.3-2 — IDN DNS domain name matching MUST convert U-labels to A-labels and compare case-insensitively | MUST | rev2 | untestable | Exercising this rule requires a hostname that is genuinely an internationalized domain name (containing a U-label such as a non-ASCII label), routed through the harness's loopback TLS fixtures with a certificate carrying the corresponding A-label DNS-ID. Whether this client implements IDNA U-label-to-A-label conversion at all is an existence-of-affordance question a single black-box connection attempt cannot establish or refute across the implementation's whole input space (a failure to connect with a U-label hostname could mean either 'not IDNA-aware' or 'the fixture's DNS/hostname plumbing rejected the non-ASCII label before TLS was reached', which this harness cannot distinguish). |
| RFC9525-6.3-3 — Client MUST match wildcard presented identifiers meeting the single-left-most-label requirements | MUST | rev1 | untestable | This harness's compliance driver connects the client by loopback IP address (127.0.0.1), not by DNS hostname, and wildcard patterns (e.g., '*.example.test') match DNS names only — IP addresses are never matched against wildcard patterns (RFC9525-6.4-1's octet-for-octet IP-ID rule has no wildcard concept at all). Exercising this rule requires the client to connect by a DNS hostname resolved against a fixture certificate carrying a wildcard DNS-ID SAN, which is unavailable in this loopback harness without a local DNS resolver/hosts-file fixture. Mirrors the same finding recorded for RFC 3501 §11.1's consolidated certificate-matching entry (the wildcard sub-clause there): the limitation is a harness/environment one, not an intrinsic property of the requirement, and is theme-tagged 'environment-limit' accordingly (rather than 'internal-decision') because a future DNS-hostname-capable harness could observe it directly. |
| RFC9525-6.3-3 — Client MUST match wildcard presented identifiers meeting the single-left-most-label requirements | MUST | rev2 | untestable | This harness's compliance driver connects the client by loopback IP address (127.0.0.1), not by DNS hostname, and wildcard patterns (e.g., '*.example.test') match DNS names only — IP addresses are never matched against wildcard patterns (RFC9525-6.4-1's octet-for-octet IP-ID rule has no wildcard concept at all). Exercising this rule requires the client to connect by a DNS hostname resolved against a fixture certificate carrying a wildcard DNS-ID SAN, which is unavailable in this loopback harness without a local DNS resolver/hosts-file fixture. Mirrors the same finding recorded for RFC 3501 §11.1's consolidated certificate-matching entry (the wildcard sub-clause there): the limitation is a harness/environment one, not an intrinsic property of the requirement, and is theme-tagged 'environment-limit' accordingly (rather than 'internal-decision') because a future DNS-hostname-capable harness could observe it directly. |
| RFC9525-6.3-4 — A presented wildcard label matches at most one reference-identifier label | MUST | rev1 | untestable | Same harness limitation as RFC9525-6.3-3: this is a further constraint on the same wildcard-matching behavior (a wildcard label matches exactly one reference-identifier label, not the DNS-wildcard 'one-or-more-labels' semantics), reachable only via a DNS-hostname connection this loopback-IP harness does not support. |
| RFC9525-6.3-4 — A presented wildcard label matches at most one reference-identifier label | MUST | rev2 | untestable | Same harness limitation as RFC9525-6.3-3: this is a further constraint on the same wildcard-matching behavior (a wildcard label matches exactly one reference-identifier label, not the DNS-wildcard 'one-or-more-labels' semantics), reachable only via a DNS-hostname connection this loopback-IP harness does not support. |
| RFC9525-6.3-5 — Presented identifier failing wildcard validity requirements MUST be ignored | MUST | rev1 | untestable | Same harness limitation as RFC9525-6.3-3: exercising an invalid wildcard presented identifier (e.g., two wildcard characters, or a wildcard in a non-left-most label) still requires the client to be connecting by DNS hostname so that wildcard-form SANs are even candidates for matching; this loopback-IP harness cannot reach that code path. |
| RFC9525-6.3-5 — Presented identifier failing wildcard validity requirements MUST be ignored | MUST | rev2 | untestable | Same harness limitation as RFC9525-6.3-3: exercising an invalid wildcard presented identifier (e.g., two wildcard characters, or a wildcard in a non-left-most label) still requires the client to be connecting by DNS hostname so that wildcard-form SANs are even candidates for matching; this loopback-IP harness cannot reach that code path. |
| RFC9525-6.4-1 — IP-ID matching MUST be octet-for-octet | MUST | rev1 | pass |  |
| RFC9525-6.4-1 — IP-ID matching MUST be octet-for-octet | MUST | rev2 | pass |  |
| RFC9525-6.4-2 — URI-ID host-component IP address matching parses as IPv6address or IPv4address | MUST | rev1 | untestable | This client never constructs a URI-ID reference identifier (per RFC9525-6.1.1-3, IMAP has no formal-protocol-document mandate for URI-IDs and this implementation does not derive reference identifiers from a URI scheme+host). Whether the client possesses IP-address-in-URI-ID parsing logic at all cannot be established or refuted by black-box IMAP-over-TLS connection testing, since the code path is never reachable through this client's actual connection API. |
| RFC9525-6.4-2 — URI-ID host-component IP address matching parses as IPv6address or IPv4address | MUST | rev2 | untestable | This client never constructs a URI-ID reference identifier (per RFC9525-6.1.1-3, IMAP has no formal-protocol-document mandate for URI-IDs and this implementation does not derive reference identifiers from a URI scheme+host). Whether the client possesses IP-address-in-URI-ID parsing logic at all cannot be established or refuted by black-box IMAP-over-TLS connection testing, since the code path is never reachable through this client's actual connection API. |
| RFC9525-6.5-1 — Client MUST check the combined service-type+domain-name pairing and MUST NOT check unpaired combinations | MUST NOT | rev1 | untestable | This client never constructs an SRV-ID or URI-ID reference identifier (it connects directly to a caller-supplied hostname/port and does not perform DNS SRV discovery per RFC9525-6.1.1-3's default DNS-ID branch), so the illustrated cross-pairing mistake (checking a service-type/domain-name combination that was never actually paired in the reference-identifier list) has no reachable code path to observe either compliance or violation of. |
| RFC9525-6.5-1 — Client MUST check the combined service-type+domain-name pairing and MUST NOT check unpaired combinations | MUST NOT | rev2 | untestable | This client never constructs an SRV-ID or URI-ID reference identifier (it connects directly to a caller-supplied hostname/port and does not perform DNS SRV discovery per RFC9525-6.1.1-3's default DNS-ID branch), so the illustrated cross-pairing mistake (checking a service-type/domain-name combination that was never actually paired in the reference-identifier list) has no reachable code path to observe either compliance or violation of. |
| RFC9525-6.5-2 — SRV-ID application service name matching MUST be case-insensitive | MUST | rev1 | untestable | This client does not construct SRV-ID reference identifiers (no DNS SRV discovery), so SRV-ID service-name matching has no reachable code path in this implementation to observe from outside. |
| RFC9525-6.5-2 — SRV-ID application service name matching MUST be case-insensitive | MUST | rev2 | untestable | This client does not construct SRV-ID reference identifiers (no DNS SRV discovery), so SRV-ID service-name matching has no reachable code path in this implementation to observe from outside. |
| RFC9525-6.5-3 — URI-ID scheme name matching MUST be case-insensitive | MUST | rev1 | untestable | This client does not construct URI-ID reference identifiers (it is not configured via a URI scheme with a formal server-certificate URI mandate), so URI-ID scheme matching has no reachable code path in this implementation to observe from outside. |
| RFC9525-6.5-3 — URI-ID scheme name matching MUST be case-insensitive | MUST | rev2 | untestable | This client does not construct URI-ID reference identifiers (it is not configured via a URI scheme with a formal server-certificate URI mandate), so URI-ID scheme matching has no reachable code path in this implementation to observe from outside. |
| RFC9525-6.6-2 — On a successful match, client MUST use the matched reference identifier as the validated identity | MUST | rev1 | untestable | Whether the client internally records/uses 'the matched reference identifier' specifically (as opposed to, say, some other equivalent string) as the validated identity of the service is an internal bookkeeping detail. The client's only externally observable behavior in the success case is that the connection proceeds (already covered by the negative case, RFC9525-6.6-1, and by the matching-rule entries in §6.2-6.5); this client exposes no public API surface that reports back 'the validated identity of the application service' as a distinguishable value a black-box test could assert on. |
| RFC9525-6.6-2 — On a successful match, client MUST use the matched reference identifier as the validated identity | MUST | rev2 | untestable | Whether the client internally records/uses 'the matched reference identifier' specifically (as opposed to, say, some other equivalent string) as the validated identity of the service is an internal bookkeeping detail. The client's only externally observable behavior in the success case is that the connection proceeds (already covered by the negative case, RFC9525-6.6-1, and by the matching-rule entries in §6.2-6.5); this client exposes no public API surface that reports back 'the validated identity of the application service' as a distinguishable value a black-box test could assert on. |
| RFC9525-6.6-3 — A human-controlled client SHOULD inform the user and auto-terminate on identity mismatch | SHOULD | rev1 | untestable | This client is a headless protocol library with no built-in end-user interface; it is not itself 'a client that is directly controlled by a human user' in the sense this sentence addresses (that role belongs to whatever mail application embeds this library, which is outside this codebase's boundary). Whether *this* library counts as such a client — and therefore whether this SHOULD binds it at all — is an API-design/deployment-context classification question, not a protocol-observable behavior; unlike the ALERT-text duty (RFC3501 §7.1, flipped testable via logger capture per the untestability taxonomy), 'inform the user' here has no minimum library-boundary realization to test for, because the RFC's own SHOULD is conditioned on a client *architecture* (direct human control) that this library does not have. |
| RFC9525-6.6-3 — A human-controlled client SHOULD inform the user and auto-terminate on identity mismatch | SHOULD | rev2 | untestable | This client is a headless protocol library with no built-in end-user interface; it is not itself 'a client that is directly controlled by a human user' in the sense this sentence addresses (that role belongs to whatever mail application embeds this library, which is outside this codebase's boundary). Whether *this* library counts as such a client — and therefore whether this SHOULD binds it at all — is an API-design/deployment-context classification question, not a protocol-observable behavior; unlike the ALERT-text duty (RFC3501 §7.1, flipped testable via logger capture per the untestability taxonomy), 'inform the user' here has no minimum library-boundary realization to test for, because the RFC's own SHOULD is conditioned on a client *architecture* (direct human control) that this library does not have. |
| RFC9525-6.6-4 — Human-controlled client MAY let advanced users proceed despite mismatch, with caution | MAY | rev1 | untestable | A MAY-level permission grants no constraining envelope: providing or not providing an override affordance are both compliant, so there is no pass/fail boundary a black-box test could assert on even in principle. Additionally, as with RFC9525-6.6-3, this permission is addressed to a human-controlled client, a role this headless library does not itself occupy. |
| RFC9525-6.6-4 — Human-controlled client MAY let advanced users proceed despite mismatch, with caution | MAY | rev2 | untestable | A MAY-level permission grants no constraining envelope: providing or not providing an override affordance are both compliant, so there is no pass/fail boundary a black-box test could assert on even in principle. Additionally, as with RFC9525-6.6-3, this permission is addressed to a human-controlled client, a role this headless library does not itself occupy. |
| RFC9525-6.6-5 — Ad hoc certificate pinning SHOULD NOT restrict future connections to just the pinned certificate | SHOULD NOT | rev1 | untestable | This client has no public API for accepting-and-pinning a certificate across connections at all; the MAY-level affordance ('present the user with the ability to accept ... for subsequent connections') is not implemented, so there is no pinning behavior to observe as either present-and-compliant or present-and-overly-restrictive. Absence of a capability across a client's whole configuration space cannot be established by a single black-box connection attempt. |
| RFC9525-6.6-5 — Ad hoc certificate pinning SHOULD NOT restrict future connections to just the pinned certificate | SHOULD NOT | rev2 | untestable | This client has no public API for accepting-and-pinning a certificate across connections at all; the MAY-level affordance ('present the user with the ability to accept ... for subsequent connections') is not implemented, so there is no pinning behavior to observe as either present-and-compliant or present-and-overly-restrictive. Absence of a capability across a client's whole configuration space cannot be established by a single black-box connection attempt. |
| RFC9525-6.6-6 — Static per-peer certificate policy SHOULD be prior configuration, not a just-in-time override | SHOULD | rev1 | untestable | This client has no built-in per-peer static certificate-pinning policy feature (prior-configuration or just-in-time) at all; whether such a feature, if it existed, would be surfaced only as prior configuration is unobservable because the feature itself does not exist to inspect. |
| RFC9525-6.6-6 — Static per-peer certificate policy SHOULD be prior configuration, not a just-in-time override | SHOULD | rev2 | untestable | This client has no built-in per-peer static certificate-pinning policy feature (prior-configuration or just-in-time) at all; whether such a feature, if it existed, would be surfaced only as prior configuration is unobservable because the feature itself does not exist to inspect. |
| RFC9525-7.2-1 — URI-ID MUST include both scheme and host (reg-name); otherwise it is invalid and MUST be ignored | MUST | rev1 | untestable | This client never constructs or evaluates URI-ID reference identifiers (see RFC9525-6.5-3's rationale); URI-ID validity screening has no reachable code path in this implementation's actual connection flow to observe from outside. |
| RFC9525-7.2-1 — URI-ID MUST include both scheme and host (reg-name); otherwise it is invalid and MUST be ignored | MUST | rev2 | untestable | This client never constructs or evaluates URI-ID reference identifiers (see RFC9525-6.5-3's rationale); URI-ID validity screening has no reachable code path in this implementation's actual connection flow to observe from outside. |
| RFC9525-7.6-1 — Client constructing multi-type reference identifiers SHOULD ensure issuing CAs are appropriately constrained | SHOULD | rev1 | untestable | This client constructs only a single reference-identifier type (DNS-ID) per RFC9525-6.1.1-3, so the multi-type precondition never arises for it. More fundamentally, 'taking care to ensure CAs are appropriately constrained' is a policy/trust-configuration duty discharged through CA agreements or name-constraint enforcement in the trust store — conduct that happens outside the IMAP-over-TLS protocol exchange entirely. A black-box connection test can observe whether a given certificate chain is accepted or rejected against the platform trust store, but cannot establish whether the client 'took care' about issuing-CA constraint policy in general; that requires auditing configuration or vendor attestation, not protocol observation. |
| RFC9525-7.6-1 — Client constructing multi-type reference identifiers SHOULD ensure issuing CAs are appropriately constrained | SHOULD | rev2 | untestable | This client constructs only a single reference-identifier type (DNS-ID) per RFC9525-6.1.1-3, so the multi-type precondition never arises for it. More fundamentally, 'taking care to ensure CAs are appropriately constrained' is a policy/trust-configuration duty discharged through CA agreements or name-constraint enforcement in the trust store — conduct that happens outside the IMAP-over-TLS protocol exchange entirely. A black-box connection test can observe whether a given certificate chain is accepted or rejected against the platform trust store, but cannot establish whether the client 'took care' about issuing-CA constraint policy in general; that requires auditing configuration or vendor attestation, not protocol observation. |
| RFC9051-2.2-1 — Client sends CRLF-terminated lines; reads lines or octet-counted sequences | MUST | rev2 | pass |  |
| RFC9051-2.2.1-1 — Client generates a unique tag per command (server MUST accept reuse) | SHOULD | rev2 | pass |  |
| RFC9051-2.2.1-2 — Client follows command syntax strictly | MUST | rev2 | pass |  |
| RFC9051-2.2.1-3 — Client completes a command (all continuations) before initiating a new one | MUST | rev2 | pass |  |
| RFC9051-2.2.2-1 — Client dispatches responses by first token (tag, *, or +) | MUST | rev2 | untestable | This describes internal client dispatch logic. The correctness of how the client routes a response to the right handler is not directly observable at the protocol layer from a black-box perspective; only downstream effects (e.g., incorrect command completion handling) are observable. |
| RFC9051-2.2.2-2 — Client accepts any server response at all times | MUST | rev2 | pass |  |
| RFC9051-2.2.2-3 — Client SHOULD remember (cache) server data to avoid redundant requests | SHOULD | rev2 | untestable | Whether the client caches server data internally and avoids redundant re-fetch commands is an implementation quality matter. A black-box test could observe whether the client issues unnecessary commands, but the RFC permits such commands and does not make their absence a MUST, so there is no definitive pass/fail boundary at the protocol layer. |
| RFC9051-2.2.2-4 — Client MUST remember certain categories of server data | MUST | rev2 | untestable | The RFC does not enumerate in §2.2.2 which specific server data items are subject to this MUST; those are identified in the Server Responses section. This entry captures the general obligation. Whether the client has correctly recorded a specific data item can sometimes be inferred from subsequent client behavior (e.g., it does not re-fetch when it should not need to), but the general requirement is not directly verifiable at the black-box protocol layer without exercising every applicable data type. |
| RFC9051-2.3.1.1-1 — Client relies on UIDVALIDITY to detect UID discontinuity and invalidate its UID cache | SHOULD | rev2 | untestable | The RFC does not phrase this as a direct client imperative ('the client MUST discard its UID cache'); it states that a UID discontinuity 'MUST be detectable' (a server-side guarantee about the UIDVALIDITY mechanism's sufficiency) and separately warns that a UIDVALIDITY change 'can be very disruptive to client message caching'. The client-facing duty here is only inferable: a client that caches messages by UID is expected to key that cache on (mailbox name, UIDVALIDITY, UID) and to invalidate/discard cached UID-keyed state when it observes a new UIDVALIDITY value at SELECT/EXAMINE time. This is a cross-session cache-management duty with no required wire behavior: a client that (incorrectly) keeps stale UID-keyed data after a UIDVALIDITY change produces no distinguishing protocol trace by itself — only if it then issues commands against UIDs that no longer denote the same message would a divergence become observable, and that divergence is already covered by testable, command-specific catalog entries (e.g., UID-based FETCH/STORE argument validity) rather than by this general caching duty. Mechanism (b) of the untestability taxonomy (sequential multi-connection arm()) was evaluated explicitly and rebutted: two scripted sessions can present a changed UIDVALIDITY value across sessions, but the harness can only show the value changing — it cannot observe whether the client discarded its cache, because UID caching is consumer-delegated in this headless library (the library itself keeps no cross-session UID cache), so no wire trace in the second session distinguishes compliance from violation. |
| RFC9051-2.3.2-1 — Client treats mutually-set $Junk/$NotJunk as unset and SHOULD clear both on the server | MUST | rev2 | fail | unimplemented: $Junk and $NotJunk are mutually exclusive. If more than one of these is set for a message, the client MUST treat it as if none are set, and it SHOULD unset both… |
| RFC9051-2.3.2-2 — Client SHOULD NOT clear the $Forwarded keyword once set | SHOULD NOT | rev2 | fail | unimplemented: $Forwarded Message has been forwarded to another email address by being embedded within, or attached to a new message. An email client sets this keyword when it… |
| RFC9051-3-1 — Client must not attempt a command while the connection is in an inappropriate state | MUST NOT | rev2 | fail | unimplemented: It is a protocol error for the client to attempt a command while the connection is in an inappropriate state, and the server will respond with a BAD or NO (depe… |
| RFC9051-3.1-1 — Client must supply authentication credentials in Not Authenticated state | MUST | rev2 | pass |  |
| RFC9051-3.2-1 — Client must select a mailbox before commands that affect messages will be permitted | MUST | rev2 | fail | unimplemented: In the authenticated state, the client is authenticated and MUST select a mailbox to access before commands that affect messages will be permitted. |
| RFC9051-3.4-1 — Client reads tagged OK before closing after LOGOUT | MUST | rev2 | pass |  |
| RFC9051-3.4-2 — Client SHOULD NOT unilaterally close the connection | SHOULD NOT | rev2 | pass |  |
| RFC9051-4.1-1 — Atom must consist of one or more non-special characters | MUST | rev2 | pass |  |
| RFC9051-4.1.1-1 — Sequence set never contains UIDs; UID set never contains the '*' wildcard | MUST | rev2 | fail | unimplemented: A sequence set never contains unique identifiers. A "UID set" is similar to the sequence set, but uses unique identifiers instead of message sequence numbers, a… |
| RFC9051-4.2-1 — Number must consist of one or more digit characters | MUST | rev2 | fail | unimplemented: A number consists of one or more digit characters and represents a numeric value. |
| RFC9051-4.3-1 — Client must wait for continuation request before sending synchronizing-literal octet data | MUST | rev2 | fail | unimplemented: In the case of synchronizing literals transmitted from client to server, the client MUST wait to receive a command continuation request (described later in this… |
| RFC9051-4.3-2 — Non-synchronizing literal MUST NOT exceed 4096 octets unless an extension says otherwise | MUST NOT | rev2 | fail | unimplemented: Unless otherwise specified in an IMAP extension, non-synchronizing literals MUST NOT be larger than 4096 octets. |
| RFC9051-4.3-3 — Literal larger than 4096 bytes must be sent as a synchronizing literal | MUST | rev2 | fail | unimplemented: Any literal larger than 4096 bytes MUST be sent as a synchronizing literal. |
| RFC9051-4.3-4 — Quoted string is zero or more UTF-8-encoded Unicode characters excluding CR and LF | MUST | rev2 | pass |  |
| RFC9051-4.3-5 — Client must wait for continuation request even for zero-octet synchronizing literals | MUST | rev2 | fail | unimplemented: Note: Even if the octet count is 0, a client transmitting a synchronizing literal MUST wait to receive a command continuation request. |
| RFC9051-4.3.1-1 — Implementations MAY transmit 8-bit or multi-octet characters in literals but SHOULD only do so when CHARSET is identified | SHOULD | rev2 | fail | unimplemented: IMAP4rev2 implementations MAY transmit 8-bit or multi-octet characters in literals but SHOULD do so only when the [CHARSET] is identified. |
| RFC9051-4.3.1-2 — Implementations MUST accept and MAY transmit UTF-8 text in quoted-strings free of NUL, CR, LF | MUST | rev2 | pass |  |
| RFC9051-4.3.1-3 — Client and server implementations must encode binary data into textual form before transmitting | MUST | rev2 | fail | unimplemented: Unless returned in response to BINARY.PEEK[...]/BINARY[...] FETCH, client and server implementations MUST encode binary data into a textual form, such as base64… |
| RFC9051-4.3.1-4 — Unencoded binary strings are not permitted except when returned via a literal8 BINARY/BINARY.PEEK FETCH response | MUST NOT | rev2 | fail | unimplemented: Although a BINARY content transfer encoding is defined, unencoded binary strings are not permitted, unless returned in a <literal8> in response to a BINARY.PEEK… |
| RFC9051-4.3.1-5 — String with excessive CTL characters MAY be treated as binary | MAY | rev2 | fail | unimplemented: A string with an excessive amount of CTL characters MAY also be considered to be binary. |
| RFC9051-5.1-1 — Client MAY create Net-Unicode mailbox names and MUST interpret 8-bit LIST names as Net-Unicode | MUST | rev2 | pass |  |
| RFC9051-5.1-2 — INBOX is a case-insensitive special name; interpretation of other names is implementation dependent | MUST | rev2 | fail | unimplemented: The case-insensitive mailbox name INBOX is a special name reserved to mean "the primary mailbox for this user on this server". |
| RFC9051-5.1-4 — Client MUST be able to interact with any server case-sensitivity model | MUST | rev2 | untestable | Direct carry-forward of RFC3501-5.1-3's untestability analysis: this is a blanket obligation to interoperate with all three server case-sensitivity models (fully case-sensitive in ASCII range, case-insensitive but case-preserving, case-insensitive with case-coercion) enumerated in the first quoted sentence. There is no single observable protocol interaction that proves general compliance; a client can pass any particular test case yet still fail with an untested server variant. The harness could script all three server models, but no single client wire behavior would distinguish universal compliance — mailbox names originate with the consuming application and the library forwards them, so the 'interacts with any model' property has no finite black-box observable. Full coverage would require an open-ended integration property, not a single assertion. |
| RFC9051-5.1-5 — Mailbox names containing atom-specials MUST be quoted-string or literal | MUST | rev2 | fail | unimplemented: Any character that is one of the atom-specials (see "Formal Syntax" in Section 9) will require that the mailbox name be represented as a quoted string or litera… |
| RFC9051-5.1-6 — '#' and '&' have conventional meanings and should be avoided outside that convention | SHOULD | rev2 | fail | unimplemented: Two characters, "#" and "&", have meanings by convention and should be avoided except when used in that convention. |
| RFC9051-5.1.1-1 — Exported hierarchical mailbox names MUST be left-to-right with a single separator character | MUST | rev2 | fail | unimplemented: If it is desired to export hierarchical mailbox names, mailbox names MUST be left-to-right hierarchical, using a single ASCII character to separate levels of hi… |
| RFC9051-5.2-1 — Client MUST remember mailbox size updates | MUST | rev2 | pass |  |
| RFC9051-5.2-2 — Client MUST NOT assume subsequent commands return mailbox size | MUST NOT | rev2 | pass |  |
| RFC9051-5.5-1 — Client MAY pipeline commands without waiting for completion | MAY | rev2 | pass |  |
| RFC9051-5.5-2 — Command continuation request and response MUST be negotiated before next command | MUST | rev2 | fail | unimplemented: However, any command continuation request responses and command continuations MUST be negotiated before any subsequent command is initiated. |
| RFC9051-5.5-3 — Client MUST wait for completion before sending sequence-number command after non-FETCH/STORE/SEARCH | MUST | rev2 | fail | unimplemented: Therefore, if the client sends any command other than FETCH, STORE, or SEARCH, it MUST wait for the completion result response before sending a command with mes… |
| RFC9051-5.5-4 — Client MUST wait for completion after a UID command before sending a sequence-number command | MUST | rev2 | fail | unimplemented: Note: EXPUNGE responses are permitted while UID FETCH, UID STORE, and UID SEARCH are in progress. If the client sends a UID command, it MUST wait for a completi… |
| RFC9051-5.5-5 — Sequence numbers in a UID SEARCH argument are associated with messages prior to that command's EXPUNGE effects | MUST | rev2 | fail | unimplemented: Any message sequence numbers in an argument to UID SEARCH are associated with messages prior to the effect of any untagged EXPUNGE responses returned by the UID… |
| RFC9051-6.1.1-1 — Client MUST implement STARTTLS and LOGINDISABLED capabilities on cleartext ports | MUST | rev2 | pass |  |
| RFC9051-6.1.1-2 — Client MUST implement AUTH=PLAIN capability on both cleartext and Implicit TLS ports | MUST | rev2 | pass |  |
| RFC9051-6.1.2-1 — NOOP usable as a periodic poll | MAY | rev2 | pass |  |
| RFC9051-6.2.1-1 — Client MUST NOT send further commands until TLS negotiation is complete | MUST NOT | rev2 | pass |  |
| RFC9051-6.2.1-2 — Client discards cached capabilities after STARTTLS | MUST | rev2 | pass |  |
| RFC9051-6.2.1-3 — Client re-issues CAPABILITY after STARTTLS | SHOULD | rev2 | pass |  |
| RFC9051-6.2.2-1 — Client MUST implement the AUTHENTICATE command | MUST | rev2 | pass |  |
| RFC9051-6.2.2-2 — Client cancels AUTHENTICATE exchange by sending a single '*' line | MUST | rev2 | pass |  |
| RFC9051-6.2.2-3 — Client MUST encode the SASL initial response as base64 and use a pad character for zero-length responses | MUST | rev2 | pass |  |
| RFC9051-6.2.2-4 — Client SHOULD implement additional SASL mechanisms beyond PLAIN | SHOULD | rev2 | untestable | Which specific SASL mechanisms are supported is a deployment/configuration choice; black-box testing cannot determine whether the client has implemented 'additional' non-plaintext mechanisms or merely chosen not to advertise them in a given test environment. |
| RFC9051-6.2.2-5 — Client MUST re-issue CAPABILITY after AUTHENTICATE when security layer was negotiated | MUST | rev2 | pass |  |
| RFC9051-6.2.2-6 — Client MAY retry authentication after AUTHENTICATE NO response | MAY | rev2 | pass |  |
| RFC9051-6.2.3-1 — Client SHOULD NOT use LOGIN except as a last resort | SHOULD NOT | rev2 | untestable | 'Last resort' is a policy judgment about the client's authentication preference ordering which cannot be observed in a single black-box exchange — a client that always offers LOGIN immediately might still be compliant if no other mechanisms were available, and a test harness cannot distinguish intentional last-resort use from indiscriminate use. |
| RFC9051-6.2.3-2 — Client implementations SHOULD have a means to disable automatic LOGIN | SHOULD | rev2 | untestable | This is a requirement about the client's configuration/UI surface ('have a means to disable'), not about observable wire behavior. A behavioral test could only exercise a public configuration affordance that actually exists (set the disable option, observe no LOGIN on the wire); the requirement instead asserts the EXISTENCE of such an affordance, which is an API-inventory fact a black-box protocol exchange cannot establish. |
| RFC9051-6.2.3-3 — Clients MUST NOT use LOGIN on unsecure networks | MUST NOT | rev2 | untestable | 'Unsecure network' is a deployment-environment property with no wire signature: plaintext-at-the-socket does not imply an unsecure network (localhost connections, trusted LANs, VPN tunnels, and TLS-terminating proxies all present a plaintext socket over a secure path). A plaintext LOGIN on a trusted network is fully compliant and produces a wire trace indistinguishable from a violating plaintext LOGIN on the open Internet, so a black-box harness that withholds STARTTLS/Implicit TLS cannot construct 'an unsecure network' — it can only construct a plaintext socket, whose security is the deployment's fact, not the wire's. The enforceable half of this concern is the LOGINDISABLED prohibition (RFC9051-6.2.3-4), which is wire-observable and separately catalogued. |
| RFC9051-6.2.3-4 — Client MUST NOT send LOGIN when LOGINDISABLED is advertised | MUST NOT | rev2 | pass |  |
| RFC9051-6.3.1-1 — Client SHOULD only include extensions that need to be enabled | SHOULD | rev2 | untestable | Whether a given extension 'needs to be enabled' is the client's own internal determination about its capability negotiation strategy, not something observable from the ENABLE command on the wire. A client that includes an extension it happens to support (but does not strictly need enabled) is behaviorally indistinguishable, at the protocol level, from one that first checks necessity before including it — both send a syntactically identical ENABLE argument list. No black-box test can distinguish a principled 'only if needed' client from an unconditional one, because 'needs to be enabled' is a design-time property of the extension (does it change wire behavior the client must adapt to?), not a per-connection runtime signal. |
| RFC9051-6.3.1-2 — Client MUST NOT issue ENABLE after SELECT/EXAMINE | MUST NOT | rev2 | fail | unimplemented: Clients MUST NOT issue ENABLE once they SELECT/EXAMINE a mailbox; |
| RFC9051-6.3.2-1 — Client should assume all flags are permanent when PERMANENTFLAGS is omitted | SHOULD | rev2 | fail | unimplemented: OK [PERMANENTFLAGS (<list of flags>)] A list of message flags that the client can change permanently. If this is missing, the client should assume that all flag… |
| RFC9051-6.3.2-2 — Client must track that a failed SELECT leaves no mailbox selected | MUST | rev2 | fail | unimplemented: The SELECT command automatically deselects any currently selected mailbox before attempting the new selection. Consequently, if a mailbox is selected and a SELE… |
| RFC9051-6.3.2-3 — Pure IMAP4rev2 client is advised to ignore an untagged RECENT response | SHOULD | rev2 | fail | unimplemented: Note that IMAP4rev1-compliant servers can also send the untagged RECENT response that was deprecated in IMAP4rev2, e.g., "* 0 RECENT". Pure IMAP4rev2 clients ar… |
| RFC9051-6.3.3-1 — Client must recognise [READ-ONLY] in tagged OK response to EXAMINE | MUST | rev2 | fail | unimplemented: The text of the tagged OK response to the EXAMINE command MUST begin with the "[READ-ONLY]" response code. |
| RFC9051-6.3.6-1 — Client needs to be able to handle failure of a RENAME of INBOX | MUST | rev2 | fail | unimplemented: Renaming INBOX is permitted and does not result in a tagged BAD response, and it has special behavior: It moves all messages in INBOX to a new mailbox with the … |
| RFC9051-6.3.9-1 — Clients SHOULD use the empty reference argument | SHOULD | rev2 | fail | unimplemented: Clients SHOULD use the empty reference argument. |
| RFC9051-6.3.9-2 — Client SHOULD NOT use non-standard reference argument except at user request | SHOULD NOT | rev2 | untestable | 'At the explicit request of the user' is a behavioral/UI policy that cannot be observed over the wire. A black-box test cannot determine whether a reference argument was sent in response to a user request or autonomously by the client — the wire shows what was sent, never why. |
| RFC9051-6.3.9-3 — Hierarchical browser MUST NOT assume server reference interpretation | MUST NOT | rev2 | untestable | 'Making assumptions' is an internal client implementation decision not directly observable on the wire. A test can verify the client sends syntactically correct LIST commands but cannot observe whether it is internally assuming a particular server interpretation of the reference component. |
| RFC9051-6.3.9-4 — Clients MUST be able to handle extra mailbox information the server returns | MUST | rev2 | untestable | 'Being able to handle' unsolicited extension data is a robustness property of the parser, not a distinct wire action. There is no request the harness can send whose absence of a crash or hang proves general handling of *any* additional information the server might choose to return; the harness can only exercise the specific extended data items already catalogued elsewhere (e.g. CHILDINFO, OLDNAME), which are separately testable duties. |
| RFC9051-6.3.9-5 — Client MUST NOT send a LIST option the server has not advertised | MUST NOT | rev2 | fail | unimplemented: a client MUST NOT send an option for which the server has not advertised support. |
| RFC9051-6.3.9-6 — Client SHOULD NOT specify a LIST option more than once | SHOULD NOT | rev2 | fail | unimplemented: The client SHOULD NOT specify any option more than once; however, if the client does this, the server MUST act as if it received the option only once. |
| RFC9051-6.3.9.1-1 — Client MUST handle a CHILDINFO response with no matching submailboxes | MUST | rev2 | untestable | 'Being able to handle' a race-condition edge case (server-reported children that no longer exist by the time the client acts on them) is a robustness property of client-side logic, not a wire action the harness can assert on directly — any wire behavior after receiving CHILDINFO followed by an empty/failed follow-up LIST is compliant provided the client doesn't crash, and 'doesn't crash' is not a protocol-level pass/fail boundary this catalog otherwise encodes. |
| RFC9051-6.3.9.4-1 — All clients MUST treat a stronger LIST attribute as implying weaker inferable attributes | MUST | rev2 | fail | unimplemented: All clients MUST treat a LIST attribute with a stronger meaning as implying any attribute that can be inferred from it. (See Section 7.3.1 for the list of curre… |
| RFC9051-6.3.9.5-1 — Client must be prepared for \HasChildren with no child mailbox listed | MUST | rev2 | untestable | Same reasoning as the CHILDINFO entry (RFC9051-6.3.9.1-1): 'being prepared to deal with' a race-condition edge case (a \HasChildren-marked mailbox whose children were deleted or made inaccessible before the client could list them) is a robustness property of client-side logic, not a wire action the harness can assert on directly — any wire behavior after receiving \HasChildren followed by an empty/failed child LIST is compliant provided the client doesn't crash, and 'doesn't crash' is not a protocol-level pass/fail boundary this catalog otherwise encodes. |
| RFC9051-6.3.10-1 — Client MUST be prepared for multiple Personal/Other-Users' Namespaces | MUST | rev2 | untestable | 'Being prepared for' an open-ended cardinality of namespaces is a robustness property of client-side namespace-handling logic, not a single observable wire action. A test can script a NAMESPACE response with multiple Personal Namespace pairs and confirm the client parses all of them without error, but 'prepared for them' as a general duty extends beyond any one scripted scenario to the client's whole design; the parseable-multiplicity slice of this duty is itself just confirming the client doesn't crash on valid syntax, which has no independent pass/fail boundary distinct from ordinary NAMESPACE-response parsing. |
| RFC9051-6.3.10-2 — Client SHOULD let the user choose among multiple Personal Namespaces, or default to the first | SHOULD | rev2 | untestable | This is a UI/policy choice between two client-side strategies (prompt the user, or silently default to the first Personal Namespace) that are both compliant under the SHOULD. The wire trace of a client that defaults to the first namespace is indistinguishable from a client whose user was prompted and happened to pick the first one; no black-box observation can establish which branch of the disjunction, if either, was actually implemented. |
| RFC9051-6.3.11-1 — Client SHOULD NOT use STATUS on the currently selected mailbox | SHOULD NOT | rev2 | fail | unimplemented: Because the STATUS command can cause the mailbox to be opened internally, and because this information is available by other means on the selected mailbox, the … |
| RFC9051-6.3.11-2 — Client MUST NOT use STATUS as a check for new messages in the selected mailbox | MUST NOT | rev2 | fail | unimplemented: The STATUS command MUST NOT be used as a "check for new messages in the selected mailbox" operation (refer to Sections 7 and 7.4.1 for more information about th… |
| RFC9051-6.3.11-3 — Clients should use STATUS SIZE cautiously | SHOULD | rev2 | untestable | 'Using cautiously' binds the client's operational judgment/expectations about latency, not a discrete wire action. A client that issues STATUS SIZE freely is not thereby sending a malformed or prohibited command — only a client whose architecture assumes SIZE is cheap is non-compliant with the spirit of the caution, and that assumption has no wire signature. Same reasoning as RFC3501-6.3.10-3 (SHOULD NOT expect reasonable performance from bulk STATUS), which this SIZE-specific caution narrows to a single status data item. |
| RFC9051-6.3.12-1 — APPEND literal argument SHOULD be in RFC 5322 / I18N-HDRS message format | SHOULD | rev2 | fail | unimplemented: The APPEND command appends the literal argument as a new message to the end of the specified destination mailbox. This argument SHOULD be in the format of an [R… |
| RFC9051-6.3.12-2 — Client MAY issue NOOP after APPEND if server sends no untagged EXISTS | MAY | rev2 | fail | unimplemented: If the mailbox is currently selected, normal new message actions SHOULD occur. Specifically, the server SHOULD notify the client immediately via an untagged EXI… |
| RFC9051-6.3.13-1 — Client MUST NOT send a command while the server awaits DONE | MUST NOT | rev2 | fail | unimplemented: The client MUST NOT send a command while the server is waiting for the DONE, since the server will not be able to distinguish a command from a continuation. |
| RFC9051-6.3.13-2 — Clients using IDLE are advised to terminate and reissue it at least every 29 minutes | SHOULD | rev2 | untestable | The 29-minute terminate-and-reissue duty IS wire behavior in principle — a DONE followed by a fresh IDLE inside the window would be directly observable on the wire. The untestability is the combination of (a) the test-window timescale: no harness can realistically hold a connection idling for the tens of minutes per assertion needed to observe the 29-minute boundary, and (b) the absence of any enforced wire rule at shorter timescales that could proxy for it — server-side inactivity timeouts vary and are not standardized (29 minutes is a safety margin under a hypothetical 30-minute timeout, not a hard protocol deadline), so a client that never reissues IDLE within any shorter, testable window violates no wire-level rule; it only risks an implementation-specific server-side logoff whose timing the RFC itself declines to standardize ('if such a server has an inactivity timeout'). |
| RFC9051-6.3.13-3 — Client terminates IDLE by sending the DONE continuation | MUST | rev2 | fail | unimplemented: The IDLE command is terminated by the receipt of a "DONE" continuation from the client; such response satisfies the server's continuation request. |
| RFC9051-6.4.1-1 — Client MAY issue SELECT, EXAMINE, or LOGOUT without a prior CLOSE | MAY | rev2 | fail | unimplemented: Even if a mailbox is selected, a SELECT, EXAMINE, or LOGOUT command MAY be issued without previously issuing a CLOSE command. |
| RFC9051-6.4.2-1 — UNSELECT deselects like CLOSE but without permanently removing messages | MUST | rev2 | fail | unimplemented: The UNSELECT command frees a session's resources associated with the selected mailbox and returns the server to the authenticated state. This command performs t… |
| RFC9051-6.4.4-1 — rev2-only clients MUST ignore legacy untagged SEARCH responses | MUST | rev2 | fail | unimplemented: Note that IMAP4rev1 used SEARCH responses [RFC3501] instead of ESEARCH responses. Clients that support only IMAP4rev2 MUST ignore SEARCH responses. |
| RFC9051-6.4.4-2 — ESEARCH is still sent on no matches, without the requested item | MUST | rev2 | fail | unimplemented: If the SEARCH results in no matches, the server MUST NOT include the MIN result option in the ESEARCH response; however, it still MUST send the ESEARCH response… |
| RFC9051-6.4.4-3 — Client MUST NOT assume ESEARCH ALL results are in any particular order | MUST NOT | rev2 | fail | unimplemented: Return all message numbers/UIDs that satisfy the SEARCH criteria using the sequence-set syntax. Note that the client MUST NOT assume that messages/UIDs will be … |
| RFC9051-6.4.4-4 — SAVE as the only result option suppresses the ESEARCH response | MUST | rev2 | fail | unimplemented: In absence of any other SEARCH result option, the SAVE result option also suppresses any ESEARCH response that would have been otherwise returned by the SEARCH … |
| RFC9051-6.4.4-5 — CHARSET specification is the word CHARSET plus a registered charset name | MUST | rev2 | fail | unimplemented: The OPTIONAL [CHARSET] specification consists of the word "CHARSET" followed by the name of a character set from the registry [CHARSET-REG]. It indicates the [C… |
| RFC9051-6.4.4-6 — Clients SHOULD use UTF-8 in SEARCH; omitting CHARSET implies UTF-8 | SHOULD | rev2 | fail | unimplemented: Clients SHOULD use UTF-8. Note that if CHARSET is not provided, IMAP4rev2 servers MUST assume UTF-8, so selecting CHARSET UTF-8 is redundant. It is permitted fo… |
| RFC9051-6.4.4-7 — Client must treat tagged NO (with BADCHARSET) as the unsupported-CHARSET outcome | MUST | rev2 | fail | unimplemented: If the server does not support the specified [CHARSET], it MUST return a tagged NO response (not a BAD). This response SHOULD contain the BADCHARSET response co… |
| RFC9051-6.4.4.2-1 — Client MAY pipeline SEARCH RETURN (SAVE) with '$'-using commands absent ambiguity | MAY | rev2 | fail | unimplemented: A client MAY pipeline a SEARCH RETURN (SAVE) command with one or more commands using the "$" marker, as long as this doesn't create an ambiguity, as described i… |
| RFC9051-6.4.4.3-1 — Refused SAVE arrives as tagged NO with NOTSAVED; '$' becomes empty | MUST | rev2 | fail | unimplemented: In some cases, the server MAY refuse to save a SEARCH (SAVE) result, for example, if an internal limit on the number of saved results is reached. In this case, … |
| RFC9051-6.4.5-1 — FETCH macros (ALL/FAST/FULL) must be used by themselves | MUST | rev2 | fail | unimplemented: There are three macros that specify commonly used sets of data items and can be used instead of data items. A macro must be used by itself and not in conjunctio… |
| RFC9051-6.4.5-2 — BINARY data items can only be requested for leaf body parts | MUST | rev2 | fail | unimplemented: Note that this data item can only be requested for leaf body parts: those that have media types other than multipart/*, message/rfc822, or message/global. |
| RFC9051-6.4.5-3 — BODY[<section>] implicitly sets \Seen; BODY.PEEK is the non-setting alternative | MUST | rev2 | fail | unimplemented: The \Seen flag is implicitly set; if this causes the flags to change, they SHOULD be included as part of the FETCH responses. ... An alternate form of BODY[<sec… |
| RFC9051-6.4.5-4 — Clients are cautioned that BINARY.SIZE can be expensive; avoid needless requests | SHOULD | rev2 | untestable | 'Needlessly issuing this request' binds the client's operational judgment about when the decoded size is actually needed, not a discrete wire action: any individual BINARY.SIZE request is syntactically legal and RFC-permitted, so a client that issues them freely produces the same compliant wire trace as one that issues them sparingly out of genuine need — whether a given request was 'needless' has no wire signature. Same reasoning as RFC9051-6.3.11-3 (the STATUS SIZE caution). |
| RFC9051-6.4.5.1-1 — Nested parts MUST be indicated by a period followed by the part number | MUST | rev2 | fail | unimplemented: Multipart messages are assigned consecutive part numbers, as they occur in the message. If a particular part is of type message or multipart, its parts MUST be … |
| RFC9051-6.4.5.1-2 — Part-specifier prefix rules; MIME MUST be prefixed by numeric specifiers | MUST | rev2 | fail | unimplemented: The HEADER, HEADER.FIELDS, HEADER.FIELDS.NOT, and TEXT part specifiers can be the sole part specifier or can be prefixed by one or more numeric part specifiers,… |
| RFC9051-6.4.6-1 — Untagged FETCH may arrive for external flag changes even with .SILENT | SHOULD | rev2 | fail | unimplemented: Regardless of whether or not the ".SILENT" suffix was used, the server SHOULD send an untagged FETCH response if a change to a message's flags from an external … |
| RFC9051-6.4.8-1 — COPYUID for MOVE arrives in an untagged OK before the EXPUNGEs | MUST | rev2 | fail | unimplemented: Servers are also REQUIRED to send the COPYUID response code in an untagged OK before sending EXPUNGE or similar responses. |
| RFC9051-6.4.8-2 — No sequence-number commands while the server is processing MOVE | MUST NOT | rev2 | fail | unimplemented: The server may send EXPUNGE responses before the tagged response, so the client cannot safely send more commands with message sequence number arguments while th… |
| RFC9051-6.4.9-1 — ESEARCH for UID SEARCH carries the UID indicator; its numbers are UIDs | MUST | rev2 | fail | unimplemented: The interpretation of the arguments is the same as with SEARCH; however, the numbers returned in an ESEARCH response for a UID SEARCH command are unique identif… |
| RFC9051-6.4.9-2 — Number after '*' in untagged FETCH/EXPUNGE is a sequence number, even for UID commands | MUST | rev2 | fail | unimplemented: The number after the "*" in an untagged FETCH or EXPUNGE response is always a message sequence number, not a unique identifier, even for a UID command response. |
| RFC9051-6.4.9-3 — FETCH responses caused by UID commands implicitly include the UID data item | MUST | rev2 | fail | unimplemented: However, server implementations MUST implicitly include the UID message data item as part of any FETCH response caused by a UID command, regardless of whether a… |
| RFC9051-6.5-1 — Non-spec commands MUST have an associated capability name | MUST | rev2 | untestable | Whether a non-standard command the client emits 'has a capability name associated with it' is a fact about the command's defining extension document, not about any observable wire behavior of this client: a session trace showing the client sending XFOO is identical whether or not some specification associates XFOO with a capability. Establishing compliance requires auditing documentation outside the protocol exchange. |
| RFC9051-7-1 — Client must be prepared to accept any server response at all times | MUST | rev2 | pass |  |
| RFC9051-7-2 — Client must remember certain critical server data when received | MUST | rev2 | pass |  |
| RFC9051-7-3 — Client should remember other server data for later reference | SHOULD | rev2 | untestable | Whether a client retains or discards server data for later reference is an internal state-management decision not directly observable via the wire protocol in a black-box test; the RFC's own 'can be ignored' escape hatch means no wire-observable pass/fail boundary exists. Matches RFC3501-7-3's internal-state classification. |
| RFC9051-7.1-1 — Client should ignore ALERT content received without TLS/SASL confidentiality | SHOULD | rev2 | fail | violation: Content of ALERT response codes received on a connection without TLS or SASL security-layer confidentiality SHOULD be ignored by clients. |
| RFC9051-7.1-2 — Client must clearly mark displayed ALERT text as potentially suspicious | MUST | rev2 | pass |  |
| RFC9051-7.1-3 — Client must present ALERT text to the user after TLS/SASL confidentiality is established | MUST | rev2 | pass |  |
| RFC9051-7.1-4 — Client interprets PERMANENTFLAGS to know which flags it can set permanently | MUST | rev2 | fail | unimplemented: Followed by a parenthesized list of flags and indicates which of the known flags the client can change permanently. Any flags that are in the FLAGS untagged res… |
| RFC9051-7.1-5 — Server must send new PERMANENTFLAGS without \* when keyword limit reached (client must track updated list) | MUST | rev2 | untestable | This sentence is a server-side MUST binding the server's re-send duty when its keyword limit is reached, not a client action. The client-side residue — treating a re-sent PERMANENTFLAGS (now lacking \*) as authoritative and no longer assuming arbitrary-keyword creation is possible — is already covered by the general PERMANENTFLAGS-tracking duty in RFC9051-7.1-4; there is no additional, separately observable client behavior distinguishing 'client correctly reacted to the keyword-limit re-send' from 'client just applies its ordinary PERMANENTFLAGS-tracking logic to whatever list arrives'. |
| RFC9051-7.1-6 — Client may retry APPEND/COPY/MOVE with CREATE after TRYCREATE hint | MAY | rev2 | fail | unimplemented: An APPEND, COPY, or MOVE attempt is failing because the target mailbox does not exist (as opposed to some other reason). This is a hint to the client that the o… |
| RFC9051-7.1-7 — Servers should avoid UIDNOTSTICKY mail stores (client must not expect APPENDUID/COPYUID there) | SHOULD NOT | rev2 | untestable | This SHOULD NOT binds server implementers' storage-architecture choices, not client behavior. The client-facing consequence — 'APPEND or COPY to this mailbox will not return an APPENDUID or COPYUID response code' — is a description of server output, and the client's corresponding duty is simply to not require those response codes when UIDNOTSTICKY is present, which is the absence of an assertion rather than a positive observable client behavior; no black-box wire trace distinguishes a client that 'expected' APPENDUID from one that didn't, since both proceed identically without it. |
| RFC9051-7.1-8 — Client must ignore unrecognized response codes | MUST | rev2 | pass |  |
| RFC9051-7.1-9 — CLOSED response code marks the response boundary between the previously and newly selected mailboxes | MUST | rev2 | fail | unimplemented: The CLOSED response code serves as a boundary between responses for the previously opened mailbox (which was closed) and the newly selected mailbox; all respons… |
| RFC9051-7.1.1-1 — Client accepts the untagged OK greeting and treats it as Not Authenticated state | MUST | rev2 | pass |  |
| RFC9051-7.1.1-2 — Client may present OK human-readable text to user as information | MAY | rev2 | untestable | Whether a client surfaces OK human-readable text to the user is an internal UI decision not observable via the wire protocol in a black-box test. Matches RFC3501-7.1.1-2's classification exactly: even with logger capture available, MAY grants pure permission with no constraining envelope, so surfacing and not surfacing are both compliant — no observation has a pass/fail boundary. No flip (unlike the ALERT MUST entries above). |
| RFC9051-7.1.4-1 — Client treats PREAUTH greeting as already authenticated | MUST | rev2 | pass |  |
| RFC9051-7.1.4-2 — Client requiring mandatory TLS must close connection on unprotected-port PREAUTH | MUST | rev2 | pass |  |
| RFC9051-7.1.5-1 — Client recognizes BYE greeting as connection rejection | MUST | rev2 | pass |  |
| RFC9051-7.1.5-2 — Client should continue reading responses after BYE until connection closes | SHOULD | rev2 | pass |  |
| RFC9051-7.2.1-1 — Client interprets ENABLED response as the authoritative set of successfully enabled extensions | MUST | rev2 | pass |  |
| RFC9051-7.2.2-1 — Client must recognize IMAP4rev2 in capability listing regardless of position | MUST | rev2 | pass |  |
| RFC9051-7.2.2-2 — Client must not issue LOGIN when LOGINDISABLED capability is advertised | MUST NOT | rev2 | pass |  |
| RFC9051-7.2.2-3 — Client must conform to base document semantics unless it uses a capability requiring otherwise | MUST | rev2 | untestable | These sentences are server MUSTs governing which response-format contract (RFC3501 vs RFC9051) the server itself must honor at a given moment; they define what the client may rely on but impose no separate client action. A client's compliance would only be observable through its interpretation of ambiguous wire data under dual-capability conditions — an internal parsing/dispatch decision with no distinct wire signature separable from ordinary response handling, matching the internal-decision theme's defining property (compliant and non-compliant implementations can produce identical wire traces). |
| RFC9051-7.2.2-4 — Client should not require capabilities beyond IMAP4rev2/STARTTLS/LOGINDISABLED and must ignore unknown ones | MUST | rev2 | pass |  |
| RFC9051-7.3.1-1 — Client encountering conflicting \HasChildren/\HasNoChildren must treat both as absent | SHOULD | rev2 | fail | unimplemented: A client that encounters a LIST response with both \HasChildren and \HasNoChildren attributes present should act as if both are absent in the LIST response. |
| RFC9051-7.3.1-2 — Client must be prepared for a mailbox marked \HasChildren to show no children in the LIST response | MUST | rev2 | fail | unimplemented: Note that even though the \HasChildren attribute for a mailbox must be correct at the time of processing the mailbox, a client must be prepared to deal with a s… |
| RFC9051-7.3.1-3 — Client must ignore unrecognized LIST extended fields | MUST | rev2 | fail | unimplemented: The client MUST ignore all extended fields it doesn't recognize. |
| RFC9051-7.3.1-4 — Client must treat mailbox name as valid LIST reference, and (unless \Noselect/\NonExistent) as valid selectable-command argument | MUST | rev2 | untestable | Both MUSTs bind the server (the name the server returns must be usable in those ways); the client-side counterpart is simply to use the returned name as-is in later commands, which is ordinary command construction covered elsewhere and has no separately observable pass/fail signature distinct from a client's general command-building behavior. |
| RFC9051-7.3.4-1 — Client must not assume any particular ordering of message numbers/UIDs in ESEARCH ALL data | MUST NOT | rev2 | untestable | This binds an internal assumption the client does not act on, not an observable behavior; the ESEARCH ALL return item is itself defined as a complete sequence-set (RFC9051-7.3.4-2), so a compliant client that processes the returned set correctly (regardless of the order the server happened to choose) produces identical downstream wire behavior to one that silently 'assumed' an order but happened not to be contradicted by this particular server's output. No black-box observation distinguishes 'does not assume ordering' from 'assumed an order matching what the server sent'. |
| RFC9051-7.3.4-2 — Server must always send ESEARCH response and complete sets even with no matches (client must handle absent MIN/MAX/ALL) | MUST | rev2 | untestable | These are server-side MUSTs about what the server includes/omits; the corresponding client duty is simply parsing an ESEARCH response whose MIN/MAX/ALL items may legitimately be absent — ordinary optional-field parsing with no distinct client behavior to assert beyond not crashing, which is already covered by the general parser-robustness expectation rather than a distinguishable protocol action. |
| RFC9051-7.3.5-1 — Client must remember the update from the FLAGS response | MUST | rev2 | pass |  |
| RFC9051-7.4.1-1 — Client must remember the update from the EXISTS response | MUST | rev2 | pass |  |
| RFC9051-7.5.1-1 — Client must remember the update from the EXPUNGE response | MUST | rev2 | pass |  |
| RFC9051-7.5.2-1 — Client should interpret BODY section string per content transfer encoding, type, and subtype | SHOULD | rev2 | untestable | How a client internally interprets a BODY section string (decoding, content-type handling) is not directly observable through the IMAP wire protocol in a black-box test. Nor does the library's public API boundary help: interpretation per content transfer encoding/type/subtype is a rendering-layer duty that the library legitimately delegates to the consuming application by handing over the section data as received, so no API-output observation has a mandated pass/fail boundary at this layer. |
| RFC9051-7.5.2-2 — Client must decode transfer-encoded binary data to derive original binary | MUST | rev2 | untestable | Whether the client correctly decodes transfer-encoded binary data internally is not directly observable via the IMAP protocol in a black-box test; it is a processing obligation on received data. The duty binds whichever component derives the original binary; a protocol library that hands the raw transfer-encoded string to the consuming application (which then decodes) is compliant, so an API-output assertion (decoded vs raw) would encode an API design choice rather than the RFC duty. |
| RFC9051-7.5.2-3 — Client doing BODYSTRUCTURE fetch must be prepared to accept unknown extension data | MUST | rev2 | pass |  |
| RFC9051-7.5.2-4 — Client should treat NIL and the empty string as identical in ENVELOPE members | SHOULD | rev2 | pass |  |
| RFC9051-7.6-1 — Client must not send synchronizing-literal octets until server signals readiness | MUST NOT | rev2 | fail | unimplemented: The client is not permitted to send the octets of the synchronizing literal unless the server indicates that it is expected. This permits the server to process … |
| RFC9051-9-1 — ABNF alternative-rule priority: earlier rule takes priority | MUST | rev2 | pass |  |
| RFC9051-9-2 — Case-insensitivity of alphabetic token strings | MUST | rev2 | pass |  |
| RFC9051-9-3 — SP is exactly one space; TAB and LWSP substitution prohibited | MUST NOT | rev2 | pass |  |
| RFC9051-9-4 — ASCII NUL character (%x00) must never be used except in OCTET | MUST NOT | rev2 | pass |  |
| RFC9051-9-5 — Client MUST accept flag-extension flags | MUST | rev2 | pass |  |
| RFC9051-9-6 — All case variants of INBOX MUST be interpreted as INBOX | MUST | rev2 | pass |  |
| RFC9051-9-7 — Client MUST accept body-extension fields (cross-reference) | MUST | rev2 | fail | unimplemented: ; Future expansion. Client implementations ; MUST accept body-extension fields. Server ; implementations MUST NOT generate ; body-extension fields except as def… |
| RFC9051-11.1-1 — Client MUST comply with relevant TLS recommendations from RFC 8314 | MUST | rev2 | untestable | RFC 8314 is an external document establishing a broad basket of TLS/implicit-TLS recommendations (e.g., prefer implicit TLS, upgrade mechanisms, deployment guidance) rather than a single wire-observable behavior. Compliance with 'relevant recommendations' quantifies over an open-ended external document and cannot be reduced to one black-box assertion; the concrete, individually testable duties RFC 9051 pulls from that basket (TLS 1.2+ MUST, cipher-suite MUST, hostname-check MUST) are separately catalogued as RFC9051-11.1-2 through RFC9051-11.1-6 and RFC9051-11.2-1/2. |
| RFC9051-11.1-2 — Clients MUST implement TLS 1.2 or newer | MUST | rev2 | pass |  |
| RFC9051-11.1-3 — Use of TLS 1.3 is RECOMMENDED for clients | SHOULD | rev2 | pass |  |
| RFC9051-11.1-4 — Client MUST implement TLS_ECDHE_RSA_WITH_AES_128_GCM_SHA256 when using TLS 1.2 | MUST | rev2 | pass |  |
| RFC9051-11.1-5 — Other RFC 7525-recommended TLS 1.2 cipher suites are RECOMMENDED for clients | SHOULD | rev2 | pass |  |
| RFC9051-11.1-6 — Client MUST verify server hostname against server certificate during TLS negotiation | MUST | rev2 | pass |  |
| RFC9051-11.1-7 — Client MUST check result of STARTTLS command and TLS negotiation for acceptable security | MUST | rev2 | pass |  |
| RFC9051-11.2-1 — Client MUST implement both Implicit TLS and STARTTLS negotiation | MUST | rev2 | pass |  |
| RFC9051-11.2-2 — Clients SHOULD try both port 993 and port 143 (and both IPv4/IPv6) concurrently by default | SHOULD | rev2 | untestable | This client is a headless protocol library, not an end-user mail application: every connection it makes is initiated with a host/port explicitly supplied by the consuming application's configuration (there is no 'no configuration provided' state in which the library autonomously decides which port(s) to dial). The duty's own text carries an escape hatch — 'unless overridden by ... user configuration' — and for a library-shaped client that escape hatch is unconditionally in effect on every connection: the port is always user-supplied, so the SHOULD to concurrently race ports 'by default' never actually applies. Whether a given connection reflects 'the user configured a specific port' versus 'the client defaulted and should have raced' is a question about user intent invisible on the wire (and invisible in the API call) — indistinguishable from any other user-intent-policy duty in the taxonomy. No pass/fail boundary exists: a client that always uses the configured port is compliant precisely because the escape hatch always applies. |
| RFC9051-11.3-1 — Before authentication, clients SHOULD ignore responses other than CAPABILITY and status responses | SHOULD | rev2 | pass |  |
| RFC9051-11.3-2 — Clients SHOULD ignore the ALERT response code until TLS/SASL confidentiality is negotiated | SHOULD | rev2 | pass |  |
| RFC9051-11.3-3 — Outside selected state, clients MUST ignore responses/response codes related to message and mailbox status | MUST | rev2 | pass |  |
| RFC9051-A-1 — Client MUST issue ENABLE IMAP4rev2 to use IMAP4rev2 when both revisions are advertised | MUST | rev2 | fail | violation: If both IMAP4rev1 and IMAP4rev2 are advertised, an IMAP client that wants to use IMAP4rev2 MUST issue an "ENABLE IMAP4rev2" command. |
| RFC9051-A-2 — Clients intending IMAP4rev1-server compatibility MUST be compatible with the Mailbox International Naming Convention | MUST | rev2 | untestable | The sentence binds two distinct parties in one clause (servers advertising both revisions; clients intending IMAP4rev1-server compatibility) with a single compound 'MUST be compatible with' duty. For the client half, 'compatible with' the naming convention as a whole is an implementation-wide capability claim, not one wire-observable behavior: it is realized through the convention's individual constituent rules (each itemized separately below as RFC9051-A-4/A-5/A-6/A-7), and a black-box test can only exercise those constituent rules as they are individually triggered, not 'compatibility' as a holistic property. This pointer duty is retained for traceability; its operative content is realized through the testable sub-duties in Appendix A.1. |
| RFC9051-A-3 — Mailbox International Naming Convention support not required for IMAP4rev2-only clients | MAY | rev2 | untestable | This sentence carries no RFC 2119 keyword; it states a scoping fact ('not required') rather than a prohibition or mandate. Framed here as the MAY-equivalent baseline that RFC9051-A-2's conditional MUST narrows: a client that is IMAP4rev2-only has no compatibility duty at all, so there is no pass/fail wire behavior to test — non-observance and observance of the convention are both compliant for such a client. A black-box test cannot distinguish 'IMAP4rev2-only, convention not needed' from 'intends IMAP4rev1 compatibility but non-compliant', since both look identical unless the convention is actually triggered. Retagged from capability-inventory to user-intent-policy: the defining obstacle here is not an unobservable inventory of affordances but intent-invisibility — whether a given client 'intends to be compatible with IMAP4rev1 servers' (RFC9051-A-2's trigger condition) is exactly the kind of user/deployment intent the taxonomy's user-intent-policy theme describes: the wire shows what the client sent, never why it chose to (or not to) apply the convention. |
| RFC9051-A-4 — Modified base64 MUST NOT represent a printable US-ASCII character that can represent itself | MUST NOT | rev2 | fail | unimplemented: Modified base64 MUST NOT be used to represent any printing of a US-ASCII character that can represent itself. Only characters inside the modified base64 alphabe… |
| RFC9051-A-5 — Modified UTF-7 names MUST end in US-ASCII | MUST | rev2 | fail | unimplemented: However, all names start in US-ASCII and MUST end in US-ASCII; that is, a name that ends with a non-ASCII ISO-10646 character MUST end with a "-". |
| RFC9051-A-6 — Client MUST NOT depend on the server validating embedded-ampersand mailbox names | MUST NOT | rev2 | untestable | 'MUST NOT depend upon' constrains the client's internal assumption/reliance on server-side validation behavior, not a wire-observable action. A compliant client (that independently validates or simply never emits invalid names) and a non-compliant client that happens to only ever send valid names because it never exercised the failure path produce identical wire traces; the duty only becomes observable if the client actually emits a non-conformant embedded-'&' name — but sending such a name would itself violate the paired SHOULD NOT duty (RFC9051-A-7) rather than demonstrate reliance on server validation. There is no black-box action that isolates 'depends upon the server' from 'independently validates or never triggers the case'. |
| RFC9051-A-7 — Client SHOULD NOT attempt to create an embedded-ampersand mailbox name unless it complies with modified UTF-7 syntax | SHOULD NOT | rev2 | fail | unimplemented: However, client implementations MUST NOT depend upon the server doing this and SHOULD NOT attempt to create a mailbox name with an embedded "&" character unless… |
| RFC9051-A-8 — No implicit shift back to US-ASCII; null shifts in modified UTF-7 are not permitted | MUST NOT | rev2 | fail | unimplemented: There is no implicit shift from base64 to US-ASCII, and null shifts ("-&" while in base64; note that "&-" while in US-ASCII means "&") are not permitted. |
| RFC9051-D-1 — Client implementations have to expect 63-bit-long body part / message sizes | MUST | rev2 | pass |  |
| RFC8314-3.2-1 — Client MUST implement RFC 7817 certificate validation for Implicit TLS on IMAP | MUST | rev1 | pass |  |
| RFC8314-3.2-1 — Client MUST implement RFC 7817 certificate validation for Implicit TLS on IMAP | MUST | rev2 | pass |  |
| RFC8314-3.4-1 — Client SHOULD initiate exchange of TLS close alerts before closing an Implicit TLS connection | SHOULD | rev1 | untestable | Whether the client sends a TLS close_notify alert before closing the underlying TCP socket is, in principle, observable at the TLS record layer. However, this client's public API (Connection/Session in src/index) does not expose a driver-observable 'graceful TLS shutdown' surface distinct from ordinary socket teardown, and Node's tls.TLSSocket.end()/destroy() behavior around close_notify is an internal decision of the underlying platform TLS stack the library delegates to, not a client-authored protocol action the harness's ComplianceDriver can distinguish from an abrupt close without new low-level TLS-record instrumentation the harness does not currently have. A compliant (close_notify sent) and non-compliant (abrupt close) shutdown produce indistinguishable outcomes at the IMAP protocol layer this harness observes (both end the session); only TLS-record-level capture would surface the difference. |
| RFC8314-3.4-1 — Client SHOULD initiate exchange of TLS close alerts before closing an Implicit TLS connection | SHOULD | rev2 | untestable | Whether the client sends a TLS close_notify alert before closing the underlying TCP socket is, in principle, observable at the TLS record layer. However, this client's public API (Connection/Session in src/index) does not expose a driver-observable 'graceful TLS shutdown' surface distinct from ordinary socket teardown, and Node's tls.TLSSocket.end()/destroy() behavior around close_notify is an internal decision of the underlying platform TLS stack the library delegates to, not a client-authored protocol action the harness's ComplianceDriver can distinguish from an abrupt close without new low-level TLS-record instrumentation the harness does not currently have. A compliant (close_notify sent) and non-compliant (abrupt close) shutdown produce indistinguishable outcomes at the IMAP protocol layer this harness observes (both end the session); only TLS-record-level capture would surface the difference. |
| RFC8314-3.4-2 — Client MAY close the TCP connection without waiting for the server's TLS close alert | MAY | rev1 | untestable | MAY-level permission with no constraining envelope: waiting or not waiting for the server's TLS close response are both compliant, so no assertion has a pass/fail boundary. Also depends on RFC8314-3.4-1's close_notify send, which is itself untestable at this harness's observation layer (see that entry's rationale). |
| RFC8314-3.4-2 — Client MAY close the TCP connection without waiting for the server's TLS close alert | MAY | rev2 | untestable | MAY-level permission with no constraining envelope: waiting or not waiting for the server's TLS close response are both compliant, so no assertion has a pass/fail boundary. Also depends on RFC8314-3.4-1's close_notify send, which is itself untestable at this harness's observation layer (see that entry's rationale). |
| RFC8314-5-1 — MUAs SHOULD prominently indicate the confidentiality level of an account connection to the user | SHOULD | rev1 | untestable | This client is a headless protocol library with no user interface of its own (no lock icon, no visual/audio indicator); it exposes only the public logger callback and public events as notification channels (per the untestability-themes taxonomy's mechanism (a) analysis). Unlike RFC3501-7.1-1's ALERT-presentation duty (which was flipped to testable because it binds surfacing a specific, server-supplied piece of text through that channel), this duty asks for an ambient, always-on UI affordance ('lock icon', 'changed background color') describing the account's persistent confidentiality posture — there is no discrete server-supplied event to surface through the logger, and 'the client's connection is using TLS' is not itself modeled as a loggable occurrence anywhere in src/. Building and asserting on such a UI is an application-layer concern the library delegates to its consumer by design. |
| RFC8314-5-1 — MUAs SHOULD prominently indicate the confidentiality level of an account connection to the user | SHOULD | rev2 | untestable | This client is a headless protocol library with no user interface of its own (no lock icon, no visual/audio indicator); it exposes only the public logger callback and public events as notification channels (per the untestability-themes taxonomy's mechanism (a) analysis). Unlike RFC3501-7.1-1's ALERT-presentation duty (which was flipped to testable because it binds surfacing a specific, server-supplied piece of text through that channel), this duty asks for an ambient, always-on UI affordance ('lock icon', 'changed background color') describing the account's persistent confidentiality posture — there is no discrete server-supplied event to surface through the logger, and 'the client's connection is using TLS' is not itself modeled as a loggable occurrence anywhere in src/. Building and asserting on such a UI is an application-layer concern the library delegates to its consumer by design. |
| RFC8314-5-3 — MUA providing a confidentiality indication MUST NOT indicate confidentiality below TLS 1.1 + cert verification + minimum requirements | MUST NOT | rev1 | untestable | Constrains the content of the client's confidentiality INDICATION to the user (the 'lock icon' / background-color / audible affordance of RFC8314-5-1), not a wire behavior: it says the MUA must not TELL the user a connection is confidential unless it uses at least TLS 1.1 with certificate verification and meets the account's minimum confidentiality requirements. This headless protocol library has no such user-facing confidentiality indicator (same mechanism (a) analysis as RFC8314-5-1 and RFC8314-5.4-4): there is no discrete server-supplied event to route through the public logger, and the ambient 'this connection is confidential' UI affordance being constrained is delegated to the consuming application by design. The underlying TLS-1.1-floor / certificate-verification substance is separately and testably enforced at the connection level via RFC8314-5-2 / RFC9051-11.1-2 (TLS 1.2 MUST) and the cert-validation entries cross-referenced at RFC8314-3.2-1 / RFC8314-5.3-1 — this entry's untestability is about the indication framing, not the TLS/cert content. |
| RFC8314-5-3 — MUA providing a confidentiality indication MUST NOT indicate confidentiality below TLS 1.1 + cert verification + minimum requirements | MUST NOT | rev2 | untestable | Constrains the content of the client's confidentiality INDICATION to the user (the 'lock icon' / background-color / audible affordance of RFC8314-5-1), not a wire behavior: it says the MUA must not TELL the user a connection is confidential unless it uses at least TLS 1.1 with certificate verification and meets the account's minimum confidentiality requirements. This headless protocol library has no such user-facing confidentiality indicator (same mechanism (a) analysis as RFC8314-5-1 and RFC8314-5.4-4): there is no discrete server-supplied event to route through the public logger, and the ambient 'this connection is confidential' UI affordance being constrained is delegated to the consuming application by design. The underlying TLS-1.1-floor / certificate-verification substance is separately and testably enforced at the connection level via RFC8314-5-2 / RFC9051-11.1-2 (TLS 1.2 MUST) and the cert-validation entries cross-referenced at RFC8314-3.2-1 / RFC8314-5.3-1 — this entry's untestability is about the indication framing, not the TLS/cert content. |
| RFC8314-5-2 — MUAs MUST implement TLS 1.2 or later | MUST | rev1 | pass |  |
| RFC8314-5-2 — MUAs MUST implement TLS 1.2 or later | MUST | rev2 | pass |  |
| RFC8314-5-4 — MUAs SHOULD implement the recommended TLS ciphersuites of RFC 7525 (or a successor) | SHOULD | rev1 | untestable | Asserts the implementation's TLS ciphersuite inventory includes the RFC 7525 recommended set. This client does not select or enumerate TLS ciphersuites itself: it delegates the entire TLS handshake — including ciphersuite negotiation — to Node's tls module / the underlying OpenSSL build, and exposes no ciphersuite-selection surface in its public API (src/connection). The set of offered ciphersuites is therefore a property of the host runtime's OpenSSL configuration, not a client-authored decision this harness can enumerate or assert against across the whole negotiation space — the defining capability-inventory shape. This mirrors the RFC 2595 / RFC 8314 environment reasoning applied elsewhere, but is filed as capability-inventory rather than environment-limit because the RFC 7525 suites ARE modern and negotiable by the platform (nothing in the environment prevents them); the gap is purely that the client offers no ciphersuite-selection affordance of its own to inspect. |
| RFC8314-5-4 — MUAs SHOULD implement the recommended TLS ciphersuites of RFC 7525 (or a successor) | SHOULD | rev2 | untestable | Asserts the implementation's TLS ciphersuite inventory includes the RFC 7525 recommended set. This client does not select or enumerate TLS ciphersuites itself: it delegates the entire TLS handshake — including ciphersuite negotiation — to Node's tls module / the underlying OpenSSL build, and exposes no ciphersuite-selection surface in its public API (src/connection). The set of offered ciphersuites is therefore a property of the host runtime's OpenSSL configuration, not a client-authored decision this harness can enumerate or assert against across the whole negotiation space — the defining capability-inventory shape. This mirrors the RFC 2595 / RFC 8314 environment reasoning applied elsewhere, but is filed as capability-inventory rather than environment-limit because the RFC 7525 suites ARE modern and negotiable by the platform (nothing in the environment prevents them); the gap is purely that the client offers no ciphersuite-selection affordance of its own to inspect. |
| RFC8314-5-5 — MUA not requiring minimum confidentiality SHOULD detect TLS availability and offer to upgrade the account | SHOULD | rev1 | untestable | Binds two affordances this headless library does not have: (a) an autonomous 'detect when TLS becomes available' capability (via RFC 6186 SRV discovery or 'other means'), which is the same absent SRV/auto-configuration affordance gated in RFC8314-5.1-1 through -5.1-6 (host/port/TLS mode are always caller-supplied, there is no discovery code path), and (b) an 'offer to upgrade the account' UI action at the account-management layer this per-connection library does not model. A duty conditioned on absent capabilities has no exercise path, and the absence itself is a capability-inventory fact outside black-box reach. |
| RFC8314-5-5 — MUA not requiring minimum confidentiality SHOULD detect TLS availability and offer to upgrade the account | SHOULD | rev2 | untestable | Binds two affordances this headless library does not have: (a) an autonomous 'detect when TLS becomes available' capability (via RFC 6186 SRV discovery or 'other means'), which is the same absent SRV/auto-configuration affordance gated in RFC8314-5.1-1 through -5.1-6 (host/port/TLS mode are always caller-supplied, there is no discovery code path), and (b) an 'offer to upgrade the account' UI action at the account-management layer this per-connection library does not model. A duty conditioned on absent capabilities has no exercise path, and the absence itself is a capability-inventory fact outside black-box reach. |
| RFC8314-5.1-1 — User-configurable MUAs SHOULD support RFC 6186 SRV records for account setup | SHOULD | rev1 | untestable | Asserts the implementation possesses an autonomous account-setup/discovery affordance (DNS SRV lookup per RFC 6186) that this client does not have and, by its own architecture, delegates entirely to the consuming application: this is a headless protocol library where every connection's host/port is supplied explicitly by the caller (src/index Connection/Session constructors take host/port directly), with no 'discover servers for this account' code path to exercise or assert absence of. Same capability-inventory pattern as the already-catalogued RFC9051-11.2-2 rationale for this exact client shape (test/compliance/catalog/rfc9051/s9-syntax-security.ts). |
| RFC8314-5.1-1 — User-configurable MUAs SHOULD support RFC 6186 SRV records for account setup | SHOULD | rev2 | untestable | Asserts the implementation possesses an autonomous account-setup/discovery affordance (DNS SRV lookup per RFC 6186) that this client does not have and, by its own architecture, delegates entirely to the consuming application: this is a headless protocol library where every connection's host/port is supplied explicitly by the caller (src/index Connection/Session constructors take host/port directly), with no 'discover servers for this account' code path to exercise or assert absence of. Same capability-inventory pattern as the already-catalogued RFC9051-11.2-2 rationale for this exact client shape (test/compliance/catalog/rfc9051/s9-syntax-security.ts). |
| RFC8314-5.1-2 — MUAs using RFC 6186 config SHOULD ignore advertised services below minimum confidentiality absent explicit user override | SHOULD | rev1 | untestable | Conditional on the client first possessing the SRV-based auto-configuration affordance of RFC8314-5.1-1, which this client does not implement (see that entry's rationale); a duty conditioned on an absent capability has no exercise path. |
| RFC8314-5.1-2 — MUAs using RFC 6186 config SHOULD ignore advertised services below minimum confidentiality absent explicit user override | SHOULD | rev2 | untestable | Conditional on the client first possessing the SRV-based auto-configuration affordance of RFC8314-5.1-1, which this client does not implement (see that entry's rationale); a duty conditioned on an absent capability has no exercise path. |
| RFC8314-5.1-3 — MUAs using RFC 6186 config SHOULD NOT auto-establish non-TLS configs when a TLS one is advertised | SHOULD NOT | rev1 | untestable | Same SRV-auto-configuration-affordance gate as RFC8314-5.1-1/-2: this client has no autonomous 'establish a new configuration from SRV records' code path for the duty to bind against. |
| RFC8314-5.1-3 — MUAs using RFC 6186 config SHOULD NOT auto-establish non-TLS configs when a TLS one is advertised | SHOULD NOT | rev2 | untestable | Same SRV-auto-configuration-affordance gate as RFC8314-5.1-1/-2: this client has no autonomous 'establish a new configuration from SRV records' code path for the duty to bind against. |
| RFC8314-5.1-4 — MUA choosing a non-TLS RFC 6186 config SHOULD warn the user before authenticating | SHOULD | rev1 | untestable | Downstream of RFC8314-5.1-3's SRV-auto-configuration gate: this client never autonomously chooses a non-TLS configuration from SRV records (no such affordance exists), so the warn-before-authenticating duty has no triggering state to observe. |
| RFC8314-5.1-4 — MUA choosing a non-TLS RFC 6186 config SHOULD warn the user before authenticating | SHOULD | rev2 | untestable | Downstream of RFC8314-5.1-3's SRV-auto-configuration gate: this client never autonomously chooses a non-TLS configuration from SRV records (no such affordance exists), so the warn-before-authenticating duty has no triggering state to observe. |
| RFC8314-5.1-5 — MUA establishing config from SRV records SHOULD verify DNSSEC signing or FQDN match | SHOULD | rev1 | untestable | Explicitly IMAP-naming duty, but gated on the same unimplemented SRV-based configuration-establishment affordance as RFC8314-5.1-1 through -5.1-4. |
| RFC8314-5.1-5 — MUA establishing config from SRV records SHOULD verify DNSSEC signing or FQDN match | SHOULD | rev2 | untestable | Explicitly IMAP-naming duty, but gated on the same unimplemented SRV-based configuration-establishment affordance as RFC8314-5.1-1 through -5.1-4. |
| RFC8314-5.1-6 — MUA MUST NOT consult unsigned SRV records on every connection attempt | MUST NOT | rev1 | untestable | A prohibition on a capability this client does not possess (per-connection SRV consultation) is vacuously satisfied by the absence of that capability, and the absence itself is a capability-inventory fact this harness cannot establish across the whole configuration space (per the taxonomy's capability-inventory defining property). |
| RFC8314-5.1-6 — MUA MUST NOT consult unsigned SRV records on every connection attempt | MUST NOT | rev2 | untestable | A prohibition on a capability this client does not possess (per-connection SRV consultation) is vacuously satisfied by the absence of that capability, and the absence itself is a capability-inventory fact this harness cannot establish across the whole configuration space (per the taxonomy's capability-inventory defining property). |
| RFC8314-5.1-7 — MUA MUST NOT test a Mail Account configuration by submitting credentials without a minimum-confidentiality TLS session | MUST NOT | rev1 | pass |  |
| RFC8314-5.1-7 — MUA MUST NOT test a Mail Account configuration by submitting credentials without a minimum-confidentiality TLS session | MUST NOT | rev2 | pass |  |
| RFC8314-5.2-1 — MUAs SHOULD, by default, require a minimum level of confidentiality per account | SHOULD | rev1 | untestable | Binds the existence of a policy affordance ('a default minimum-confidentiality requirement, configurable per account') at the MUA/account-management layer. This headless library has no account/profile concept of its own (host, port, and TLS mode are supplied per-connection by the caller via src/connection/types.ts options) and no 'default policy absent explicit configuration' state to observe — every connection's security mode is the caller's explicit choice. Same pattern as the already-catalogued RFC9051-11.2-2 rationale: the duty's escape hatch (caller-supplied configuration) is unconditionally in effect for a library-shaped client, so there is no black-box observable that separates a compliant default policy from its absence. |
| RFC8314-5.2-1 — MUAs SHOULD, by default, require a minimum level of confidentiality per account | SHOULD | rev2 | untestable | Binds the existence of a policy affordance ('a default minimum-confidentiality requirement, configurable per account') at the MUA/account-management layer. This headless library has no account/profile concept of its own (host, port, and TLS mode are supplied per-connection by the caller via src/connection/types.ts options) and no 'default policy absent explicit configuration' state to observe — every connection's security mode is the caller's explicit choice. Same pattern as the already-catalogued RFC9051-11.2-2 rationale: the duty's escape hatch (caller-supplied configuration) is unconditionally in effect for a library-shaped client, so there is no black-box observable that separates a compliant default policy from its absence. |
| RFC8314-5.2-2 — Default minimum confidentiality MUST require cert validation and SHOULD require TLS 1.1+ | MUST | rev1 | untestable | Conditional on the account-level 'default minimum confidentiality' policy affordance of RFC8314-5.2-1, which this client does not implement as a distinct policy layer (see that entry's rationale) — there is no 'new account' creation flow to observe a default against. |
| RFC8314-5.2-2 — Default minimum confidentiality MUST require cert validation and SHOULD require TLS 1.1+ | MUST | rev2 | untestable | Conditional on the account-level 'default minimum confidentiality' policy affordance of RFC8314-5.2-1, which this client does not implement as a distinct policy layer (see that entry's rationale) — there is no 'new account' creation flow to observe a default against. |
| RFC8314-5.2-3 — MUA disabling minimum confidentiality MUST warn the user privacy is not assured | MUST | rev1 | untestable | Conditional on the same account-level minimum-confidentiality-policy affordance (RFC8314-5.2-1) this client does not implement; there is no 'disable requirement' account-configuration action to trigger the warning against. |
| RFC8314-5.2-3 — MUA disabling minimum confidentiality MUST warn the user privacy is not assured | MUST | rev2 | untestable | Conditional on the same account-level minimum-confidentiality-policy affordance (RFC8314-5.2-1) this client does not implement; there is no 'disable requirement' account-configuration action to trigger the warning against. |
| RFC8314-5.2-4 — MUA requiring minimum confidentiality MUST NOT perform non-discovery operations until it is met | MUST NOT | rev1 | pass |  |
| RFC8314-5.2-4 — MUA requiring minimum confidentiality MUST NOT perform non-discovery operations until it is met | MUST NOT | rev2 | pass |  |
| RFC8314-5.2-5 — MUAs SHOULD NOT allow easy click-through access when minimum confidentiality is not met | SHOULD NOT | rev1 | untestable | 'Allow users to easily access' describes a UI affordance (a 'click through' dialog, per the RFC's own example) that this headless library has no equivalent of — it has no user-facing dialog/confirmation surface, only the public logger callback and events. Unlike the flipped RFC3501-7.1-1 ALERT duty, there is no discrete server-supplied message to route through the logger here; the duty is about the absence/presence of an application-level UI escape hatch, which is delegated to the consuming application by this library's architecture. |
| RFC8314-5.2-5 — MUAs SHOULD NOT allow easy click-through access when minimum confidentiality is not met | SHOULD NOT | rev2 | untestable | 'Allow users to easily access' describes a UI affordance (a 'click through' dialog, per the RFC's own example) that this headless library has no equivalent of — it has no user-facing dialog/confirmation surface, only the public logger callback and events. Unlike the flipped RFC3501-7.1-1 ALERT duty, there is no discrete server-supplied message to route through the logger here; the duty is about the absence/presence of an application-level UI escape hatch, which is delegated to the consuming application by this library's architecture. |
| RFC8314-5.2-6 — MUA without minimum confidentiality requirement SHOULD still attempt the most secure available means | SHOULD | rev1 | untestable | 'Most secure means available' is an internal preference-ordering decision the client makes when the caller has not pinned a specific security mode; for this library, the caller always explicitly selects the TLS mode via the TLSSetting configuration option (src/connection/types.ts: 'off' \| 'starttls' \| 'on'), so the library never autonomously chooses among Implicit TLS/STARTTLS/cleartext on the caller's behalf. A compliant 'always prefer the most secure means' internal default and a non-compliant one are wire-indistinguishable once the caller has supplied an explicit TLSSetting, and there is no 'no configuration supplied' state in which the library's own default-selection logic could be observed choosing between modes. |
| RFC8314-5.2-6 — MUA without minimum confidentiality requirement SHOULD still attempt the most secure available means | SHOULD | rev2 | untestable | 'Most secure means available' is an internal preference-ordering decision the client makes when the caller has not pinned a specific security mode; for this library, the caller always explicitly selects the TLS mode via the TLSSetting configuration option (src/connection/types.ts: 'off' \| 'starttls' \| 'on'), so the library never autonomously chooses among Implicit TLS/STARTTLS/cleartext on the caller's behalf. A compliant 'always prefer the most secure means' internal default and a non-compliant one are wire-indistinguishable once the caller has supplied an explicit TLSSetting, and there is no 'no configuration supplied' state in which the library's own default-selection logic could be observed choosing between modes. |
| RFC8314-5.3-1 — MUAs MUST validate TLS server certificates per RFC 7817 and PKIX | MUST | rev1 | pass |  |
| RFC8314-5.3-1 — MUAs MUST validate TLS server certificates per RFC 7817 and PKIX | MUST | rev2 | pass |  |
| RFC8314-5.3-2 — MUAs MAY support DANE as an additional certificate-validation means | MAY | rev1 | untestable | MAY-level permission asserting the possible existence of a DANE-validation affordance; this client implements no DANE/TLSA-record support (no DNS lookups of any kind appear in src/), and a MAY grants pure permission with no constraining envelope — absence is as compliant as presence, so no black-box assertion has a pass/fail boundary. |
| RFC8314-5.3-2 — MUAs MAY support DANE as an additional certificate-validation means | MAY | rev2 | untestable | MAY-level permission asserting the possible existence of a DANE-validation affordance; this client implements no DANE/TLSA-record support (no DNS lookups of any kind appear in src/), and a MAY grants pure permission with no constraining envelope — absence is as compliant as presence, so no black-box assertion has a pass/fail boundary. |
| RFC8314-5.3-3 — MUA supporting certificate pinning MUST NOT count it toward minimum confidentiality | MUST NOT | rev1 | untestable | Conditional on the client implementing certificate pinning at all (a MAY-gated affordance this library does not implement — src/ performs standard PKI chain validation with no persistent per-host trust-override store), and further conditioned on the internal 'minimum confidentiality' policy determination of RFC8314-5.2-1, itself untestable for this client shape. See RFC8314-5.4-* for the fuller certificate-pinning entries this MUST NOT summarizes. |
| RFC8314-5.3-3 — MUA supporting certificate pinning MUST NOT count it toward minimum confidentiality | MUST NOT | rev2 | untestable | Conditional on the client implementing certificate pinning at all (a MAY-gated affordance this library does not implement — src/ performs standard PKI chain validation with no persistent per-host trust-override store), and further conditioned on the internal 'minimum confidentiality' policy determination of RFC8314-5.2-1, itself untestable for this client shape. See RFC8314-5.4-* for the fuller certificate-pinning entries this MUST NOT summarizes. |
| RFC8314-5.4-1 — MUA MUST NOT offer certificate pinning except during Mail Account setup | MUST NOT | rev1 | untestable | Conditional on the client implementing certificate pinning at all (see RFC8314-5.3-3); this library implements no pinning affordance, offered at setup time or otherwise, so there is no 'offer pinning' UI action to observe the absence or presence of, and no 'Mail Account setup' concept distinct from an ordinary connection attempt. |
| RFC8314-5.4-1 — MUA MUST NOT offer certificate pinning except during Mail Account setup | MUST NOT | rev2 | untestable | Conditional on the client implementing certificate pinning at all (see RFC8314-5.3-3); this library implements no pinning affordance, offered at setup time or otherwise, so there is no 'offer pinning' UI action to observe the absence or presence of, and no 'Mail Account setup' concept distinct from an ordinary connection attempt. |
| RFC8314-5.4-2 — MUA MUST NOT let a certificate pinned for one account validate connections for another | MUST NOT | rev1 | untestable | Conditional on the client implementing certificate pinning at all, which it does not (see RFC8314-5.4-1); the duty additionally presupposes a multi-account model this per-connection library has no equivalent of. |
| RFC8314-5.4-2 — MUA MUST NOT let a certificate pinned for one account validate connections for another | MUST NOT | rev2 | untestable | Conditional on the client implementing certificate pinning at all, which it does not (see RFC8314-5.4-1); the duty additionally presupposes a multi-account model this per-connection library has no equivalent of. |
| RFC8314-5.4-3 — MUA supporting certificate pinning MUST allow the user to undo a pin | MUST | rev1 | untestable | Conditional on the client implementing certificate pinning at all, which it does not (see RFC8314-5.4-1); a revoke-pin affordance cannot be exercised or shown absent without the pinning feature itself first existing. |
| RFC8314-5.4-3 — MUA supporting certificate pinning MUST allow the user to undo a pin | MUST | rev2 | untestable | Conditional on the client implementing certificate pinning at all, which it does not (see RFC8314-5.4-1); a revoke-pin affordance cannot be exercised or shown absent without the pinning feature itself first existing. |
| RFC8314-5.4-4 — MUA MUST NOT indicate confidentiality is provided when authenticity relies on a pinned certificate | MUST NOT | rev1 | untestable | Compound gate: (a) conditional on the client implementing certificate pinning at all, which it does not (see RFC8314-5.4-1), and (b) even if it did, 'indicate to the user' describes the same UI-affordance category as RFC8314-5-1 (lock icon / confidentiality indicator) that this headless library delegates to its consuming application, with no discrete server-supplied event to route through the logger. |
| RFC8314-5.4-4 — MUA MUST NOT indicate confidentiality is provided when authenticity relies on a pinned certificate | MUST NOT | rev2 | untestable | Compound gate: (a) conditional on the client implementing certificate pinning at all, which it does not (see RFC8314-5.4-1), and (b) even if it did, 'indicate to the user' describes the same UI-affordance category as RFC8314-5-1 (lock icon / confidentiality indicator) that this headless library delegates to its consuming application, with no discrete server-supplied event to route through the logger. |
| RFC8314-5.5-1 — MUAs MAY implement client certificate authentication on the Implicit TLS port | MAY | rev1 | untestable | MAY-level permission with no constraining envelope, and this client implements no TLS client-certificate authentication affordance at all (src/connection has no client-cert configuration option) — absence is fully compliant with a MAY, and establishing non-existence of the affordance is itself a capability-inventory fact outside black-box reach. |
| RFC8314-5.5-1 — MUAs MAY implement client certificate authentication on the Implicit TLS port | MAY | rev2 | untestable | MAY-level permission with no constraining envelope, and this client implements no TLS client-certificate authentication affordance at all (src/connection has no client-cert configuration option) — absence is fully compliant with a MAY, and establishing non-existence of the affordance is itself a capability-inventory fact outside black-box reach. |
| RFC8314-5.5-2 — MUA MUST NOT provide a client certificate unless requested and authorized for that account | MUST NOT | rev1 | untestable | Conditional on the client implementing client-certificate authentication at all (see RFC8314-5.5-1), which it does not — there is no client-certificate-supplying code path whose restraint could be observed or violated. |
| RFC8314-5.5-2 — MUA MUST NOT provide a client certificate unless requested and authorized for that account | MUST NOT | rev2 | untestable | Conditional on the client implementing client-certificate authentication at all (see RFC8314-5.5-1), which it does not — there is no client-certificate-supplying code path whose restraint could be observed or violated. |
| RFC8314-5.5-3 — Installing a client cert for one account MUST NOT auto-authorize it for other accounts | MUST NOT | rev1 | untestable | Conditional on client-certificate authentication support (RFC8314-5.5-1, unimplemented) and a multi-account model this per-connection library has no equivalent of. |
| RFC8314-5.5-3 — Installing a client cert for one account MUST NOT auto-authorize it for other accounts | MUST NOT | rev2 | untestable | Conditional on client-certificate authentication support (RFC8314-5.5-1, unimplemented) and a multi-account model this per-connection library has no equivalent of. |
| RFC8314-5.5-4 — Client supporting client-cert auth on Implicit TLS MUST implement SASL EXTERNAL | MUST | rev1 | untestable | Conditional on the client implementing client-certificate authentication at all (see RFC8314-5.5-1); this client implements no client-certificate support and consequently no SASL EXTERNAL mechanism (grepping src/ for SASL mechanism implementations shows no EXTERNAL support), so the duty's trigger condition is never met and there is no AUTHENTICATE EXTERNAL exchange to observe. |
| RFC8314-5.5-4 — Client supporting client-cert auth on Implicit TLS MUST implement SASL EXTERNAL | MUST | rev2 | untestable | Conditional on the client implementing client-certificate authentication at all (see RFC8314-5.5-1); this client implements no client-certificate support and consequently no SASL EXTERNAL mechanism (grepping src/ for SASL mechanism implementations shows no EXTERNAL support), so the duty's trigger condition is never met and there is no AUTHENTICATE EXTERNAL exchange to observe. |
| RFC2595-2.1-1 — Implementations REQUIRED to implement TLS_DHE_DSS_WITH_3DES_EDE_CBC_SHA cipher suite | MUST | rev1 | untestable | 3DES suites are disabled at OpenSSL security level 2 in modern Node builds (and deprecated by RFC 8996); no modern TLS stack can negotiate the mandated suite, so compliance is unverifiable black-box on this platform. |
| RFC2595-2.1-1 — Implementations REQUIRED to implement TLS_DHE_DSS_WITH_3DES_EDE_CBC_SHA cipher suite | MUST | rev2 | untestable | 3DES suites are disabled at OpenSSL security level 2 in modern Node builds (and deprecated by RFC 8996); no modern TLS stack can negotiate the mandated suite, so compliance is unverifiable black-box on this platform. |
| RFC2595-2.2-1 — Clients SHOULD have a privacy-required operational mode refusing authentication without encryption | SHOULD | rev1 | untestable | Whether the client implementation possesses a distinct 'privacy operational mode' configuration is a capability-existence fact, not a wire behavior; a black-box test can exercise only whichever mode is currently configured, and absence of such a mode in one session does not establish absence across the implementation's whole configuration surface. No public knob exposes this mode today. |
| RFC2595-2.2-1 — Clients SHOULD have a privacy-required operational mode refusing authentication without encryption | SHOULD | rev2 | untestable | Whether the client implementation possesses a distinct 'privacy operational mode' configuration is a capability-existence fact, not a wire behavior; a black-box test can exercise only whichever mode is currently configured, and absence of such a mode in one session does not establish absence across the implementation's whole configuration surface. No public knob exposes this mode today. |
| RFC2595-2.2-2 — Clients MAY have an operational mode using encryption only when advertised, with authentication proceeding regardless | MAY | rev1 | untestable | MAY grants pure permission for an optional operational mode; there is no constraining envelope to test, and possession of such a mode is a capability-existence fact rather than an observable wire behavior. |
| RFC2595-2.2-2 — Clients MAY have an operational mode using encryption only when advertised, with authentication proceeding regardless | MAY | rev2 | untestable | MAY grants pure permission for an optional operational mode; there is no constraining envelope to test, and possession of such a mode is a capability-existence fact rather than an observable wire behavior. |
| RFC2595-2.3-1 — Clients implementing STARTTLS MUST be configurable to refuse clear-text login without adequate encryption | MUST | rev1 | untestable | 'MUST be configurable to refuse' asserts the existence of a configuration option; a black-box test can only exercise a configuration state that is set, not establish that a refuse-clear-text knob exists across the implementation's whole configuration space. No public knob of this kind is exposed by the client today. |
| RFC2595-2.3-1 — Clients implementing STARTTLS MUST be configurable to refuse clear-text login without adequate encryption | MUST | rev2 | untestable | 'MUST be configurable to refuse' asserts the existence of a configuration option; a black-box test can only exercise a configuration state that is set, not establish that a refuse-clear-text knob exists across the implementation's whole configuration space. No public knob of this kind is exposed by the client today. |
| RFC2595-2.4-1 — Client MUST check server hostname against certificate identity during TLS negotiation | MUST | rev1 | pass |  |
| RFC2595-2.4-1 — Client MUST check server hostname against certificate identity during TLS negotiation | MUST | rev2 | pass |  |
| RFC2595-2.4-2 — Client MUST use the connection hostname, not an insecurely-derived one, for certificate comparison | MUST | rev1 | untestable | Which hostname string the client internally feeds into certificate comparison is not observable via black-box IMAP protocol testing; the harness connects the client by an address it controls and cannot induce a divergent insecure-DNS-derived hostname path, and TLS SNI reveals what name the client sends, not what it compares against the certificate. Requires white-box inspection of the TLS implementation. |
| RFC2595-2.4-2 — Client MUST use the connection hostname, not an insecurely-derived one, for certificate comparison | MUST | rev2 | untestable | Which hostname string the client internally feeds into certificate comparison is not observable via black-box IMAP protocol testing; the harness connects the client by an address it controls and cannot induce a divergent insecure-DNS-derived hostname path, and TLS SNI reveals what name the client sends, not what it compares against the certificate. Requires white-box inspection of the TLS implementation. |
| RFC2595-2.4-3 — Client MUST NOT use a server hostname derived from an insecure remote source for certificate comparison | MUST NOT | rev1 | untestable | The provenance of the hostname string used internally for certificate verification is not observable via black-box IMAP protocol testing; requires white-box inspection of the TLS implementation. |
| RFC2595-2.4-3 — Client MUST NOT use a server hostname derived from an insecure remote source for certificate comparison | MUST NOT | rev2 | untestable | The provenance of the hostname string used internally for certificate verification is not observable via black-box IMAP protocol testing; requires white-box inspection of the TLS implementation. |
| RFC2595-2.4-4 — Client SHOULD support subjectAltName dNSName as source of server identity when present | SHOULD | rev1 | pass |  |
| RFC2595-2.4-4 — Client SHOULD support subjectAltName dNSName as source of server identity when present | SHOULD | rev2 | pass |  |
| RFC2595-2.4-5 — Certificate matching is case-insensitive | MUST | rev1 | pass |  |
| RFC2595-2.4-5 — Certificate matching is case-insensitive | MUST | rev2 | pass |  |
| RFC2595-2.4-6 — Client MAY use a leftmost wildcard character in certificate name matching | MAY | rev1 | untestable | The compliance driver connects the client by loopback IP address (127.0.0.1); wildcard certificate patterns (e.g. *.example.test) match DNS names only, never IP addresses, so wildcard matching can never be exercised on this harness without a local DNS resolver fixture connecting the client by hostname. |
| RFC2595-2.4-6 — Client MAY use a leftmost wildcard character in certificate name matching | MAY | rev2 | untestable | The compliance driver connects the client by loopback IP address (127.0.0.1); wildcard certificate patterns (e.g. *.example.test) match DNS names only, never IP addresses, so wildcard matching can never be exercised on this harness without a local DNS resolver fixture connecting the client by hostname. |
| RFC2595-2.4-7 — A certificate match against any one of multiple present names is acceptable | MUST | rev1 | pass |  |
| RFC2595-2.4-7 — A certificate match against any one of multiple present names is acceptable | MUST | rev2 | pass |  |
| RFC2595-2.4-8 — On hostname/certificate mismatch, client SHOULD ask for user confirmation or terminate and flag suspect identity | SHOULD | rev1 | pass |  |
| RFC2595-2.4-8 — On hostname/certificate mismatch, client SHOULD ask for user confirmation or terminate and flag suspect identity | SHOULD | rev2 | pass |  |
| RFC2595-2.5-1 — Client MUST check the result of STARTTLS and TLS negotiation for acceptable security | MUST | rev1 | pass |  |
| RFC2595-2.5-1 — Client MUST check the result of STARTTLS and TLS negotiation for acceptable security | MUST | rev2 | pass |  |
| RFC2595-3.1-1 — Client MUST NOT issue further commands after STARTTLS until server response and TLS negotiation complete | MUST NOT | rev1 | pass |  |
| RFC2595-3.1-1 — Client MUST NOT issue further commands after STARTTLS until server response and TLS negotiation complete | MUST NOT | rev2 | pass |  |
| RFC2595-3.1-2 — Client MUST discard cached server capability information once TLS has started | MUST | rev1 | pass |  |
| RFC2595-3.1-2 — Client MUST discard cached server capability information once TLS has started | MUST | rev2 | pass |  |
| RFC2595-3.1-3 — Client SHOULD re-issue CAPABILITY command once TLS has started | SHOULD | rev1 | pass |  |
| RFC2595-3.1-3 — Client SHOULD re-issue CAPABILITY command once TLS has started | SHOULD | rev2 | pass |  |
| RFC2595-3.2-1 — Compliant client MUST NOT issue LOGIN when LOGINDISABLED capability is present | MUST NOT | rev1 | pass |  |
| RFC2595-3.2-1 — Compliant client MUST NOT issue LOGIN when LOGINDISABLED capability is present | MUST NOT | rev2 | pass |  |
| RFC2595-9-1 — Client SHOULD warn the user when session privacy is not active, and/or refuse to proceed without acceptable security | SHOULD | rev1 | pass |  |
| RFC2595-9-1 — Client SHOULD warn the user when session privacy is not active, and/or refuse to proceed without acceptable security | SHOULD | rev2 | pass |  |
| RFC2595-9-2 — Client implementations SHOULD be configurable to refuse weak mechanisms or cipher suites | SHOULD | rev1 | untestable | 'SHOULD be configurable to refuse' asserts the existence of a configuration surface; a black-box test can exercise only whichever cipher-suite policy is currently configured (which is delegated to the underlying TLS stack/Node runtime in this client), not establish that a refuse-weak-suites knob exists as client-level configuration across the whole implementation surface. |
| RFC2595-9-2 — Client implementations SHOULD be configurable to refuse weak mechanisms or cipher suites | SHOULD | rev2 | untestable | 'SHOULD be configurable to refuse' asserts the existence of a configuration surface; a black-box test can exercise only whichever cipher-suite policy is currently configured (which is delegated to the underlying TLS stack/Node runtime in this client), not establish that a refuse-weak-suites knob exists as client-level configuration across the whole implementation surface. |
| RFC2595-9-3 — Clients MUST discard cached server-capability information from before the TLS handshake | MUST | rev1 | pass |  |
| RFC2595-9-3 — Clients MUST discard cached server-capability information from before the TLS handshake | MUST | rev2 | pass |  |
| RFC7817-3-1 — Client MUST check server identity against reference identifiers during TLS | MUST | rev1 | pass |  |
| RFC7817-3-1 — Client MUST check server identity against reference identifiers during TLS | MUST | rev2 | pass |  |
| RFC7817-3-2 — Identity check performed only after certification path validation | MUST | rev1 | untestable | This sentence carries no RFC 2119 keyword (it is descriptive: 'is only performed'), and it constrains the internal ordering of two verification steps (chain validation, then identity matching) inside the TLS/certificate stack. A black-box harness observes only the final accept/reject outcome of the connection attempt, not the sequence in which the underlying TLS library evaluated chain validity versus hostname identity — both a compliant and a non-compliant ordering can converge on the same wire-visible pass/fail. This is further downstream of the client's own code, since Node's TLS/OpenSSL stack performs path validation, not the IMAP client library itself. |
| RFC7817-3-2 — Identity check performed only after certification path validation | MUST | rev2 | untestable | This sentence carries no RFC 2119 keyword (it is descriptive: 'is only performed'), and it constrains the internal ordering of two verification steps (chain validation, then identity matching) inside the TLS/certificate stack. A black-box harness observes only the final accept/reject outcome of the connection attempt, not the sequence in which the underlying TLS library evaluated chain validity versus hostname identity — both a compliant and a non-compliant ordering can converge on the same wire-visible pass/fail. This is further downstream of the client's own code, since Node's TLS/OpenSSL stack performs path validation, not the IMAP client library itself. |
| RFC7817-3-3 — Matching performed per RFC 6125 §6 rules (order, pinning, failure procedure) | MUST | rev1 | untestable | This is a pointer/incorporation-by-reference sentence delegating the algorithmic details (identifier-type precedence order, optional certificate-pinning override, and the exact match-failure procedure) to RFC 6125 §6. The match-failure outcome itself is separately catalogued and testable as RFC9525-6.6-1 (connection must fail on mismatch). The internal precedence order in which a client tries DNS-ID vs. CN-ID vs. SRV-ID candidates before landing on a match/no-match verdict is not independently observable on the wire — only the final verdict is, and that verdict is what RFC9525-6.6-1 tests. 'Certificate pinning' is an optional client-local override mechanism (RFC 6125 §6.1) whose presence or absence cannot be established by a black-box protocol exchange. |
| RFC7817-3-3 — Matching performed per RFC 6125 §6 rules (order, pinning, failure procedure) | MUST | rev2 | untestable | This is a pointer/incorporation-by-reference sentence delegating the algorithmic details (identifier-type precedence order, optional certificate-pinning override, and the exact match-failure procedure) to RFC 6125 §6. The match-failure outcome itself is separately catalogued and testable as RFC9525-6.6-1 (connection must fail on mismatch). The internal precedence order in which a client tries DNS-ID vs. CN-ID vs. SRV-ID candidates before landing on a match/no-match verdict is not independently observable on the wire — only the final verdict is, and that verdict is what RFC9525-6.6-1 tests. 'Certificate pinning' is an optional client-local override mechanism (RFC 6125 §6.1) whose presence or absence cannot be established by a black-box protocol exchange. |
| RFC7817-3-4 — Reference identifiers for DNS-ID/CN-ID: email domain and/or connection hostname | MUST | rev1 | untestable | This constrains which internal value(s) the client selects as candidate reference identifiers before comparison, not the comparison outcome itself. A harness can only observe the connect hostname it supplied and the final accept/reject verdict on a given certificate; it cannot distinguish a client that internally used 'the hostname it used to open the connection' as its sole reference identifier from one that also (compliantly, per the MAY) derived and used a DNSSEC-validated value, when both converge on the same match/no-match result against a given certificate. This client is a headless library that receives a caller-supplied host option (see RFC3501-11.1-5/-6, which cover the MUST/MUST NOT half of hostname provenance already) and has no email-address-domain input at the protocol layer to begin with, so option (a) does not apply to this client's architecture; option (c) is a MAY and is vacuous for testing regardless. |
| RFC7817-3-4 — Reference identifiers for DNS-ID/CN-ID: email domain and/or connection hostname | MUST | rev2 | untestable | This constrains which internal value(s) the client selects as candidate reference identifiers before comparison, not the comparison outcome itself. A harness can only observe the connect hostname it supplied and the final accept/reject verdict on a given certificate; it cannot distinguish a client that internally used 'the hostname it used to open the connection' as its sole reference identifier from one that also (compliantly, per the MAY) derived and used a DNSSEC-validated value, when both converge on the same match/no-match result against a given certificate. This client is a headless library that receives a caller-supplied host option (see RFC3501-11.1-5/-6, which cover the MUST/MUST NOT half of hostname provenance already) and has no email-address-domain input at the protocol layer to begin with, so option (a) does not apply to this client's architecture; option (c) is a MAY and is vacuous for testing regardless. |
| RFC7817-3-5 — SRV-ID reference identifier required when using RFC 6186 service discovery | MUST | rev1 | untestable | This duty is conditional on the client implementing RFC 6186 DNS SRV/TXT-based email service autoconfiguration, a discovery mechanism this IMAP client library does not implement — it is given a host/port directly by its caller and performs no SRV lookup. A duty conditioned on possessing an unimplemented optional capability cannot be exercised; there is no reference-identifier-construction behavior to observe because the triggering precondition (RFC 6186 discovery) never occurs. Vacuously satisfied by non-implementation of RFC 6186, and there is no black-box test that distinguishes 'doesn't implement RFC 6186' from 'has a latent bug in SRV-ID reference-identifier construction' since neither path exists on the wire. |
| RFC7817-3-5 — SRV-ID reference identifier required when using RFC 6186 service discovery | MUST | rev2 | untestable | This duty is conditional on the client implementing RFC 6186 DNS SRV/TXT-based email service autoconfiguration, a discovery mechanism this IMAP client library does not implement — it is given a host/port directly by its caller and performs no SRV lookup. A duty conditioned on possessing an unimplemented optional capability cannot be exercised; there is no reference-identifier-construction behavior to observe because the triggering precondition (RFC 6186 discovery) never occurs. Vacuously satisfied by non-implementation of RFC 6186, and there is no black-box test that distinguishes 'doesn't implement RFC 6186' from 'has a latent bug in SRV-ID reference-identifier construction' since neither path exists on the wire. |
| RFC7817-3-6 — Supplemental identifier-type rules: DNS-ID required, SRV-ID conditional, URI-ID forbidden, CN-ID permitted, wildcard MAY | MUST NOT | rev1 | pass |  |
| RFC7817-3-6 — Supplemental identifier-type rules: DNS-ID required, SRV-ID conditional, URI-ID forbidden, CN-ID permitted, wildcard MAY | MUST NOT | rev2 | pass |  |
| RFC7817-3-7 — URI-ID identifier type MUST NOT be used for server verification | MUST NOT | rev1 | pass |  |
| RFC7817-3-7 — URI-ID identifier type MUST NOT be used for server verification | MUST NOT | rev2 | pass |  |
| RFC7817-A-1 — RFC 3501 §11.1 hostname-check paragraph is replaced by a pointer to RFC 7817 §3 | MUST | rev1 | pass |  |
| RFC7817-A-1 — RFC 3501 §11.1 hostname-check paragraph is replaced by a pointer to RFC 7817 §3 | MUST | rev2 | pass |  |
| RFC4422-3-1 — Client MUST NOT send an initial response when the mechanism does not allow client-first data | MUST NOT | rev1 | pass |  |
| RFC4422-3-1 — Client MUST NOT send an initial response when the mechanism does not allow client-first data | MUST NOT | rev2 | pass |  |
| RFC4422-3.1-1 — SASL mechanism names the client selects/sends are 1-20 uppercase ASCII/digit/hyphen/underscore characters | MUST | rev1 | pass |  |
| RFC4422-3.1-1 — SASL mechanism names the client selects/sends are 1-20 uppercase ASCII/digit/hyphen/underscore characters | MUST | rev2 | pass |  |
| RFC4422-3.2-1 — Client selects the best supported and suitable mechanism from the server's advertised list | MUST | rev1 | untestable | No RFC 2119 keyword; 'Commonly' signals a description of typical protocol behavior, and which mechanism a client judges 'best' among several it supports is an internal selection policy. Two compliant clients presented with the same AUTH= list can pick different (both 'suitable') mechanisms and produce different, equally compliant wire traces. A black-box test can observe which single mechanism name a client sent, but cannot observe the selection logic behind it, and RFC 4422 imposes no ordering rule the harness could check against (contrast RFC 3501/9051 §6.2.2's explicit 'decreasing order of preference' retry guidance, which is separately catalogued and is itself a MAY). |
| RFC4422-3.2-1 — Client selects the best supported and suitable mechanism from the server's advertised list | MUST | rev2 | untestable | No RFC 2119 keyword; 'Commonly' signals a description of typical protocol behavior, and which mechanism a client judges 'best' among several it supports is an internal selection policy. Two compliant clients presented with the same AUTH= list can pick different (both 'suitable') mechanisms and produce different, equally compliant wire traces. A black-box test can observe which single mechanism name a client sent, but cannot observe the selection logic behind it, and RFC 4422 imposes no ordering rule the harness could check against (contrast RFC 3501/9051 §6.2.2's explicit 'decreasing order of preference' retry guidance, which is separately catalogued and is itself a MAY). |
| RFC4422-3.3-1 — Client initiates the exchange by sending the chosen mechanism's name, optionally with an initial response | MUST | rev1 | pass |  |
| RFC4422-3.3-1 — Client initiates the exchange by sending the chosen mechanism's name, optionally with an initial response | MUST | rev2 | pass |  |
| RFC4422-3.4-1 — After receiving a challenge, the client's only two legal moves are to respond or abort | MAY | rev1 | pass |  |
| RFC4422-3.4-1 — After receiving a challenge, the client's only two legal moves are to respond or abort | MAY | rev2 | pass |  |
| RFC4422-3.4.1-1 — Authorization identity string the client sends MUST NOT contain NUL; absent and empty are equivalent | MUST NOT | rev1 | pass |  |
| RFC4422-3.4.1-1 — Authorization identity string the client sends MUST NOT contain NUL; absent and empty are equivalent | MUST NOT | rev2 | pass |  |
| RFC4422-3.4.1-2 — A non-empty authorization identity string the client sends requests to act as that specific identity | MUST | rev1 | untestable | No RFC 2119 keyword; the sentence states what a non-empty string *means* rather than constraining wire bytes. The client-side duty this implies — only send a non-empty authzid when the caller genuinely intends proxy authorization to that identity — is a matter of invoking-application intent that the wire cannot distinguish from any other reason a non-empty string might have been supplied. A black-box test can observe that a non-empty authzid was sent, never why. |
| RFC4422-3.4.1-2 — A non-empty authorization identity string the client sends requests to act as that specific identity | MUST | rev2 | untestable | No RFC 2119 keyword; the sentence states what a non-empty string *means* rather than constraining wire bytes. The client-side duty this implies — only send a non-empty authzid when the caller genuinely intends proxy authorization to that identity — is a matter of invoking-application intent that the wire cannot distinguish from any other reason a non-empty string might have been supplied. A black-box test can observe that a non-empty authzid was sent, never why. |
| RFC4422-3.5-1 — Client aborts an authentication exchange by sending a protocol-specific abort message | MAY | rev1 | pass |  |
| RFC4422-3.5-1 — Client aborts an authentication exchange by sending a protocol-specific abort message | MAY | rev2 | pass |  |
| RFC4422-3.6-1 — Client MUST install the negotiated security layer upon receipt of a successful outcome | MUST | rev1 | fail | unimplemented: If the outcome is successful and a security layer was negotiated, this layer is then installed. |
| RFC4422-3.6-1 — Client MUST install the negotiated security layer upon receipt of a successful outcome | MUST | rev2 | fail | unimplemented: If the outcome is successful and a security layer was negotiated, this layer is then installed. |
| RFC4422-3.7-1 — Client MUST close the connection if its active security layer cannot produce or decode buffers | MUST | rev1 | fail | unimplemented: If at any time the security layer is unable or unwilling to continue producing buffers protecting protocol data, the underlying transport connection MUST be clo… |
| RFC4422-3.7-1 — Client MUST close the connection if its active security layer cannot produce or decode buffers | MUST | rev2 | fail | unimplemented: If at any time the security layer is unable or unwilling to continue producing buffers protecting protocol data, the underlying transport connection MUST be clo… |
| RFC4422-3.7-2 — Client's outgoing protected buffer MUST NOT exceed the size the peer negotiated as its maximum | MUST | rev1 | fail | unimplemented: The length of the protected data buffer MUST be no larger than the maximum size that the other side expects. |
| RFC4422-3.7-2 — Client's outgoing protected buffer MUST NOT exceed the size the peer negotiated as its maximum | MUST | rev2 | fail | unimplemented: The length of the protected data buffer MUST be no larger than the maximum size that the other side expects. |
| RFC4422-3.7-3 — Client SHOULD close the connection on receipt of an oversized protected-buffer length field | SHOULD | rev1 | fail | unimplemented: Upon the receipt of a length field whose value is greater than the maximum size, the receiver SHOULD close the connection, as this might be a sign of an attack. |
| RFC4422-3.7-3 — Client SHOULD close the connection on receipt of an oversized protected-buffer length field | SHOULD | rev2 | fail | unimplemented: Upon the receipt of a length field whose value is greater than the maximum size, the receiver SHOULD close the connection, as this might be a sign of an attack. |
| RFC4422-6.1.1-1 — Client SHOULD close the connection when its security layer reports a data-integrity failure | SHOULD | rev1 | fail | unimplemented: Implementations SHOULD close the connection when the security services in a SASL security layer report protocol data report lack of data integrity. |
| RFC4422-6.1.1-1 — Client SHOULD close the connection when its security layer reports a data-integrity failure | SHOULD | rev2 | fail | unimplemented: Implementations SHOULD close the connection when the security services in a SASL security layer report protocol data report lack of data integrity. |
| RFC4422-6.1.2-1 — Client SHOULD NOT advertise, enter, or continue below its minimum security requirements, and SHOULD verify the negotiated outcome meets them | SHOULD NOT | rev1 | untestable | 'Minimum security requirements' is a deployment/configuration policy value the client (or its embedding application) sets for itself; RFC 4422 does not define what the minimum is or how it is expressed. A black-box test can observe which mechanism a client selected and whether it proceeded with an exchange, but cannot determine whether that mechanism met or violated a policy threshold that exists only in the client's own (unobservable, and in this library's case, currently nonexistent) configuration. This mirrors RFC3501-6.2.3-2's 'last resort' user-intent-policy determination. |
| RFC4422-6.1.2-1 — Client SHOULD NOT advertise, enter, or continue below its minimum security requirements, and SHOULD verify the negotiated outcome meets them | SHOULD NOT | rev2 | untestable | 'Minimum security requirements' is a deployment/configuration policy value the client (or its embedding application) sets for itself; RFC 4422 does not define what the minimum is or how it is expressed. A black-box test can observe which mechanism a client selected and whether it proceeded with an exchange, but cannot determine whether that mechanism met or violated a policy threshold that exists only in the client's own (unobservable, and in this library's case, currently nonexistent) configuration. This mirrors RFC3501-6.2.3-2's 'last resort' user-intent-policy determination. |
| RFC4422-6.1.5-1 — Client MUST NOT blindly allocate memory equal to an inbound protected buffer's declared size | MUST NOT | rev1 | untestable | 'Blindly allocate' describes an internal memory-management strategy (allocate unconditionally on the declared size vs. allocate incrementally / cap / stream). Two clients can both process the same oversized-length-field input without an out-of-memory crash — one by capping allocation, one by never having been vulnerable due to its runtime's allocator behavior — and a black-box harness cannot distinguish 'complied with this MUST NOT' from 'happened not to be affected'. The RFC's own follow-on clause (RFC4422-6.1.5-2, close the connection on an oversized block) is the wire-observable consequence a harness can actually assert; this entry is the internal-implementation-strategy antecedent of it. |
| RFC4422-6.1.5-1 — Client MUST NOT blindly allocate memory equal to an inbound protected buffer's declared size | MUST NOT | rev2 | untestable | 'Blindly allocate' describes an internal memory-management strategy (allocate unconditionally on the declared size vs. allocate incrementally / cap / stream). Two clients can both process the same oversized-length-field input without an out-of-memory crash — one by capping allocation, one by never having been vulnerable due to its runtime's allocator behavior — and a black-box harness cannot distinguish 'complied with this MUST NOT' from 'happened not to be affected'. The RFC's own follow-on clause (RFC4422-6.1.5-2, close the connection on an oversized block) is the wire-observable consequence a harness can actually assert; this entry is the internal-implementation-strategy antecedent of it. |
| RFC4422-6.1.5-2 — Client SHOULD close the connection on receipt of an oversized protected data buffer | SHOULD | rev1 | fail | unimplemented: When use of a security layer is negotiated by the authentication protocol exchange, the receiver SHOULD handle gracefully any protected data buffer larger than … |
| RFC4422-6.1.5-2 — Client SHOULD close the connection on receipt of an oversized protected data buffer | SHOULD | rev2 | fail | unimplemented: When use of a security layer is negotiated by the authentication protocol exchange, the receiver SHOULD handle gracefully any protected data buffer larger than … |
| RFC4422-A.1-1 — EXTERNAL is a client-first mechanism: client sends data first, or the server issues an empty challenge before the client's initial response | MUST | rev1 | pass |  |
| RFC4422-A.1-1 — EXTERNAL is a client-first mechanism: client sends data first, or the server issues an empty challenge before the client's initial response | MUST | rev2 | pass |  |
| RFC4422-A.1-2 — Client's EXTERNAL response is the UTF-8 encoding of the authorization identity string, empty or non-empty per the requested identity | MUST | rev1 | pass |  |
| RFC4422-A.1-2 — Client's EXTERNAL response is the UTF-8 encoding of the authorization identity string, empty or non-empty per the requested identity | MUST | rev2 | pass |  |
| RFC4422-A.1-3 — EXTERNAL is a single-message exchange: no additional challenges or responses follow the client's initial response | MUST | rev1 | pass |  |
| RFC4422-A.1-3 — EXTERNAL is a single-message exchange: no additional challenges or responses follow the client's initial response | MUST | rev2 | pass |  |
| RFC4616-1-1 — Advertise/use PLAIN only with adequate data security | SHOULD | rev1 | pass |  |
| RFC4616-1-1 — Advertise/use PLAIN only with adequate data security | SHOULD | rev2 | pass |  |
| RFC4616-2-1 — PLAIN client message format: authzid NUL authcid NUL passwd | MUST | rev1 | pass |  |
| RFC4616-2-1 — PLAIN client message format: authzid NUL authcid NUL passwd | MUST | rev2 | pass |  |
| RFC4616-2-2 — authzid/authcid/passwd/NUL transferred as UTF-8 | MUST | rev1 | pass |  |
| RFC4616-2-2 — authzid/authcid/passwd/NUL transferred as UTF-8 | MUST | rev2 | pass |  |
| RFC4616-2-3 — NUL MUST NOT appear inside authzid, authcid, or passwd | MUST NOT | rev1 | pass |  |
| RFC4616-2-3 — NUL MUST NOT appear inside authzid, authcid, or passwd | MUST NOT | rev2 | pass |  |
| RFC4616-2-4 — Discourage non-visible or hard-to-type characters | SHOULD NOT | rev1 | untestable | "Discouraged" has no RFC 2119 keyword and binds authoring/input choices for authcid/authzid content (typically end-user-supplied credentials or an operator-configured authzid), not client protocol behavior. The client has no way to judge or reject a caller-supplied identity string as "non-visible" or "hard to type on some keyboards" — that judgment is inherently about human input ergonomics/policy upstream of the wire, not a wire-observable encoding rule. Judgment call: recorded at SHOULD NOT strength given the advisory language, though the RFC itself uses no keyword here. |
| RFC4616-2-4 — Discourage non-visible or hard-to-type characters | SHOULD NOT | rev2 | untestable | "Discouraged" has no RFC 2119 keyword and binds authoring/input choices for authcid/authzid content (typically end-user-supplied credentials or an operator-configured authzid), not client protocol behavior. The client has no way to judge or reject a caller-supplied identity string as "non-visible" or "hard to type on some keyboards" — that judgment is inherently about human input ergonomics/policy upstream of the wire, not a wire-observable encoding rule. Judgment call: recorded at SHOULD NOT strength given the advisory language, though the RFC itself uses no keyword here. |
| RFC4616-5-1 — SHOULD NOT advertise/use PLAIN without adequate data security | SHOULD NOT | rev1 | pass |  |
| RFC4616-5-1 — SHOULD NOT advertise/use PLAIN without adequate data security | SHOULD NOT | rev2 | pass |  |
| RFC4616-5-2 — Encourage an operational mode disabling password-revealing mechanisms | MAY | rev1 | untestable | "Encouraged" is permissive/advisory with no RFC 2119 keyword, treated as MAY strength. The duty asserts the client should possess a configuration mode (an affordance) across its whole configuration surface; a black-box exchange can show whether PLAIN was used in one session but cannot establish the existence or absence of such an operational mode in general, and there is no public knob today to probe (mirrors the RFC3501/RFC9051 LOGIN-disable-knob capability-inventory entries already in this catalog). |
| RFC4616-5-2 — Encourage an operational mode disabling password-revealing mechanisms | MAY | rev2 | untestable | "Encouraged" is permissive/advisory with no RFC 2119 keyword, treated as MAY strength. The duty asserts the client should possess a configuration mode (an affordance) across its whole configuration surface; a black-box exchange can show whether PLAIN was used in one session but cannot establish the existence or absence of such an operational mode in general, and there is no public knob today to probe (mirrors the RFC3501/RFC9051 LOGIN-disable-knob capability-inventory entries already in this catalog). |
| RFC2195-2-1 — Client response is 'username SP digest' | MUST | rev1 | pass |  |
| RFC2195-2-1 — Client response is 'username SP digest' | MUST | rev2 | pass |  |
| RFC2195-2-2 — Digest is keyed-MD5(shared secret, timestamp-with-angle-brackets) | MUST | rev1 | pass |  |
| RFC2195-2-2 — Digest is keyed-MD5(shared secret, timestamp-with-angle-brackets) | MUST | rev2 | pass |  |
| RFC2195-2-3 — Digest is a 16-octet value sent as lower-case hex | MUST | rev1 | pass |  |
| RFC2195-2-3 — Digest is a 16-octet value sent as lower-case hex | MUST | rev2 | pass |  |
| RFC2195-2-4 — Base64 framing of CRAM challenge/response belongs to AUTHENTICATE, not CRAM | MUST | rev1 | pass |  |
| RFC2195-2-4 — Base64 framing of CRAM challenge/response belongs to AUTHENTICATE, not CRAM | MUST | rev2 | pass |  |
| RFC7628-3-1 — TLS MUST be used for OAUTHBEARER | MUST | rev1 | pass |  |
| RFC7628-3-1 — TLS MUST be used for OAUTHBEARER | MUST | rev2 | pass |  |
| RFC7628-3-2 — Client MUST send an additional message after a failed authentication | MUST | rev1 | pass |  |
| RFC7628-3-2 — Client MUST send an additional message after a failed authentication | MUST | rev2 | pass |  |
| RFC7628-3.1-1 — Initial client response is a GS2 header plus key/value pairs | MUST | rev1 | pass |  |
| RFC7628-3.1-1 — Initial client response is a GS2 header plus key/value pairs | MUST | rev2 | pass |  |
| RFC7628-3.1-2 — Client response MUST include the REQUIRED 'auth' key carrying the Bearer payload | MUST | rev1 | pass |  |
| RFC7628-3.1-2 — Client response MUST include the REQUIRED 'auth' key carrying the Bearer payload | MUST | rev2 | pass |  |
| RFC7628-3.1-3 — Single-kvsep client response is valid only in the authentication-failure context | MUST | rev1 | pass |  |
| RFC7628-3.1-3 — Single-kvsep client response is valid only in the authentication-failure context | MUST | rev2 | pass |  |
| RFC7628-3.2.3-1 — Client MUST send a dummy response or SASL abort after a failure challenge | MUST | rev1 | pass |  |
| RFC7628-3.2.3-1 — Client MUST send a dummy response or SASL abort after a failure challenge | MUST | rev2 | pass |  |
| XOAUTH2-format-1 — SASL XOAUTH2 initial client response byte format | MUST | rev1 | pass |  |
| XOAUTH2-format-1 — SASL XOAUTH2 initial client response byte format | MUST | rev2 | pass |  |
| XOAUTH2-format-2 — Base64 encoding per RFC 4648 | MUST | rev1 | pass |  |
| XOAUTH2-format-2 — Base64 encoding per RFC 4648 | MUST | rev2 | pass |  |
| XOAUTH2-exchange-1 — AUTHENTICATE XOAUTH2 command invocation | MUST | rev1 | pass |  |
| XOAUTH2-exchange-1 — AUTHENTICATE XOAUTH2 command invocation | MUST | rev2 | pass |  |
| XOAUTH2-exchange-2 — SASL-IR enables single-round-trip AUTHENTICATE | MAY | rev1 | pass |  |
| XOAUTH2-exchange-2 — SASL-IR enables single-round-trip AUTHENTICATE | MAY | rev2 | pass |  |
| XOAUTH2-error-1 — Client sends empty response after error challenge | MUST | rev1 | untestable | Observing this duty requires the harness to first induce the server side of an XOAUTH2 failure (an invalid/expired bearer token rejected with a base64 JSON error challenge) and then verify the client's very next line on the wire is exactly an empty CRLF continuation — which in turn requires the client to actually implement XOAUTH2 as a SASL mechanism. Per XOAUTH2-format-1's notes and this module's extractionNote, XOAUTH2 support is self-actualizing (unimplemented in this client today): there is no code path that sends AUTHENTICATE XOAUTH2, receives a challenge, or would emit any second-line response at all, so no black-box observation of 'what the client sends after an error challenge' is currently possible. Once XOAUTH2 auth is implemented client-side this becomes directly testable (arm a scripted server that replies to AUTHENTICATE XOAUTH2 with a base64 error challenge, assert the next client line is bare CRLF) — the gap is capability, not intrinsic unobservability of the wire behavior. |
| XOAUTH2-error-1 — Client sends empty response after error challenge | MUST | rev2 | untestable | Observing this duty requires the harness to first induce the server side of an XOAUTH2 failure (an invalid/expired bearer token rejected with a base64 JSON error challenge) and then verify the client's very next line on the wire is exactly an empty CRLF continuation — which in turn requires the client to actually implement XOAUTH2 as a SASL mechanism. Per XOAUTH2-format-1's notes and this module's extractionNote, XOAUTH2 support is self-actualizing (unimplemented in this client today): there is no code path that sends AUTHENTICATE XOAUTH2, receives a challenge, or would emit any second-line response at all, so no black-box observation of 'what the client sends after an error challenge' is currently possible. Once XOAUTH2 auth is implemented client-side this becomes directly testable (arm a scripted server that replies to AUTHENTICATE XOAUTH2 with a base64 error challenge, assert the next client line is bare CRLF) — the gap is capability, not intrinsic unobservability of the wire behavior. |
| XOAUTH2-error-2 — SASL protocol requires empty response to error challenge | MUST | rev1 | untestable | Same duty as XOAUTH2-error-1 (this sentence is the doc's own restatement, using 'requires' instead of describing it in the third person), so the same gap applies: XOAUTH2 authentication is not implemented client-side today, so there is no code path that could emit a post-challenge response of any shape, empty or otherwise, for the harness to observe. See XOAUTH2-error-1's rationale for the concrete future test sketch. |
| XOAUTH2-error-2 — SASL protocol requires empty response to error challenge | MUST | rev2 | untestable | Same duty as XOAUTH2-error-1 (this sentence is the doc's own restatement, using 'requires' instead of describing it in the third person), so the same gap applies: XOAUTH2 authentication is not implemented client-side today, so there is no code path that could emit a post-challenge response of any shape, empty or otherwise, for the harness to observe. See XOAUTH2-error-1's rationale for the concrete future test sketch. |
| RFC4959-3-1 — AUTHENTICATE accepts an optional initial-response second argument | MAY | rev1 | pass |  |
| RFC4959-3-2 — Client MUST base64-encode the initial response and use a pad character for zero-length responses | MUST | rev1 | pass |  |
| RFC4959-3-3 — Client MUST NOT send an initial response unless the server advertised SASL-IR, and MUST fall back to the standard exchange otherwise | MUST NOT | rev1 | pass |  |
| RFC5161-3.1-1 — Client SHOULD only include extensions that need to be enabled | SHOULD | rev1 | untestable | Whether a given extension 'needs to be enabled' is the client's own internal determination about its capability negotiation strategy, not something observable from the ENABLE command on the wire. A client that includes an extension it happens to support (but does not strictly need enabled) is behaviorally indistinguishable, at the protocol level, from one that first checks necessity before including it — both send a syntactically identical ENABLE argument list. No black-box test can distinguish a principled 'only if needed' client from an unconditional one. |
| RFC5161-3.1-2 — Client MUST NOT issue ENABLE after SELECT/EXAMINE | MUST NOT | rev1 | fail | unimplemented: Clients MUST NOT issue ENABLE once they SELECT/EXAMINE a mailbox |
| RFC5161-3.1-3 — Client (implicit) MUST only ENABLE extensions it itself supports | MUST | rev1 | untestable | No sentence in this document says in RFC 2119 terms 'a client MUST NOT name an extension it does not support'; the duty is inferred from ENABLE's stated purpose — an ENABLE argument is defined as 'an explicit indication from the client that it supports particular extensions', so naming an extension the client does not in fact support would make that indication false. Whether the client 'supports' a named extension is an internal fact about its own implementation, not something the ENABLE command's wire form can attest to either way: a truthful and an untruthful ENABLE argument list are syntactically identical, and any divergence would surface only through the named extension's own subsequent behavior (already covered, where testable, by that extension's own catalog entries) rather than through ENABLE itself. Judgment call: this is an implicit-MUST derived from the command's defining semantics rather than an explicit RFC 2119 keyword sentence, in the same vein as other implicit-MUST entries carried in this catalog family. |
| RFC5161-3.1-3 — Client (implicit) MUST only ENABLE extensions it itself supports | MUST | rev2 | untestable | No sentence in this document says in RFC 2119 terms 'a client MUST NOT name an extension it does not support'; the duty is inferred from ENABLE's stated purpose — an ENABLE argument is defined as 'an explicit indication from the client that it supports particular extensions', so naming an extension the client does not in fact support would make that indication false. Whether the client 'supports' a named extension is an internal fact about its own implementation, not something the ENABLE command's wire form can attest to either way: a truthful and an untruthful ENABLE argument list are syntactically identical, and any divergence would surface only through the named extension's own subsequent behavior (already covered, where testable, by that extension's own catalog entries) rather than through ENABLE itself. Judgment call: this is an implicit-MUST derived from the command's defining semantics rather than an explicit RFC 2119 keyword sentence, in the same vein as other implicit-MUST entries carried in this catalog family. |
| RFC5161-3.2-1 — Client (implicit) MUST NOT treat an empty/no-op ENABLE as an error | MUST | rev1 | pass |  |
| RFC5161-3.2-1 — Client (implicit) MUST NOT treat an empty/no-op ENABLE as an error | MUST | rev2 | pass |  |
| RFC4978-1-1 — Client MAY use COMPRESS or TLS compression; SHOULD prefer TLS if both available | MAY | rev1 | untestable | This is a compound permission-plus-preference sentence: the MAY clause grants pure permission (using COMPRESS at all is optional and any choice is compliant), and the RECOMMENDED (SHOULD-level) clause binds a policy choice between two compliant mechanisms (COMPRESS vs. TLS compression) that depends on what the deployment/operator has configured and prefers, not on any wire-observable pass/fail boundary — a client that chooses COMPRESS when both are available is still a fully compliant client engaging a fully specified extension. No black-box exchange can distinguish 'chose COMPRESS because TLS compression was unavailable' from 'chose COMPRESS despite TLS compression being available', and TLS compression itself is deprecated/disabled in modern stacks (RFC 7457/9325 discourage or forbid it), so the preference is largely moot in this environment regardless. |
| RFC4978-1-1 — Client MAY use COMPRESS or TLS compression; SHOULD prefer TLS if both available | MAY | rev2 | untestable | This is a compound permission-plus-preference sentence: the MAY clause grants pure permission (using COMPRESS at all is optional and any choice is compliant), and the RECOMMENDED (SHOULD-level) clause binds a policy choice between two compliant mechanisms (COMPRESS vs. TLS compression) that depends on what the deployment/operator has configured and prefers, not on any wire-observable pass/fail boundary — a client that chooses COMPRESS when both are available is still a fully compliant client engaging a fully specified extension. No black-box exchange can distinguish 'chose COMPRESS because TLS compression was unavailable' from 'chose COMPRESS despite TLS compression being available', and TLS compression itself is deprecated/disabled in modern stacks (RFC 7457/9325 discourage or forbid it), so the preference is largely moot in this environment regardless. |
| RFC4978-3-1 — No further commands until the COMPRESS result is seen | MUST NOT | rev1 | fail | unimplemented: The client MUST NOT send any further commands until it has seen the result of COMPRESS. |
| RFC4978-3-1 — No further commands until the COMPRESS result is seen | MUST NOT | rev2 | fail | unimplemented: The client MUST NOT send any further commands until it has seen the result of COMPRESS. |
| RFC4978-3-2 — Client MUST compress starting with the first command after a COMPRESS OK | MUST | rev1 | untestable | The observable core of this duty is the byte-level content of the client's first command after the tagged COMPRESS OK: a compliant client's bytes are DEFLATE-compressed octets, a non-compliant client's bytes are plaintext IMAP. The compliance driver has no DEFLATE/zlib codec on its server-scripting side, so it cannot inflate what it receives to verify a compressed frame arrived, nor can it distinguish a compressed frame from noise. This is an instrumental gap, not intrinsic: a DEFLATE-aware driver (Node's built-in zlib is readily available) could inflateRaw the stream and assert on the recovered plaintext, which is exactly why the theme is 'compressed-framing-opacity' and not 'environment-limit' (the latter is reserved for mechanisms the runtime genuinely cannot perform, e.g. RC4/3DES removed from OpenSSL). A single added codec on the driver's server-scripting side unblocks this entry; it is the first re-evaluation candidate for the theme. |
| RFC4978-3-2 — Client MUST compress starting with the first command after a COMPRESS OK | MUST | rev2 | untestable | The observable core of this duty is the byte-level content of the client's first command after the tagged COMPRESS OK: a compliant client's bytes are DEFLATE-compressed octets, a non-compliant client's bytes are plaintext IMAP. The compliance driver has no DEFLATE/zlib codec on its server-scripting side, so it cannot inflate what it receives to verify a compressed frame arrived, nor can it distinguish a compressed frame from noise. This is an instrumental gap, not intrinsic: a DEFLATE-aware driver (Node's built-in zlib is readily available) could inflateRaw the stream and assert on the recovered plaintext, which is exactly why the theme is 'compressed-framing-opacity' and not 'environment-limit' (the latter is reserved for mechanisms the runtime genuinely cannot perform, e.g. RC4/3DES removed from OpenSSL). A single added codec on the driver's server-scripting side unblocks this entry; it is the first re-evaluation candidate for the theme. |
| RFC4978-3-3 — Client MUST NOT turn on compression after a BAD or NO COMPRESS result | MUST NOT | rev1 | fail | unimplemented: If the server response was BAD or NO, the client MUST NOT turn on compression. |
| RFC4978-3-3 — Client MUST NOT turn on compression after a BAD or NO COMPRESS result | MUST NOT | rev2 | fail | unimplemented: If the server response was BAD or NO, the client MUST NOT turn on compression. |
| RFC4978-3-4 — Send-side layering order: compress, then SASL-sign/encrypt, then TLS-encrypt | MUST | rev1 | untestable | Verifying this layering order requires decoding the outermost (TLS and/or SASL security layer) wrapper and then inflating the DEFLATE payload beneath it to confirm compression happened before signing/encryption rather than after. The harness has no DEFLATE codec, so even where it can terminate TLS or a SASL security layer it cannot verify what lies beneath is a compressed stream in the mandated position. Instrumental gap, not intrinsic — a DEFLATE-aware driver with access to the negotiated layer secrets could peel each layer in order and assert on the recovered plaintext. COMPOUND DEPENDENCY (distinguishing this entry from 3-2): unblocking it requires not only the DEFLATE codec but ALSO a negotiated SASL security layer, which neither the client library nor the harness implements — so adding a codec alone makes 3-2 testable but leaves 3-4/3-5 still blocked on SASL-layer machinery. Both gaps are instrumental (the layered bytes exist on the wire and are decodable in principle with the right layer secrets); there is no deeper black-box barrier. First re-evaluation candidate for the theme once both the codec and a SASL security layer exist. |
| RFC4978-3-4 — Send-side layering order: compress, then SASL-sign/encrypt, then TLS-encrypt | MUST | rev2 | untestable | Verifying this layering order requires decoding the outermost (TLS and/or SASL security layer) wrapper and then inflating the DEFLATE payload beneath it to confirm compression happened before signing/encryption rather than after. The harness has no DEFLATE codec, so even where it can terminate TLS or a SASL security layer it cannot verify what lies beneath is a compressed stream in the mandated position. Instrumental gap, not intrinsic — a DEFLATE-aware driver with access to the negotiated layer secrets could peel each layer in order and assert on the recovered plaintext. COMPOUND DEPENDENCY (distinguishing this entry from 3-2): unblocking it requires not only the DEFLATE codec but ALSO a negotiated SASL security layer, which neither the client library nor the harness implements — so adding a codec alone makes 3-2 testable but leaves 3-4/3-5 still blocked on SASL-layer machinery. Both gaps are instrumental (the layered bytes exist on the wire and are decodable in principle with the right layer secrets); there is no deeper black-box barrier. First re-evaluation candidate for the theme once both the codec and a SASL security layer exist. |
| RFC4978-3-5 — Receive-side processing order MUST be the reverse of the send-side order | MUST | rev1 | untestable | The receive-side counterpart of 3-4: the client must undo TLS, then SASL, then DEFLATE, in that order, when processing server data. Confirming this requires the harness to construct a correctly-layered compressed-then-signed-then-encrypted server response and observe that the client successfully decodes it, which in turn requires a DEFLATE codec the harness does not have. Instrumental gap, same as 3-4 — and carrying the same COMPOUND DEPENDENCY: it needs both the DEFLATE codec and a negotiated SASL security layer, so a codec alone does not unblock it. No deeper black-box barrier. First re-evaluation candidate for the theme once both the codec and a SASL security layer exist. |
| RFC4978-3-5 — Receive-side processing order MUST be the reverse of the send-side order | MUST | rev2 | untestable | The receive-side counterpart of 3-4: the client must undo TLS, then SASL, then DEFLATE, in that order, when processing server data. Confirming this requires the harness to construct a correctly-layered compressed-then-signed-then-encrypted server response and observe that the client successfully decodes it, which in turn requires a DEFLATE codec the harness does not have. Instrumental gap, same as 3-4 — and carrying the same COMPOUND DEPENDENCY: it needs both the DEFLATE codec and a negotiated SASL security layer, so a codec alone does not unblock it. No deeper black-box barrier. First re-evaluation candidate for the theme once both the codec and a SASL security layer exist. |
| RFC4978-5-1 — Case-insensitive acceptance of COMPRESS/algorithm/resp-text-code strings | MUST | rev1 | fail | unimplemented: Implementations MUST accept these strings in a case-insensitive fashion. |
| RFC4978-5-1 — Case-insensitive acceptance of COMPRESS/algorithm/resp-text-code strings | MUST | rev2 | fail | unimplemented: Implementations MUST accept these strings in a case-insensitive fashion. |
| RFC8437-3-1 — Client must issue UNAUTHENTICATE only when advertised and in a valid state | MUST | rev1 | fail | unimplemented: Note that a BAD response only occurs if UNAUTHENTICATE is issued in an invalid state, is not advertised by the server, or does not follow the command syntax in … |
| RFC8437-3-1 — Client must issue UNAUTHENTICATE only when advertised and in a valid state | MUST | rev2 | fail | unimplemented: Note that a BAD response only occurs if UNAUTHENTICATE is issued in an invalid state, is not advertised by the server, or does not follow the command syntax in … |
| RFC8437-3-2 — UNAUTHENTICATE returns the connection to not authenticated state | MUST | rev1 | untestable | This sentence is descriptive ('directs the server to reset', 'is placed in') and binds the server's own state bookkeeping, not a client action — the reset itself is server-internal connection state (mailbox selection, cached identity, enabled extensions, etc., itemized in §4.1) that has no single client-observable wire signature beyond the tagged OK completion the client already receives. The client's *consequence* of this transition — that it must treat itself as not-authenticated afterward and may not issue authenticated/selected-state commands until it re-authenticates — is the client-facing half of this duty, and that half is realized as the re-authenticate entry RFC8437-3-5 (freedom/need to issue a new AUTHENTICATE or LOGIN) and via the general state-machine command-availability tests already exercised elsewhere in the catalog (RFC3501/RFC9051 §3 entries). No additional client wire behavior is uniquely attributable to 'not authenticated state was reached' beyond what a tagged OK plus subsequent command legality already shows. |
| RFC8437-3-2 — UNAUTHENTICATE returns the connection to not authenticated state | MUST | rev2 | untestable | This sentence is descriptive ('directs the server to reset', 'is placed in') and binds the server's own state bookkeeping, not a client action — the reset itself is server-internal connection state (mailbox selection, cached identity, enabled extensions, etc., itemized in §4.1) that has no single client-observable wire signature beyond the tagged OK completion the client already receives. The client's *consequence* of this transition — that it must treat itself as not-authenticated afterward and may not issue authenticated/selected-state commands until it re-authenticates — is the client-facing half of this duty, and that half is realized as the re-authenticate entry RFC8437-3-5 (freedom/need to issue a new AUTHENTICATE or LOGIN) and via the general state-machine command-availability tests already exercised elsewhere in the catalog (RFC3501/RFC9051 §3 entries). No additional client wire behavior is uniquely attributable to 'not authenticated state was reached' beyond what a tagged OK plus subsequent command legality already shows. |
| RFC8437-3-3 — Selected mailbox ceases to be selected without an expunge event | MUST | rev1 | fail | unimplemented: If a mailbox was selected, the mailbox ceases to be selected, but no expunge event is generated. |
| RFC8437-3-3 — Selected mailbox ceases to be selected without an expunge event | MUST | rev2 | fail | unimplemented: If a mailbox was selected, the mailbox ceases to be selected, but no expunge event is generated. |
| RFC8437-3-4 — Client's outgoing SASL security layer terminates after the UNAUTHENTICATE CRLF | MUST | rev1 | fail | unimplemented: If a Simple Authentication and Security Layer (SASL) [RFC4422] was active, the server terminates its outgoing security layer immediately after sending the CRLF … |
| RFC8437-3-4 — Client's outgoing SASL security layer terminates after the UNAUTHENTICATE CRLF | MUST | rev2 | fail | unimplemented: If a Simple Authentication and Security Layer (SASL) [RFC4422] was active, the server terminates its outgoing security layer immediately after sending the CRLF … |
| RFC8437-3-5 — Client is free to re-authenticate with a new AUTHENTICATE or LOGIN after UNAUTHENTICATE | MAY | rev1 | fail | unimplemented: After sending this command, the client is free to issue a new AUTHENTICATE or LOGIN command as permitted based on the server's capabilities. |
| RFC8437-3-5 — Client is free to re-authenticate with a new AUTHENTICATE or LOGIN after UNAUTHENTICATE | MAY | rev2 | fail | unimplemented: After sending this command, the client is free to issue a new AUTHENTICATE or LOGIN command as permitted based on the server's capabilities. |
| RFC8437-3-6 — Client may pipeline UNAUTHENTICATE with a subsequent AUTHENTICATE when no SASL layer is active | MAY | rev1 | fail | unimplemented: If no SASL security layer was active, the client is permitted to pipeline the UNAUTHENTICATE command with a subsequent AUTHENTICATE command. If the IMAP server … |
| RFC8437-3-6 — Client may pipeline UNAUTHENTICATE with a subsequent AUTHENTICATE when no SASL layer is active | MAY | rev2 | fail | unimplemented: If no SASL security layer was active, the client is permitted to pipeline the UNAUTHENTICATE command with a subsequent AUTHENTICATE command. If the IMAP server … |
| RFC8437-3-7 — Client may need to issue CAPABILITY after authentication to learn UNAUTHENTICATE availability | SHOULD | rev1 | fail | unimplemented: Servers MAY choose to advertise the UNAUTHENTICATE capability only after authentication has completed. As a result, clients may need to issue an IMAP CAPABILITY… |
| RFC8437-3-7 — Client may need to issue CAPABILITY after authentication to learn UNAUTHENTICATE availability | SHOULD | rev2 | fail | unimplemented: Servers MAY choose to advertise the UNAUTHENTICATE capability only after authentication has completed. As a result, clients may need to issue an IMAP CAPABILITY… |
| RFC8437-4.1-1 — Client's outgoing COMPRESS layer terminates after the UNAUTHENTICATE CRLF | MUST | rev1 | fail | unimplemented: If IMAP COMPRESS [RFC4978] is active, the server terminates its outgoing compression layer after it sends the CRLF following the OK response. The client termina… |
| RFC8437-4.1-1 — Client's outgoing COMPRESS layer terminates after the UNAUTHENTICATE CRLF | MUST | rev2 | fail | unimplemented: If IMAP COMPRESS [RFC4978] is active, the server terminates its outgoing compression layer after it sends the CRLF following the OK response. The client termina… |
| RFC8437-4.2-1 — UNAUTHENTICATE breaks the application-level TLS-credential binding without discarding the credentials | MUST | rev1 | untestable | Descriptive present tense ('breaks', 'does not discard'), no RFC 2119 keyword; judged as a MUST-level invariant per RFC 8174 since it states a mandatory outcome, not an option. This binds internal application-layer bookkeeping: whether the client retains its TLS client certificate/key material after UNAUTHENTICATE (it must) while no longer treating the prior authenticated identity as bound to that certificate at the IMAP application layer (it must not). The TLS layer itself is explicitly unaffected (per §3, 'except for the state of the TLS layer'), so nothing about the TLS connection changes on the wire; the only observable consequence is the client's subsequent ability to issue AUTHENTICATE EXTERNAL again using the same still-held credentials (RFC8437-4.2-2 captures that observable half). This entry's specific claim — that the *application-level binding* record, not the credential material, is what changes — is internal client state with no independent wire signature of its own. |
| RFC8437-4.2-1 — UNAUTHENTICATE breaks the application-level TLS-credential binding without discarding the credentials | MUST | rev2 | untestable | Descriptive present tense ('breaks', 'does not discard'), no RFC 2119 keyword; judged as a MUST-level invariant per RFC 8174 since it states a mandatory outcome, not an option. This binds internal application-layer bookkeeping: whether the client retains its TLS client certificate/key material after UNAUTHENTICATE (it must) while no longer treating the prior authenticated identity as bound to that certificate at the IMAP application layer (it must not). The TLS layer itself is explicitly unaffected (per §3, 'except for the state of the TLS layer'), so nothing about the TLS connection changes on the wire; the only observable consequence is the client's subsequent ability to issue AUTHENTICATE EXTERNAL again using the same still-held credentials (RFC8437-4.2-2 captures that observable half). This entry's specific claim — that the *application-level binding* record, not the credential material, is what changes — is internal client state with no independent wire signature of its own. |
| RFC8437-4.2-2 — Administrative client may answer a PREAUTH greeting with UNAUTHENTICATE then AUTHENTICATE EXTERNAL | MAY | rev1 | fail | unimplemented: As a result, TLS client certificates cannot be used for administrative proxy authentication with the imaps port unless the UNAUTHENTICATE command is also advert… |
| RFC8437-4.2-2 — Administrative client may answer a PREAUTH greeting with UNAUTHENTICATE then AUTHENTICATE EXTERNAL | MAY | rev2 | fail | unimplemented: As a result, TLS client certificates cannot be used for administrative proxy authentication with the imaps port unless the UNAUTHENTICATE command is also advert… |
| RFC7888-3-1 — Non-synchronizing literals usable only when LITERAL+ or LITERAL- is advertised; otherwise synchronizing form only | MUST | rev1 | fail | unimplemented: Non-synchronizing literals may be used with any IMAP server implementation that returns "LITERAL+" or "LITERAL-" as one of the supported capabilities to the CAP… |
| RFC7888-3-1 — Non-synchronizing literals usable only when LITERAL+ or LITERAL- is advertised; otherwise synchronizing form only | MUST | rev2 | fail | unimplemented: Non-synchronizing literals may be used with any IMAP server implementation that returns "LITERAL+" or "LITERAL-" as one of the supported capabilities to the CAP… |
| RFC7888-3-2 — Client is not required to wait for a continuation request before sending non-synchronizing literal octets | MAY | rev1 | fail | unimplemented: clients are not required to wait before sending the octets of a non-synchronizing literal. |
| RFC7888-3-2 — Client is not required to wait for a continuation request before sending non-synchronizing literal octets | MAY | rev2 | fail | unimplemented: clients are not required to wait before sending the octets of a non-synchronizing literal. |
| RFC7888-5-1 — Under LITERAL-, non-synchronizing literals MUST NOT exceed 4096 bytes | MUST NOT | rev1 | fail | unimplemented: when LITERAL- is advertised, non-synchronizing literals used in any command MUST NOT be larger than 4096 bytes. |
| RFC7888-5-1 — Under LITERAL-, non-synchronizing literals MUST NOT exceed 4096 bytes | MUST NOT | rev2 | fail | unimplemented: when LITERAL- is advertised, non-synchronizing literals used in any command MUST NOT be larger than 4096 bytes. |
| RFC7888-5-2 — Under LITERAL-, a literal larger than 4096 bytes MUST be sent as a synchronizing literal | MUST | rev1 | fail | unimplemented: Any literal larger than 4096 bytes MUST be sent as a synchronizing literal as specified in RFC 3501. |
| RFC7888-5-2 — Under LITERAL-, a literal larger than 4096 bytes MUST be sent as a synchronizing literal | MUST | rev2 | fail | unimplemented: Any literal larger than 4096 bytes MUST be sent as a synchronizing literal as specified in RFC 3501. |
| RFC7888-5-3 — Non-synchronizing literal syntax is unchanged under LITERAL- (still uses '+'); forms are not otherwise mixed | MUST | rev1 | fail | unimplemented: Note that the form of the non-synchronizing literal does not change: it still uses the "+" in the literal itself, even if the applicable extension is LITERAL-. |
| RFC7888-5-3 — Non-synchronizing literal syntax is unchanged under LITERAL- (still uses '+'); forms are not otherwise mixed | MUST | rev2 | fail | unimplemented: Note that the form of the non-synchronizing literal does not change: it still uses the "+" in the literal itself, even if the applicable extension is LITERAL-. |
| RFC6855-3-1 — Client MUST ENABLE UTF8=ACCEPT before using UTF-8 in quoted-strings | MUST | rev1 | pass |  |
| RFC6855-3-2 — ENABLE UTF8=ACCEPT is only valid in the authenticated state | MUST | rev1 | pass |  |
| RFC6855-3-3 — Client MAY use extended UTF-8 quoted syntax once UTF8=ACCEPT is enabled | MAY | rev1 | pass |  |
| RFC6855-3-4 — Client MUST NOT send SEARCH with a CHARSET specification after enabling UTF8=ACCEPT | MUST NOT | rev1 | fail | unimplemented: Once an IMAP client has enabled UTF-8 support with the "ENABLE UTF8=ACCEPT" command, it MUST NOT issue a "SEARCH" command that contains a charset specification. |
| RFC6855-4-1 — Client MUST use the UTF8 APPEND data extension to send UTF-8 message headers | MUST | rev1 | fail | unimplemented: A client that sends a message with UTF-8 headers to the server MUST send them using the "UTF8" data extension to the "APPEND" command. |
| RFC6855-4-2 — Client MAY reuse the UTF8 data extension inside a CATENATE part | MAY | rev1 | fail | unimplemented: If the server also advertises the "CATENATE" capability [RFC4469], the client can use the same data extension to include such a message in a catenated message p… |
| RFC6855-5-1 — LOGIN is not extended for UTF-8 usernames/passwords | MUST NOT | rev1 | pass |  |
| RFC6855-5-2 — Client MUST use AUTHENTICATE for UTF-8 usernames/passwords | MUST | rev1 | pass |  |
| RFC6855-6-1 — Client MUST ENABLE UTF8=ACCEPT before using a UTF8=ONLY server | MUST | rev1 | pass |  |
| RFC6855-6-2 — Client always sends ENABLE UTF8=ACCEPT, never ENABLE UTF8=ONLY | MUST NOT | rev1 | pass |  |
| RFC6855-6-3 — Clients are encouraged to detect UTF8=ONLY and inform the user if unsupported | SHOULD | rev1 | untestable | 'Encouraged to... provide an informative error message to the end-user' is a user-facing presentation duty conditioned on the client's own internal judgment of whether UTF8=ONLY support is 'problematic' for it — a client that fully implements UTF8=ONLY has nothing to report and is equally compliant. Even for a client that does find it problematic, 'informative error message' has no wire signature: this headless library's only user-facing notification channels are the public logger callback and public events (per the taxonomy's ui-presentation mechanism), and no wire-observable action distinguishes 'silently declined to proceed' from 'declined and informed the user via some out-of-protocol UI the harness cannot see'. Even with logger capture, 'informative' has no crisp pass/fail content-quality boundary the way the ALERT-text-presence duty (RFC3501-7.1-1) does. Graded SHOULD, not MUST ('are encouraged to' + lowercase intent, softer than even a SHOULD keyword — RFC 6855 predates RFC 8174; judgment call rounding this advisory encouragement up to the nearest keyword tier), which further weakens any pass/fail envelope. |
| RFC6855-7-1 — Client upgrading UTF8=ACCEPT-awareness MUST discard its message cache | MUST | rev1 | untestable | Full sentence context: 'in order to cope with the case when a server compliant with this extension returns the same UIDVALIDITY to both legacy and "UTF8=ACCEPT"-aware clients, a client upgraded from being non-"UTF8=ACCEPT"-aware MUST discard its cache of messages downloaded from the server.' This binds whether a (hypothetical, consumer-side) local message cache is discarded across a software-version upgrade boundary — the same defining property as the taxonomy's internal-state theme (cf. RFC3501-2.2.2-3/-4, RFC9051-2.3.1.1-1's cross-session discussion): this headless library keeps no cross-session message cache of its own (caching, if any, belongs to the consuming application per the content-processing theme's API-boundary delegation argument), and the trigger condition ('upgraded from being non-aware') is a fact about client software versioning across time, not a single-session wire behavior. No two-session scripted scenario can distinguish a compliant discard from a stale-cache retention purely on the wire, because the library itself has no cache to inspect; the only wire-observable downstream effect (issuing a UID command against a since-invalidated UID) is already covered by testable, command-specific UID entries elsewhere in the catalog, not by this general caching duty. |
| RFC6855-7-2 — Downgrading implementations SHOULD use a standardized algorithm (RFC 6857 or RFC 6858) | SHOULD | rev1 | untestable | 'Downgrading' in §7 is the server-side (or gateway-side) act of constructing a surrogate/simplified message representation for a client that has not enabled UTF-8 support — the surrounding paragraphs frame this entirely as an implementer/operator choice about server or mail-store behavior ('choices available to the server include...'). This IMAP client library does not perform message downgrading at all (it consumes whatever the server sends); the sentence's 'implementations' scope is most naturally the server/gateway side, not this client. Recorded as conditionally client-binding only in the narrow, unlikely case that some future consumer built on this library implements its own client-side downgrade/surrogate-construction logic for locally cached messages — which RFC 6857/6858 conformance is then an out-of-band algorithm-choice fact about that consumer's code, not observable on the wire between this client and an IMAP server. No black-box protocol observation can establish which (if any) downgrade algorithm a piece of software implements internally. |
| RFC4315-2.1-1 — UID EXPUNGE only expunges \Deleted messages within the given UID set | MUST | rev1 | fail | unimplemented: The UID EXPUNGE command permanently removes all messages that both have the \Deleted flag set and have a UID that is included in the specified sequence set from… |
| RFC4315-2.1-1 — UID EXPUNGE only expunges \Deleted messages within the given UID set | MUST | rev2 | fail | unimplemented: The UID EXPUNGE command permanently removes all messages that both have the \Deleted flag set and have a UID that is included in the specified sequence set from… |
| RFC4315-2.1-2 — Fallback when server lacks UIDPLUS: STORE-toggle \Deleted around EXPUNGE | SHOULD | rev1 | untestable | This is a keyword-less recommendation (lowercase 'should', graded SHOULD by judgment) describing an internal fallback strategy the client may adopt when the server does not advertise UIDPLUS. Which resynchronization strategy a client chooses — this STORE-toggle-then-EXPUNGE dance, the plain-EXPUNGE shortcut of 2.1-3, or some other application-level approach — is an internal implementation decision with no single mandated wire form: a client that never marks messages \Deleted in the first place, or that manages deletion entirely differently, is equally compliant and would emit none of these commands. No black-box exchange can distinguish 'chose not to run the STORE-toggle fallback because it uses a different deletion model' from 'failed to implement the recommended fallback', so there is no distinguishable pass/fail boundary. Conditional on the server lacking UIDPLUS and on the client wanting selective expunge behavior at all. |
| RFC4315-2.1-2 — Fallback when server lacks UIDPLUS: STORE-toggle \Deleted around EXPUNGE | SHOULD | rev2 | untestable | This is a keyword-less recommendation (lowercase 'should', graded SHOULD by judgment) describing an internal fallback strategy the client may adopt when the server does not advertise UIDPLUS. Which resynchronization strategy a client chooses — this STORE-toggle-then-EXPUNGE dance, the plain-EXPUNGE shortcut of 2.1-3, or some other application-level approach — is an internal implementation decision with no single mandated wire form: a client that never marks messages \Deleted in the first place, or that manages deletion entirely differently, is equally compliant and would emit none of these commands. No black-box exchange can distinguish 'chose not to run the STORE-toggle fallback because it uses a different deletion model' from 'failed to implement the recommended fallback', so there is no distinguishable pass/fail boundary. Conditional on the server lacking UIDPLUS and on the client wanting selective expunge behavior at all. |
| RFC4315-2.1-3 — Alternative fallback: plain EXPUNGE, risking unintended removals | MAY | rev1 | untestable | A keyword-less permission (lowercase 'may', graded MAY by judgment) offering an alternative fallback to 2.1-2: when the server lacks UIDPLUS, the client may just issue a plain EXPUNGE and accept that other clients' \Deleted-marked messages could be removed. Being pure permission with an accepted risk, it imposes no observable duty — a client that takes this shortcut, one that takes the safer 2.1-2 route, and one that uses neither are all compliant, and their choice is an internal policy decision the wire cannot attribute (a plain EXPUNGE is indistinguishable from an EXPUNGE issued for any other reason). No pass/fail boundary exists. Conditional on the server lacking UIDPLUS. |
| RFC4315-2.1-3 — Alternative fallback: plain EXPUNGE, risking unintended removals | MAY | rev2 | untestable | A keyword-less permission (lowercase 'may', graded MAY by judgment) offering an alternative fallback to 2.1-2: when the server lacks UIDPLUS, the client may just issue a plain EXPUNGE and accept that other clients' \Deleted-marked messages could be removed. Being pure permission with an accepted risk, it imposes no observable duty — a client that takes this shortcut, one that takes the safer 2.1-2 route, and one that uses neither are all compliant, and their choice is an internal policy decision the wire cannot attribute (a plain EXPUNGE is indistinguishable from an EXPUNGE issued for any other reason). No pass/fail boundary exists. Conditional on the server lacking UIDPLUS. |
| RFC4315-3-1 — Discover copy/append destinations via SELECT/FETCH/SEARCH when resp codes absent | MAY | rev1 | untestable | A keyword-less capability statement (lowercase 'can', graded MAY by judgment) telling the client how it MAY recover the destination UIDs when the server omits APPENDUID/COPYUID: select the destination and use FETCH/SEARCH (e.g. on Message-ID). Whether a client performs this recovery, and how it correlates messages, is an internal application-level decision with no mandated wire form — a client that does not need the UIDs, or that tracks them by other means, is equally compliant and would issue none of these follow-up commands. No black-box exchange distinguishes 'chose not to recover UIDs because it did not need them' from 'failed to implement the recovery path', so there is no pass/fail boundary. Conditional on the server omitting the APPENDUID/COPYUID response codes. |
| RFC4315-3-1 — Discover copy/append destinations via SELECT/FETCH/SEARCH when resp codes absent | MAY | rev2 | untestable | A keyword-less capability statement (lowercase 'can', graded MAY by judgment) telling the client how it MAY recover the destination UIDs when the server omits APPENDUID/COPYUID: select the destination and use FETCH/SEARCH (e.g. on Message-ID). Whether a client performs this recovery, and how it correlates messages, is an internal application-level decision with no mandated wire form — a client that does not need the UIDs, or that tracks them by other means, is equally compliant and would issue none of these follow-up commands. No black-box exchange distinguishes 'chose not to recover UIDs because it did not need them' from 'failed to implement the recovery path', so there is no pass/fail boundary. Conditional on the server omitting the APPENDUID/COPYUID response codes. |
| RFC4315-3-2 — Client (implicit) MUST accept the APPENDUID response code in the tagged OK to APPEND | MUST | rev1 | pass |  |
| RFC4315-3-2 — Client (implicit) MUST accept the APPENDUID response code in the tagged OK to APPEND | MUST | rev2 | pass |  |
| RFC4315-3-3 — Client (implicit) MUST accept the COPYUID response code in the tagged OK to COPY | MUST | rev1 | pass |  |
| RFC4315-3-3 — Client (implicit) MUST accept the COPYUID response code in the tagged OK to COPY | MUST | rev2 | pass |  |
| RFC4315-3-4 — Client (implicit) MUST accept the UIDNOTSTICKY response code in the untagged NO to SELECT | MUST | rev1 | pass |  |
| RFC4315-3-4 — Client (implicit) MUST accept the UIDNOTSTICKY response code in the untagged NO to SELECT | MUST | rev2 | pass |  |
| RFC6851-3.3-1 — MOVE command form: MOVE SP sequence-set SP mailbox | MUST | rev1 | fail | unimplemented: move = "MOVE" SP sequence-set SP mailbox |
| RFC6851-3.3-2 — UID MOVE command form: UID SP MOVE ... | MUST | rev1 | fail | unimplemented: uid = "UID" SP (copy / fetch / search / store / move) |
| RFC6851-3.3-3 — Client accepts COPY and EXPUNGE codes for a MOVE, with no STORE code and no \Deleted | MUST | rev1 | fail | unimplemented: In particular, though the COPY and EXPUNGE response codes will be returned, response codes for a STORE MUST NOT be generated and the \DELETED flag MUST NOT be s… |
| RFC6851-3.3-3 — Client accepts COPY and EXPUNGE codes for a MOVE, with no STORE code and no \Deleted | MUST | rev2 | fail | unimplemented: In particular, though the COPY and EXPUNGE response codes will be returned, response codes for a STORE MUST NOT be generated and the \DELETED flag MUST NOT be s… |
| RFC6851-3.3-4 — No message-sequence-number commands while the server is processing MOVE | MUST NOT | rev1 | fail | unimplemented: The server may send EXPUNGE (or VANISHED) responses before the tagged response, so the client cannot safely send more commands with message sequence number argu… |
| RFC6851-3.3-5 — Do not pipeline sequence-number commands (or renumbering commands) around a MOVE | MUST NOT | rev1 | untestable | The pipelining elaboration of the 3.3-4 hazard: it forbids a client's command-scheduling choice (pipelining a sequence-number-dependent command after a MOVE, or pipelining MOVE behind a command that might renumber). Whether the client 'pipelined' is a decision about when it flushes bytes relative to responses it has not yet read; a black-box test cannot force a compliant client to reveal that it WOULD have pipelined but chose not to, and a client that simply serializes every command (never pipelining anything) satisfies this duty vacuously and is wire-indistinguishable from one that reasons about renumbering safety. There is no observable pass/fail boundary for the avoidance itself distinct from the already-cataloged 'do not send sequence-number commands mid-MOVE' duty (3.3-4), which captures the observable half. |
| RFC6851-3.3-6 — Moving to the currently selected mailbox is allowed when copying to it is allowed | MAY | rev1 | untestable | A permission statement: it grants that a MOVE whose target equals the selected (source) mailbox is legal exactly when the corresponding COPY-to-self is legal. It imposes no client duty — a client that never moves a message to the currently selected mailbox is fully compliant, and a client that does so is exercising a permitted (but optional) capability whose acceptability is governed by the server's copy-to-self policy, not by any wire-observable client obligation. Whether the client ever chooses to issue a self-target MOVE is an application/user policy decision with no pass/fail boundary to assert against. |
| RFC6851-3.3-6 — Moving to the currently selected mailbox is allowed when copying to it is allowed | MAY | rev2 | untestable | A permission statement: it grants that a MOVE whose target equals the selected (source) mailbox is legal exactly when the corresponding COPY-to-self is legal. It imposes no client duty — a client that never moves a message to the currently selected mailbox is fully compliant, and a client that does so is exercising a permitted (but optional) capability whose acceptability is governed by the server's copy-to-self policy, not by any wire-observable client obligation. Whether the client ever chooses to issue a self-target MOVE is an application/user policy decision with no pass/fail boundary to assert against. |
| RFC6851-3.3-7 — A tagged NO to MOVE does not imply nothing was moved | MUST | rev1 | untestable | The 'This' refers to the atomicity guarantee that each message ends up in at least one mailbox (and should not be in both) — the sentence extends that guarantee to the tagged-NO case. The client-binding residue is a state-reconciliation duty: a MOVE that completes with a tagged NO may still have moved (and expunged) some messages, so the client MUST NOT treat NO as 'nothing happened' and MUST fold in whatever EXPUNGE / COPYUID responses it received when updating its local mailbox state. Whether the client's INTERNAL model correctly reflects the partial move is not forced by any single observable exchange: two clients that received the same EXPUNGE/NO sequence can hold divergent internal state while emitting identical subsequent wire traffic, and the RFC prescribes no follow-up command that would externalize the reconciled state for a black-box observer. Judgment level (implicit client-handling MUST derived from a server-directed atomicity sentence). |
| RFC6851-3.3-7 — A tagged NO to MOVE does not imply nothing was moved | MUST | rev2 | untestable | The 'This' refers to the atomicity guarantee that each message ends up in at least one mailbox (and should not be in both) — the sentence extends that guarantee to the tagged-NO case. The client-binding residue is a state-reconciliation duty: a MOVE that completes with a tagged NO may still have moved (and expunged) some messages, so the client MUST NOT treat NO as 'nothing happened' and MUST fold in whatever EXPUNGE / COPYUID responses it received when updating its local mailbox state. Whether the client's INTERNAL model correctly reflects the partial move is not forced by any single observable exchange: two clients that received the same EXPUNGE/NO sequence can hold divergent internal state while emitting identical subsequent wire traffic, and the RFC prescribes no follow-up command that would externalize the reconciled state for a black-box observer. Judgment level (implicit client-handling MUST derived from a server-directed atomicity sentence). |
| RFC6851-4.3-1 — Client parses a COPYUID delivered in an untagged OK before the EXPUNGEs | SHOULD | rev1 | pass |  |
| RFC6851-4.4-1 — QRESYNC-enabled client handles both VANISHED and EXPUNGE for UID MOVE | MUST | rev1 | fail | unimplemented: The same requirement applies to MOVE, and a QRESYNC-enabled client needs to handle both VANISHED and EXPUNGE responses to a UID MOVE command. |
| RFC6851-4.4-1 — QRESYNC-enabled client handles both VANISHED and EXPUNGE for UID MOVE | MUST | rev2 | fail | unimplemented: The same requirement applies to MOVE, and a QRESYNC-enabled client needs to handle both VANISHED and EXPUNGE responses to a UID MOVE command. |
| RFC6851-5-1 — Case-insensitive acceptance of the MOVE strings | MUST | rev1 | pass |  |
| RFC6851-5-1 — Case-insensitive acceptance of the MOVE strings | MUST | rev2 | pass |  |
| RFC2342-3-1 — Client SHOULD allow the user to manually enter a namespace prefix | SHOULD | rev1 | untestable | Whether a client offers the user a UI affordance to type in a namespace prefix by hand is a property of the client's user interface, not of any command it puts on the wire. The NAMESPACE command and every subsequent LIST/CREATE are byte-identical whether the prefix the client used was auto-discovered or hand-entered by the user, so no black-box exchange can establish that the manual-entry affordance exists. The full context ('A server MAY choose to make available to the NAMESPACE command only a subset of the complete set of namespaces the server supports. To provide the ability to access these namespaces ...') makes this conditional on the deployment having namespaces the NAMESPACE command does not surface. |
| RFC2342-3-1 — Client SHOULD allow the user to manually enter a namespace prefix | SHOULD | rev2 | untestable | Whether a client offers the user a UI affordance to type in a namespace prefix by hand is a property of the client's user interface, not of any command it puts on the wire. The NAMESPACE command and every subsequent LIST/CREATE are byte-identical whether the prefix the client used was auto-discovered or hand-entered by the user, so no black-box exchange can establish that the manual-entry affordance exists. The full context ('A server MAY choose to make available to the NAMESPACE command only a subset of the complete set of namespaces the server supports. To provide the ability to access these namespaces ...') makes this conditional on the deployment having namespaces the NAMESPACE command does not surface. |
| RFC2342-5-1 — Client MUST be prepared for multiple Personal/Other Users' Namespaces | MUST | rev1 | untestable | 'Being prepared for' an open-ended number of namespaces is a robustness property of the client's namespace-handling logic, not a single observable wire action. A test can script a `* NAMESPACE` response carrying multiple Personal Namespace pairs and confirm the client parses them without error, but that slice only confirms the parser accepts valid syntax — it has no pass/fail boundary distinct from ordinary NAMESPACE-response parsing, and 'prepared for them' as a general duty extends beyond any one scripted scenario to the client's whole design. Same reasoning the rev2 restatement (RFC9051-6.3.10-1) records for the identical duty. |
| RFC2342-5-2 — Client SHOULD let the user select which namespace to create the mailbox in | SHOULD | rev1 | untestable | 'These situations' is when a client configured to create a mailbox faces multiple Personal Namespaces and it is unclear which should receive it. Whether the client prompts the user to choose is a UI/policy decision with no distinguishing wire signature: the resulting CREATE command names one concrete mailbox path either way, and a client that silently picked a namespace is byte-indistinguishable from one whose user was prompted and chose that same namespace. No black-box observation can establish which branch, if either, was implemented. |
| RFC2342-5-3 — Client MAY append '%' to the Other Users' prefix to discover other users' namespaces | MAY | rev1 | untestable | This is permissive guidance ('a client can construct'), granting the client an optional technique for discovering other users' namespaces — it imposes no duty and forbids nothing. Whether a client ever chooses to build such a LIST is entirely its own feature decision; a client that never issues an 'Other Users/%' LIST is fully compliant. There is no pass/fail boundary to observe: the presence of such a LIST proves the client exercised an option, and its absence proves nothing (the option was simply not taken). No black-box test can compel or forbid the technique. |
| RFC2342-5-3 — Client MAY append '%' to the Other Users' prefix to discover other users' namespaces | MAY | rev2 | untestable | This is permissive guidance ('a client can construct'), granting the client an optional technique for discovering other users' namespaces — it imposes no duty and forbids nothing. Whether a client ever chooses to build such a LIST is entirely its own feature decision; a client that never issues an 'Other Users/%' LIST is fully compliant. There is no pass/fail boundary to observe: the presence of such a LIST proves the client exercised an option, and its absence proves nothing (the option was simply not taken). No black-box test can compel or forbid the technique. |
| RFC2342-5-4 — Client (implicit) MUST accept a NIL for any unavailable namespace class | MUST | rev1 | pass |  |
| RFC2342-5-4 — Client (implicit) MUST accept a NIL for any unavailable namespace class | MUST | rev2 | pass |  |
| RFC5258-1-1 — Clients needing only a subscribed-mailbox list SHOULD continue to use LSUB | SHOULD | rev1 | untestable | Whether a client 'simply needs a list of subscribed mailboxes' (and should therefore prefer LSUB) versus 'specifically wants the additional information offered by LIST (SUBSCRIBED)' is a UI/feature-policy decision internal to the client, not observable on the wire. A client that issues LSUB and one that issues LIST (SUBSCRIBED) are both compliant; no black-box exchange can determine whether the richer command was chosen because the client genuinely needed accurate flags/attributes or merely by preference. The SHOULD expresses an efficiency/appropriateness preference between two legal mechanisms, with no pass/fail wire boundary. |
| RFC5258-3-1 — Client MUST NOT send a LIST option the server has not advertised | MUST NOT | rev1 | pass |  |
| RFC5258-3-2 — Client SHOULD NOT specify a LIST option more than once | SHOULD NOT | rev1 | pass |  |
| RFC5258-3-4 — Client MUST ignore unrecognized LIST extended fields | MUST | rev1 | pass |  |
| RFC5258-3.1-1 — Clients SHOULD continue to use LSUB unless they want LIST (SUBSCRIBED) information | SHOULD | rev1 | untestable | Whether the client 'specifically wants the additional information offered by LIST (SUBSCRIBED)' — accurate mailbox flags/attributes that LSUB does not guarantee — is a policy decision about the client's own needs, not a wire-observable fact. A client choosing LIST (SUBSCRIBED) and one choosing LSUB are both compliant; no black-box exchange can distinguish 'needed the extra attribute accuracy' from 'chose the newer command by preference'. The SHOULD arbitrates between two legal mechanisms on grounds internal to the client's requirements. |
| RFC5258-3.1-2 — RECURSIVEMATCH MUST NOT be the only selection option | MUST NOT | rev1 | pass |  |
| RFC5258-3.1-2 — RECURSIVEMATCH MUST NOT be the only selection option | MUST NOT | rev2 | pass |  |
| RFC5258-3.1-3 — Client MUST handle a CHILDINFO item with no matching submailboxes | MUST | rev1 | untestable | 'Being able to handle' a race-condition edge case (a CHILDINFO item reporting subscribed/matching children that were deleted or renamed before the client could access them) is a robustness property of client-side logic, not a distinct wire action. Any wire behavior after receiving CHILDINFO followed by an empty or failed follow-up LIST is compliant provided the client does not crash or wedge, and 'does not crash' is not a protocol-level pass/fail boundary this catalog otherwise encodes. |
| RFC5258-3.4-1 — Client MUST treat a stronger LIST attribute as implying weaker inferable attributes | MUST | rev1 | pass |  |
| RFC5258-4-1 — Client must be prepared for \HasChildren with no child mailbox listed | MUST | rev1 | untestable | 'Being prepared to deal with' a race-condition edge case (a \HasChildren-marked mailbox whose children were deleted or made inaccessible before the server could list them) is a robustness property of client-side logic, not a wire action the harness can assert on directly. Any wire behavior after receiving \HasChildren followed by an empty or failed child LIST is compliant provided the client does not crash or wedge, and 'does not crash' is not a protocol-level pass/fail boundary this catalog otherwise encodes. |
| RFC5819-2-1 — Client (implicit) emits LIST ... RETURN (STATUS (<status items>)) to request STATUS in LIST | MUST | rev1 | pass |  |
| RFC5819-2-1 — Client (implicit) emits LIST ... RETURN (STATUS (<status items>)) to request STATUS in LIST | MUST | rev2 | pass |  |
| RFC5819-2-2 — Client (implicit) MUST associate each untagged STATUS response with its preceding LIST entry | MUST | rev1 | untestable | The quoted sentence binds the SERVER ('the server MUST return'); the implicit client duty is to ASSOCIATE each untagged '* STATUS' response with the '* LIST' entry it immediately follows, so the STATUS counts are attributed to the correct mailbox. That association is a purely internal client bookkeeping decision with no wire signature: a client that pairs the responses correctly and one that mis-pairs (or ignores the ordering) both send an identical LIST-STATUS command and both consume the same untagged responses off the wire — the divergence surfaces only in the client's internal model / its UI, never in the bytes it emits. No black-box test can distinguish a correctly-associating client from a mis-associating one. Same class as the RFC5161-3.1-3 / RFC9051-6.3.9-3 internal-decision entries. |
| RFC5819-2-2 — Client (implicit) MUST associate each untagged STATUS response with its preceding LIST entry | MUST | rev2 | untestable | The quoted sentence binds the SERVER ('the server MUST return'); the implicit client duty is to ASSOCIATE each untagged '* STATUS' response with the '* LIST' entry it immediately follows, so the STATUS counts are attributed to the correct mailbox. That association is a purely internal client bookkeeping decision with no wire signature: a client that pairs the responses correctly and one that mis-pairs (or ignores the ordering) both send an identical LIST-STATUS command and both consume the same untagged responses off the wire — the divergence surfaces only in the client's internal model / its UI, never in the bytes it emits. No black-box test can distinguish a correctly-associating client from a mis-associating one. Same class as the RFC5161-3.1-3 / RFC9051-6.3.9-3 internal-decision entries. |
| RFC5819-2-3 — Client (implicit) MUST accept STATUS for selectable mailboxes only (listed \NoSelect entry with no STATUS is not an error) | MUST | rev1 | pass |  |
| RFC5819-2-3 — Client (implicit) MUST accept STATUS for selectable mailboxes only (listed \NoSelect entry with no STATUS is not an error) | MUST | rev2 | pass |  |
| RFC5819-2-4 — Client (implicit) MUST accept a tagged OK completion even when some STATUS replies were dropped | MUST | rev1 | pass |  |
| RFC5819-2-4 — Client (implicit) MUST accept a tagged OK completion even when some STATUS replies were dropped | MUST | rev2 | pass |  |
| RFC6154-2-1 — Client MAY request only special-use mailboxes via the SPECIAL-USE selection option | MAY | rev1 | pass |  |
| RFC6154-2-1 — Client MAY request only special-use mailboxes via the SPECIAL-USE selection option | MAY | rev2 | pass |  |
| RFC6154-2-2 — Client MAY request special-use attributes be returned via the SPECIAL-USE return option | MAY | rev1 | pass |  |
| RFC6154-2-2 — Client MAY request special-use attributes be returned via the SPECIAL-USE return option | MAY | rev2 | pass |  |
| RFC6154-3-1 — Client MUST NOT use the USE parameter unless CREATE-SPECIAL-USE is advertised | MUST NOT | rev1 | pass |  |
| RFC6154-3-1 — Client MUST NOT use the USE parameter unless CREATE-SPECIAL-USE is advertised | MUST NOT | rev2 | pass |  |
| RFC6154-3-2 — Client CREATE with special use MAY carry the USE parameter (parenthesized attribute list) | MAY | rev1 | pass |  |
| RFC6154-3-2 — Client CREATE with special use MAY carry the USE parameter (parenthesized attribute list) | MAY | rev2 | pass |  |
| RFC6154-3-3 — Client MUST handle a tagged NO carrying the [USEATTR] response code on CREATE refusal | MUST | rev1 | pass |  |
| RFC6154-3-3 — Client MUST handle a tagged NO carrying the [USEATTR] response code on CREATE refusal | MUST | rev2 | pass |  |
| RFC6154-6-1 — Client MUST accept the seven special-use name-attributes in LIST responses | MUST | rev1 | pass |  |
| RFC6154-6-1 — Client MUST accept the seven special-use name-attributes in LIST responses | MUST | rev2 | pass |  |
| RFC6154-6-2 — Client MUST ignore list attributes it does not understand | MUST | rev1 | pass |  |
| RFC6154-6-2 — Client MUST ignore list attributes it does not understand | MUST | rev2 | pass |  |
| RFC4314-2-1 — Client rights strings use only lowercase standard / digit rights (no uppercase) | MUST NOT | rev1 | untestable | The document forbids uppercase rights letters ('uppercase rights are not allowed') and reserves lowercase for standard rights and digits for implementation-defined rights. Whether a client 'understands' this reservation scheme is an internal fact about how it constructs a rights string; on the wire, a compliant client simply emits a lowercase/digit rights atom, which is byte-for-byte identical to what any other conformant client would emit. There is no black-box stimulus that distinguishes a client reasoning about the reserved-letter rules from one that hard-codes a valid string — a client that emitted an uppercase right would be rejected by the SERVER (§3.1 'Uppercase rights are not allowed'), and that rejection tests the server, not the client's internal composition logic. Judgment call: the 'uppercase rights are not allowed' clause is parenthetical prose, not an RFC 2119 keyword sentence, but it states an absolute prohibition on the rights-string form a client may emit, so it is recorded at MUST NOT level with this note. |
| RFC4314-2-1 — Client rights strings use only lowercase standard / digit rights (no uppercase) | MUST NOT | rev2 | untestable | The document forbids uppercase rights letters ('uppercase rights are not allowed') and reserves lowercase for standard rights and digits for implementation-defined rights. Whether a client 'understands' this reservation scheme is an internal fact about how it constructs a rights string; on the wire, a compliant client simply emits a lowercase/digit rights atom, which is byte-for-byte identical to what any other conformant client would emit. There is no black-box stimulus that distinguishes a client reasoning about the reserved-letter rules from one that hard-codes a valid string — a client that emitted an uppercase right would be rejected by the SERVER (§3.1 'Uppercase rights are not allowed'), and that rejection tests the server, not the client's internal composition logic. Judgment call: the 'uppercase rights are not allowed' clause is parenthetical prose, not an RFC 2119 keyword sentence, but it states an absolute prohibition on the rights-string form a client may emit, so it is recorded at MUST NOT level with this note. |
| RFC4314-2.1.1-1 — Client 'd' virtual right means the union of all 'delete' member rights | MUST | rev1 | untestable | This defines the meaning a client attaches to the compound/virtual 'd' right when it composes a rights list: 'd' is shorthand for the union of the 'delete' member rights (a legacy RFC 2086 compatibility right; the exact membership — 'e','t' or 'e','t','x' — is server-defined per §2.1.1). Whether a client 'treats d as' the member union is a fact about its internal rights model, not something observable on the wire: the client either emits the literal string 'd' (equivalent to the union) or emits the expanded member letters — both are legal SETACL rights arguments, and the RFC explicitly says specifying both 'd' and members together 'is no different' in effect. No black-box test can distinguish a client that models 'd' as the union from one that treats it opaquely, because the two produce indistinguishable (indeed the RFC declares equivalent) wire forms, and any divergence surfaces only in the SERVER's resulting ACL, not in the client's request. |
| RFC4314-2.1.1-1 — Client 'd' virtual right means the union of all 'delete' member rights | MUST | rev2 | untestable | This defines the meaning a client attaches to the compound/virtual 'd' right when it composes a rights list: 'd' is shorthand for the union of the 'delete' member rights (a legacy RFC 2086 compatibility right; the exact membership — 'e','t' or 'e','t','x' — is server-defined per §2.1.1). Whether a client 'treats d as' the member union is a fact about its internal rights model, not something observable on the wire: the client either emits the literal string 'd' (equivalent to the union) or emits the expanded member letters — both are legal SETACL rights arguments, and the RFC explicitly says specifying both 'd' and members together 'is no different' in effect. No black-box test can distinguish a client that models 'd' as the union from one that treats it opaquely, because the two produce indistinguishable (indeed the RFC declares equivalent) wire forms, and any divergence surfaces only in the SERVER's resulting ACL, not in the client's request. |
| RFC4314-2.1.1-2 — Client 'c' virtual right means the union of all 'create' member rights | MUST | rev1 | untestable | Parallel to the 'd' virtual right (RFC4314-2.1.1-1): 'c' is the client-side shorthand for the union of the 'create' member rights (server-defined as either the 'k' right alone, or the union of 'k' and 'x', per the RFC 2086 implementation split in §2.1.1). As with 'd', whether a client treats 'c' as the member union is internal to its rights model and produces no distinguishing wire form: emitting 'c' or the expanded members, or both together, are declared equivalent by the RFC. No black-box stimulus separates a client that expands 'c' from one that passes it through opaque, since the resulting SETACL rights argument is legal in every case and the effect materializes only in the server's stored ACL. |
| RFC4314-2.1.1-2 — Client 'c' virtual right means the union of all 'create' member rights | MUST | rev2 | untestable | Parallel to the 'd' virtual right (RFC4314-2.1.1-1): 'c' is the client-side shorthand for the union of the 'create' member rights (server-defined as either the 'k' right alone, or the union of 'k' and 'x', per the RFC 2086 implementation split in §2.1.1). As with 'd', whether a client treats 'c' as the member union is internal to its rights model and produces no distinguishing wire form: emitting 'c' or the expanded members, or both together, are declared equivalent by the RFC. No black-box stimulus separates a client that expands 'c' from one that passes it through opaque, since the resulting SETACL rights argument is legal in every case and the effect materializes only in the server's stored ACL. |
| RFC4314-2.1.1-3 — Client MUST ignore virtual 'd'/'c' rights in MYRIGHTS/ACL/LISTRIGHTS responses | MUST | rev1 | fail | unimplemented: Clients conforming to this document MUST ignore the virtual "d" and "c" rights in MYRIGHTS, ACL, and LISTRIGHTS responses. |
| RFC4314-2.1.1-3 — Client MUST ignore virtual 'd'/'c' rights in MYRIGHTS/ACL/LISTRIGHTS responses | MUST | rev2 | fail | unimplemented: Clients conforming to this document MUST ignore the virtual "d" and "c" rights in MYRIGHTS, ACL, and LISTRIGHTS responses. |
| RFC4314-3.1-1 — Client SETACL rights argument: optional +/- prefix then rights characters | MUST | rev1 | fail | unimplemented: The third argument is a string containing an optional plus ("+") or minus ("-") prefix, followed by zero or more rights characters. If the string starts with a … |
| RFC4314-3.1-1 — Client SETACL rights argument: optional +/- prefix then rights characters | MUST | rev2 | fail | unimplemented: The third argument is a string containing an optional plus ("+") or minus ("-") prefix, followed by zero or more rights characters. If the string starts with a … |
| RFC4314-3.1-2 — Client MUST NOT emit uppercase or unsupported rights in SETACL | MUST | rev1 | fail | unimplemented: Note that an unrecognized right MUST cause the command to return the BAD response. In particular, the server MUST NOT silently ignore unrecognized rights. |
| RFC4314-3.1-2 — Client MUST NOT emit uppercase or unsupported rights in SETACL | MUST | rev2 | fail | unimplemented: Note that an unrecognized right MUST cause the command to return the BAD response. In particular, the server MUST NOT silently ignore unrecognized rights. |
| RFC4314-6-1 — Client SHOULD warn before granting broad rights to identifier 'anyone' | SHOULD | rev1 | untestable | The warning is a user-facing presentation act — the client SHOULD surface a caution to the user before it emits a SETACL granting full access (or the administer 'a' right) to the universal 'anyone' identity. Whether such a warning is shown is a property of the client's user interface, not of any IMAP wire exchange: the SETACL command that eventually goes out (if the user proceeds) is byte-identical whether or not a warning was displayed first. A black-box IMAP-protocol harness observes only the wire and cannot detect the presence, absence, or content of a UI warning dialog. |
| RFC4314-6-1 — Client SHOULD warn before granting broad rights to identifier 'anyone' | SHOULD | rev2 | untestable | The warning is a user-facing presentation act — the client SHOULD surface a caution to the user before it emits a SETACL granting full access (or the administer 'a' right) to the universal 'anyone' identity. Whether such a warning is shown is a property of the client's user interface, not of any IMAP wire exchange: the SETACL command that eventually goes out (if the user proceeds) is byte-identical whether or not a warning was displayed first. A black-box IMAP-protocol harness observes only the wire and cannot detect the presence, absence, or content of a UI warning dialog. |
| RFC4314-5.1.2-1 — Read+update-ACL client MUST preserve unrecognized rights it can't change | MUST | rev1 | fail | unimplemented: A client implementation that allows a user to read and update ACLs MUST preserve unrecognized rights that it doesn't allow the user to change. That is, if the c… |
| RFC4314-5.1.2-1 — Read+update-ACL client MUST preserve unrecognized rights it can't change | MUST | rev2 | fail | unimplemented: A client implementation that allows a user to read and update ACLs MUST preserve unrecognized rights that it doesn't allow the user to change. That is, if the c… |
| RFC9208-1-1 — Client MUST NOT rely on QUOTA responses/codes absent a 'QUOTA=' capability | MUST NOT | rev1 | fail | unimplemented: Some responses and response codes defined in this document are not present in such servers ... and clients MUST NOT rely on their presence in the absence of any… |
| RFC9208-1-1 — Client MUST NOT rely on QUOTA responses/codes absent a 'QUOTA=' capability | MUST NOT | rev2 | fail | unimplemented: Some responses and response codes defined in this document are not present in such servers ... and clients MUST NOT rely on their presence in the absence of any… |
| RFC9208-3.1.2-1 — Client MUST NOT compare an available resource between two quota roots | MUST NOT | rev1 | untestable | The prohibition governs an internal computation the client might perform on two usage integers it has already received — it forbids DRAWING a comparison, not sending or withholding any particular command. A client that internally compares 'available resource' across two quota roots and one that never does emit byte-for-byte identical protocol traffic; the forbidden act has no obligatory wire consequence a black-box probe could observe. No test can distinguish a client that respects this rule from one that ignores it. |
| RFC9208-3.1.2-1 — Client MUST NOT compare an available resource between two quota roots | MUST NOT | rev2 | untestable | The prohibition governs an internal computation the client might perform on two usage integers it has already received — it forbids DRAWING a comparison, not sending or withholding any particular command. A client that internally compares 'available resource' across two quota roots and one that never does emit byte-for-byte identical protocol traffic; the forbidden act has no obligatory wire consequence a black-box probe could observe. No test can distinguish a client that respects this rule from one that ignores it. |
| RFC9208-3.2-1 — Client SHOULD treat a quota root name as an opaque string | SHOULD | rev1 | untestable | Whether a client treats a quota root name as opaque or instead parses it for structure (e.g., inferring a mailbox relationship) is an internal data-handling decision. Both an opaque-treating client and a structure-inferring one echo the same astring back on the wire (a GETQUOTA or SETQUOTA carries the root name verbatim regardless), so the distinction never manifests as a distinguishable protocol form. No black-box probe can observe how the client internally interprets the string. |
| RFC9208-3.2-1 — Client SHOULD treat a quota root name as an opaque string | SHOULD | rev2 | untestable | Whether a client treats a quota root name as opaque or instead parses it for structure (e.g., inferring a mailbox relationship) is an internal data-handling decision. Both an opaque-treating client and a structure-inferring one echo the same astring back on the wire (a GETQUOTA or SETQUOTA carries the root name verbatim regardless), so the distinction never manifests as a distinguishable protocol form. No black-box probe can observe how the client internally interprets the string. |
| RFC9208-3.2-2 — Client MUST be prepared for a SETQUOTA command to fail if a limit cannot be set | MUST | rev1 | fail | unimplemented: A client MUST be prepared for a SETQUOTA (Section 4.1.3) command to fail if a limit cannot be set. |
| RFC9208-3.2-2 — Client MUST be prepared for a SETQUOTA command to fail if a limit cannot be set | MUST | rev2 | fail | unimplemented: A client MUST be prepared for a SETQUOTA (Section 4.1.3) command to fail if a limit cannot be set. |
| RFC9208-4.1.1-1 — Client (judgment) must emit GETQUOTA/GETQUOTAROOT/SETQUOTA in the defined command forms | MUST | rev1 | fail | unimplemented: getquota = "GETQUOTA" SP quota-root-name ... getquotaroot = "GETQUOTAROOT" SP mailbox ... setquota = "SETQUOTA" SP quota-root-name SP setquota-list |
| RFC9208-4.1.1-1 — Client (judgment) must emit GETQUOTA/GETQUOTAROOT/SETQUOTA in the defined command forms | MUST | rev2 | fail | unimplemented: getquota = "GETQUOTA" SP quota-root-name ... getquotaroot = "GETQUOTAROOT" SP mailbox ... setquota = "SETQUOTA" SP quota-root-name SP setquota-list |
| RFC9208-4.2.1-1 — Client (judgment) must accept untagged QUOTA and QUOTAROOT responses | MUST | rev1 | pass |  |
| RFC9208-4.2.1-1 — Client (judgment) must accept untagged QUOTA and QUOTAROOT responses | MUST | rev2 | pass |  |
| RFC9208-5.1-1 — Client MUST NOT use the usage figure for anything other than informational purposes | MUST NOT | rev1 | untestable | The rule forbids the client from letting a STORAGE usage figure gate an outgoing APPEND (and, more generally, from acting on the figure beyond informational display). A compliant client that always attempts the APPEND and a violating client that suppresses the APPEND when it computes the mailbox as 'full' are distinguishable only if the client can be induced to WITHHOLD a command — but a black-box probe cannot force the client to want to APPEND, and a client that simply never withholds is indistinguishable from one that respects the rule by design. The permitted MAY-warn is a UI act with no obligatory wire form. Internal policy decision, not observable. |
| RFC9208-5.1-1 — Client MUST NOT use the usage figure for anything other than informational purposes | MUST NOT | rev2 | untestable | The rule forbids the client from letting a STORAGE usage figure gate an outgoing APPEND (and, more generally, from acting on the figure beyond informational display). A compliant client that always attempts the APPEND and a violating client that suppresses the APPEND when it computes the mailbox as 'full' are distinguishable only if the client can be induced to WITHHOLD a command — but a black-box probe cannot force the client to want to APPEND, and a client that simply never withholds is indistinguishable from one that respects the rule by design. The permitted MAY-warn is a UI act with no obligatory wire form. Internal policy decision, not observable. |
| RFC9208-5.2-1 — Client MUST NOT assume a change in MESSAGE usage indicates a change in the number of messages | MUST NOT | rev1 | untestable | The prohibition forbids an inference the client might draw internally from a delta in the MESSAGE usage figure — it does not require or forbid any command. A client that (wrongly) infers 'the message count changed' from a usage change and one that draws no such inference produce identical wire traffic; the mistaken inference surfaces, if at all, only in client-internal state or UI, neither of which a black-box probe observes. Not distinguishable. |
| RFC9208-5.2-1 — Client MUST NOT assume a change in MESSAGE usage indicates a change in the number of messages | MUST NOT | rev2 | untestable | The prohibition forbids an inference the client might draw internally from a delta in the MESSAGE usage figure — it does not require or forbid any command. A client that (wrongly) infers 'the message count changed' from a usage change and one that draws no such inference produce identical wire traffic; the mistaken inference surfaces, if at all, only in client-internal state or UI, neither of which a black-box probe observes. Not distinguishable. |
| RFC9208-5.3-1 — Client MUST NOT assume a change in MAILBOX usage indicates a change in the number of mailboxes | MUST NOT | rev1 | untestable | As with the MESSAGE analogue (§5.2-1), this forbids an internal inference from a delta in the MAILBOX usage figure, not any wire action. A client that infers a mailbox-count change from a usage change and one that does not emit identical protocol traffic, so no black-box probe can tell them apart. Internal-decision, unobservable. |
| RFC9208-5.3-1 — Client MUST NOT assume a change in MAILBOX usage indicates a change in the number of mailboxes | MUST NOT | rev2 | untestable | As with the MESSAGE analogue (§5.2-1), this forbids an internal inference from a delta in the MAILBOX usage figure, not any wire action. A client that infers a mailbox-count change from a usage change and one that does not emit identical protocol traffic, so no black-box probe can tell them apart. Internal-decision, unobservable. |
| RFC9208-7-1 — Client MUST accept the QUOTA protocol strings in a case-insensitive fashion | MUST | rev1 | pass |  |
| RFC9208-7-1 — Client MUST accept the QUOTA protocol strings in a case-insensitive fashion | MUST | rev2 | pass |  |
| RFC5464-3.2-1 — Client-emitted entry name MUST NOT have consecutive or trailing slash | MUST NOT | rev1 | fail | unimplemented: An entry name MUST NOT contain two consecutive "/" characters and MUST NOT end with a "/" character. |
| RFC5464-3.2-1 — Client-emitted entry name MUST NOT have consecutive or trailing slash | MUST NOT | rev2 | fail | unimplemented: An entry name MUST NOT contain two consecutive "/" characters and MUST NOT end with a "/" character. |
| RFC5464-3.2-2 — Client MUST use CRLF for line ends in multi-line string values | MUST | rev1 | untestable | Whether a bare LF or a CRLF was intended as a 'line end' inside an opaque annotation value is a fact about how the client composed the value's payload bytes, not a structural property of the METADATA wire framing. A value carrying a lone 0x0A is a perfectly legal literal8/nstring on the wire (the octet count still frames it correctly), so the server — and any black-box observer — cannot distinguish a client that dutifully normalized its internal line breaks to CRLF from one that shipped raw LFs: both produce a syntactically valid SETMETADATA. The duty governs the semantic content of user-supplied value data, which the compliance harness supplies rather than the client generating from real editor input, so no black-box test can attribute a CRLF (or its absence) to the client's own normalization discipline. |
| RFC5464-3.2-2 — Client MUST use CRLF for line ends in multi-line string values | MUST | rev2 | untestable | Whether a bare LF or a CRLF was intended as a 'line end' inside an opaque annotation value is a fact about how the client composed the value's payload bytes, not a structural property of the METADATA wire framing. A value carrying a lone 0x0A is a perfectly legal literal8/nstring on the wire (the octet count still frames it correctly), so the server — and any black-box observer — cannot distinguish a client that dutifully normalized its internal line breaks to CRLF from one that shipped raw LFs: both produce a syntactically valid SETMETADATA. The duty governs the semantic content of user-supplied value data, which the compliance harness supplies rather than the client generating from real editor input, so no black-box test can attribute a CRLF (or its absence) to the client's own normalization discipline. |
| RFC5464-3.2-3 — Client-emitted entry name MUST NOT contain *, %, non-ASCII, or control octets | MUST NOT | rev1 | fail | unimplemented: Entry names MUST NOT contain asterisk ("*") or percent ("%") characters and MUST NOT contain non-ASCII characters or characters with octet values in the range 0… |
| RFC5464-3.2-3 — Client-emitted entry name MUST NOT contain *, %, non-ASCII, or control octets | MUST NOT | rev2 | fail | unimplemented: Entry names MUST NOT contain asterisk ("*") or percent ("%") characters and MUST NOT contain non-ASCII characters or characters with octet values in the range 0… |
| RFC5464-4.1-1 — Client (implicit) MUST ENABLE the correct METADATA capability for unsolicited responses | MUST | rev1 | untestable | The source sentence is worded as a server duty ('servers MUST only send ...'); the client-side obligation it implies — that a client which WANTS unsolicited METADATA responses must first ENABLE the correct capability string ('METADATA' or 'METADATA-SERVER', matching what the server advertised) — is an internal decision about whether the client opts into the third-party-change notification feature at all. A client that never issues ENABLE simply never receives unsolicited responses, which is fully compliant; there is no wire artifact that distinguishes 'chose not to enable' from 'forgot to enable'. Whether the client picked the right capability string for the server's advertised capability is a correctness question about its own capability-negotiation logic, and (like RFC5161-3.1-3, the only-ENABLE-what-you-support duty) a wrong choice yields a legal ENABLE command indistinguishable on the wire from a right one — divergence would surface only as the absence of unsolicited responses, which is also the compliant no-op outcome. |
| RFC5464-4.1-1 — Client (implicit) MUST ENABLE the correct METADATA capability for unsolicited responses | MUST | rev2 | untestable | The source sentence is worded as a server duty ('servers MUST only send ...'); the client-side obligation it implies — that a client which WANTS unsolicited METADATA responses must first ENABLE the correct capability string ('METADATA' or 'METADATA-SERVER', matching what the server advertised) — is an internal decision about whether the client opts into the third-party-change notification feature at all. A client that never issues ENABLE simply never receives unsolicited responses, which is fully compliant; there is no wire artifact that distinguishes 'chose not to enable' from 'forgot to enable'. Whether the client picked the right capability string for the server's advertised capability is a correctness question about its own capability-negotiation logic, and (like RFC5161-3.1-3, the only-ENABLE-what-you-support duty) a wrong choice yields a legal ENABLE command indistinguishable on the wire from a right one — divergence would surface only as the absence of unsolicited responses, which is also the compliant no-op outcome. |
| RFC5464-4.2-1 — GETMETADATA is only valid in authenticated or selected state | MUST | rev1 | fail | unimplemented: This command is only available in authenticated or selected state [RFC3501]. |
| RFC5464-4.2-1 — GETMETADATA is only valid in authenticated or selected state | MUST | rev2 | fail | unimplemented: This command is only available in authenticated or selected state [RFC3501]. |
| RFC5464-4.2.1-1 — Client MUST parse the [METADATA LONGENTRIES n] tagged-OK response code | MUST | rev1 | fail | unimplemented: If there are any entries with values larger than the MAXSIZE limit, the server MUST include the METADATA LONGENTRIES response code in the tagged OK response for… |
| RFC5464-4.2.1-1 — Client MUST parse the [METADATA LONGENTRIES n] tagged-OK response code | MUST | rev2 | fail | unimplemented: If there are any entries with values larger than the MAXSIZE limit, the server MUST include the METADATA LONGENTRIES response code in the tagged OK response for… |
| RFC5464-4.2.2-1 — Client MUST treat an omitted DEPTH option as DEPTH 0 | MAY | rev1 | fail | unimplemented: If the DEPTH option is not specified, this is the same as specifying "DEPTH 0". |
| RFC5464-4.2.2-1 — Client MUST treat an omitted DEPTH option as DEPTH 0 | MAY | rev2 | fail | unimplemented: If the DEPTH option is not specified, this is the same as specifying "DEPTH 0". |
| RFC5464-4.3-1 — SETMETADATA is only valid in authenticated or selected state | MUST | rev1 | fail | unimplemented: This command is only available in authenticated or selected state [RFC3501]. |
| RFC5464-4.3-1 — SETMETADATA is only valid in authenticated or selected state | MUST | rev2 | fail | unimplemented: This command is only available in authenticated or selected state [RFC3501]. |
| RFC5464-4.3-2 — Client uses NIL as the value to remove an entry | MAY | rev1 | fail | unimplemented: Clients can use NIL for the value of entries it wants to remove. |
| RFC5464-4.3-2 — Client uses NIL as the value to remove an entry | MAY | rev2 | fail | unimplemented: Clients can use NIL for the value of entries it wants to remove. |
| RFC5464-4.3-3 — Client MUST NOT assume a METADATA response; MUST treat success as changed | MUST | rev1 | untestable | Both halves of this MUST govern the client's internal model of annotation state, not an observable wire action. 'MUST NOT assume that a METADATA response will be sent' forbids a client from BLOCKING or waiting for an echo response after SETMETADATA — but a client that (wrongly) waits and one that (rightly) proceeds on the tagged OK both send an identical SETMETADATA and both consume the same tagged OK; the divergence is purely in the client's internal control flow / cache-update logic and produces no distinguishing wire artifact. 'MUST assume that if the command succeeds, then the annotation has been changed' likewise dictates the client's post-success cache/model update, which is invisible to a black-box observer: whether the client believes the annotation changed is an internal-state fact, surfaced only by a later independent GETMETADATA whose result reflects the SERVER's store, not the client's assumption. |
| RFC5464-4.3-3 — Client MUST NOT assume a METADATA response; MUST treat success as changed | MUST | rev2 | untestable | Both halves of this MUST govern the client's internal model of annotation state, not an observable wire action. 'MUST NOT assume that a METADATA response will be sent' forbids a client from BLOCKING or waiting for an echo response after SETMETADATA — but a client that (wrongly) waits and one that (rightly) proceeds on the tagged OK both send an identical SETMETADATA and both consume the same tagged OK; the divergence is purely in the client's internal control flow / cache-update logic and produces no distinguishing wire artifact. 'MUST assume that if the command succeeds, then the annotation has been changed' likewise dictates the client's post-success cache/model update, which is invisible to a black-box observer: whether the client believes the annotation changed is an internal-state fact, surfaced only by a later independent GETMETADATA whose result reflects the SERVER's store, not the client's assumption. |
| RFC5464-4.3-4 — Client MUST parse the SETMETADATA-failure NO response codes | MUST | rev1 | fail | unimplemented: the server MUST return a tagged NO response with a "[METADATA MAXSIZE NNN]" response code when NNN is the maximum octet count that it is willing to accept. |
| RFC5464-4.3-4 — Client MUST parse the SETMETADATA-failure NO response codes | MUST | rev2 | fail | unimplemented: the server MUST return a tagged NO response with a "[METADATA MAXSIZE NNN]" response code when NNN is the maximum octet count that it is willing to accept. |
| RFC5464-4.3-5 — Client MUST parse the [METADATA TOOMANY] NO response code | MUST | rev1 | fail | unimplemented: the server MUST return a tagged NO response with a "[METADATA TOOMANY]" response code. |
| RFC5464-4.3-5 — Client MUST parse the [METADATA TOOMANY] NO response code | MUST | rev2 | fail | unimplemented: the server MUST return a tagged NO response with a "[METADATA TOOMANY]" response code. |
| RFC5464-4.3-6 — Client MUST parse the [METADATA NOPRIVATE] NO response code | MUST | rev1 | fail | unimplemented: the server MUST return a tagged NO response with a "[METADATA NOPRIVATE]" response code. |
| RFC5464-4.3-6 — Client MUST parse the [METADATA NOPRIVATE] NO response code | MUST | rev2 | fail | unimplemented: the server MUST return a tagged NO response with a "[METADATA NOPRIVATE]" response code. |
| RFC5464-4.4-1 — Client MUST accept the untagged METADATA response (solicited or unsolicited) | MUST | rev1 | fail | unimplemented: The METADATA response displays results of a GETMETADATA command, or can be returned as an unsolicited response at any time by the server in response to a change… |
| RFC5464-4.4-1 — Client MUST accept the untagged METADATA response (solicited or unsolicited) | MUST | rev2 | fail | unimplemented: The METADATA response displays results of a GETMETADATA command, or can be returned as an unsolicited response at any time by the server in response to a change… |
| RFC5464-4.4-2 — Client MUST accept a value-less unsolicited METADATA response (entry names only) | MUST | rev1 | pass |  |
| RFC5464-4.4-2 — Client MUST accept a value-less unsolicited METADATA response (entry names only) | MUST | rev2 | pass |  |
| RFC5464-4.4-3 — Client must GETMETADATA to update cached values after a value-less notification | MAY | rev1 | fail | unimplemented: If the client wants to update any cached values, it must explicitly retrieve those using a GETMETADATA command. |
| RFC5464-4.4-3 — Client must GETMETADATA to update cached values after a value-less notification | MAY | rev2 | fail | unimplemented: If the client wants to update any cached values, it must explicitly retrieve those using a GETMETADATA command. |
| RFC5464-7-1 — Client MUST treat annotation values as untrusted data | MUST | rev1 | untestable | 'Treat as untrusted' is a directive about how the client processes and renders the payload bytes of an annotation value AFTER it has parsed them off the wire — e.g. escaping/sanitizing before display, not executing embedded content, bounding sizes. None of that has a protocol-level wire signature: a client that faithfully sanitizes a malicious value and one that blindly trusts it issue byte-identical GETMETADATA commands and consume byte-identical METADATA responses. The obligation lives entirely in the client's internal content-handling of already-received data, which a black-box IMAP-protocol harness cannot observe or provoke into a distinguishing wire difference. |
| RFC5464-7-1 — Client MUST treat annotation values as untrusted data | MUST | rev2 | untestable | 'Treat as untrusted' is a directive about how the client processes and renders the payload bytes of an annotation value AFTER it has parsed them off the wire — e.g. escaping/sanitizing before display, not executing embedded content, bounding sizes. None of that has a protocol-level wire signature: a client that faithfully sanitizes a malicious value and one that blindly trusts it issue byte-identical GETMETADATA commands and consume byte-identical METADATA responses. The obligation lives entirely in the client's internal content-handling of already-received data, which a black-box IMAP-protocol harness cannot observe or provoke into a distinguishing wire difference. |
| RFC8514-4.2-1 — Client (implicit) MUST emit the SAVEDATE FETCH data item to request the save date | MUST | rev1 | fail | unimplemented: This extension defines one new data item for the FETCH command: SAVEDATE The save date of the message. |
| RFC8514-4.2-1 — Client (implicit) MUST emit the SAVEDATE FETCH data item to request the save date | MUST | rev2 | fail | unimplemented: This extension defines one new data item for the FETCH command: SAVEDATE The save date of the message. |
| RFC8514-4.2-2 — Client (implicit) MUST accept the SAVEDATE FETCH response as a date-time string or NIL | MUST | rev1 | fail | unimplemented: This extension defines one new data item for the FETCH response: SAVEDATE A string representing the save date of the message. However, if the underlying mailbox… |
| RFC8514-4.2-2 — Client (implicit) MUST accept the SAVEDATE FETCH response as a date-time string or NIL | MUST | rev2 | fail | unimplemented: This extension defines one new data item for the FETCH response: SAVEDATE A string representing the save date of the message. However, if the underlying mailbox… |
| RFC8514-4.3-1 — Client (implicit) MUST emit SAVEDBEFORE <date> to search by save date earlier than a date | MUST | rev1 | fail | unimplemented: This extension defines four new search keys for the SEARCH command: SAVEDBEFORE <date> Messages whose save date (disregarding time and timezone) is earlier than… |
| RFC8514-4.3-1 — Client (implicit) MUST emit SAVEDBEFORE <date> to search by save date earlier than a date | MUST | rev2 | fail | unimplemented: This extension defines four new search keys for the SEARCH command: SAVEDBEFORE <date> Messages whose save date (disregarding time and timezone) is earlier than… |
| RFC8514-4.3-2 — Client (implicit) MUST emit SAVEDON <date> to search by save date within a date | MUST | rev1 | fail | unimplemented: SAVEDON <date> Messages whose save date (disregarding time and timezone) is within the specified date. |
| RFC8514-4.3-2 — Client (implicit) MUST emit SAVEDON <date> to search by save date within a date | MUST | rev2 | fail | unimplemented: SAVEDON <date> Messages whose save date (disregarding time and timezone) is within the specified date. |
| RFC8514-4.3-3 — Client (implicit) MUST emit SAVEDSINCE <date> to search by save date within or later than a date | MUST | rev1 | fail | unimplemented: SAVEDSINCE <date> Messages whose save date (disregarding time and timezone) is within or later than the specified date. |
| RFC8514-4.3-3 — Client (implicit) MUST emit SAVEDSINCE <date> to search by save date within or later than a date | MUST | rev2 | fail | unimplemented: SAVEDSINCE <date> Messages whose save date (disregarding time and timezone) is within or later than the specified date. |
| RFC8514-4.3-4 — Client (implicit) MUST emit SAVEDATESUPPORTED to search by save-date support | MUST | rev1 | fail | unimplemented: SAVEDATESUPPORTED Matches all messages in the mailbox when the underlying storage of that mailbox supports the save date attribute. Conversely, it matches no me… |
| RFC8514-4.3-4 — Client (implicit) MUST emit SAVEDATESUPPORTED to search by save-date support | MUST | rev2 | fail | unimplemented: SAVEDATESUPPORTED Matches all messages in the mailbox when the underlying storage of that mailbox supports the save date attribute. Conversely, it matches no me… |
| RFC8474-1-1 — Client MUST accept NIL for THREADID when the server lacks threading | MUST | rev1 | fail | unimplemented: A server that does not implement threading will return NIL to all requests for THREADID. |
| RFC8474-1-1 — Client MUST accept NIL for THREADID when the server lacks threading | MUST | rev2 | fail | unimplemented: A server that does not implement threading will return NIL to all requests for THREADID. |
| RFC8474-4.1-1 — Client MUST accept the MAILBOXID response code in a tagged OK for CREATE | MUST | rev1 | fail | unimplemented: A server advertising the OBJECTID capability MUST include the MAILBOXID response code in the tagged OK response to all successful CREATE commands. |
| RFC8474-4.1-1 — Client MUST accept the MAILBOXID response code in a tagged OK for CREATE | MUST | rev2 | fail | unimplemented: A server advertising the OBJECTID capability MUST include the MAILBOXID response code in the tagged OK response to all successful CREATE commands. |
| RFC8474-4.2-1 — Client MUST accept the untagged * OK [MAILBOXID (...)] on SELECT/EXAMINE | MUST | rev1 | pass |  |
| RFC8474-4.2-1 — Client MUST accept the untagged * OK [MAILBOXID (...)] on SELECT/EXAMINE | MUST | rev2 | pass |  |
| RFC8474-4.3-1 — Client MAY emit the MAILBOXID attribute in a STATUS command | MAY | rev1 | fail | unimplemented: Syntax: "MAILBOXID" The attribute in the STATUS command. |
| RFC8474-4.3-1 — Client MAY emit the MAILBOXID attribute in a STATUS command | MAY | rev2 | fail | unimplemented: Syntax: "MAILBOXID" The attribute in the STATUS command. |
| RFC8474-4.3-2 — Client MUST parse the MAILBOXID (objectid) STATUS response item | MUST | rev1 | fail | unimplemented: Syntax: "MAILBOXID" SP "(" objectid ")" The response item in the STATUS response contains the ObjectID assigned by the server for this mailbox. |
| RFC8474-4.3-2 — Client MUST parse the MAILBOXID (objectid) STATUS response item | MUST | rev2 | fail | unimplemented: Syntax: "MAILBOXID" SP "(" objectid ")" The response item in the STATUS response contains the ObjectID assigned by the server for this mailbox. |
| RFC8474-5.1-1 — Client MUST accept an EMAILID FETCH item as an opaque content identifier | MUST | rev1 | fail | unimplemented: The EMAILID data item is an ObjectID that uniquely identifies the content of a single message. |
| RFC8474-5.1-1 — Client MUST accept an EMAILID FETCH item as an opaque content identifier | MUST | rev2 | fail | unimplemented: The EMAILID data item is an ObjectID that uniquely identifies the content of a single message. |
| RFC8474-5.2-1 — Client MUST accept THREADID as OPTIONAL and NIL-valued when unsupported | MUST | rev1 | fail | unimplemented: THREADID is OPTIONAL; if the server doesn't support THREADID or is unable to calculate relationships between messages, it MUST return NIL to all FETCH responses… |
| RFC8474-5.2-1 — Client MUST accept THREADID as OPTIONAL and NIL-valued when unsupported | MUST | rev2 | fail | unimplemented: THREADID is OPTIONAL; if the server doesn't support THREADID or is unable to calculate relationships between messages, it MUST return NIL to all FETCH responses… |
| RFC8474-5.3-1 — Client MAY request the EMAILID FETCH message data item | MAY | rev1 | fail | unimplemented: The EMAILID message data item causes the server to return EMAILID FETCH response data items. |
| RFC8474-5.3-1 — Client MAY request the EMAILID FETCH message data item | MAY | rev2 | fail | unimplemented: The EMAILID message data item causes the server to return EMAILID FETCH response data items. |
| RFC8474-5.3-2 — Client MAY request the THREADID FETCH message data item | MAY | rev1 | fail | unimplemented: The THREADID message data item causes the server to return THREADID FETCH response data items. |
| RFC8474-5.3-2 — Client MAY request the THREADID FETCH message data item | MAY | rev2 | fail | unimplemented: The THREADID message data item causes the server to return THREADID FETCH response data items. |
| RFC8474-5.3-3 — Client MUST parse the EMAILID FETCH response data item | MUST | rev1 | fail | unimplemented: The EMAILID response data item contains the server-assigned ObjectID for each message. |
| RFC8474-5.3-3 — Client MUST parse the EMAILID FETCH response data item | MUST | rev2 | fail | unimplemented: The EMAILID response data item contains the server-assigned ObjectID for each message. |
| RFC8474-5.3-4 — Client MUST parse the THREADID FETCH response data item | MUST | rev1 | fail | unimplemented: The THREADID response data item contains the server-assigned ObjectID for the set of related messages to which this message belongs. |
| RFC8474-5.3-4 — Client MUST parse the THREADID FETCH response data item | MUST | rev2 | fail | unimplemented: The THREADID response data item contains the server-assigned ObjectID for the set of related messages to which this message belongs. |
| RFC8474-5.3-5 — Client MUST accept NIL as the THREADID response data item value | MUST | rev1 | fail | unimplemented: The NIL value is returned for the THREADID response data item when the server mailbox does not support THREADID calculation. |
| RFC8474-5.3-5 — Client MUST accept NIL as the THREADID response data item value | MUST | rev2 | fail | unimplemented: The NIL value is returned for the THREADID response data item when the server mailbox does not support THREADID calculation. |
| RFC8474-7-1 — Client MUST treat ObjectID values as case-sensitive (opaque) | MUST | rev1 | untestable | The document carves ObjectID values out of IMAP's default case-insensitive atom matching ('Except as noted otherwise, all alphabetic characters are case insensitive ... Implementations MUST accept these strings in a case-insensitive fashion. Please note specifically that ObjectID values are case sensitive.'), so a compliant client must compare two ObjectIDs case-SENSITIVELY (treating 'Mabc' and 'MABC' as different messages/mailboxes). But whether the client compares case-sensitively or case-insensitively is a decision made entirely inside its own cache-matching logic and is never expressed on the wire: the client emits and receives ObjectIDs as opaque byte strings either way, so a black-box probe cannot distinguish a case-sensitive comparator from a case-insensitive one. Judgment level (implicit client MUST derived from the case-sensitivity carve-out). |
| RFC8474-7-1 — Client MUST treat ObjectID values as case-sensitive (opaque) | MUST | rev2 | untestable | The document carves ObjectID values out of IMAP's default case-insensitive atom matching ('Except as noted otherwise, all alphabetic characters are case insensitive ... Implementations MUST accept these strings in a case-insensitive fashion. Please note specifically that ObjectID values are case sensitive.'), so a compliant client must compare two ObjectIDs case-SENSITIVELY (treating 'Mabc' and 'MABC' as different messages/mailboxes). But whether the client compares case-sensitively or case-insensitively is a decision made entirely inside its own cache-matching logic and is never expressed on the wire: the client emits and receives ObjectIDs as opaque byte strings either way, so a black-box probe cannot distinguish a case-sensitive comparator from a case-insensitive one. Judgment level (implicit client MUST derived from the case-sensitivity carve-out). |
| RFC8474-7-2 — Client MUST treat objectid as an opaque token per its ABNF (no assumed structure) | MUST | rev1 | untestable | The objectid production (with its comment 'characters in object identifiers are case significant') defines the identifier purely as a 1–255-character flat token drawn from ALPHA/DIGIT/'_'/'-'. It carries NO internal structure, so a client MUST treat it as opaque and MUST NOT parse, split, or infer meaning from its bytes (e.g. the 'M'/'T' prefixes shown in examples are a server convention, not a client-parseable field). Whether a client honors that opacity is invisible on the wire — an opaque-treating client and a wrongly-structure-parsing client accept and echo byte-identical ObjectIDs; the divergence only ever manifests in the client's internal handling, never in observable protocol traffic. Judgment level (implicit client MUST derived from the token grammar). |
| RFC8474-7-2 — Client MUST treat objectid as an opaque token per its ABNF (no assumed structure) | MUST | rev2 | untestable | The objectid production (with its comment 'characters in object identifiers are case significant') defines the identifier purely as a 1–255-character flat token drawn from ALPHA/DIGIT/'_'/'-'. It carries NO internal structure, so a client MUST treat it as opaque and MUST NOT parse, split, or infer meaning from its bytes (e.g. the 'M'/'T' prefixes shown in examples are a server convention, not a client-parseable field). Whether a client honors that opacity is invisible on the wire — an opaque-treating client and a wrongly-structure-parsing client accept and echo byte-identical ObjectIDs; the divergence only ever manifests in the client's internal handling, never in observable protocol traffic. Judgment level (implicit client MUST derived from the token grammar). |
| RFC8474-8.1-1 — Client MUST accept any ObjectID within the 1–255-char base64url set | MUST | rev1 | untestable | Restates the objectid value space (the base64url alphabet, length 1–255). The client-binding duty is to accommodate ANY id in this space when allocating storage and comparing — i.e. not to reject or truncate ids that use the full length or the full alphabet, and not to assume a narrower shape (such as UUID formatting). That accommodation is an internal storage/handling decision: a client that silently mishandles a maximal-length or unusual-but-valid id produces no distinguishing wire signal (it still echoes whatever bytes it received in subsequent commands), so a black-box probe cannot confirm the client sized its buffers or comparisons to the full value space. Judgment level (implicit client MUST derived from the value-space definition). Paired with RFC8474-7-1/-7-2 as the opaque-treatment cluster. |
| RFC8474-8.1-1 — Client MUST accept any ObjectID within the 1–255-char base64url set | MUST | rev2 | untestable | Restates the objectid value space (the base64url alphabet, length 1–255). The client-binding duty is to accommodate ANY id in this space when allocating storage and comparing — i.e. not to reject or truncate ids that use the full length or the full alphabet, and not to assume a narrower shape (such as UUID formatting). That accommodation is an internal storage/handling decision: a client that silently mishandles a maximal-length or unusual-but-valid id produces no distinguishing wire signal (it still echoes whatever bytes it received in subsequent commands), so a black-box probe cannot confirm the client sized its buffers or comparisons to the full value space. Judgment level (implicit client MUST derived from the value-space definition). Paired with RFC8474-7-1/-7-2 as the opaque-treatment cluster. |
| RFC8474-8.3-1 — Offline-caching client should fetch EMAILID of new messages | SHOULD | rev1 | untestable | Lowercase 'should' — non-normative per RFC 8174, carried at judgment level (a caching recommendation, not an RFC 2119 SHOULD). Even taken as a soft duty it is untestable: whether a client fetches EMAILID for caching purposes is a policy of its offline-cache subsystem, indistinguishable on the wire from a client that fetches EMAILID for any other reason or a non-caching client that never fetches it at all. No black-box probe can attribute (or require) a FETCH EMAILID to a caching strategy. |
| RFC8474-8.3-1 — Offline-caching client should fetch EMAILID of new messages | SHOULD | rev2 | untestable | Lowercase 'should' — non-normative per RFC 8174, carried at judgment level (a caching recommendation, not an RFC 2119 SHOULD). Even taken as a soft duty it is untestable: whether a client fetches EMAILID for caching purposes is a policy of its offline-cache subsystem, indistinguishable on the wire from a client that fetches EMAILID for any other reason or a non-caching client that never fetches it at all. No black-box probe can attribute (or require) a FETCH EMAILID to a caching strategy. |
| RFC8474-8.3-2 — Client should fetch MAILBOXID before discarding a mailbox's cache | SHOULD | rev1 | untestable | Lowercase 'should' — non-normative per RFC 8174, carried at judgment level. The recommendation concerns the ORDER of internal cache operations (fetch MAILBOXID for new mailboxes before evicting a vanished mailbox's cache, so a rename can be detected). Cache eviction ordering is entirely internal to the client and produces no wire-observable signature: a client that fetches MAILBOXID at the recommended moment and one that fetches it (or never fetches it) at another are indistinguishable to a black-box server. |
| RFC8474-8.3-2 — Client should fetch MAILBOXID before discarding a mailbox's cache | SHOULD | rev2 | untestable | Lowercase 'should' — non-normative per RFC 8174, carried at judgment level. The recommendation concerns the ORDER of internal cache operations (fetch MAILBOXID for new mailboxes before evicting a vanished mailbox's cache, so a rename can be detected). Cache eviction ordering is entirely internal to the client and produces no wire-observable signature: a client that fetches MAILBOXID at the recommended moment and one that fetches it (or never fetches it) at another are indistinguishable to a black-box server. |
| RFC8474-8.4-1 — Client SHOULD fall back to RFC 3501 guarantees on inconsistent ObjectIDs | SHOULD | rev1 | fail | unimplemented: In a case where a client detects inconsistent ObjectID responses from a server, it SHOULD fall back to relying on the guarantees of RFC 3501. |
| RFC8474-8.4-1 — Client SHOULD fall back to RFC 3501 guarantees on inconsistent ObjectIDs | SHOULD | rev2 | fail | unimplemented: In a case where a client detects inconsistent ObjectID responses from a server, it SHOULD fall back to relying on the guarantees of RFC 3501. |
| RFC8474-8.4-2 — Client MAY discard its entire cache and resync instead of fine-grained fallback | MAY | rev1 | fail | unimplemented: For simplicity, a client MAY instead choose to discard its entire cache and resync all state from the server. |
| RFC8474-8.4-2 — Client MAY discard its entire cache and resync instead of fine-grained fallback | MAY | rev2 | fail | unimplemented: For simplicity, a client MAY instead choose to discard its entire cache and resync all state from the server. |
| RFC8474-8.4-3 — Client MUST NOT loop forever discarding cache and re-fetching the same data | MUST | rev1 | fail | unimplemented: Client authors protecting against server misbehavior MUST ensure that their design cannot get into an infinite loop of discarding cache and fetching the same da… |
| RFC8474-8.4-3 — Client MUST NOT loop forever discarding cache and re-fetching the same data | MUST | rev2 | fail | unimplemented: Client authors protecting against server misbehavior MUST ensure that their design cannot get into an infinite loop of discarding cache and fetching the same da… |
| RFC3502-intro-1 — MULTIAPPEND is atomic (all-or-nothing) — client must handle it as such | MUST | rev1 | fail | unimplemented: A MULTIAPPEND APPEND operation is atomic; either all messages are successfully appended, or no messages are appended. |
| RFC3502-intro-1 — MULTIAPPEND is atomic (all-or-nothing) — client must handle it as such | MUST | rev2 | fail | unimplemented: A MULTIAPPEND APPEND operation is atomic; either all messages are successfully appended, or no messages are appended. |
| RFC3502-6.3.11-1 — Client MAY send multiple message-literal groups in one APPEND | MAY | rev1 | fail | unimplemented: one or more messages to upload, specified as: OPTIONAL flag parenthesized list OPTIONAL date/time string message literal ... append = "APPEND" SP mailbox 1*appe… |
| RFC3502-6.3.11-1 — Client MAY send multiple message-literal groups in one APPEND | MAY | rev2 | fail | unimplemented: one or more messages to upload, specified as: OPTIONAL flag parenthesized list OPTIONAL date/time string message literal ... append = "APPEND" SP mailbox 1*appe… |
| RFC3502-6.3.11-2 — Each appended message SHOULD be in RFC-2822 format | SHOULD | rev1 | untestable | Whether the octets the client uploads for a given message-literal argument 'are in the format of an RFC-2822 message' is a judgment about the content/shape of the message body, not about the APPEND command's wire framing. A client that uploads a well-formed RFC-2822 message and one that uploads arbitrary octets of the same declared length produce byte-for-byte-identical APPEND framing on the wire (same literal count, same flags, same date-time); the SHOULD can only be assessed by parsing and semantically validating the uploaded body against RFC 2822, which the compliance harness deliberately does not do (the client supplies message bodies opaquely). No black-box test of the APPEND exchange can distinguish a compliant RFC-2822 upload from a non-conforming one. |
| RFC3502-6.3.11-2 — Each appended message SHOULD be in RFC-2822 format | SHOULD | rev2 | untestable | Whether the octets the client uploads for a given message-literal argument 'are in the format of an RFC-2822 message' is a judgment about the content/shape of the message body, not about the APPEND command's wire framing. A client that uploads a well-formed RFC-2822 message and one that uploads arbitrary octets of the same declared length produce byte-for-byte-identical APPEND framing on the wire (same literal count, same flags, same date-time); the SHOULD can only be assessed by parsing and semantically validating the uploaded body against RFC 2822, which the compliance harness deliberately does not do (the client supplies message bodies opaquely). No black-box test of the APPEND exchange can distinguish a compliant RFC-2822 upload from a non-conforming one. |
| RFC3502-6.3.11-3 — On failure the mailbox is restored (no partial append) — client-relied guarantee | MUST | rev1 | fail | unimplemented: If the append is unsuccessful for any reason (including being cancelled), the mailbox MUST be restored to its state before the APPEND attempt; no partial append… |
| RFC3502-6.3.11-3 — On failure the mailbox is restored (no partial append) — client-relied guarantee | MUST | rev2 | fail | unimplemented: If the append is unsuccessful for any reason (including being cancelled), the mailbox MUST be restored to its state before the APPEND attempt; no partial append… |
| RFC3502-6.3.11-4 — Server MAY abort before processing all messages — client must handle early failure | MAY | rev1 | fail | unimplemented: The server MAY return an error before processing all the message arguments. |
| RFC3502-6.3.11-4 — Server MAY abort before processing all messages — client must handle early failure | MAY | rev2 | fail | unimplemented: The server MAY return an error before processing all the message arguments. |
| RFC3502-6.3.11-5 — Client MAY issue NOOP/CHECK after APPEND if no unsolicited EXISTS arrives | MAY | rev1 | fail | unimplemented: If the server does not do so, the client MAY issue a NOOP command (or failing that, a CHECK command) after one or more APPEND commands. |
| RFC3502-6.3.11-5 — Client MAY issue NOOP/CHECK after APPEND if no unsolicited EXISTS arrives | MAY | rev2 | fail | unimplemented: If the server does not do so, the client MAY issue a NOOP command (or failing that, a CHECK command) after one or more APPEND commands. |
| RFC3502-uidplus-1 — Client must accept a set-valued APPENDUID response for a MULTIAPPEND batch | MUST | rev1 | fail | unimplemented: resp-code-apnd = "APPENDUID" SP nz-number SP set ... That is, the APPENDUID response code returns as many UIDs as there were messages appended in the multiple a… |
| RFC3502-uidplus-1 — Client must accept a set-valued APPENDUID response for a MULTIAPPEND batch | MUST | rev2 | fail | unimplemented: resp-code-apnd = "APPENDUID" SP nz-number SP set ... That is, the APPENDUID response code returns as many UIDs as there were messages appended in the multiple a… |
| RFC4469-2-1 — Client (implicit) only uses CATENATE when the server advertises the capability | MUST | rev1 | untestable | The document states the capability contract from the server's side (the server advertises "CATENATE" via CAPABILITY); the reciprocal client duty — do not send the extended CATENATE APPEND form to a server that has not advertised CATENATE — is implicit in that contract. Whether the client consulted the advertised capability list before emitting CATENATE is an internal decision about its own capability inventory: a client that issues CATENATE speculatively without checking, and a client that checked and found it advertised, produce byte-identical wire forms. Any divergence surfaces only as the server's own BAD/NO rejection of an unsupported command, which is the server's behavior, not an observable property of the client's capability bookkeeping. |
| RFC4469-2-1 — Client (implicit) only uses CATENATE when the server advertises the capability | MUST | rev2 | untestable | The document states the capability contract from the server's side (the server advertises "CATENATE" via CAPABILITY); the reciprocal client duty — do not send the extended CATENATE APPEND form to a server that has not advertised CATENATE — is implicit in that contract. Whether the client consulted the advertised capability list before emitting CATENATE is an internal decision about its own capability inventory: a client that issues CATENATE speculatively without checking, and a client that checked and found it advertised, produce byte-identical wire forms. Any divergence surfaces only as the server's own BAD/NO rejection of an unsupported command, which is the server's behavior, not an observable property of the client's capability bookkeeping. |
| RFC4469-3-1 — Client emits the extended APPEND with CATENATE and a parenthesized part list | MUST | rev1 | fail | unimplemented: If the extended form is used, "CATENATE" and a parenthesized list of message literals and message URLs follows, each of which is appended to the new message. |
| RFC4469-3-1 — Client emits the extended APPEND with CATENATE and a parenthesized part list | MUST | rev2 | fail | unimplemented: If the extended form is used, "CATENATE" and a parenthesized list of message literals and message URLs follows, each of which is appended to the new message. |
| RFC4469-3-2 — Client encodes a TEXT cat-part as the atom TEXT followed by a literal | MUST | rev1 | fail | unimplemented: text-literal = "TEXT" SP literal |
| RFC4469-3-2 — Client encodes a TEXT cat-part as the atom TEXT followed by a literal | MUST | rev2 | fail | unimplemented: text-literal = "TEXT" SP literal |
| RFC4469-3-3 — Client encodes a URL cat-part as the atom URL followed by an IMAP-URL astring | MUST | rev1 | fail | unimplemented: url = "URL" SP astring |
| RFC4469-3-3 — Client encodes a URL cat-part as the atom URL followed by an IMAP-URL astring | MUST | rev2 | fail | unimplemented: url = "URL" SP astring |
| RFC4469-3-4 — Client includes at least one cat-part in the CATENATE list | MUST | rev1 | fail | unimplemented: append-data =/ "CATENATE" SP "(" cat-part *(SP cat-part) ")" |
| RFC4469-3-4 — Client includes at least one cat-part in the CATENATE list | MUST | rev2 | fail | unimplemented: append-data =/ "CATENATE" SP "(" cat-part *(SP cat-part) ")" |
| RFC4469-3-5 — Client (implicit) uses only relative current-session IMAP URLs in CATENATE | SHOULD | rev1 | untestable | This scopes CATENATE URLs to relative message/part references resolvable from the current authenticated session; the following note warns that 'a server implementing only this specification would return NO' to an absolute or out-of-session URL. Whether a given URL the client composes stays within this relative/current-session scope is a decision about what data the client chooses to reference, driven by its own intent — not a fixed protocol framing a black-box test can pin. A relative and an out-of-scope URL are both syntactically valid astrings on the wire; the only consequence of overreach is the server's own NO, which is server behavior. The client's adherence to the intended scope is thus not observable independently of the server's rejection. |
| RFC4469-3-5 — Client (implicit) uses only relative current-session IMAP URLs in CATENATE | SHOULD | rev2 | untestable | This scopes CATENATE URLs to relative message/part references resolvable from the current authenticated session; the following note warns that 'a server implementing only this specification would return NO' to an absolute or out-of-session URL. Whether a given URL the client composes stays within this relative/current-session scope is a decision about what data the client chooses to reference, driven by its own intent — not a fixed protocol framing a black-box test can pin. A relative and an out-of-scope URL are both syntactically valid astrings on the wire; the only consequence of overreach is the server's own NO, which is server behavior. The client's adherence to the intended scope is thus not observable independently of the server's rejection. |
| RFC4469-3-6 — Client (implicit) ensures the catenated result is a valid RFC 2822/MIME message | MUST | rev1 | untestable | The duty is that the message assembled from the catenated TEXT and URL parts is a well-formed Internet Message Format (RFC 2822) or MIME message. This is a property of the message content the client composes from its parts, not of the IMAP protocol framing: the client cannot even see the octets a URL part will contribute (the server splices them server-side), and whether the final assembled message parses as valid RFC 2822/MIME is a content-processing judgment over message bodies rather than a wire-observable command form. A black-box compliance harness observing the IMAP exchange cannot decide message-format validity of the server-assembled result. |
| RFC4469-3-6 — Client (implicit) ensures the catenated result is a valid RFC 2822/MIME message | MUST | rev2 | untestable | The duty is that the message assembled from the catenated TEXT and URL parts is a well-formed Internet Message Format (RFC 2822) or MIME message. This is a property of the message content the client composes from its parts, not of the IMAP protocol framing: the client cannot even see the octets a URL part will contribute (the server splices them server-side), and whether the final assembled message parses as valid RFC 2822/MIME is a content-processing judgment over message bodies rather than a wire-observable command form. A black-box compliance harness observing the IMAP exchange cannot decide message-format validity of the server-assembled result. |
| RFC4469-3-7 — Client (implicit) inserts MIME boundaries and Content-Type/CTE lines as needed | MUST | rev1 | untestable | Because the server copies URL-referenced octets unchanged and does no data conversion or MIME verification, the client must itself supply the MIME structure gluing the catenated parts together: boundary delimiters between body parts and the Content-Type / Content-Transfer-Encoding header lines in the right places. This is content-processing over the message the client assembles — the correctness of the MIME scaffolding lives in the message body octets, not in the IMAP command framing, and a black-box harness cannot judge whether the boundaries and header lines the client emitted are 'appropriate' for the parts being joined. |
| RFC4469-3-7 — Client (implicit) inserts MIME boundaries and Content-Type/CTE lines as needed | MUST | rev2 | untestable | Because the server copies URL-referenced octets unchanged and does no data conversion or MIME verification, the client must itself supply the MIME structure gluing the catenated parts together: boundary delimiters between body parts and the Content-Type / Content-Transfer-Encoding header lines in the right places. This is content-processing over the message the client assembles — the correctness of the MIME scaffolding lives in the message body octets, not in the IMAP command framing, and a black-box harness cannot judge whether the boundaries and header lines the client emitted are 'appropriate' for the parts being joined. |
| RFC4469-4.1-1 — Client accepts a BADURL resp-code on a CATENATE append failure | MUST | rev1 | pass |  |
| RFC4469-4.1-1 — Client accepts a BADURL resp-code on a CATENATE append failure | MUST | rev2 | pass |  |
| RFC4469-4.2-1 — Client accepts a TOOBIG resp-code on a CATENATE append failure | MUST | rev1 | pass |  |
| RFC4469-4.2-1 — Client accepts a TOOBIG resp-code on a CATENATE append failure | MUST | rev2 | pass |  |
| RFC3516-4.2-1 — Client MAY request a section decoded via the BINARY / BINARY.PEEK FETCH data item | MAY | rev1 | fail | unimplemented: Requests that the specified section be transmitted after performing CTE-related decoding. |
| RFC3516-4.2-1 — Client MAY request a section decoded via the BINARY / BINARY.PEEK FETCH data item | MAY | rev2 | fail | unimplemented: Requests that the specified section be transmitted after performing CTE-related decoding. |
| RFC3516-4.2-2 — Partial FETCH BINARY <partial> arguments refer to the DECODED section data | MUST | rev1 | fail | unimplemented: The <partial> argument, if present, requests that a subset of the data be returned. The semantics of a partial FETCH BINARY command are the same as for a partia… |
| RFC3516-4.2-2 — Partial FETCH BINARY <partial> arguments refer to the DECODED section data | MUST | rev2 | fail | unimplemented: The <partial> argument, if present, requests that a subset of the data be returned. The semantics of a partial FETCH BINARY command are the same as for a partia… |
| RFC3516-4.2-3 — Client SHOULD NOT needlessly issue BINARY.SIZE (potentially expensive server operation) | SHOULD | rev1 | untestable | 'Needlessly issuing this request' binds the client's operational judgment about when the decoded size is actually needed, not a discrete wire action: any individual BINARY.SIZE request is syntactically legal and RFC-permitted, so a client that issues them freely produces the same compliant wire trace as one that issues them sparingly out of genuine need — whether a given request was 'needless' has no wire signature. Identical reasoning to the rev2-core counterpart RFC9051-6.4.5-4 (and to RFC9051-6.3.11-3, the STATUS SIZE caution). |
| RFC3516-4.3-1 — Client MUST accept a BINARY[...] value as either an <nstring> or a <literal8> (NUL-permitting) | MUST | rev1 | pass |  |
| RFC3516-4.3-2 — Client MUST read the BINARY.SIZE response as the decoded octet count | MUST | rev1 | fail | unimplemented: Requests the decoded size of the section (i.e., the size to expect in response to the corresponding FETCH BINARY request). |
| RFC3516-4.3-2 — Client MUST read the BINARY.SIZE response as the decoded octet count | MUST | rev2 | fail | unimplemented: Requests the decoded size of the section (i.e., the size to expect in response to the corresponding FETCH BINARY request). |
| RFC3516-4.4-1 — Client MAY APPEND NUL-containing data using the <literal8> (~{n}) syntax | MAY | rev1 | fail | unimplemented: The APPEND command is extended to allow the client to append data containing NULs by using the <literal8> syntax. |
| RFC3516-6-1 — Client supporting BINARY SHOULD be prepared to perform its own CTE decoding | SHOULD | rev1 | untestable | Whether a client is 'prepared to perform its own CTE decoding' is an internal content-processing capability, not a wire-observable action: the BINARY extension is an optimization that lets the SERVER pre-decode, so a compliant client that has BINARY available may never need to exercise its own decoder on the wire, and one that lacks the decoder is indistinguishable at the protocol level until it actually encounters CTE-encoded content via a non-BINARY FETCH BODY — at which point the decoding happens inside the client (or, for a protocol library, is legitimately delegated to the consuming application by handing over the raw transfer-encoded string). No black-box exchange has a mandated pass/fail boundary for this readiness. Same reasoning as the rev2-core counterpart RFC9051-7.5.2-2 (client MUST decode transfer-encoded binary). |
| RFC8508-3.2-1 — Client MUST emit the REPLACE command with seq-number, mailbox, and append-message | MUST | rev1 | fail | unimplemented: The REPLACE and UID REPLACE commands take five arguments: a message identifier, a named mailbox, an optional parenthesized flag list, an optional message date/t… |
| RFC8508-3.2-1 — Client MUST emit the REPLACE command with seq-number, mailbox, and append-message | MUST | rev2 | fail | unimplemented: The REPLACE and UID REPLACE commands take five arguments: a message identifier, a named mailbox, an optional parenthesized flag list, an optional message date/t… |
| RFC8508-3.2-2 — Client MUST treat the REPLACE responses as a single atomic action | MUST | rev1 | fail | unimplemented: The message literal will be appended to the named mailbox, and the message specified by the message identifier will be removed from the selected mailbox. These … |
| RFC8508-3.2-2 — Client MUST treat the REPLACE responses as a single atomic action | MUST | rev2 | fail | unimplemented: The message literal will be appended to the named mailbox, and the message specified by the message identifier will be removed from the selected mailbox. These … |
| RFC8508-3.3-1 — Client MUST emit UID REPLACE as UID SP REPLACE with a UID first parameter | MUST | rev1 | fail | unimplemented: This extends the first form of the UID command (see Section 6.4.8 of [RFC3501]) to add the REPLACE command defined above as a valid argument. This form of REPLA… |
| RFC8508-3.3-1 — Client MUST emit UID REPLACE as UID SP REPLACE with a UID first parameter | MUST | rev2 | fail | unimplemented: This extends the first form of the UID command (see Section 6.4.8 of [RFC3501]) to add the REPLACE command defined above as a valid argument. This form of REPLA… |
| RFC8508-3.4-1 — Client MUST accept APPEND and EXPUNGE response codes and expect no STORE response | MUST | rev1 | fail | unimplemented: In particular, the response codes for APPEND and EXPUNGE will be returned while those for the STORE operation MUST NOT be generated. |
| RFC8508-3.4-1 — Client MUST accept APPEND and EXPUNGE response codes and expect no STORE response | MUST | rev2 | fail | unimplemented: In particular, the response codes for APPEND and EXPUNGE will be returned while those for the STORE operation MUST NOT be generated. |
| RFC8508-3.4-3 — Client MAY target a mailbox other than the selected one | MAY | rev1 | untestable | This is a permissive statement lifting any requirement that the REPLACE target mailbox equal the selected mailbox — it grants the client latitude rather than imposing an observable duty. Which mailbox a client names in a REPLACE (the same special-use \Drafts it has selected, or a different one such as \Sent after submission) is the client's own compose/submission-workflow policy; a REPLACE naming the selected mailbox and a REPLACE naming a different mailbox are both syntactically legal command forms, so no black-box test can distinguish a client that 'correctly allows different mailboxes' from one that only ever happens to name the selected one — there is no wrong wire form to catch. The removal always applies to the message in the selected mailbox (identified by the seq/UID) while the append goes to the named mailbox; that split is fixed by RFC8508-3.2-2, independent of whether the two mailboxes coincide. |
| RFC8508-3.4-3 — Client MAY target a mailbox other than the selected one | MAY | rev2 | untestable | This is a permissive statement lifting any requirement that the REPLACE target mailbox equal the selected mailbox — it grants the client latitude rather than imposing an observable duty. Which mailbox a client names in a REPLACE (the same special-use \Drafts it has selected, or a different one such as \Sent after submission) is the client's own compose/submission-workflow policy; a REPLACE naming the selected mailbox and a REPLACE naming a different mailbox are both syntactically legal command forms, so no black-box test can distinguish a client that 'correctly allows different mailboxes' from one that only ever happens to name the selected one — there is no wrong wire form to catch. The removal always applies to the message in the selected mailbox (identified by the seq/UID) while the append goes to the named mailbox; that split is fixed by RFC8508-3.2-2, independent of whether the two mailboxes coincide. |
| RFC8508-3.4-4 — Client SHOULD handle APPEND-affecting extension response codes on REPLACE (e.g. TRYCREATE) | SHOULD | rev1 | fail | unimplemented: Because of the similarity of REPLACE to APPEND, extensions that affect APPEND affect REPLACE in the same way. |
| RFC8508-3.4-4 — Client SHOULD handle APPEND-affecting extension response codes on REPLACE (e.g. TRYCREATE) | SHOULD | rev2 | fail | unimplemented: Because of the similarity of REPLACE to APPEND, extensions that affect APPEND affect REPLACE in the same way. |
| RFC8508-3.5-1 — Client MUST NOT issue REPLACE / UID REPLACE outside the selected state | MUST NOT | rev1 | fail | unimplemented: Unlike the APPEND command, which is valid in the authenticated state, the REPLACE and UID REPLACE commands MUST only be valid in the selected state. |
| RFC8508-3.5-1 — Client MUST NOT issue REPLACE / UID REPLACE outside the selected state | MUST NOT | rev2 | fail | unimplemented: Unlike the APPEND command, which is valid in the authenticated state, the REPLACE and UID REPLACE commands MUST only be valid in the selected state. |
| RFC8508-3.5-2 — Client MUST NOT issue UID REPLACE from the authenticated state | MUST NOT | rev1 | fail | unimplemented: Additionally, the REPLACE extension intentionally follows the convention for UID commands found in Section 6.4.8 of [RFC3501] in that the UID variant of the com… |
| RFC8508-3.5-2 — Client MUST NOT issue UID REPLACE from the authenticated state | MUST NOT | rev2 | fail | unimplemented: Additionally, the REPLACE extension intentionally follows the convention for UID commands found in Section 6.4.8 of [RFC3501] in that the UID variant of the com… |
| RFC8508-4.3-1 — Client MUST accept APPENDUID whether it precedes or follows the EXPUNGE | SHOULD | rev1 | untestable | The 'advised to send ... before' guidance binds the SERVER's response ordering; the client-facing reflection is that a client must be able to correlate the APPENDUID for the new message with the REPLACE regardless of whether it arrives before or after the EXPUNGE for the replaced message (the parenthetical warns that receiving EXPUNGE first 'can be unnecessarily difficult to process that sequence usefully'). Whether a client internally correlates APPENDUID to the right REPLACE is not distinguishable on the wire: a client that robustly handles either ordering and a client that only tolerates the recommended ordering both consume exactly the same bytes and emit no observable difference in either the good or the awkward case — the correlation is an internal bookkeeping decision. No black-box test can separate a compliant tolerant client from a fragile one absent a subsequent command that reveals the client used the wrong UID, which would be that other command's duty, not this ordering-tolerance duty. |
| RFC8508-4.3-1 — Client MUST accept APPENDUID whether it precedes or follows the EXPUNGE | SHOULD | rev2 | untestable | The 'advised to send ... before' guidance binds the SERVER's response ordering; the client-facing reflection is that a client must be able to correlate the APPENDUID for the new message with the REPLACE regardless of whether it arrives before or after the EXPUNGE for the replaced message (the parenthetical warns that receiving EXPUNGE first 'can be unnecessarily difficult to process that sequence usefully'). Whether a client internally correlates APPENDUID to the right REPLACE is not distinguishable on the wire: a client that robustly handles either ordering and a client that only tolerates the recommended ordering both consume exactly the same bytes and emit no observable difference in either the good or the awkward case — the correlation is an internal bookkeeping decision. No black-box test can separate a compliant tolerant client from a fragile one absent a subsequent command that reveals the client used the wrong UID, which would be that other command's duty, not this ordering-tolerance duty. |
| RFC7162-3.1-1 — CONDSTORE-aware client MUST accept mod-sequence data in all subsequent untagged FETCH responses | MUST | rev1 | pass |  |
| RFC7162-3.1-1 — CONDSTORE-aware client MUST accept mod-sequence data in all subsequent untagged FETCH responses | MUST | rev2 | pass |  |
| RFC7162-3.1.1-1 — Client (implicit) MUST NOT use CONDSTORE protocol changes unless CONDSTORE is advertised | MUST NOT | rev1 | pass |  |
| RFC7162-3.1.1-1 — Client (implicit) MUST NOT use CONDSTORE protocol changes unless CONDSTORE is advertised | MUST NOT | rev2 | pass |  |
| RFC7162-3.1.2-1 — Client MUST accept HIGHESTMODSEQ or NOMODSEQ on post-enabling SELECT/EXAMINE | MUST | rev1 | pass |  |
| RFC7162-3.1.2-1 — Client MUST accept HIGHESTMODSEQ or NOMODSEQ on post-enabling SELECT/EXAMINE | MUST | rev2 | pass |  |
| RFC7162-3.1.2.1-1 — Client MUST delete its cached HIGHESTMODSEQ when UIDVALIDITY changes | MUST | rev1 | untestable | An explicit client MUST, but its object is a cross-session cache entry, not a wire action: deleting (or failing to delete) the cached HIGHESTMODSEQ produces no protocol trace in the session where the UIDVALIDITY change is observed. Taxonomy mechanism (b) (sequential multi-connection scripts) was evaluated and rebutted: a second scripted session could in principle reveal a stale cached value if the client emitted it in a QRESYNC parameter or CHANGEDSINCE argument, but this headless library keeps no cross-session synchronization cache — the modseq values it emits are supplied by the caller per command — so no second-session wire trace can attribute a stale value to the client's own (non-existent) cache discipline rather than to harness input. Same rebuttal pattern as RFC9051-2.3.1.1-1. |
| RFC7162-3.1.2.1-1 — Client MUST delete its cached HIGHESTMODSEQ when UIDVALIDITY changes | MUST | rev2 | untestable | An explicit client MUST, but its object is a cross-session cache entry, not a wire action: deleting (or failing to delete) the cached HIGHESTMODSEQ produces no protocol trace in the session where the UIDVALIDITY change is observed. Taxonomy mechanism (b) (sequential multi-connection scripts) was evaluated and rebutted: a second scripted session could in principle reveal a stale cached value if the client emitted it in a QRESYNC parameter or CHANGEDSINCE argument, but this headless library keeps no cross-session synchronization cache — the modseq values it emits are supplied by the caller per command — so no second-session wire trace can attribute a stale value to the client's own (non-existent) cache discipline rather than to harness input. Same rebuttal pattern as RFC9051-2.3.1.1-1. |
| RFC7162-3.1.2.1-2 — Client MAY use SEARCH MODSEQ or FETCH CHANGEDSINCE to update its cache | MAY | rev1 | untestable | A pure permission pair: both clauses grant alternative, equally compliant resynchronization strategies (SEARCH MODSEQ vs. FETCH CHANGEDSINCE), and a client may equally well use neither (e.g. refetch everything). Which mechanism a client picks — or whether it resynchronizes at all — is an internal strategy decision with no pass/fail wire boundary: any of the three observable behaviors is compliant, so no black-box exchange can falsify the duty. The command FORMS the permissions point at are separately scored as testable entries (RFC7162-3.1.5-1, RFC7162-3.1.4.1-1). |
| RFC7162-3.1.2.1-2 — Client MAY use SEARCH MODSEQ or FETCH CHANGEDSINCE to update its cache | MAY | rev2 | untestable | A pure permission pair: both clauses grant alternative, equally compliant resynchronization strategies (SEARCH MODSEQ vs. FETCH CHANGEDSINCE), and a client may equally well use neither (e.g. refetch everything). Which mechanism a client picks — or whether it resynchronizes at all — is an internal strategy decision with no pass/fail wire boundary: any of the three observable behaviors is compliant, so no black-box exchange can falsify the duty. The command FORMS the permissions point at are separately scored as testable entries (RFC7162-3.1.5-1, RFC7162-3.1.4.1-1). |
| RFC7162-3.1.2.2-1 — Client (implicit) MUST NOT use CONDSTORE modifiers on a NOMODSEQ mailbox | MUST NOT | rev1 | fail | unimplemented: A server that returned the NOMODSEQ response code for a mailbox MUST reject (with a tagged BAD response) any of the following commands while the mailbox remains… |
| RFC7162-3.1.2.2-1 — Client (implicit) MUST NOT use CONDSTORE modifiers on a NOMODSEQ mailbox | MUST NOT | rev2 | fail | unimplemented: A server that returned the NOMODSEQ response code for a mailbox MUST reject (with a tagged BAD response) any of the following commands while the mailbox remains… |
| RFC7162-3.1.3-1 — UNCHANGEDSINCE STORE modifier form | MAY | rev1 | fail | unimplemented: This document defines the following STORE modifier (see Section 2.5 of [RFC4466]): UNCHANGEDSINCE <mod-sequence> ... If the mod-sequence of every metadata item … |
| RFC7162-3.1.3-1 — UNCHANGEDSINCE STORE modifier form | MAY | rev2 | fail | unimplemented: This document defines the following STORE modifier (see Section 2.5 of [RFC4466]): UNCHANGEDSINCE <mod-sequence> ... If the mod-sequence of every metadata item … |
| RFC7162-3.1.3-2 — Client MUST accept the untagged FETCH with MODSEQ even for .SILENT stores | MUST | rev1 | pass |  |
| RFC7162-3.1.3-2 — Client MUST accept the untagged FETCH with MODSEQ even for .SILENT stores | MUST | rev2 | pass |  |
| RFC7162-3.1.3-3 — Client MUST accept the [MODIFIED set] response code on tagged OK (and NO) | MUST | rev1 | pass |  |
| RFC7162-3.1.3-3 — Client MUST accept the [MODIFIED set] response code on tagged OK (and NO) | MUST | rev2 | pass |  |
| RFC7162-3.1.3-4 — Client MUST be prepared for MODIFIED when the watched item hasn't changed | MUST | rev1 | untestable | 'MUST be prepared to deal with' binds the client's internal conflict-resolution logic (its atomic-change algorithm must not assume MODIFIED implies the watched item changed), not a specific wire action. The wire-observable half — accepting the MODIFIED response code at all — is already scored as RFC7162-3.1.3-3, and the observable recovery steps the RFC recommends are separately scored as RFC7162-3.1.3-5/-6 (SHOULD probe, SHOULD retry). What remains of THIS sentence is the client's readiness assumption: a client whose logic correctly tolerates spurious MODIFIED and one that would deadlock or corrupt its model both emit identical traffic in any single scripted exchange, because 'dealing with' the case is an internal-design property that only manifests through the already-scored recovery behaviors — scoring it separately as testable would double-count -5/-6's observables. |
| RFC7162-3.1.3-4 — Client MUST be prepared for MODIFIED when the watched item hasn't changed | MUST | rev2 | untestable | 'MUST be prepared to deal with' binds the client's internal conflict-resolution logic (its atomic-change algorithm must not assume MODIFIED implies the watched item changed), not a specific wire action. The wire-observable half — accepting the MODIFIED response code at all — is already scored as RFC7162-3.1.3-3, and the observable recovery steps the RFC recommends are separately scored as RFC7162-3.1.3-5/-6 (SHOULD probe, SHOULD retry). What remains of THIS sentence is the client's readiness assumption: a client whose logic correctly tolerates spurious MODIFIED and one that would deadlock or corrupt its model both emit identical traffic in any single scripted exchange, because 'dealing with' the case is an internal-design property that only manifests through the already-scored recovery behaviors — scoring it separately as testable would double-count -5/-6's observables. |
| RFC7162-3.1.3-5 — On MODIFIED without explanatory FETCH, client SHOULD probe via FETCH or NOOP | SHOULD | rev1 | fail | unimplemented: Unless the server has included an unsolicited FETCH to update the client's knowledge about messages that have failed the UNCHANGEDSINCE test, upon receipt of th… |
| RFC7162-3.1.3-5 — On MODIFIED without explanatory FETCH, client SHOULD probe via FETCH or NOOP | SHOULD | rev2 | fail | unimplemented: Unless the server has included an unsolicited FETCH to update the client's knowledge about messages that have failed the UNCHANGEDSINCE test, upon receipt of th… |
| RFC7162-3.1.3-6 — Client SHOULD retry with the new mod-sequence; allow at least 2 retries | SHOULD | rev1 | fail | unimplemented: If the required metadata items haven't changed, the client SHOULD retry the command with the new mod-sequence. The client needs to allow for a reasonable number… |
| RFC7162-3.1.3-6 — Client SHOULD retry with the new mod-sequence; allow at least 2 retries | SHOULD | rev2 | fail | unimplemented: If the required metadata items haven't changed, the client SHOULD retry the command with the new mod-sequence. The client needs to allow for a reasonable number… |
| RFC7162-3.1.4.1-1 — CHANGEDSINCE FETCH modifier form; implicitly adds the MODSEQ data item | MAY | rev1 | fail | unimplemented: CHANGEDSINCE <mod-sequence>: The CHANGEDSINCE FETCH modifier allows the client to further subset the list of messages described by the sequence set. The informa… |
| RFC7162-3.1.4.1-1 — CHANGEDSINCE FETCH modifier form; implicitly adds the MODSEQ data item | MAY | rev2 | fail | unimplemented: CHANGEDSINCE <mod-sequence>: The CHANGEDSINCE FETCH modifier allows the client to further subset the list of messages described by the sequence set. The informa… |
| RFC7162-3.1.4.2-1 — MODSEQ message data item in the FETCH command | MAY | rev1 | fail | unimplemented: CONDSTORE adds a MODSEQ message data item to the FETCH command. The MODSEQ message data item allows clients to retrieve mod-sequence values for a range of messa… |
| RFC7162-3.1.4.2-1 — MODSEQ message data item in the FETCH command | MAY | rev2 | fail | unimplemented: CONDSTORE adds a MODSEQ message data item to the FETCH command. The MODSEQ message data item allows clients to retrieve mod-sequence values for a range of messa… |
| RFC7162-3.1.4.2-2 — Client MUST accept the MODSEQ (n) FETCH response data item | MUST | rev1 | pass |  |
| RFC7162-3.1.4.2-2 — Client MUST accept the MODSEQ (n) FETCH response data item | MUST | rev2 | pass |  |
| RFC7162-3.1.5-1 — MODSEQ search criterion form, incl. quoted entry-name escaping | MAY | rev1 | fail | unimplemented: Syntax: MODSEQ [<entry-name> <entry-type-req>] <mod-sequence-valzer> Messages that have modification values that are equal to or greater than <mod-sequence-valz… |
| RFC7162-3.1.5-1 — MODSEQ search criterion form, incl. quoted entry-name escaping | MAY | rev2 | fail | unimplemented: Syntax: MODSEQ [<entry-name> <entry-type-req>] <mod-sequence-valzer> Messages that have modification values that are equal to or greater than <mod-sequence-valz… |
| RFC7162-3.1.6-1 — Client MUST accept the extended untagged SEARCH response with (MODSEQ n) | MUST | rev1 | pass |  |
| RFC7162-3.1.7-1 — HIGHESTMODSEQ status data item: request form and value acceptance (incl. 0) | MUST | rev1 | fail | unimplemented: HIGHESTMODSEQ: The highest mod-sequence value of all messages in the mailbox. This is the same value that is returned by the server in the HIGHESTMODSEQ respons… |
| RFC7162-3.1.7-1 — HIGHESTMODSEQ status data item: request form and value acceptance (incl. 0) | MUST | rev2 | fail | unimplemented: HIGHESTMODSEQ: The highest mod-sequence value of all messages in the mailbox. This is the same value that is returned by the server in the HIGHESTMODSEQ respons… |
| RFC7162-3.1.8-1 — CONDSTORE select parameter form | MAY | rev1 | fail | unimplemented: The CONDSTORE extension defines a single optional select parameter, "CONDSTORE", which tells the server that it MUST include the MODSEQ FETCH response data item… |
| RFC7162-3.1.8-1 — CONDSTORE select parameter form | MAY | rev2 | fail | unimplemented: The CONDSTORE extension defines a single optional select parameter, "CONDSTORE", which tells the server that it MUST include the MODSEQ FETCH response data item… |
| RFC7162-3.1.9-1 — Client MUST accept the extended untagged SORT response with (MODSEQ n) | MUST | rev1 | pass |  |
| RFC7162-3.1.9-1 — Client MUST accept the extended untagged SORT response with (MODSEQ n) | MUST | rev2 | pass |  |
| RFC7162-3.1.11-1 — Client MUST NOT rely on servers suppressing no-op flag-change mod-sequence bumps | MUST NOT | rev1 | untestable | 'MUST NOT rely on' forbids a design assumption (that adding an already-present flag or removing an absent one never changes the mod-sequence — the server SHOULD in the same section), not any wire behavior. A client whose synchronization logic wrongly bakes in that assumption and one that correctly treats every mod-sequence bump as a potential real change issue identical commands; the divergence lives in how the client's internal cache-update logic classifies an observed bump, and the RFC itself supplies the reason no observer can adjudicate it: the two candidate causes (server SHOULD-violation vs. a genuine set-then-unset in another session) are declared indistinguishable on the wire even to the client. No black-box exchange can therefore construct a pass/fail boundary for 'relying'. |
| RFC7162-3.1.11-1 — Client MUST NOT rely on servers suppressing no-op flag-change mod-sequence bumps | MUST NOT | rev2 | untestable | 'MUST NOT rely on' forbids a design assumption (that adding an already-present flag or removing an absent one never changes the mod-sequence — the server SHOULD in the same section), not any wire behavior. A client whose synchronization logic wrongly bakes in that assumption and one that correctly treats every mod-sequence bump as a potential real change issue identical commands; the divergence lives in how the client's internal cache-update logic classifies an observed bump, and the RFC itself supplies the reason no observer can adjudicate it: the two candidate causes (server SHOULD-violation vs. a genuine set-then-unset in another session) are declared indistinguishable on the wire even to the client. No black-box exchange can therefore construct a pass/fail boundary for 'relying'. |
| RFC7162-4-1 — Client should keep command lines to ~8192 octets; split or use literals | SHOULD | rev1 | untestable | A quality/efficiency recommendation with no crisp compliance boundary: the limit is 'approximately 8192 octets', so no matcher can draw a defensible pass/fail line (8193? 9000? 12000?) without inventing a threshold the RFC deliberately left fuzzy. The grouping/splitting duty also operates on data the client does not originate in this harness: the UID sets that would blow past the limit are supplied verbatim by the caller (the library performs no set compaction or request splitting of its own), so an over-long line would attribute to harness input, not to the client's range-grouping discipline. Like the taxonomy's defining member, the sentence encodes an expectation about resource-conscious behavior rather than a bounded protocol obligation. |
| RFC7162-4-1 — Client should keep command lines to ~8192 octets; split or use literals | SHOULD | rev2 | untestable | A quality/efficiency recommendation with no crisp compliance boundary: the limit is 'approximately 8192 octets', so no matcher can draw a defensible pass/fail line (8193? 9000? 12000?) without inventing a threshold the RFC deliberately left fuzzy. The grouping/splitting duty also operates on data the client does not originate in this harness: the UID sets that would blow past the limit are supplied verbatim by the caller (the library performs no set compaction or request splitting of its own), so an over-long line would attribute to harness input, not to the client's range-grouping discipline. Like the taxonomy's defining member, the sentence encodes an expectation about resource-conscious behavior rather than a bounded protocol obligation. |
| RFC7162-3.2.3-1 — Client making use of QRESYNC MUST issue ENABLE QRESYNC once authenticated | MUST | rev1 | pass |  |
| RFC7162-3.2.3-1 — Client making use of QRESYNC MUST issue ENABLE QRESYNC once authenticated | MUST | rev2 | pass |  |
| RFC7162-3.2.3-2 — Client (implicit) MUST NOT use QRESYNC param or VANISHED modifier without ENABLED QRESYNC | MUST NOT | rev1 | pass |  |
| RFC7162-3.2.3-2 — Client (implicit) MUST NOT use QRESYNC param or VANISHED modifier without ENABLED QRESYNC | MUST NOT | rev2 | pass |  |
| RFC7162-3.2.5-1 — QRESYNC select parameter form: uidvalidity, modseq, optional known-uids, optional seq-match-data | MAY | rev1 | fail | unimplemented: The Quick Resynchronization parameter to SELECT/EXAMINE commands has four arguments: o the last known UIDVALIDITY, o the last known modification sequence, o the… |
| RFC7162-3.2.5-1 — QRESYNC select parameter form: uidvalidity, modseq, optional known-uids, optional seq-match-data | MAY | rev2 | fail | unimplemented: The Quick Resynchronization parameter to SELECT/EXAMINE commands has four arguments: o the last known UIDVALIDITY, o the last known modification sequence, o the… |
| RFC7162-3.2.5.1-1 — Client MUST accept the QRESYNC resync stream: UID-bearing FETCH plus expunge reports | MUST | rev1 | pass |  |
| RFC7162-3.2.5.1-1 — Client MUST accept the QRESYNC resync stream: UID-bearing FETCH plus expunge reports | MUST | rev2 | pass |  |
| RFC7162-3.2.5.2-1 — seq-match-data MAY be provided; both sets MUST be in ascending order | MUST | rev1 | fail | unimplemented: A client MAY provide a parenthesized list of a message sequence set and the corresponding UID sets. Both MUST be provided in ascending order. The server uses th… |
| RFC7162-3.2.5.2-1 — seq-match-data MAY be provided; both sets MUST be in ascending order | MUST | rev2 | fail | unimplemented: A client MAY provide a parenthesized list of a message sequence set and the corresponding UID sets. Both MUST be provided in ascending order. The server uses th… |
| RFC7162-3.2.6-1 — Client (implicit) MUST NOT use the VANISHED modifier with plain FETCH | MUST NOT | rev1 | fail | unimplemented: Note that the VANISHED UID FETCH modifier is NOT allowed with a FETCH command. The server MUST return a tagged BAD response if this response is specified as a m… |
| RFC7162-3.2.6-1 — Client (implicit) MUST NOT use the VANISHED modifier with plain FETCH | MUST NOT | rev2 | fail | unimplemented: Note that the VANISHED UID FETCH modifier is NOT allowed with a FETCH command. The server MUST return a tagged BAD response if this response is specified as a m… |
| RFC7162-3.2.6-2 — VANISHED modifier MUST only be specified together with CHANGEDSINCE | MUST | rev1 | fail | unimplemented: The VANISHED UID FETCH modifier MUST only be specified together with the CHANGEDSINCE UID FETCH modifier. If the VANISHED UID FETCH modifier is used without the… |
| RFC7162-3.2.6-2 — VANISHED modifier MUST only be specified together with CHANGEDSINCE | MUST | rev2 | fail | unimplemented: The VANISHED UID FETCH modifier MUST only be specified together with the CHANGEDSINCE UID FETCH modifier. If the VANISHED UID FETCH modifier is used without the… |
| RFC7162-3.2.6-3 — UID FETCH (CHANGEDSINCE n VANISHED) form and its VANISHED (EARLIER) reply | MAY | rev1 | fail | unimplemented: The VANISHED UID FETCH modifier instructs the server to report those messages from the UID set parameter that have been expunged and whose associated mod-sequen… |
| RFC7162-3.2.6-3 — UID FETCH (CHANGEDSINCE n VANISHED) form and its VANISHED (EARLIER) reply | MAY | rev2 | fail | unimplemented: The VANISHED UID FETCH modifier instructs the server to report those messages from the UID set parameter that have been expunged and whose associated mod-sequen… |
| RFC7162-3.2.7-1 — Client MUST accept [HIGHESTMODSEQ n] on the tagged OK of (UID) EXPUNGE | MUST | rev1 | pass |  |
| RFC7162-3.2.7-1 — Client MUST accept [HIGHESTMODSEQ n] on the tagged OK of (UID) EXPUNGE | MUST | rev2 | pass |  |
| RFC7162-3.2.7-2 — Client MUST accept expunge results reported as VANISHED or EXPUNGE responses | MUST | rev1 | pass |  |
| RFC7162-3.2.7-2 — Client MUST accept expunge results reported as VANISHED or EXPUNGE responses | MUST | rev2 | pass |  |
| RFC7162-3.2.10.1-1 — Client MUST accept the VANISHED (EARLIER) response to UID FETCH (VANISHED) / SELECT (QRESYNC) | MUST | rev1 | pass |  |
| RFC7162-3.2.10.1-1 — Client MUST accept the VANISHED (EARLIER) response to UID FETCH (VANISHED) / SELECT (QRESYNC) | MUST | rev2 | pass |  |
| RFC7162-3.2.10.1-2 — Client MUST NOT decrement message sequence numbers on VANISHED (EARLIER) | MUST NOT | rev1 | untestable | The prohibition governs the client's internal message-number model: on VANISHED (EARLIER) — unlike EXPUNGE or a plain VANISHED — the client must leave its sequence-number-to-message mapping untouched, because the reported UIDs were never part of the current session's numbering. The client emits nothing in response, so a compliant and a non-compliant bookkeeper produce byte-identical traffic at the moment of receipt. The divergence could only ever surface through a LATER command that depends on the client's own sequence-number arithmetic, and in this headless library no such arithmetic exists to observe: sequence numbers in subsequent commands are chosen by the caller and passed through verbatim (the library maintains no seq-to-UID map), so no black-box exchange can attribute a shifted model to the client. Same consumer-delegation rebuttal as RFC9051-2.3.1.1-1. |
| RFC7162-3.2.10.1-2 — Client MUST NOT decrement message sequence numbers on VANISHED (EARLIER) | MUST NOT | rev2 | untestable | The prohibition governs the client's internal message-number model: on VANISHED (EARLIER) — unlike EXPUNGE or a plain VANISHED — the client must leave its sequence-number-to-message mapping untouched, because the reported UIDs were never part of the current session's numbering. The client emits nothing in response, so a compliant and a non-compliant bookkeeper produce byte-identical traffic at the moment of receipt. The divergence could only ever surface through a LATER command that depends on the client's own sequence-number arithmetic, and in this headless library no such arithmetic exists to observe: sequence numbers in subsequent commands are chosen by the caller and passed through verbatim (the library maintains no seq-to-UID map), so no black-box exchange can attribute a shifted model to the client. Same consumer-delegation rebuttal as RFC9051-2.3.1.1-1. |
| RFC7162-3.2.10.2-1 — After ENABLED QRESYNC, client MUST accept VANISHED in lieu of EXPUNGE for the whole connection | MUST | rev1 | pass |  |
| RFC7162-3.2.10.2-1 — After ENABLED QRESYNC, client MUST accept VANISHED in lieu of EXPUNGE for the whole connection | MUST | rev2 | pass |  |
| RFC7162-3.2.10.2-2 — Client decrements message count / adjusts numbering per VANISHED and MUST record the update | MUST | rev1 | untestable | All three quoted sentences govern the client's internal mailbox model after a tag-less VANISHED: shrink the message count by one per listed UID, renumber the remaining messages EXPUNGE-style, and record the update. None of that produces a wire artifact at the time it happens — the client sends nothing in response (§3.2.10.2 even notes the server need not send EXISTS 'because of this housekeeping', removing the one exchange that could have echoed the client's count). As with RFC7162-3.2.10.1-2, the divergence could only surface through later sequence-number-dependent commands, and this headless library maintains no sequence-number model of its own — message numbers in later commands are caller-supplied and passed through — so no black-box exchange can attribute a wrong count or stale numbering to the client's bookkeeping. |
| RFC7162-3.2.10.2-2 — Client decrements message count / adjusts numbering per VANISHED and MUST record the update | MUST | rev2 | untestable | All three quoted sentences govern the client's internal mailbox model after a tag-less VANISHED: shrink the message count by one per listed UID, renumber the remaining messages EXPUNGE-style, and record the update. None of that produces a wire artifact at the time it happens — the client sends nothing in response (§3.2.10.2 even notes the server need not send EXISTS 'because of this housekeeping', removing the one exchange that could have echoed the client's count). As with RFC7162-3.2.10.1-2, the divergence could only surface through later sequence-number-dependent commands, and this headless library maintains no sequence-number model of its own — message numbers in later commands are caller-supplied and passed through — so no black-box exchange can attribute a wrong count or stale numbering to the client's bookkeeping. |
| RFC7162-3.2.11-1 — Client MUST accept [CLOSED] and treat it as the old/new mailbox response boundary | MUST | rev1 | pass |  |
| RFC7162-6-1 — Disconnected client SHOULD use QRESYNC when supported, else CONDSTORE | SHOULD | rev1 | untestable | A feature-adoption preference between compliant alternatives: a client that synchronizes without either extension is fully protocol-compliant (both are OPTIONAL capabilities), so no black-box exchange can construct a failing observation — the absence of QRESYNC/CONDSTORE traffic is also the compliant behavior of a client whose deployment or product policy chose not to be an 'advanced disconnected mail client' at all. Whether the implementation is in the duty's addressed class is a product-policy fact with no wire signature, the same structure as RFC4978-1-1 (MAY use COMPRESS, RECOMMENDED prefer TLS compression — user-intent-policy). |
| RFC7162-6-1 — Disconnected client SHOULD use QRESYNC when supported, else CONDSTORE | SHOULD | rev2 | untestable | A feature-adoption preference between compliant alternatives: a client that synchronizes without either extension is fully protocol-compliant (both are OPTIONAL capabilities), so no black-box exchange can construct a failing observation — the absence of QRESYNC/CONDSTORE traffic is also the compliant behavior of a client whose deployment or product policy chose not to be an 'advanced disconnected mail client' at all. Whether the implementation is in the duty's addressed class is a product-policy fact with no wire signature, the same structure as RFC4978-1-1 (MAY use COMPRESS, RECOMMENDED prefer TLS compression — user-intent-policy). |
| RFC7162-6-2 — Client MUST cache HIGHESTMODSEQ after sync and update it on later response codes | MUST | rev1 | untestable | Both MUSTs govern the client's synchronization cache, whose only purpose — and only potential observable — is the value the client presents in a FUTURE session's QRESYNC parameter or CHANGEDSINCE argument. Within the session where the caching duty fires, compliant and non-compliant clients emit identical traffic. Taxonomy mechanism (b) (sequential multi-connection scripts) evaluated and rebutted: a second scripted connection could observe the re-presented value only if the client itself sourced it from its cache, but this headless library keeps no cross-session synchronization state — every modseq the driver emits is caller-supplied per command — so a second-session value attributes to harness input, not to the client's caching discipline (RFC9051-2.3.1.1-1 rebuttal pattern). |
| RFC7162-6-2 — Client MUST cache HIGHESTMODSEQ after sync and update it on later response codes | MUST | rev2 | untestable | Both MUSTs govern the client's synchronization cache, whose only purpose — and only potential observable — is the value the client presents in a FUTURE session's QRESYNC parameter or CHANGEDSINCE argument. Within the session where the caching duty fires, compliant and non-compliant clients emit identical traffic. Taxonomy mechanism (b) (sequential multi-connection scripts) evaluated and rebutted: a second scripted connection could observe the re-presented value only if the client itself sourced it from its cache, but this headless library keeps no cross-session synchronization state — every modseq the driver emits is caller-supplied per command — so a second-session value attributes to harness input, not to the client's caching discipline (RFC9051-2.3.1.1-1 rebuttal pattern). |
| RFC7162-6-3 — HIGHESTMODSEQ recalculation discipline at each tagged response | MUST | rev1 | untestable | An arithmetic discipline over the client's internal copy of HIGHESTMODSEQ (response-code-wins over observed MODSEQ items, batch recalculation at each tagged response, monotonic take-if-bigger). The computation consumes wire inputs but produces no wire output in the session where it runs: a client that follows the algorithm and one that, say, eagerly adopts every MODSEQ item as it arrives (the exact mistake §6's safety note warns against, because servers may deliver MODSEQ items out of order and delay EXPUNGE/VANISHED with smaller mod-sequences) send identical bytes. The stored result only matters as input to a future session's resynchronization, and — per the cross-session rebuttal on RFC7162-6-2 — this library keeps no such state for a second scripted session to expose; the within-session bookkeeping itself is classic internal-state with no pass/fail wire boundary. |
| RFC7162-6-3 — HIGHESTMODSEQ recalculation discipline at each tagged response | MUST | rev2 | untestable | An arithmetic discipline over the client's internal copy of HIGHESTMODSEQ (response-code-wins over observed MODSEQ items, batch recalculation at each tagged response, monotonic take-if-bigger). The computation consumes wire inputs but produces no wire output in the session where it runs: a client that follows the algorithm and one that, say, eagerly adopts every MODSEQ item as it arrives (the exact mistake §6's safety note warns against, because servers may deliver MODSEQ items out of order and delay EXPUNGE/VANISHED with smaller mod-sequences) send identical bytes. The stored result only matters as input to a future session's resynchronization, and — per the cross-session rebuttal on RFC7162-6-2 — this library keeps no such state for a second scripted session to expose; the within-session bookkeeping itself is classic internal-state with no pass/fail wire boundary. |
| RFC7162-6-4 — On UIDVALIDITY change, client MUST empty its cache, forget HIGHESTMODSEQ, drop pending UID actions | MUST | rev1 | untestable | A three-part purge of cross-session state (message cache, cached HIGHESTMODSEQ, queued offline actions) triggered by a UIDVALIDITY mismatch after SELECT/EXAMINE (QRESYNC). None of the three purges emits anything: a client that purges and one that keeps stale state complete the SELECT identically. Mechanism (b) rebuttal as on RFC7162-6-2: a later session could betray un-forgotten state only if the client re-presented cached values or replayed stale UID actions of its own accord, but this library neither persists a message cache nor queues offline actions — both are consumer-delegated — so no sequential-connection script can attribute stale emissions to the client's purge discipline. The trailing note ('this doesn't affect actions performed on client-generated fake UIDs') further conditions the duty on [IMAP-DISC] machinery the library does not implement. |
| RFC7162-6-4 — On UIDVALIDITY change, client MUST empty its cache, forget HIGHESTMODSEQ, drop pending UID actions | MUST | rev2 | untestable | A three-part purge of cross-session state (message cache, cached HIGHESTMODSEQ, queued offline actions) triggered by a UIDVALIDITY mismatch after SELECT/EXAMINE (QRESYNC). None of the three purges emits anything: a client that purges and one that keeps stale state complete the SELECT identically. Mechanism (b) rebuttal as on RFC7162-6-2: a later session could betray un-forgotten state only if the client re-presented cached values or replayed stale UID actions of its own accord, but this library neither persists a message cache nor queues offline actions — both are consumer-delegated — so no sequential-connection script can attribute stale emissions to the client's purge discipline. The trailing note ('this doesn't affect actions performed on client-generated fake UIDs') further conditions the duty on [IMAP-DISC] machinery the library does not implement. |
| RFC7162-6-5 — On NOMODSEQ after SELECT (QRESYNC), client MUST drop its cached HIGHESTMODSEQ | MUST | rev1 | untestable | The removal of a cached HIGHESTMODSEQ (plus a fallback to [IMAP-DISC]'s generic resynchronization procedure) is cache maintenance whose effect is only meaningful for a future session's sync decision; in the session where NOMODSEQ arrives the client's next commands are the generic resync either way, and whether the stale value was actually deleted has no wire signature. Mechanism (b) rebuttal as on RFC7162-6-2/-6-4: the library holds no cross-session HIGHESTMODSEQ cache — the QRESYNC modseq argument is caller-supplied — so a second scripted session cannot attribute a re-presented stale value to the client's own cache. The wire-facing half (accepting the NOMODSEQ response code at all) is separately scored and testable as RFC7162-3.1.2-1. |
| RFC7162-6-5 — On NOMODSEQ after SELECT (QRESYNC), client MUST drop its cached HIGHESTMODSEQ | MUST | rev2 | untestable | The removal of a cached HIGHESTMODSEQ (plus a fallback to [IMAP-DISC]'s generic resynchronization procedure) is cache maintenance whose effect is only meaningful for a future session's sync decision; in the session where NOMODSEQ arrives the client's next commands are the generic resync either way, and whether the stale value was actually deleted has no wire signature. Mechanism (b) rebuttal as on RFC7162-6-2/-6-4: the library holds no cross-session HIGHESTMODSEQ cache — the QRESYNC modseq argument is caller-supplied — so a second scripted session cannot attribute a re-presented stale value to the client's own cache. The wire-facing half (accepting the NOMODSEQ response code at all) is separately scored and testable as RFC7162-3.1.2-1. |
| RFC7162-7-1 — Client MUST accept full-range unsigned 63-bit mod-sequence values | MUST | rev1 | pass |  |
| RFC7162-7-1 — Client MUST accept full-range unsigned 63-bit mod-sequence values | MUST | rev2 | pass |  |
| RFC5256-1-1 — Client (implicit) recognizes the SORT capability family by its 'SORT' prefix | MUST | rev1 | untestable | The client-binding half of this server-advertisement rule is a capability-inventory interpretation duty: when deciding whether base-level SORT is available, a client must recognize any capability name starting with 'SORT' (e.g., a future 'SORT=X' token) as also indicating base-level support, rather than requiring the bare token. That determination lives entirely in the client's internal capability inventory: it produces no mandated wire exchange of its own, and whether the library would gate or permit a SORT command based on a prefix-matched token is only observable through user-initiated commands the library does not condition on capabilities. A prefix-matching client and an exact-token-matching client are wire-indistinguishable against any server that (as all known ones do) advertises the bare SORT token alongside any refinements. |
| RFC5256-1-1 — Client (implicit) recognizes the SORT capability family by its 'SORT' prefix | MUST | rev2 | untestable | The client-binding half of this server-advertisement rule is a capability-inventory interpretation duty: when deciding whether base-level SORT is available, a client must recognize any capability name starting with 'SORT' (e.g., a future 'SORT=X' token) as also indicating base-level support, rather than requiring the bare token. That determination lives entirely in the client's internal capability inventory: it produces no mandated wire exchange of its own, and whether the library would gate or permit a SORT command based on a prefix-matched token is only observable through user-initiated commands the library does not condition on capabilities. A prefix-matching client and an exact-token-matching client are wire-indistinguishable against any server that (as all known ones do) advertises the bare SORT token alongside any refinements. |
| RFC5256-1-2 — Client (implicit) uses only threading algorithms the server advertises via THREAD= | MUST | rev1 | fail | unimplemented: A server that supports the THREAD extension indicates this with one or more capability names consisting of "THREAD=" followed by a supported threading algorithm… |
| RFC5256-1-2 — Client (implicit) uses only threading algorithms the server advertises via THREAD= | MUST | rev2 | fail | unimplemented: A server that supports the THREAD extension indicates this with one or more capability names consisting of "THREAD=" followed by a supported threading algorithm… |
| RFC5256-2.1-1 — Disconnected clients MUST use exactly the base-subject extraction algorithm | MUST | rev1 | untestable | This is the one sentence in §2.1 that binds clients, and it binds only DISCONNECTED clients — those that sort/thread a local message cache themselves instead of issuing SORT/THREAD to the server. The duty's substance is a local content computation (steps (1)-(7) over the Subject header, using the §5 subject ABNF): its output feeds a locally computed sorted/threaded view and never appears on the wire toward the server, so a scripted-server black-box harness has no observation point — a client that deviates from the algorithm produces byte-identical protocol traffic to one that follows it exactly. The same reasoning covers the §2.2 sent-date rules for a disconnected client sorting locally (cataloged here by reference rather than as a separate entry, since §2.2 never names clients). |
| RFC5256-2.1-1 — Disconnected clients MUST use exactly the base-subject extraction algorithm | MUST | rev2 | untestable | This is the one sentence in §2.1 that binds clients, and it binds only DISCONNECTED clients — those that sort/thread a local message cache themselves instead of issuing SORT/THREAD to the server. The duty's substance is a local content computation (steps (1)-(7) over the Subject header, using the §5 subject ABNF): its output feeds a locally computed sorted/threaded view and never appears on the wire toward the server, so a scripted-server black-box harness has no observation point — a client that deviates from the algorithm produces byte-identical protocol traffic to one that follows it exactly. The same reasoning covers the §2.2 sent-date rules for a disconnected client sorting locally (cataloged here by reference rather than as a separate entry, since §2.2 never names clients). |
| RFC5256-BASE.6.4.SORT-1 — SORT command form: parenthesized sort criteria, then charset, then search keys | MUST | rev1 | fail | unimplemented: The SORT command is a variant of SEARCH with sorting semantics for the results. There are two arguments before the searching criteria argument: a parenthesized … |
| RFC5256-BASE.6.4.SORT-1 — SORT command form: parenthesized sort criteria, then charset, then search keys | MUST | rev2 | fail | unimplemented: The SORT command is a variant of SEARCH with sorting semantics for the results. There are two arguments before the searching criteria argument: a parenthesized … |
| RFC5256-BASE.6.4.SORT-2 — Client MUST include the charset argument in SORT (mandatory, unlike SEARCH) | MUST | rev1 | fail | unimplemented: The charset argument is mandatory (unlike SEARCH) and indicates the [CHARSET] of the strings that appear in the searching criteria. |
| RFC5256-BASE.6.4.SORT-2 — Client MUST include the charset argument in SORT (mandatory, unlike SEARCH) | MUST | rev2 | fail | unimplemented: The charset argument is mandatory (unlike SEARCH) and indicates the [CHARSET] of the strings that appear in the searching criteria. |
| RFC5256-BASE.6.4.SORT-3 — US-ASCII/UTF-8 charset floor for SORT; all other charsets optional | MUST | rev1 | fail | unimplemented: The US-ASCII and [UTF-8] charsets MUST be implemented. All other charsets are optional. |
| RFC5256-BASE.6.4.SORT-3 — US-ASCII/UTF-8 charset floor for SORT; all other charsets optional | MUST | rev2 | fail | unimplemented: The US-ASCII and [UTF-8] charsets MUST be implemented. All other charsets are optional. |
| RFC5256-BASE.6.4.SORT-4 — UID SORT: identical argument interpretation; results are unique identifiers | MUST | rev1 | fail | unimplemented: There is also a UID SORT command that returns unique identifiers instead of message sequence numbers. Note that there are separate searching criteria for messag… |
| RFC5256-BASE.6.4.SORT-4 — UID SORT: identical argument interpretation; results are unique identifiers | MUST | rev2 | fail | unimplemented: There is also a UID SORT command that returns unique identifiers instead of message sequence numbers. Note that there are separate searching criteria for messag… |
| RFC5256-BASE.6.4.SORT-5 — Client must be prepared for untagged EXPUNGE during a UID SORT response | MUST | rev1 | fail | unimplemented: Untagged EXPUNGE responses are not permitted while the server is responding to a SORT command, but are permitted during a UID SORT command. |
| RFC5256-BASE.6.4.SORT-5 — Client must be prepared for untagged EXPUNGE during a UID SORT response | MUST | rev2 | fail | unimplemented: Untagged EXPUNGE responses are not permitted while the server is responding to a SORT command, but are permitted during a UID SORT command. |
| RFC5256-BASE.6.4.THREAD-1 — THREAD command form: threading algorithm, then charset, then search keys | MUST | rev1 | fail | unimplemented: The THREAD command is a variant of SEARCH with threading semantics for the results. Thread has two arguments before the searching criteria argument: a threading… |
| RFC5256-BASE.6.4.THREAD-1 — THREAD command form: threading algorithm, then charset, then search keys | MUST | rev2 | fail | unimplemented: The THREAD command is a variant of SEARCH with threading semantics for the results. Thread has two arguments before the searching criteria argument: a threading… |
| RFC5256-BASE.6.4.THREAD-2 — Client MUST include the charset argument in THREAD (mandatory, unlike SEARCH) | MUST | rev1 | fail | unimplemented: The charset argument is mandatory (unlike SEARCH) and indicates the [CHARSET] of the strings that appear in the searching criteria. |
| RFC5256-BASE.6.4.THREAD-2 — Client MUST include the charset argument in THREAD (mandatory, unlike SEARCH) | MUST | rev2 | fail | unimplemented: The charset argument is mandatory (unlike SEARCH) and indicates the [CHARSET] of the strings that appear in the searching criteria. |
| RFC5256-BASE.6.4.THREAD-3 — US-ASCII/UTF-8 charset floor for THREAD; all other charsets optional | MUST | rev1 | fail | unimplemented: The US-ASCII and [UTF-8] charsets MUST be implemented. All other charsets are optional. |
| RFC5256-BASE.6.4.THREAD-3 — US-ASCII/UTF-8 charset floor for THREAD; all other charsets optional | MUST | rev2 | fail | unimplemented: The US-ASCII and [UTF-8] charsets MUST be implemented. All other charsets are optional. |
| RFC5256-BASE.6.4.THREAD-4 — UID THREAD: identical argument interpretation; results are unique identifiers | MUST | rev1 | fail | unimplemented: There is also a UID THREAD command that returns unique identifiers instead of message sequence numbers. Note that there are separate searching criteria for mess… |
| RFC5256-BASE.6.4.THREAD-4 — UID THREAD: identical argument interpretation; results are unique identifiers | MUST | rev2 | fail | unimplemented: There is also a UID THREAD command that returns unique identifiers instead of message sequence numbers. Note that there are separate searching criteria for mess… |
| RFC5256-BASE.6.4.THREAD-5 — Client must be prepared for untagged EXPUNGE during a UID THREAD response | MUST | rev1 | fail | unimplemented: Untagged EXPUNGE responses are not permitted while the server is responding to a THREAD command, but are permitted during a UID THREAD command. |
| RFC5256-BASE.6.4.THREAD-5 — Client must be prepared for untagged EXPUNGE during a UID THREAD response | MUST | rev2 | fail | unimplemented: Untagged EXPUNGE responses are not permitted while the server is responding to a THREAD command, but are permitted during a UID THREAD command. |
| RFC5256-BASE.6.4.THREAD-6 — Client SHOULD treat descendents of a child in an ORDEREDSUBJECT response as siblings | SHOULD | rev1 | fail | unimplemented: Client implementations SHOULD treat descendents of a child in a server response as being siblings of that child. |
| RFC5256-BASE.6.4.THREAD-6 — Client SHOULD treat descendents of a child in an ORDEREDSUBJECT response as siblings | SHOULD | rev2 | fail | unimplemented: Client implementations SHOULD treat descendents of a child in a server response as being siblings of that child. |
| RFC5256-BASE.7.2.SORT-1 — Accept untagged SORT response: zero or more space-delimited numbers (seq or UID) | MUST | rev1 | pass |  |
| RFC5256-BASE.7.2.SORT-1 — Accept untagged SORT response: zero or more space-delimited numbers (seq or UID) | MUST | rev2 | pass |  |
| RFC5256-BASE.7.2.THREAD-1 — Accept untagged THREAD response: zero or more parenthesized threads (seq or UID) | MUST | rev1 | pass |  |
| RFC5256-BASE.7.2.THREAD-1 — Accept untagged THREAD response: zero or more parenthesized threads (seq or UID) | MUST | rev2 | pass |  |
| RFC5256-BASE.7.2.THREAD-2 — Parse thread structure: parent/child chains, sub-thread splits, unlimited nesting | MUST | rev1 | pass |  |
| RFC5256-BASE.7.2.THREAD-2 — Parse thread structure: parent/child chains, sub-thread splits, unlimited nesting | MUST | rev2 | pass |  |
| RFC5256-5-1 — SORT grammar: defined criteria atoms only; REVERSE prefixes a single sort-key | MUST | rev1 | fail | unimplemented: sort = ["UID" SP] "SORT" SP sort-criteria SP search-criteria sort-criteria = "(" sort-criterion *(SP sort-criterion) ")" sort-criterion = ["REVERSE" SP] sort-ke… |
| RFC5256-5-1 — SORT grammar: defined criteria atoms only; REVERSE prefixes a single sort-key | MUST | rev2 | fail | unimplemented: sort = ["UID" SP] "SORT" SP sort-criteria SP search-criteria sort-criteria = "(" sort-criterion *(SP sort-criterion) ")" sort-criterion = ["REVERSE" SP] sort-ke… |
| RFC5256-5-2 — THREAD grammar: registered algorithm atom; charset then one or more search keys | MUST | rev1 | fail | unimplemented: thread = ["UID" SP] "THREAD" SP thread-alg SP search-criteria thread-alg = "ORDEREDSUBJECT" / "REFERENCES" / thread-alg-ext ... search-criteria = charset 1*(SP … |
| RFC5256-5-2 — THREAD grammar: registered algorithm atom; charset then one or more search keys | MUST | rev2 | fail | unimplemented: thread = ["UID" SP] "THREAD" SP thread-alg SP search-criteria thread-alg = "ORDEREDSUBJECT" / "REFERENCES" / thread-alg-ext ... search-criteria = charset 1*(SP … |
| RFC5957-1-1 — Client (implicit) MUST NOT send DISPLAYFROM/DISPLAYTO unless the server advertises SORT=DISPLAY (with SORT) | MUST NOT | rev1 | fail | unimplemented: A server that supports the full [SORT] extension as well as both the DISPLAYFROM and DISPLAYTO sort criteria indicates this by returning "SORT=DISPLAY" in its C… |
| RFC5957-1-1 — Client (implicit) MUST NOT send DISPLAYFROM/DISPLAYTO unless the server advertises SORT=DISPLAY (with SORT) | MUST NOT | rev2 | fail | unimplemented: A server that supports the full [SORT] extension as well as both the DISPLAYFROM and DISPLAYTO sort criteria indicates this by returning "SORT=DISPLAY" in its C… |
| RFC5957-4-1 — Client MAY request the DISPLAYFROM/DISPLAYTO orderings as SORT sort criteria | MAY | rev1 | fail | unimplemented: This document introduces two new [SORT] sort criteria, DISPLAYFROM and DISPLAYTO. |
| RFC5957-4-1 — Client MAY request the DISPLAYFROM/DISPLAYTO orderings as SORT sort criteria | MAY | rev2 | fail | unimplemented: This document introduces two new [SORT] sort criteria, DISPLAYFROM and DISPLAYTO. |
| RFC5957-5-1 — Client MUST emit DISPLAYFROM/DISPLAYTO as sort-key atoms per the extended RFC 5256 ABNF | MUST | rev1 | fail | unimplemented: sort-key =/ "DISPLAYFROM" / "DISPLAYTO" |
| RFC5957-5-1 — Client MUST emit DISPLAYFROM/DISPLAYTO as sort-key atoms per the extended RFC 5256 ABNF | MUST | rev2 | fail | unimplemented: sort-key =/ "DISPLAYFROM" / "DISPLAYTO" |
| RFC4731-1-1 — Client MUST NOT use SEARCH RETURN result options unless the server advertises ESEARCH | MUST NOT | rev1 | fail | unimplemented: A server advertising the ESEARCH capability supports the following result options: minimal value, maximal value, all found messages, and number of found message… |
| RFC4731-3.1-1 — Client MAY extend SEARCH/UID SEARCH with the RETURN (MIN MAX ALL COUNT) result options | MAY | rev1 | fail | unimplemented: The SEARCH/UID SEARCH commands are extended to allow for the following result options: MIN Return the lowest message number/UID that satisfies the SEARCH criter… |
| RFC4731-3.1-1 — Client MAY extend SEARCH/UID SEARCH with the RETURN (MIN MAX ALL COUNT) result options | MAY | rev2 | fail | unimplemented: The SEARCH/UID SEARCH commands are extended to allow for the following result options: MIN Return the lowest message number/UID that satisfies the SEARCH criter… |
| RFC4731-3.1-2 — Client MUST accept ESEARCH ALL data in sequence-set syntax (unlike unextended SEARCH) | MUST | rev1 | pass |  |
| RFC4731-3.1-3 — Client MUST NOT assume ESEARCH ALL messages/UIDs are listed in any particular order | MUST NOT | rev1 | pass |  |
| RFC4731-3.1-4 — Client MUST accept an item-less ESEARCH response when the SEARCH has no matches | MUST | rev1 | pass |  |
| RFC4731-3.1-5 — Client using result options MUST accept a single ESEARCH response in place of SEARCH | MUST | rev1 | pass |  |
| RFC4731-3.1-5 — Client using result options MUST accept a single ESEARCH response in place of SEARCH | MUST | rev2 | pass |  |
| RFC4731-3.1-6 — Client MUST expect the UID indicator on ESEARCH for UID SEARCH and read all data as UIDs | MUST | rev1 | pass |  |
| RFC4731-3.1-7 — Empty RETURN () requests an ESEARCH response and is equivalent to (ALL) | MUST | rev1 | fail | unimplemented: If the list of result options is empty, that requests the server to return an ESEARCH response instead of the SEARCH response. This is equivalent to "(ALL)". |
| RFC4731-3.1-7 — Empty RETURN () requests an ESEARCH response and is equivalent to (ALL) | MUST | rev2 | fail | unimplemented: If the list of result options is empty, that requests the server to return an ESEARCH response instead of the SEARCH response. This is equivalent to "(ALL)". |
| RFC4731-3.2-1 — Client combining result options with MODSEQ search MUST accept the MODSEQ return-data pair | MUST | rev1 | pass |  |
| RFC4731-3.2-1 — Client combining result options with MODSEQ search MUST accept the MODSEQ return-data pair | MUST | rev2 | pass |  |
| RFC5267-3-1 — Client MAY extend SORT/UID SORT with a RETURN (...) return-option list | MAY | rev1 | fail | unimplemented: The SORT and UID SORT commands are extended by the addition of an optional list of return options that follow a RETURN atom immediately after the command. If th… |
| RFC5267-3-1 — Client MAY extend SORT/UID SORT with a RETURN (...) return-option list | MAY | rev2 | fail | unimplemented: The SORT and UID SORT commands are extended by the addition of an optional list of return options that follow a RETURN atom immediately after the command. If th… |
| RFC5267-3-2 — Client using extended SORT MUST accept the results in an ESEARCH response | MUST | rev1 | pass |  |
| RFC5267-3-2 — Client using extended SORT MUST accept the results in an ESEARCH response | MUST | rev2 | pass |  |
| RFC5267-3.1-1 — Client MUST NOT use SORT RETURN options unless the server advertises ESORT | MUST NOT | rev1 | fail | unimplemented: Servers advertising the capability "ESORT" support the return options specified in [ESEARCH] in the SORT command. These return options are adapted as follows: |
| RFC5267-3.1-1 — Client MUST NOT use SORT RETURN options unless the server advertises ESORT | MUST NOT | rev2 | fail | unimplemented: Servers advertising the capability "ESORT" support the return options specified in [ESEARCH] in the SORT command. These return options are adapted as follows: |
| RFC5267-3.1-2 — Client interprets ESORT MIN/MAX/ALL against the SORT order (ALL arrives in requested sort order) | MUST | rev1 | pass |  |
| RFC5267-3.1-2 — Client interprets ESORT MIN/MAX/ALL against the SORT order (ALL arrives in requested sort order) | MUST | rev2 | pass |  |
| RFC5267-3.2-1 — Client MUST expand ranges in sorted ESEARCH results in increasing numerical order per usual IMAP rules | MUST | rev1 | pass |  |
| RFC5267-3.2-1 — Client MUST expand ranges in sorted ESEARCH results in increasing numerical order per usual IMAP rules | MUST | rev2 | pass |  |
| RFC5267-3.3-1 — Empty SORT RETURN () requests an ESEARCH response carrying the ALL data item | MUST | rev1 | pass |  |
| RFC5267-3.3-1 — Empty SORT RETURN () requests an ESEARCH response carrying the ALL data item | MUST | rev2 | pass |  |
| RFC5267-4.1-1 — Client MUST NOT use CONTEXT/UPDATE/PARTIAL on SEARCH unless the server advertises CONTEXT=SEARCH | MUST NOT | rev1 | fail | unimplemented: The Contexts extension is present in any IMAP4rev1 server that includes the string "CONTEXT=SEARCH", and/or "CONTEXT=SORT", within its advertised capabilities. … |
| RFC5267-4.1-1 — Client MUST NOT use CONTEXT/UPDATE/PARTIAL on SEARCH unless the server advertises CONTEXT=SEARCH | MUST NOT | rev2 | fail | unimplemented: The Contexts extension is present in any IMAP4rev1 server that includes the string "CONTEXT=SEARCH", and/or "CONTEXT=SORT", within its advertised capabilities. … |
| RFC5267-4.1-2 — Client MUST NOT use CONTEXT/UPDATE/PARTIAL on extended SORT unless the server advertises CONTEXT=SORT | MUST NOT | rev1 | fail | unimplemented: Servers advertising CONTEXT=SORT also advertise the SORT capability, as described in [SORT], support the extended SORT command syntax described in Section 3, an… |
| RFC5267-4.1-2 — Client MUST NOT use CONTEXT/UPDATE/PARTIAL on extended SORT unless the server advertises CONTEXT=SORT | MUST NOT | rev2 | fail | unimplemented: Servers advertising CONTEXT=SORT also advertise the SORT capability, as described in [SORT], support the extended SORT command syntax described in Section 3, an… |
| RFC5267-4.2-1 — Client SHOULD use the CONTEXT return option when subsequent use of the search criteria is likely | SHOULD | rev1 | untestable | Whether 'subsequent use of the search criteria are likely' is the client's own anticipation of its future behavior — an internal fact with no wire footprint at the moment the option is (or is not) sent. A client that always sends CONTEXT, one that never does, and one that sends it exactly when it privately expects to reuse the criteria can all emit identical individual commands; the SHOULD's trigger condition (likelihood of reuse) is invisible to a black-box harness, so no test can score whether the hint was used when it should have been. Same adjudication as RFC5161-3.1-1's 'only include extensions that need to be enabled'. |
| RFC5267-4.2-1 — Client SHOULD use the CONTEXT return option when subsequent use of the search criteria is likely | SHOULD | rev2 | untestable | Whether 'subsequent use of the search criteria are likely' is the client's own anticipation of its future behavior — an internal fact with no wire footprint at the moment the option is (or is not) sent. A client that always sends CONTEXT, one that never does, and one that sends it exactly when it privately expects to reuse the criteria can all emit identical individual commands; the SHOULD's trigger condition (likelihood of reuse) is invisible to a black-box harness, so no test can score whether the hint was used when it should have been. Same adjudication as RFC5161-3.1-1's 'only include extensions that need to be enabled'. |
| RFC5267-4.3-1 — Client using UPDATE MUST accept unsolicited ESEARCH responses carrying ADDTO/REMOVEFROM update sets | MUST | rev1 | pass |  |
| RFC5267-4.3-1 — Client using UPDATE MUST accept unsolicited ESEARCH responses carrying ADDTO/REMOVEFROM update sets | MUST | rev2 | pass |  |
| RFC5267-4.3-2 — Client MUST NOT reuse tags (ESEARCH updates are correlated by command tag) | MUST NOT | rev1 | fail | unimplemented: These ESEARCH responses carry a search correlator of the searching command, hence clients MUST NOT reuse tags, as already specified in Section 2.2.1 of [IMAP]. |
| RFC5267-4.3-2 — Client MUST NOT reuse tags (ESEARCH updates are correlated by command tag) | MUST NOT | rev2 | fail | unimplemented: These ESEARCH responses carry a search correlator of the searching command, hence clients MUST NOT reuse tags, as already specified in Section 2.2.1 of [IMAP]. |
| RFC5267-4.3-3 — Client MUST NOT expect UPDATE to be scoped by other return options (notifications cover all matching messages) | MUST | rev1 | untestable | The duty is a constraint on the client's INTERPRETATION of UPDATE notifications — an assumption it holds internally: a client that correctly expects unscoped notifications and one that wrongly believes RETURN (UPDATE MIN) filters to the minimum produce identical wire traces when the notifications arrive; any observable mishandling (choking on an out-of-window ADDTO) would surface only through the already-scored acceptance duties (RFC5267-4.3-1, RFC5267-4.3.2-3), not through this expectation itself. Same do-not-assume adjudication as the RFC9051-7.3.4-1 internal-assumption reading and RFC9394-3.1-6's no-interaction clause (which cites this document's UPDATE side as the scoring home — this entry). |
| RFC5267-4.3-3 — Client MUST NOT expect UPDATE to be scoped by other return options (notifications cover all matching messages) | MUST | rev2 | untestable | The duty is a constraint on the client's INTERPRETATION of UPDATE notifications — an assumption it holds internally: a client that correctly expects unscoped notifications and one that wrongly believes RETURN (UPDATE MIN) filters to the minimum produce identical wire traces when the notifications arrive; any observable mishandling (choking on an out-of-window ADDTO) would surface only through the already-scored acceptance duties (RFC5267-4.3-1, RFC5267-4.3.2-3), not through this expectation itself. Same do-not-assume adjudication as the RFC9051-7.3.4-1 internal-assumption reading and RFC9394-3.1-6's no-interaction clause (which cites this document's UPDATE side as the scoring home — this entry). |
| RFC5267-4.3.1-1 — Client MUST accept an untagged NO with the NOUPDATE response code refusing an UPDATE request | MUST | rev1 | pass |  |
| RFC5267-4.3.1-1 — Client MUST accept an untagged NO with the NOUPDATE response code refusing an UPDATE request | MUST | rev2 | pass |  |
| RFC5267-4.3.2-1 — Client MUST process ADDTO and REMOVEFROM items in the order they appear, including within one ESEARCH response | MUST | rev1 | pass |  |
| RFC5267-4.3.2-1 — Client MUST process ADDTO and REMOVEFROM items in the order they appear, including within one ESEARCH response | MUST | rev2 | pass |  |
| RFC5267-4.3.2-2 — Client interprets update sets as message numbers or UIDs according to the searching command form | MUST | rev1 | pass |  |
| RFC5267-4.3.2-2 — Client interprets update sets as message numbers or UIDs according to the searching command form | MUST | rev2 | pass |  |
| RFC5267-4.3.2-3 — Client MUST accept ADDTO/REMOVEFROM ESEARCH responses at any time, including between commands and during IDLE | MUST | rev1 | pass |  |
| RFC5267-4.3.2-3 — Client MUST accept ADDTO/REMOVEFROM ESEARCH responses at any time, including between commands and during IDLE | MUST | rev2 | pass |  |
| RFC5267-4.3.3-1 — Client inserts ADDTO update sets at the given context position, shifting later results | MUST | rev1 | untestable | The insertion semantics govern the client's internally maintained context result list — a data structure with no wire representation. The protocol never requires the client to echo its result list back to the server, and no subsequent wire behavior is deterministically fixed by it (what the client does with its list — fetch, display, ignore — is application policy), so a client that mis-inserts and one that inserts correctly are black-box indistinguishable. The observable halves are already scored separately: surfacing the items in order (RFC5267-4.3.2-1) and accepting the responses at all (RFC5267-4.3-1, RFC5267-4.3.2-3). |
| RFC5267-4.3.3-1 — Client inserts ADDTO update sets at the given context position, shifting later results | MUST | rev2 | untestable | The insertion semantics govern the client's internally maintained context result list — a data structure with no wire representation. The protocol never requires the client to echo its result list back to the server, and no subsequent wire behavior is deterministically fixed by it (what the client does with its list — fetch, display, ignore — is application policy), so a client that mis-inserts and one that inserts correctly are black-box indistinguishable. The observable halves are already scored separately: surfacing the items in order (RFC5267-4.3.2-1) and accepting the responses at all (RFC5267-4.3-1, RFC5267-4.3.2-3). |
| RFC5267-4.3.3-2 — Client MAY insert zero-position ADDTO results so the result list stays in mailbox order | MAY | rev1 | untestable | Doubly unobservable: the choice concerns the client's internal result list (never expressed on the wire — see RFC5267-4.3.3-1), and the level is MAY with no constraining envelope — a client that maintains mailbox order for zero-position insertions and one that appends them anywhere are both fully compliant, so even with visibility into the list there would be no pass/fail boundary. Untestability follows both the internal-state theme and the MAY-level precedent of RFC3501-7.1.1-2. |
| RFC5267-4.3.3-2 — Client MAY insert zero-position ADDTO results so the result list stays in mailbox order | MAY | rev2 | untestable | Doubly unobservable: the choice concerns the client's internal result list (never expressed on the wire — see RFC5267-4.3.3-1), and the level is MAY with no constraining envelope — a client that maintains mailbox order for zero-position insertions and one that appends them anywhere are both fully compliant, so even with visibility into the list there would be no pass/fail boundary. Untestability follows both the internal-state theme and the MAY-level precedent of RFC3501-7.1.1-2. |
| RFC5267-4.3.4-1 — Client removes REMOVEFROM update sets starting at the given context position, shifting later results earlier | MUST | rev1 | untestable | Mirror of RFC5267-4.3.3-1: removal semantics govern the client's internally maintained context result list, which has no wire representation and fixes no deterministic subsequent wire behavior; a client that mis-applies positional removals is black-box indistinguishable from a correct one. The observable halves (surface items in order; accept the responses at any time) are scored as RFC5267-4.3.2-1 and RFC5267-4.3.2-3. |
| RFC5267-4.3.4-1 — Client removes REMOVEFROM update sets starting at the given context position, shifting later results earlier | MUST | rev2 | untestable | Mirror of RFC5267-4.3.3-1: removal semantics govern the client's internally maintained context result list, which has no wire representation and fixes no deterministic subsequent wire behavior; a client that mis-applies positional removals is black-box indistinguishable from a correct one. The observable halves (surface items in order; accept the responses at any time) are scored as RFC5267-4.3.2-1 and RFC5267-4.3.2-3. |
| RFC5267-4.3.4-2 — Client removes zero-position REMOVEFROM results wherever they occur in the result list | MUST | rev1 | untestable | Same internal-state adjudication as RFC5267-4.3.4-1: 'the client removes ... wherever they occur' prescribes an operation on the internally maintained result list, which never appears on the wire; compliance and non-compliance produce identical observable traffic. Unlike the ADDTO zero-position MAY (RFC5267-4.3.3-2), this is a prescriptive rule (remove-anywhere, not positional), but the prescription's subject is still unobservable state. |
| RFC5267-4.3.4-2 — Client removes zero-position REMOVEFROM results wherever they occur in the result list | MUST | rev2 | untestable | Same internal-state adjudication as RFC5267-4.3.4-1: 'the client removes ... wherever they occur' prescribes an operation on the internally maintained result list, which never appears on the wire; compliance and non-compliance produce identical observable traffic. Unlike the ADDTO zero-position MAY (RFC5267-4.3.3-2), this is a prescriptive rule (remove-anywhere, not positional), but the prescription's subject is still unobservable state. |
| RFC5267-4.3.5-1 — Client MAY issue CANCELUPDATE with one or more quoted searching-command tags to stop updates | MAY | rev1 | fail | unimplemented: When a client no longer wishes to receive updates, it may issue the CANCELUPDATE command, which will prevent all updates to the contexts named in the arguments … |
| RFC5267-4.3.5-1 — Client MAY issue CANCELUPDATE with one or more quoted searching-command tags to stop updates | MAY | rev2 | fail | unimplemented: When a client no longer wishes to receive updates, it may issue the CANCELUPDATE command, which will prevent all updates to the contexts named in the arguments … |
| RFC5267-4.4-1 — Client requests a window of results via the PARTIAL return option with a mandatory 1-based range | MUST | rev1 | fail | unimplemented: The PARTIAL search return option causes the server to provide in an ESEARCH response a subset of the results denoted by the sequence range given as the mandator… |
| RFC5267-4.4-1 — Client requests a window of results via the PARTIAL return option with a mandatory 1-based range | MUST | rev2 | fail | unimplemented: The PARTIAL search return option causes the server to provide in an ESEARCH response a subset of the results denoted by the sequence range given as the mandator… |
| RFC5267-4.4-2 — Command MUST NOT contain more than one PARTIAL or ALL return option | MUST NOT | rev1 | fail | unimplemented: A single command MUST NOT contain more than one PARTIAL or ALL search return option -- that is, either one PARTIAL, one ALL, or neither PARTIAL nor ALL is allow… |
| RFC5267-4.4-2 — Command MUST NOT contain more than one PARTIAL or ALL return option | MUST NOT | rev2 | fail | unimplemented: A single command MUST NOT contain more than one PARTIAL or ALL search return option -- that is, either one PARTIAL, one ALL, or neither PARTIAL nor ALL is allow… |
| RFC5267-4.4-3 — Client MUST accept a PARTIAL return data item with the original range and a short or NIL result set | MUST | rev1 | pass |  |
| RFC5267-4.4-3 — Client MUST accept a PARTIAL return data item with the original range and a short or NIL result set | MUST | rev2 | pass |  |
| RFC5182-1-1 — Client MAY save a search with RETURN (SAVE) and reference it with '$' in place of a sequence set | MAY | rev1 | fail | unimplemented: The SEARCH result reference extension defines a new SEARCH result option [IMAPABNF] "SAVE" that tells the server to remember the result of the SEARCH or UID SEA… |
| RFC5182-1-1 — Client MAY save a search with RETURN (SAVE) and reference it with '$' in place of a sequence set | MAY | rev2 | fail | unimplemented: The SEARCH result reference extension defines a new SEARCH result option [IMAPABNF] "SAVE" that tells the server to remember the result of the SEARCH or UID SEA… |
| RFC5182-1-2 — Client using SAVE alone MUST accept completion without any search response | MUST | rev1 | fail | unimplemented: In absence of any other SEARCH result option, the SAVE result option also suppresses any SEARCH response that would have been otherwise returned by the SEARCH c… |
| RFC5182-2.1-1 — Client MUST NOT use SAVE or the '$' marker unless the server advertises SEARCHRES | MUST NOT | rev1 | fail | unimplemented: The SEARCH result reference extension described in this document is present in any IMAP4 server implementation that returns "SEARCHRES" as one of the supported … |
| RFC5182-2.1-2 — Client MUST NOT rely on '$' surviving re-selection, a failed SAVE search, or a UIDVALIDITY change | MUST NOT | rev1 | untestable | The quoted resets are performed by the SERVER, which owns the search result variable and substitutes '$' correctly regardless of what the client believes; the client's mirror duty is not to assume '$' still denotes the pre-reset match set — an expectation with no wire signature. A subsequent command using '$' is byte-identical whether the client holds a stale belief or a correct one, and both the stale-belief client and the compliant client receive the same (empty-set) results, so no black-box exchange has a mandated pass/fail boundary. For a protocol library the '$'-lifetime bookkeeping is additionally delegable to the consuming application, which supplies sequence-set arguments opaquely. |
| RFC5182-2.1-2 — Client MUST NOT rely on '$' surviving re-selection, a failed SAVE search, or a UIDVALIDITY change | MUST NOT | rev2 | untestable | The quoted resets are performed by the SERVER, which owns the search result variable and substitutes '$' correctly regardless of what the client believes; the client's mirror duty is not to assume '$' still denotes the pre-reset match set — an expectation with no wire signature. A subsequent command using '$' is byte-identical whether the client holds a stale belief or a correct one, and both the stale-belief client and the compliant client receive the same (empty-set) results, so no black-box exchange has a mandated pass/fail boundary. For a protocol library the '$'-lifetime bookkeeping is additionally delegable to the consuming application, which supplies sequence-set arguments opaquely. |
| RFC5182-2.1-3 — Client MUST treat an empty '$' as usable and accept the OK-with-no-data outcome | MUST | rev1 | fail | unimplemented: Note that even if the "$" marker contains the empty list of messages, it must be treated by all commands accepting message sets as parameters as a valid, but no… |
| RFC5182-2.1-3 — Client MUST treat an empty '$' as usable and accept the OK-with-no-data outcome | MUST | rev2 | fail | unimplemented: Note that even if the "$" marker contains the empty list of messages, it must be treated by all commands accepting message sets as parameters as a valid, but no… |
| RFC5182-2.3-1 — Client MAY pipeline SEARCH RETURN (SAVE) with '$'-using commands absent ambiguity | MAY | rev1 | fail | unimplemented: A client supporting this extension MAY pipeline a SEARCH RETURN (SAVE) command with one or more command using the "$" marker, as long as this doesn't create an … |
| RFC5182-2.5-1 — Refused SAVE arrives as tagged NO with NOTSAVED; '$' becomes empty | MUST | rev1 | pass |  |
| RFC6203-1-1 — Client (implicit) MUST NOT use FUZZY/RELEVANCY unless SEARCH=FUZZY is advertised | MUST NOT | rev1 | fail | unimplemented: This document describes a new SEARCH=FUZZY extension that provides such functionality. |
| RFC6203-1-1 — Client (implicit) MUST NOT use FUZZY/RELEVANCY unless SEARCH=FUZZY is advertised | MUST NOT | rev2 | fail | unimplemented: This document describes a new SEARCH=FUZZY extension that provides such functionality. |
| RFC6203-3-1 — FUZZY search-key form: FUZZY takes exactly one search key as its argument | MUST | rev1 | fail | unimplemented: The FUZZY search key takes another search key as its argument. |
| RFC6203-3-1 — FUZZY search-key form: FUZZY takes exactly one search key as its argument | MUST | rev2 | fail | unimplemented: The FUZZY search key takes another search key as its argument. |
| RFC6203-3-2 — Client must warn the user that fuzzy results are not necessarily deterministic | MUST | rev1 | untestable | The duty is discharged entirely in the client's user interface — 'give the user appropriate warning' about the non-deterministic nature of fuzzy results (same query, different results at different times/for different users, per §3's preceding paragraph). A client that displays such a warning and one that does not emit byte-identical protocol traffic; no black-box wire observation can detect whether or how the warning is presented, nor what 'appropriate' means for a given UI. |
| RFC6203-3-2 — Client must warn the user that fuzzy results are not necessarily deterministic | MUST | rev2 | untestable | The duty is discharged entirely in the client's user interface — 'give the user appropriate warning' about the non-deterministic nature of fuzzy results (same query, different results at different times/for different users, per §3's preceding paragraph). A client that displays such a warning and one that does not emit byte-identical protocol traffic; no black-box wire observation can detect whether or how the warning is presented, nor what 'appropriate' means for a given UI. |
| RFC6203-4-1 — Client (implicit) MUST accept RELEVANCY score-list data (scores 1-100) in ESEARCH responses it solicited | MUST | rev1 | pass |  |
| RFC6203-4-1 — Client (implicit) MUST accept RELEVANCY score-list data (scores 1-100) in ESEARCH responses it solicited | MUST | rev2 | pass |  |
| RFC6203-4-2 — RELEVANCY return option for SEARCH only when the server also advertises ESEARCH | MUST NOT | rev1 | fail | unimplemented: If the server also advertises the ESEARCH capability as defined by [ESEARCH], the relevancy scores can be retrieved using the new RELEVANCY return option for SE… |
| RFC6203-4-2 — RELEVANCY return option for SEARCH only when the server also advertises ESEARCH | MUST NOT | rev2 | fail | unimplemented: If the server also advertises the ESEARCH capability as defined by [ESEARCH], the relevancy scores can be retrieved using the new RELEVANCY return option for SE… |
| RFC6203-4-3 — Client MUST NOT use the RELEVANCY return option without a FUZZY search key | MUST NOT | rev1 | fail | unimplemented: The RELEVANCY return option MUST NOT be used unless a FUZZY search key is also given. |
| RFC6203-4-3 — Client MUST NOT use the RELEVANCY return option without a FUZZY search key | MUST NOT | rev2 | fail | unimplemented: The RELEVANCY return option MUST NOT be used unless a FUZZY search key is also given. |
| RFC6203-4-4 — Client (implicit) MUST NOT treat SEARCH results as relevancy-ordered | MUST NOT | rev1 | untestable | How the client interprets and presents the order of a returned result set is invisible on the wire: a client that (wrongly) displays SEARCH/ESEARCH results to the user as if they were relevancy-ranked emits protocol traffic byte-identical to one that treats the order as meaningless and ranks locally by the returned RELEVANCY scores. The testable flip side — actually issuing 'SORT (RELEVANCY) ...' when server-side relevancy ordering is wanted — is covered by the §6 entries. |
| RFC6203-4-4 — Client (implicit) MUST NOT treat SEARCH results as relevancy-ordered | MUST NOT | rev2 | untestable | How the client interprets and presents the order of a returned result set is invisible on the wire: a client that (wrongly) displays SEARCH/ESEARCH results to the user as if they were relevancy-ranked emits protocol traffic byte-identical to one that treats the order as meaningless and ranks locally by the returned RELEVANCY scores. The testable flip side — actually issuing 'SORT (RELEVANCY) ...' when server-side relevancy ordering is wanted — is covered by the §6 entries. |
| RFC6203-5-1 — Client MAY apply FUZZY to any search key, including non-string keys | MAY | rev1 | fail | unimplemented: Fuzzy matching is not limited to just string matching. |
| RFC6203-5-1 — Client MAY apply FUZZY to any search key, including non-string keys | MAY | rev2 | fail | unimplemented: Fuzzy matching is not limited to just string matching. |
| RFC6203-6-1 — RELEVANCY sort criterion only when the server also advertises SORT | MUST NOT | rev1 | fail | unimplemented: If the server also advertises the SORT capability as defined by [SORT], the results can be sorted by the new RELEVANCY sort criteria |
| RFC6203-6-1 — RELEVANCY sort criterion only when the server also advertises SORT | MUST NOT | rev2 | fail | unimplemented: If the server also advertises the SORT capability as defined by [SORT], the results can be sorted by the new RELEVANCY sort criteria |
| RFC6203-6-2 — Client MUST NOT use the RELEVANCY sort criterion without a FUZZY search key | MUST NOT | rev1 | fail | unimplemented: As with the RELEVANCY return option, RELEVANCY sort criteria MUST NOT be used unless a FUZZY search key is also given. |
| RFC6203-6-2 — Client MUST NOT use the RELEVANCY sort criterion without a FUZZY search key | MUST NOT | rev2 | fail | unimplemented: As with the RELEVANCY return option, RELEVANCY sort criteria MUST NOT be used unless a FUZZY search key is also given. |
| RFC6203-6-3 — RELEVANCY return option for SORT only when the server also advertises ESORT | MUST NOT | rev1 | fail | unimplemented: If the server also advertises the ESORT capability as defined by [CONTEXT], the relevancy scores can be retrieved using the new RELEVANCY return option for SORT |
| RFC6203-6-3 — RELEVANCY return option for SORT only when the server also advertises ESORT | MUST NOT | rev2 | fail | unimplemented: If the server also advertises the ESORT capability as defined by [CONTEXT], the relevancy scores can be retrieved using the new RELEVANCY return option for SORT |
| RFC6203-8-1 — Client should let users see all search results rather than hiding low-relevancy ones | SHOULD | rev1 | untestable | Whether the client's UI offers a way for the user to see every returned result (as a defense against relevancy-ranking 'poisoning' via keywords or hidden markup) is a presentation-layer property with no protocol footprint: a client that hides low-score results and one that exposes them all issue identical commands and consume identical responses. No black-box wire test can observe the result-display policy. |
| RFC6203-8-1 — Client should let users see all search results rather than hiding low-relevancy ones | SHOULD | rev2 | untestable | Whether the client's UI offers a way for the user to see every returned result (as a defense against relevancy-ranking 'poisoning' via keywords or hidden markup) is a presentation-layer property with no protocol footprint: a client that hides low-score results and one that exposes them all issue identical commands and consume identical responses. No black-box wire test can observe the result-display policy. |
| RFC9394-3-1 — Client (implicit) only uses PARTIAL when the server advertises support | MUST | rev1 | untestable | The document states the capability contract from the server's side (the server advertises "PARTIAL" via CAPABILITY); the reciprocal client duty — do not send the PARTIAL search return option or the PARTIAL UID FETCH modifier to a server that has not advertised the governing capability — is implicit in that contract. Whether the client consulted the advertised capability list before emitting PARTIAL is an internal fact about its own capability inventory: a client that sends PARTIAL speculatively and one that checked first produce byte-identical wire forms, and divergence surfaces only as the server's own BAD/NO rejection. Same adjudication as the RFC4469-2-1 capability-gate precedent. |
| RFC9394-3-1 — Client (implicit) only uses PARTIAL when the server advertises support | MUST | rev2 | untestable | The document states the capability contract from the server's side (the server advertises "PARTIAL" via CAPABILITY); the reciprocal client duty — do not send the PARTIAL search return option or the PARTIAL UID FETCH modifier to a server that has not advertised the governing capability — is implicit in that contract. Whether the client consulted the advertised capability list before emitting PARTIAL is an internal fact about its own capability inventory: a client that sends PARTIAL speculatively and one that checked first produce byte-identical wire forms, and divergence surfaces only as the server's own BAD/NO rejection. Same adjudication as the RFC4469-2-1 capability-gate precedent. |
| RFC9394-3.1-1 — Client requests a page of SEARCH results via RETURN (PARTIAL m:n) with a mandatory 1-based range | MUST | rev1 | fail | unimplemented: The PARTIAL SEARCH return option causes the server to provide in an ESEARCH response [RFC4731] [RFC9051] a subset of the results denoted by the sequence range g… |
| RFC9394-3.1-1 — Client requests a page of SEARCH results via RETURN (PARTIAL m:n) with a mandatory 1-based range | MUST | rev2 | fail | unimplemented: The PARTIAL SEARCH return option causes the server to provide in an ESEARCH response [RFC4731] [RFC9051] a subset of the results denoted by the sequence range g… |
| RFC9394-3.1-2 — Client MAY page from the newest result using a minus-prefixed range | MAY | rev1 | fail | unimplemented: It is also possible to direct the server to start the SEARCH from the latest matching (with the highest UID) message. This can be done by prepending "-" to the … |
| RFC9394-3.1-2 — Client MAY page from the newest result using a minus-prefixed range | MAY | rev2 | fail | unimplemented: It is also possible to direct the server to start the SEARCH from the latest matching (with the highest UID) message. This can be done by prepending "-" to the … |
| RFC9394-3.1-3 — Command MUST NOT contain more than one PARTIAL or ALL return option | MUST NOT | rev1 | fail | unimplemented: A single command MUST NOT contain more than one PARTIAL or ALL search return option; that is, either one PARTIAL, one ALL, or neither PARTIAL nor ALL is allowed… |
| RFC9394-3.1-3 — Command MUST NOT contain more than one PARTIAL or ALL return option | MUST NOT | rev2 | fail | unimplemented: A single command MUST NOT contain more than one PARTIAL or ALL search return option; that is, either one PARTIAL, one ALL, or neither PARTIAL nor ALL is allowed… |
| RFC9394-3.1-4 — Client (implicit) accepts a PARTIAL return data item whose result set is shorter than the requested range | MUST | rev1 | pass |  |
| RFC9394-3.1-4 — Client (implicit) accepts a PARTIAL return data item whose result set is shorter than the requested range | MUST | rev2 | pass |  |
| RFC9394-3.1-5 — Client (implicit) accepts NIL in place of the sequence set when the whole range is out of results | MUST | rev1 | pass |  |
| RFC9394-3.1-5 — Client (implicit) accepts NIL in place of the sequence set when the whole range is out of results | MUST | rev2 | pass |  |
| RFC9394-3.1-6 — Client MAY page in any order and MAY combine PARTIAL with UPDATE under CONTEXT=SEARCH | MAY | rev1 | untestable | Both grants are choices internal to the client's paging strategy: 'need not request ... in any particular order' removes any ordering duty (every ordering of PARTIAL requests is compliant, so there is nothing to falsify), and 'might wish to use PARTIAL in combination with UPDATE' is advisory — a client that walks a large result set without UPDATE and one that combines them are both fully compliant, so no black-box test can score the choice. The trailing no-interaction clause ('these return options do not interact -- the UPDATE will provide notifications for all matching results') binds the client's INTERPRETATION of UPDATE notifications — an expectation it holds internally; any observable mishandling would surface only in its subsequent UPDATE processing, which is RFC 5267 CONTEXT=SEARCH territory, not a PARTIAL wire behavior. |
| RFC9394-3.1-6 — Client MAY page in any order and MAY combine PARTIAL with UPDATE under CONTEXT=SEARCH | MAY | rev2 | untestable | Both grants are choices internal to the client's paging strategy: 'need not request ... in any particular order' removes any ordering duty (every ordering of PARTIAL requests is compliant, so there is nothing to falsify), and 'might wish to use PARTIAL in combination with UPDATE' is advisory — a client that walks a large result set without UPDATE and one that combines them are both fully compliant, so no black-box test can score the choice. The trailing no-interaction clause ('these return options do not interact -- the UPDATE will provide notifications for all matching results') binds the client's INTERPRETATION of UPDATE notifications — an expectation it holds internally; any observable mishandling would surface only in its subsequent UPDATE processing, which is RFC 5267 CONTEXT=SEARCH territory, not a PARTIAL wire behavior. |
| RFC9394-3.2-1 — Client (implicit) interprets the "$" marker per the SAVE+PARTIAL combination rules | MUST | rev1 | untestable | The '$' marker (RFC 5182 SEARCHRES / RFC 9051 SAVE) is expanded server-side: when the client later uses '$' in a command, the wire carries only the literal '$' token regardless of what the client believes it denotes. The client's model of the saved result — the PARTIAL page alone, PARTIAL plus MIN/MAX, or all found messages when COUNT is present — is internal state that never appears on the wire; a client holding the wrong model emits byte-identical commands, and any divergence materializes only in the server's expansion. No black-box observation of the client can falsify its '$' bookkeeping. |
| RFC9394-3.2-1 — Client (implicit) interprets the "$" marker per the SAVE+PARTIAL combination rules | MUST | rev2 | untestable | The '$' marker (RFC 5182 SEARCHRES / RFC 9051 SAVE) is expanded server-side: when the client later uses '$' in a command, the wire carries only the literal '$' token regardless of what the client believes it denotes. The client's model of the saved result — the PARTIAL page alone, PARTIAL plus MIN/MAX, or all found messages when COUNT is present — is internal state that never appears on the wire; a client holding the wrong model emits byte-identical commands, and any divergence materializes only in the server's expansion. No black-box observation of the client can falsify its '$' bookkeeping. |
| RFC9394-3.3-1 — Client MAY page a UID FETCH with the PARTIAL fetch modifier | MAY | rev1 | fail | unimplemented: The PARTIAL extension also extends the UID FETCH command with a PARTIAL FETCH modifier. The PARTIAL FETCH modifier has the same syntax as the PARTIAL SEARCH res… |
| RFC9394-3.3-1 — Client MAY page a UID FETCH with the PARTIAL fetch modifier | MAY | rev2 | fail | unimplemented: The PARTIAL extension also extends the UID FETCH command with a PARTIAL FETCH modifier. The PARTIAL FETCH modifier has the same syntax as the PARTIAL SEARCH res… |
| RFC9394-4-1 — Client emits partial-range as two same-sign nz-numbers, never "*" or mixed signs | MUST | rev1 | fail | unimplemented: modifier-partial = "PARTIAL" SP partial-range ... partial-range-first = nz-number ":" nz-number ... This is similar to <seq-range> from [RFC3501] ... but cannot… |
| RFC9394-4-1 — Client emits partial-range as two same-sign nz-numbers, never "*" or mixed signs | MUST | rev2 | fail | unimplemented: modifier-partial = "PARTIAL" SP partial-range ... partial-range-first = nz-number ":" nz-number ... This is similar to <seq-range> from [RFC3501] ... but cannot… |
| RFC2177-3-1 — Client MUST NOT use IDLE unless the server advertises the IDLE capability | MUST NOT | rev1 | pass |  |
| RFC2177-3-1 — Client MUST NOT use IDLE unless the server advertises the IDLE capability | MUST NOT | rev2 | pass |  |
| RFC2177-3-2 — Client MUST continue to accept unsolicited untagged responses to ANY command | MUST | rev1 | pass |  |
| RFC2177-3-3 — Client MAY send IDLE and awaits the server's '+' continuation before continuing | MAY | rev1 | fail | unimplemented: The IDLE command is sent from the client to the server when the client is ready to accept unsolicited mailbox update messages. The server requests a response to… |
| RFC2177-3-3 — Client MAY send IDLE and awaits the server's '+' continuation before continuing | MAY | rev2 | fail | unimplemented: The IDLE command is sent from the client to the server when the client is ready to accept unsolicited mailbox update messages. The server requests a response to… |
| RFC2177-3-4 — Client must accept untagged responses arriving at any time while IDLE is active | MUST | rev1 | fail | unimplemented: The IDLE command remains active until the client responds to the continuation, and as long as an IDLE command is active, the server is now free to send untagged… |
| RFC2177-3-4 — Client must accept untagged responses arriving at any time while IDLE is active | MUST | rev2 | fail | unimplemented: The IDLE command remains active until the client responds to the continuation, and as long as an IDLE command is active, the server is now free to send untagged… |
| RFC2177-3-5 — Client terminates IDLE by sending the DONE continuation | MUST | rev1 | fail | unimplemented: The IDLE command is terminated by the receipt of a "DONE" continuation from the client; such response satisfies the server's continuation request. |
| RFC2177-3-6 — Client MUST NOT send a command while the server is waiting for the DONE | MUST NOT | rev1 | fail | unimplemented: The client MUST NOT send a command while the server is waiting for the DONE, since the server will not be able to distinguish a command from a continuation. |
| RFC2177-3-7 — Clients using IDLE are advised to terminate and re-issue it at least every 29 minutes | SHOULD | rev1 | untestable | Mirrors RFC9051-6.3.13-2's rationale: the terminate-and-reissue duty IS wire behavior in principle (a DONE followed by a fresh IDLE inside the window would be observable), but (a) no harness can realistically hold a connection idling for the tens of minutes per assertion needed to observe the 29-minute boundary, and (b) no enforced wire rule at shorter timescales proxies for it — 29 minutes is a safety margin under a hypothetical 30-minute inactivity timeout that the RFC itself declines to standardize ('if such a server has an inactivity timeout it MAY log the client off implicitly'), so a client that never re-issues within any shorter, testable window violates no wire-level rule; it only risks an implementation-specific server-side logoff. |
| RFC5465-3.1-1 — Client emits NOTIFY in one of its two forms: NOTIFY SET or NOTIFY NONE | MUST | rev1 | fail | unimplemented: The NOTIFY command has two forms. NOTIFY NONE specifies that the client is not interested in any kind of event happening on the server. NOTIFY SET replaces the … |
| RFC5465-3.1-1 — Client emits NOTIFY in one of its two forms: NOTIFY SET or NOTIFY NONE | MUST | rev2 | fail | unimplemented: The NOTIFY command has two forms. NOTIFY NONE specifies that the client is not interested in any kind of event happening on the server. NOTIFY SET replaces the … |
| RFC5465-3.1-2 — Client MUST accept implicit-NOOP accumulated changes in NOTIFY SET's response stream | MUST | rev1 | pass |  |
| RFC5465-3.1-2 — Client MUST accept implicit-NOOP accumulated changes in NOTIFY SET's response stream | MUST | rev2 | pass |  |
| RFC5465-3.1-3 — Client omitting SELECTED/SELECTED-DELAYED gets no message events for the selected mailbox | MAY | rev1 | untestable | The client-binding content is that a client which WANTS message events for the currently selected mailbox must say so with a SELECTED/SELECTED-DELAYED event-group — omission is a binding declaration of disinterest. But whether the client wanted those events is an internal decision with no wire artifact: a NOTIFY SET without a SELECTED group is fully compliant on its face, and a client that omitted the group by mistake versus by choice emits byte-identical commands. Divergence would surface only as the client's internal model wrongly expecting unsolicited message events that (compliantly) never arrive, which a black-box observer cannot distinguish from contented disinterest. |
| RFC5465-3.1-3 — Client omitting SELECTED/SELECTED-DELAYED gets no message events for the selected mailbox | MAY | rev2 | untestable | The client-binding content is that a client which WANTS message events for the currently selected mailbox must say so with a SELECTED/SELECTED-DELAYED event-group — omission is a binding declaration of disinterest. But whether the client wanted those events is an internal decision with no wire artifact: a NOTIFY SET without a SELECTED group is fully compliant on its face, and a client that omitted the group by mistake versus by choice emits byte-identical commands. Divergence would surface only as the client's internal model wrongly expecting unsolicited message events that (compliantly) never arrive, which a black-box observer cannot distinguish from contented disinterest. |
| RFC5465-3.1-4 — Client MUST accept STATUS responses before the tagged OK when using NOTIFY SET STATUS | MUST | rev1 | fail | unimplemented: If the NOTIFY command enables MessageNew, MessageExpunge, AnnotationChange, or FlagChange notifications for a mailbox other than the currently selected mailbox,… |
| RFC5465-3.1-4 — Client MUST accept STATUS responses before the tagged OK when using NOTIFY SET STATUS | MUST | rev2 | fail | unimplemented: If the NOTIFY command enables MessageNew, MessageExpunge, AnnotationChange, or FlagChange notifications for a mailbox other than the currently selected mailbox,… |
| RFC5465-3.1-5 — Client SHOULD limit the number of mailboxes watched with NOTIFY | SHOULD | rev1 | untestable | 'Limit the number of mailboxes' names no threshold: there is no wire-observable line between a prudent watch list and an excessive one. Whether a given NOTIFY SET is 'too broad' depends on the deployment's mailbox count, update rate, network, and battery constraints — a resource/performance judgment outside the protocol's observable surface. Any NOTIFY SET the client emits, however broad, is syntactically and semantically legal (the server's [NOTIFICATIONOVERFLOW] escape hatch exists precisely because clients may legally over-ask), so no black-box assertion can fail a client for watching 'too many' mailboxes. |
| RFC5465-3.1-5 — Client SHOULD limit the number of mailboxes watched with NOTIFY | SHOULD | rev2 | untestable | 'Limit the number of mailboxes' names no threshold: there is no wire-observable line between a prudent watch list and an excessive one. Whether a given NOTIFY SET is 'too broad' depends on the deployment's mailbox count, update rate, network, and battery constraints — a resource/performance judgment outside the protocol's observable surface. Any NOTIFY SET the client emits, however broad, is syntactically and semantically legal (the server's [NOTIFICATIONOVERFLOW] escape hatch exists precisely because clients may legally over-ask), so no black-box assertion can fail a client for watching 'too many' mailboxes. |
| RFC5465-3.1-6 — Client MUST accept the untagged extended LIST response with \NoAccess | MUST | rev1 | pass |  |
| RFC5465-3.1-6 — Client MUST accept the untagged extended LIST response with \NoAccess | MUST | rev2 | pass |  |
| RFC5465-3.1-7 — Client MUST accept a tagged NO [NOTIFICATIONOVERFLOW] refusal of NOTIFY | MAY | rev1 | pass |  |
| RFC5465-3.1-7 — Client MUST accept a tagged NO [NOTIFICATIONOVERFLOW] refusal of NOTIFY | MAY | rev2 | pass |  |
| RFC5465-3.1-8 — Client MUST handle a tagged NO with [BADEVENT (...)] for unsupported event types | MUST | rev1 | pass |  |
| RFC5465-3.1-8 — Client MUST handle a tagged NO with [BADEVENT (...)] for unsupported event types | MUST | rev2 | pass |  |
| RFC5465-5-1 — Client specifying FlagChange/AnnotationChange MUST also specify MessageNew and MessageExpunge | MUST | rev1 | fail | unimplemented: If the FlagChange and/or AnnotationChange events are specified, MessageNew and MessageExpunge MUST also be specified by the client. Otherwise, the server MUST r… |
| RFC5465-5-1 — Client specifying FlagChange/AnnotationChange MUST also specify MessageNew and MessageExpunge | MUST | rev2 | fail | unimplemented: If the FlagChange and/or AnnotationChange events are specified, MessageNew and MessageExpunge MUST also be specified by the client. Otherwise, the server MUST r… |
| RFC5465-5-2 — Client MUST specify MessageNew and MessageExpunge together | MUST | rev1 | fail | unimplemented: If one of MessageNew or MessageExpunge is specified, then both events MUST be specified. Otherwise, the server MUST respond with the tagged BAD response. |
| RFC5465-5-2 — Client MUST specify MessageNew and MessageExpunge together | MUST | rev2 | fail | unimplemented: If one of MessageNew or MessageExpunge is specified, then both events MUST be specified. Otherwise, the server MUST respond with the tagged BAD response. |
| RFC5465-5-3 — Client suppresses events via omission, the NONE event specifier, or NOTIFY NONE | MAY | rev1 | fail | unimplemented: The client can instruct the server not to send an event by omitting the necessary event from the list of events specified in NOTIFY SET, by using the NONE event… |
| RFC5465-5-3 — Client suppresses events via omission, the NONE event specifier, or NOTIFY NONE | MAY | rev2 | fail | unimplemented: The client can instruct the server not to send an event by omitting the necessary event from the list of events specified in NOTIFY SET, by using the NONE event… |
| RFC5465-5.1-1 — Client MUST accept unsolicited FETCH (UID + FLAGS/ANNOTATION) between commands | MUST | rev1 | pass |  |
| RFC5465-5.1-1 — Client MUST accept unsolicited FETCH (UID + FLAGS/ANNOTATION) between commands | MUST | rev2 | pass |  |
| RFC5465-5.2-1 — Client MUST accept unsolicited EXISTS followed by FETCH for MessageNew | MUST | rev1 | pass |  |
| RFC5465-5.2-1 — Client MUST accept unsolicited EXISTS followed by FETCH for MessageNew | MUST | rev2 | pass |  |
| RFC5465-5.2-2 — Client MUST accept unsolicited STATUS responses for non-selected mailboxes | MUST | rev1 | pass |  |
| RFC5465-5.2-2 — Client MUST accept unsolicited STATUS responses for non-selected mailboxes | MUST | rev2 | pass |  |
| RFC5465-5.2-3 — Client SHOULD NOT request fetch-atts that set \Seen or presuppose a bodypart | SHOULD NOT | rev1 | fail | unimplemented: The client SHOULD NOT use FETCH attributes that implicitly set the \seen flag, or that presuppose the existence of a given bodypart. UID, MODSEQ, FLAGS, ENVELOP… |
| RFC5465-5.2-3 — Client SHOULD NOT request fetch-atts that set \Seen or presuppose a bodypart | SHOULD NOT | rev2 | fail | unimplemented: The client SHOULD NOT use FETCH attributes that implicitly set the \seen flag, or that presuppose the existence of a given bodypart. UID, MODSEQ, FLAGS, ENVELOP… |
| RFC5465-5.2-4 — Client with SELECTED MessageNew cannot reference messages via '*' | MUST NOT | rev1 | fail | unimplemented: Note that if a client asks to be notified of MessageNew events with the SELECTED mailbox specifier, the number of messages can increase at any time, and therefo… |
| RFC5465-5.2-4 — Client with SELECTED MessageNew cannot reference messages via '*' | MUST NOT | rev2 | fail | unimplemented: Note that if a client asks to be notified of MessageNew events with the SELECTED mailbox specifier, the number of messages can increase at any time, and therefo… |
| RFC5465-5.3-1 — Client MUST accept unsolicited EXPUNGE (or VANISHED) between commands | MUST | rev1 | pass |  |
| RFC5465-5.3-1 — Client MUST accept unsolicited EXPUNGE (or VANISHED) between commands | MUST | rev2 | pass |  |
| RFC5465-5.3-2 — Client with SELECTED MessageExpunge cannot use MSNs; must use UID commands | MUST NOT | rev1 | fail | unimplemented: Note that if a client requests MessageExpunge with the SELECTED mailbox specifier, the meaning of an MSN can change at any time, so the client cannot use MSNs i… |
| RFC5465-5.3-2 — Client with SELECTED MessageExpunge cannot use MSNs; must use UID commands | MUST NOT | rev2 | fail | unimplemented: Note that if a client requests MessageExpunge with the SELECTED mailbox specifier, the meaning of an MSN can change at any time, so the client cannot use MSNs i… |
| RFC5465-5.4-1 — Client MUST accept unsolicited LIST responses (incl. \Nonexistent) for MailboxName events | MUST | rev1 | pass |  |
| RFC5465-5.4-1 — Client MUST accept unsolicited LIST responses (incl. \Nonexistent) for MailboxName events | MUST | rev2 | pass |  |
| RFC5465-5.4-2 — Client MUST accept the extended LIST response with the OLDNAME data item on rename | MUST | rev1 | pass |  |
| RFC5465-5.4-2 — Client MUST accept the extended LIST response with the OLDNAME data item on rename | MUST | rev2 | pass |  |
| RFC5465-5.5-1 — Client MUST accept unsolicited LIST with \Subscribed for SubscriptionChange events | MUST | rev1 | pass |  |
| RFC5465-5.5-1 — Client MUST accept unsolicited LIST with \Subscribed for SubscriptionChange events | MUST | rev2 | pass |  |
| RFC5465-5.8-1 — Client MUST accept the untagged OK [NOTIFICATIONOVERFLOW] response | MUST | rev1 | pass |  |
| RFC5465-5.8-1 — Client MUST accept the untagged OK [NOTIFICATIONOVERFLOW] response | MUST | rev2 | pass |  |
| RFC5465-5.8-2 — Client (implicit) must treat notifications as cancelled after NOTIFICATIONOVERFLOW | MUST | rev1 | untestable | The implicit client duty — after 'behave as if a NOTIFY NONE command had just been received', the client's model of its notification registrations must flip to NONE, since continuing to rely on push updates would silently desynchronize it — governs the client's internal state, not a mandated wire action. The RFC prescribes no recovery: a compliant client may re-issue a narrower NOTIFY SET, fall back to polling with NOOP/STATUS, re-SELECT to resynchronize the selected mailbox, or do nothing at all if the user no longer needs the data — and 'no visible reaction' is therefore indistinguishable on the wire from the non-compliant client that failed to update its model and still believes notifications flow. The divergence (a stale internal belief) surfaces only as absent expectations, which a black-box observer cannot detect. |
| RFC5465-5.8-2 — Client (implicit) must treat notifications as cancelled after NOTIFICATIONOVERFLOW | MUST | rev2 | untestable | The implicit client duty — after 'behave as if a NOTIFY NONE command had just been received', the client's model of its notification registrations must flip to NONE, since continuing to rely on push updates would silently desynchronize it — governs the client's internal state, not a mandated wire action. The RFC prescribes no recovery: a compliant client may re-issue a narrower NOTIFY SET, fall back to polling with NOOP/STATUS, re-SELECT to resynchronize the selected mailbox, or do nothing at all if the user no longer needs the data — and 'no visible reaction' is therefore indistinguishable on the wire from the non-compliant client that failed to update its model and still believes notifications flow. The divergence (a stale internal belief) surfaces only as absent expectations, which a black-box observer cannot detect. |
| RFC5465-6.1-1 — Client MUST NOT specify both SELECTED and SELECTED-DELAYED in one NOTIFY command | MUST NOT | rev1 | pass |  |
| RFC5465-6.1-1 — Client MUST NOT specify both SELECTED and SELECTED-DELAYED in one NOTIFY command | MUST NOT | rev2 | pass |  |
| RFC5465-6.1-2 — Client MUST NOT pair non-message events with SELECTED/SELECTED-DELAYED | MUST NOT | rev1 | pass |  |
| RFC5465-6.1-2 — Client MUST NOT pair non-message events with SELECTED/SELECTED-DELAYED | MUST NOT | rev2 | pass |  |
| RFC5465-7-1 — Client MAY request fetch-atts via the extended UPDATE return option | MAY | rev1 | fail | unimplemented: If the server that supports the NOTIFY extension also supports CONTEXT=SEARCH and/or CONTEXT=SORT as defined in [RFC5267], the UPDATE return option is extended … |
| RFC5465-7-1 — Client MAY request fetch-atts via the extended UPDATE return option | MAY | rev2 | fail | unimplemented: If the server that supports the NOTIFY extension also supports CONTEXT=SEARCH and/or CONTEXT=SORT as defined in [RFC5267], the UPDATE return option is extended … |
| RFC5465-8-1 — Client MUST NOT attach a MessageNew fetch-att list outside SELECTED/SELECTED-DELAYED event groups | MUST NOT | rev1 | pass |  |
| RFC5465-8-1 — Client MUST NOT attach a MessageNew fetch-att list outside SELECTED/SELECTED-DELAYED event groups | MUST NOT | rev2 | pass |  |
| RFC5466-3-1 — Client (implicit) only uses FILTERS machinery when the server advertises FILTERS | MUST | rev1 | untestable | The document states the capability contract from the server's side (the extension 'is present in' an implementation that advertises FILTERS); the reciprocal client duty — do not send the FILTER search key, or manage the reserved /private\|/shared/filters/* entries as filters, against a server that has not advertised FILTERS — is implicit in that contract. Whether the client consulted the advertised capability list before emitting FILTER is an internal decision about its own capability inventory: a client that issues FILTER speculatively without checking and one that checked first produce byte-identical wire forms. Divergence surfaces only as the server's own BAD/NO rejection, which is server behavior, not an observable property of the client's capability bookkeeping. Same analysis and theme as the RFC4469-2-1 CATENATE gate. |
| RFC5466-3-1 — Client (implicit) only uses FILTERS machinery when the server advertises FILTERS | MUST | rev2 | untestable | The document states the capability contract from the server's side (the extension 'is present in' an implementation that advertises FILTERS); the reciprocal client duty — do not send the FILTER search key, or manage the reserved /private\|/shared/filters/* entries as filters, against a server that has not advertised FILTERS — is implicit in that contract. Whether the client consulted the advertised capability list before emitting FILTER is an internal decision about its own capability inventory: a client that issues FILTER speculatively without checking and one that checked first produce byte-identical wire forms. Divergence surfaces only as the server's own BAD/NO rejection, which is server behavior, not an observable property of the client's capability bookkeeping. Same analysis and theme as the RFC4469-2-1 CATENATE gate. |
| RFC5466-3.1-1 — Client references a named filter with the FILTER <filter_name> search key | MUST | rev1 | fail | unimplemented: The FILTER criterion for the SEARCH command allows a client to reference by name a filter stored on the server. ... Syntax: FILTER <filter_name> |
| RFC5466-3.1-1 — Client references a named filter with the FILTER <filter_name> search key | MUST | rev2 | fail | unimplemented: The FILTER criterion for the SEARCH command allows a client to reference by name a filter stored on the server. ... Syntax: FILTER <filter_name> |
| RFC5466-3.1-2 — Client MUST accept a tagged NO carrying [UNDEFINED-FILTER <name>] on SEARCH | MUST | rev1 | pass |  |
| RFC5466-3.1-2 — Client MUST accept a tagged NO carrying [UNDEFINED-FILTER <name>] on SEARCH | MUST | rev2 | pass |  |
| RFC5466-3.1-3 — Client MUST NOT combine FILTER with an explicit CHARSET other than UTF-8/US-ASCII | MUST NOT | rev1 | fail | unimplemented: Note that use of the FILTER search key implies the CHARSET "UTF-8" parameter to the SEARCH/UID SEARCH command. If the SEARCH/UID SEARCH command includes the exp… |
| RFC5466-3.1-3 — Client MUST NOT combine FILTER with an explicit CHARSET other than UTF-8/US-ASCII | MUST NOT | rev2 | fail | unimplemented: Note that use of the FILTER search key implies the CHARSET "UTF-8" parameter to the SEARCH/UID SEARCH command. If the SEARCH/UID SEARCH command includes the exp… |
| RFC5466-3.2-1 — Client MUST encode stored filter search-key values in UTF-8 | MUST | rev1 | fail | unimplemented: Note that values of all search keys stored in these entries MUST be encoded in UTF-8. |
| RFC5466-3.2-1 — Client MUST encode stored filter search-key values in UTF-8 | MUST | rev2 | fail | unimplemented: Note that values of all search keys stored in these entries MUST be encoded in UTF-8. |
| RFC5466-3.2-2 — Client defines/modifies a filter via SETMETADATA on the reserved values entries | MUST | rev1 | fail | unimplemented: A new filter named "<filter_name>" can be created (or an existing filter can be modified) by storing a non-NIL value in the "/private/filters/values/<filter_nam… |
| RFC5466-3.2-2 — Client defines/modifies a filter via SETMETADATA on the reserved values entries | MUST | rev2 | fail | unimplemented: A new filter named "<filter_name>" can be created (or an existing filter can be modified) by storing a non-NIL value in the "/private/filters/values/<filter_nam… |
| RFC5466-3.2-3 — Client deleting a filter completely NILs both the private and shared values entries | MAY | rev1 | untestable | The binding residue beyond RFC 5464's generic NIL-to-remove mechanics (RFC5464-4.3-2, already cataloged) is the BOTH-entries requirement: because the FILTER search key falls back from the private to the shared typed filter, only NILing both entries makes the name stop resolving. But whether a given NIL store was intended as a complete filter deletion (both entries required) or as removing only one typed filter while deliberately keeping the other (a single NIL, fully legal) is user/application intent, invisible on the wire — a client that NILs only /private/filters/values/<name> produces a trace that is compliant under one intent and an incomplete deletion under the other, and no black-box observation can distinguish them. The wire shows what was stored, never why. |
| RFC5466-3.2-3 — Client deleting a filter completely NILs both the private and shared values entries | MAY | rev2 | untestable | The binding residue beyond RFC 5464's generic NIL-to-remove mechanics (RFC5464-4.3-2, already cataloged) is the BOTH-entries requirement: because the FILTER search key falls back from the private to the shared typed filter, only NILing both entries makes the name stop resolving. But whether a given NIL store was intended as a complete filter deletion (both entries required) or as removing only one typed filter while deliberately keeping the other (a single NIL, fully legal) is user/application intent, invisible on the wire — a client that NILs only /private/filters/values/<name> produces a trace that is compliant under one intent and an incomplete deletion under the other, and no black-box observation can distinguish them. The wire shows what was stored, never why. |
| RFC5466-3.2-4 — Client storing a filter value SHOULD parenthesize non-RFC3501 SEARCH keys | SHOULD | rev1 | untestable | The parenthesization discipline governs the semantic composition of the filter VALUE — a search-criteria string that, at this library's boundary, is supplied verbatim by the consuming application (the metadata surface takes an opaque value; the library composes no SEARCH criteria for storage and has no registry of which SEARCH keys are RFC 3501-defined vs extension-defined). A value with or without () around an extension key is an equally legal METADATA value on the wire, so a black-box observer cannot attribute the presence or absence of parentheses to the client's own compliance rather than to the harness-supplied content. Same boundary-delegation analysis as RFC5464-3.2-2 (CRLF inside annotation values). |
| RFC5466-3.2-4 — Client storing a filter value SHOULD parenthesize non-RFC3501 SEARCH keys | SHOULD | rev2 | untestable | The parenthesization discipline governs the semantic composition of the filter VALUE — a search-criteria string that, at this library's boundary, is supplied verbatim by the consuming application (the metadata surface takes an opaque value; the library composes no SEARCH criteria for storage and has no registry of which SEARCH keys are RFC 3501-defined vs extension-defined). A value with or without () around an extension key is an equally legal METADATA value on the wire, so a black-box observer cannot attribute the presence or absence of parentheses to the client's own compliance rather than to the harness-supplied content. Same boundary-delegation analysis as RFC5464-3.2-2 (CRLF inside annotation values). |
| RFC5466-3.2-5 — Client uses the private description in preference to the shared one | MUST | rev1 | untestable | Unlike the §1 value-precedence rule (which binds the server evaluating FILTER), this precedence rule binds the CLIENT's selection of which fetched description value to treat as THE description. Both annotation values arrive over the wire in ordinary GETMETADATA responses; the choice between them happens after the protocol exchange, inside whatever presents filters to a user. This headless library has no filter-description feature — it hands METADATA values to the consuming application unranked — and even for a client that does present descriptions, a compliant and a non-compliant selection produce identical wire traces (nothing further is sent based on the choice). No black-box observation at this library's boundary can reveal which value was 'used as the filter description'. |
| RFC5466-3.2-5 — Client uses the private description in preference to the shared one | MUST | rev2 | untestable | Unlike the §1 value-precedence rule (which binds the server evaluating FILTER), this precedence rule binds the CLIENT's selection of which fetched description value to treat as THE description. Both annotation values arrive over the wire in ordinary GETMETADATA responses; the choice between them happens after the protocol exchange, inside whatever presents filters to a user. This headless library has no filter-description feature — it hands METADATA values to the consuming application unranked — and even for a client that does present descriptions, a compliant and a non-compliant selection produce identical wire traces (nothing further is sent based on the choice). No black-box observation at this library's boundary can reveal which value was 'used as the filter description'. |
| RFC5466-3.2-6 — Client MAY display the filter name as its description when both entries are absent | MAY | rev1 | untestable | Pure display permission with no constraining envelope: displaying the filter name and displaying nothing (or anything else) are both compliant, so no observation has a pass/fail boundary even with the logger-capture mechanism (checked per the standing instruction before tagging ui-presentation) — vacuous by requirement level, exactly like the MAY-level OK-text entries RFC3501-7.1.1-2/RFC9051-7.1.1-2. The RFC itself notes why display is at issue: 'Note that filter names are restricted to a subset of US-ASCII, as described in Section 4. So they might not always be meaningful to users and thus not necessarily suitable for display purposes.' |
| RFC5466-3.2-6 — Client MAY display the filter name as its description when both entries are absent | MAY | rev2 | untestable | Pure display permission with no constraining envelope: displaying the filter name and displaying nothing (or anything else) are both compliant, so no observation has a pass/fail boundary even with the logger-capture mechanism (checked per the standing instruction before tagging ui-presentation) — vacuous by requirement level, exactly like the MAY-level OK-text entries RFC3501-7.1.1-2/RFC9051-7.1.1-2. The RFC itself notes why display is at issue: 'Note that filter names are restricted to a subset of US-ASCII, as described in Section 4. So they might not always be meaningful to users and thus not necessarily suitable for display purposes.' |
| RFC5466-3.2-7 — Client-stored description SHOULD be annotated with language tags | SHOULD | rev1 | untestable | The duty governs the semantic composition of the description VALUE (embedding Unicode plane-14 tag characters around human-readable text). At this library's boundary the description string is consumer-supplied opaque value content — a tagged and an untagged description are equally legal METADATA values on the wire, so a black-box observer cannot attribute the presence or absence of language-tag characters to the client's own annotation discipline rather than to the harness-supplied payload. Same boundary-delegation analysis as RFC5466-3.2-4 and RFC5464-3.2-2. |
| RFC5466-3.2-7 — Client-stored description SHOULD be annotated with language tags | SHOULD | rev2 | untestable | The duty governs the semantic composition of the description VALUE (embedding Unicode plane-14 tag characters around human-readable text). At this library's boundary the description string is consumer-supplied opaque value content — a tagged and an untagged description are equally legal METADATA values on the wire, so a black-box observer cannot attribute the presence or absence of language-tag characters to the client's own annotation discipline rather than to the harness-supplied payload. Same boundary-delegation analysis as RFC5466-3.2-4 and RFC5464-3.2-2. |
| RFC5466-3.2-8 — Client SHOULD assume i-default for an untagged description | SHOULD | rev1 | untestable | Reader-side counterpart of RFC5466-3.2-7: interpreting an untagged description value as i-default-language content is post-protocol processing of received value data. The library hands METADATA values onward across its public API and legitimately delegates language interpretation to the consuming application; the 'assumption' produces no wire artifact whatsoever (nothing is sent based on it), so no black-box observation at this boundary can distinguish a client that assumed i-default from one that assumed anything else. Same library-boundary delegation as the other content-processing members. |
| RFC5466-3.2-8 — Client SHOULD assume i-default for an untagged description | SHOULD | rev2 | untestable | Reader-side counterpart of RFC5466-3.2-7: interpreting an untagged description value as i-default-language content is post-protocol processing of received value data. The library hands METADATA values onward across its public API and legitimately delegates language interpretation to the consuming application; the 'assumption' produces no wire artifact whatsoever (nothing is sent based on it), so no black-box observation at this boundary can distinguish a client that assumed i-default from one that assumed anything else. Same library-boundary delegation as the other content-processing members. |
| RFC5466-4-1 — Client-emitted filter-name MUST conform to the filter-name grammar | MUST | rev1 | fail | unimplemented: filter-name = 1*<any ATOM-CHAR except "/"> ;; Note that filter-name disallows UTF-8 or ;; the following characters: "(", ")", "{", ;; " ", "%", "*", "]". See de… |
| RFC5466-4-1 — Client-emitted filter-name MUST conform to the filter-name grammar | MUST | rev2 | fail | unimplemented: filter-name = 1*<any ATOM-CHAR except "/"> ;; Note that filter-name disallows UTF-8 or ;; the following characters: "(", ")", "{", ;; " ", "%", "*", "]". See de… |
| RFC5466-5-1 — Client MUST use TLS/SASL protection when filter confidentiality matters | MUST | rev1 | untestable | The MUST is gated on 'When confidentiality of such information is important' — an importance judgment belonging to the user/deployment, invisible on the wire. A plaintext session storing filters is fully compliant under the confidentiality-not-important condition and non-compliant under the other, and no black-box observation can determine which condition held; additionally, at this library's boundary the choice of TLS (secure: true) belongs to the consuming application's configuration, not to the library's own behavior. The wire shows what protection was used, never whether confidentiality was 'important'. |
| RFC5466-5-1 — Client MUST use TLS/SASL protection when filter confidentiality matters | MUST | rev2 | untestable | The MUST is gated on 'When confidentiality of such information is important' — an importance judgment belonging to the user/deployment, invisible on the wire. A plaintext session storing filters is fully compliant under the confidentiality-not-important condition and non-compliant under the other, and no black-box observation can determine which condition held; additionally, at this library's boundary the choice of TLS (secure: true) belongs to the consuming application's configuration, not to the library's own behavior. The wire shows what protection was used, never whether confidentiality was 'important'. |
| RFC5466-5-2 — Client should use private rather than public filters unless sharing is desired | SHOULD | rev1 | untestable | The advisory is gated on 'unless they desire to share such information with other users' — the desire to share is user intent, invisible on the wire. Storing to /shared/filters/values/<name> is fully compliant whenever sharing was desired, so a trace showing a shared-entry store cannot be classified as a violation without knowing the intent behind it; conversely a private-entry store is compliant under every intent. No black-box observation has a pass/fail boundary. The choice of private vs shared additionally originates with the consuming application at this library's boundary (the entry name is caller-supplied). |
| RFC5466-5-2 — Client should use private rather than public filters unless sharing is desired | SHOULD | rev2 | untestable | The advisory is gated on 'unless they desire to share such information with other users' — the desire to share is user intent, invisible on the wire. Storing to /shared/filters/values/<name> is fully compliant whenever sharing was desired, so a trace showing a shared-entry store cannot be classified as a violation without knowing the intent behind it; conversely a private-entry store is compliant under every intent. No black-box observation has a pass/fail boundary. The choice of private vs shared additionally originates with the consuming application at this library's boundary (the entry name is caller-supplied). |
| RFC5032-1-1 — OLDER/YOUNGER search-key form: each takes one non-zero integer argument in seconds | MUST | rev1 | fail | unimplemented: This extension exposes two new search keys, OLDER and YOUNGER, each of which takes a non-zero integer argument corresponding to a time interval in seconds. |
| RFC5032-1-1 — OLDER/YOUNGER search-key form: each takes one non-zero integer argument in seconds | MUST | rev2 | fail | unimplemented: This extension exposes two new search keys, OLDER and YOUNGER, each of which takes a non-zero integer argument corresponding to a time interval in seconds. |
| RFC5032-2-1 — Client (implicit) MUST NOT use OLDER/YOUNGER unless the server advertises WITHIN | MUST NOT | rev1 | fail | unimplemented: An IMAP4 server that supports the capability described here MUST return "WITHIN" as one of the server supported capabilities in the CAPABILITY command. |
| RFC5032-2-1 — Client (implicit) MUST NOT use OLDER/YOUNGER unless the server advertises WITHIN | MUST NOT | rev2 | fail | unimplemented: An IMAP4 server that supports the capability described here MUST return "WITHIN" as one of the server supported capabilities in the CAPABILITY command. |
| RFC5032-2-2 — Client MUST be aware that dynamic WITHIN results may not reflect current mailbox state | MUST | rev1 | untestable | 'MUST be aware' binds the client's assumptions about the currency of returned data, not any discrete wire action: a client that (wrongly) treats a periodically re-evaluated OLDER/YOUNGER result as an exact snapshot of the mailbox emits protocol traffic byte-identical to one that correctly regards it as possibly stale. This is a do-not-assume duty with no wire signature — the defining property of internal-decision — and its actionable flip side (re-issuing the search when currency matters) is separately cataloged as RFC5032-2-3. |
| RFC5032-2-2 — Client MUST be aware that dynamic WITHIN results may not reflect current mailbox state | MUST | rev2 | untestable | 'MUST be aware' binds the client's assumptions about the currency of returned data, not any discrete wire action: a client that (wrongly) treats a periodically re-evaluated OLDER/YOUNGER result as an exact snapshot of the mailbox emits protocol traffic byte-identical to one that correctly regards it as possibly stale. This is a do-not-assume duty with no wire signature — the defining property of internal-decision — and its actionable flip side (re-issuing the search when currency matters) is separately cataloged as RFC5032-2-3. |
| RFC5032-2-3 — Client needing current-state results is RECOMMENDED to issue a new search | SHOULD | rev1 | untestable | The duty is conditioned entirely on the client's purpose — whether it 'needs a search result that reflects the current state of the mailbox' is invisible on the wire. The prescribed action (issuing a fresh SEARCH) is itself observable, but with no way to observe the need there is no pass/fail boundary: a client that never re-issues may simply never need currency, and one that re-issues constantly is equally compliant. The wire shows what the client sent, never why. |
| RFC5032-2-3 — Client needing current-state results is RECOMMENDED to issue a new search | SHOULD | rev2 | untestable | The duty is conditioned entirely on the client's purpose — whether it 'needs a search result that reflects the current state of the mailbox' is invisible on the wire. The prescribed action (issuing a fresh SEARCH) is itself observable, but with no way to observe the need there is no pass/fail boundary: a client that never re-issues may simply never need currency, and one that re-issues constantly is equally compliant. The wire shows what the client sent, never why. |
| RFC5255-3.1-1 — Client and server supporting LANGUAGE MUST also support NAMESPACE | MUST | rev1 | fail | unimplemented: Clients and servers that support this extension MUST also support the NAMESPACE extension [RFC2342]. |
| RFC5255-3.1-1 — Client and server supporting LANGUAGE MUST also support NAMESPACE | MUST | rev2 | fail | unimplemented: Clients and servers that support this extension MUST also support the NAMESPACE extension [RFC2342]. |
| RFC5255-3.1-2 — Client SHOULD issue LANGUAGE before authentication | SHOULD | rev1 | fail | unimplemented: The LANGUAGE command is valid in all states. Clients SHOULD issue LANGUAGE before authentication, since some servers send valuable user information as part of a… |
| RFC5255-3.1-2 — Client SHOULD issue LANGUAGE before authentication | SHOULD | rev2 | fail | unimplemented: The LANGUAGE command is valid in all states. Clients SHOULD issue LANGUAGE before authentication, since some servers send valuable user information as part of a… |
| RFC5255-3.1-3 — Client MUST re-issue LANGUAGE after a security layer is subsequently negotiated | MUST | rev1 | fail | unimplemented: If a security layer (such as SASL or TLS) is subsequently negotiated by the client, it MUST re-issue the LANGUAGE command in order to make sure that no previous… |
| RFC5255-3.1-3 — Client MUST re-issue LANGUAGE after a security layer is subsequently negotiated | MUST | rev2 | fail | unimplemented: If a security layer (such as SASL or TLS) is subsequently negotiated by the client, it MUST re-issue the LANGUAGE command in order to make sure that no previous… |
| RFC5255-3.2-1 — LANGUAGE command requests localization to a language matching an RFC 4647 language range | MUST | rev1 | fail | unimplemented: The LANGUAGE command requests that human-readable text emitted by the server be localized to a language matching one of the language range argument as described… |
| RFC5255-3.2-1 — LANGUAGE command requests localization to a language matching an RFC 4647 language range | MUST | rev2 | fail | unimplemented: The LANGUAGE command requests that human-readable text emitted by the server be localized to a language matching one of the language range argument as described… |
| RFC5255-3.2-2 — Client MUST accept the LANGUAGE response and treat the new language as effective immediately after it | MUST | rev1 | fail | unimplemented: The server MUST send a LANGUAGE response specifying the language used, and the change takes effect immediately after the LANGUAGE response. |
| RFC5255-3.2-2 — Client MUST accept the LANGUAGE response and treat the new language as effective immediately after it | MUST | rev2 | fail | unimplemented: The server MUST send a LANGUAGE response specifying the language used, and the change takes effect immediately after the LANGUAGE response. |
| RFC5255-3.2-3 — "default" is a special language-range argument requesting the server administrator's preferred language | MUST | rev1 | fail | unimplemented: The special "default" language range argument indicates a request to use a language designated as preferred by the server administrator. |
| RFC5255-3.2-3 — "default" is a special language-range argument requesting the server administrator's preferred language | MUST | rev2 | fail | unimplemented: The special "default" language range argument indicates a request to use a language designated as preferred by the server administrator. |
| RFC5255-3.3-1 — Client MUST treat a single-tag LANGUAGE response as an active-language change | MUST | rev1 | pass |  |
| RFC5255-3.3-1 — Client MUST treat a single-tag LANGUAGE response as an active-language change | MUST | rev2 | pass |  |
| RFC5255-3.3-2 — Client MUST treat a multi-tag LANGUAGE response as an enumeration with no active-language change | MUST | rev1 | pass |  |
| RFC5255-3.3-2 — Client MUST treat a multi-tag LANGUAGE response as an enumeration with no active-language change | MUST | rev2 | pass |  |
| RFC5255-3.4-1 — Client is responsible for converting between a namespace prefix and its TRANSLATION when presenting to the user | MUST | rev1 | untestable | This duty governs what the client DISPLAYS to a human user (using the localized TRANSLATION string in place of, or alongside, the raw modified-UTF-7 namespace prefix when rendering mailbox names in a UI), not any wire-observable protocol action. A client that dutifully substitutes the TRANSLATION string in its mailbox list UI and one that ignores it and shows only the raw prefix both issue identical LANGUAGE/NAMESPACE commands and consume an identical '* NAMESPACE (...) (...TRANSLATION (...)) (...)' response; the divergence is entirely in client-side rendering that a black-box protocol harness (which has no UI to inspect) cannot observe. This mirrors the established ui-presentation theme (e.g. RFC3501-7.1-1 ALERT-text display). |
| RFC5255-3.4-1 — Client is responsible for converting between a namespace prefix and its TRANSLATION when presenting to the user | MUST | rev2 | untestable | This duty governs what the client DISPLAYS to a human user (using the localized TRANSLATION string in place of, or alongside, the raw modified-UTF-7 namespace prefix when rendering mailbox names in a UI), not any wire-observable protocol action. A client that dutifully substitutes the TRANSLATION string in its mailbox list UI and one that ignores it and shows only the raw prefix both issue identical LANGUAGE/NAMESPACE commands and consume an identical '* NAMESPACE (...) (...TRANSLATION (...)) (...)' response; the divergence is entirely in client-side rendering that a black-box protocol harness (which has no UI to inspect) cannot observe. This mirrors the established ui-presentation theme (e.g. RFC3501-7.1-1 ALERT-text display). |
| RFC5255-4.2-1 — Active comparator (I18NLEVEL=1/2) applies to the BCC/BODY/CC/FROM/SUBJECT/TEXT/TO/HEADER SEARCH keys | MUST | rev1 | fail | unimplemented: The active comparator applies to the following SEARCH keys: "BCC", "BODY", "CC", "FROM", "SUBJECT", "TEXT", "TO", and "HEADER". |
| RFC5255-4.2-1 — Active comparator (I18NLEVEL=1/2) applies to the BCC/BODY/CC/FROM/SUBJECT/TEXT/TO/HEADER SEARCH keys | MUST | rev2 | fail | unimplemented: The active comparator applies to the following SEARCH keys: "BCC", "BODY", "CC", "FROM", "SUBJECT", "TEXT", "TO", and "HEADER". |
| RFC5255-4.2-2 — Active comparator (I18NLEVEL=1/2) applies to the CC/FROM/SUBJECT/TO SORT keys when SORT is advertised | MUST | rev1 | fail | unimplemented: If the server also advertises the "SORT" extension, then the active comparator applies to the following SORT keys: "CC", "FROM", "SUBJECT", and "TO". |
| RFC5255-4.2-2 — Active comparator (I18NLEVEL=1/2) applies to the CC/FROM/SUBJECT/TO SORT keys when SORT is advertised | MUST | rev2 | fail | unimplemented: If the server also advertises the "SORT" extension, then the active comparator applies to the following SORT keys: "CC", "FROM", "SUBJECT", and "TO". |
| RFC5255-4.2-3 — Active comparator (I18NLEVEL=1/2) applies to the ORDEREDSUBJECT threading algorithm when advertised | MUST | rev1 | fail | unimplemented: If the server advertises THREAD=ORDEREDSUBJECT, then the active comparator applies to the ORDEREDSUBJECT threading algorithm. |
| RFC5255-4.2-3 — Active comparator (I18NLEVEL=1/2) applies to the ORDEREDSUBJECT threading algorithm when advertised | MUST | rev2 | fail | unimplemented: If the server advertises THREAD=ORDEREDSUBJECT, then the active comparator applies to the ORDEREDSUBJECT threading algorithm. |
| RFC5255-4.2-4 — Active comparator (I18NLEVEL=1/2) applies to REFERENCES threading's subject-field comparisons when advertised | MUST | rev1 | fail | unimplemented: If the server advertises THREAD=REFERENCES, then the active comparator applies to the subject field comparisons done by REFERENCES threading algorithm. |
| RFC5255-4.2-4 — Active comparator (I18NLEVEL=1/2) applies to REFERENCES threading's subject-field comparisons when advertised | MUST | rev2 | fail | unimplemented: If the server advertises THREAD=REFERENCES, then the active comparator applies to the subject field comparisons done by REFERENCES threading algorithm. |
| RFC5255-4.7-1 — COMPARATOR command is valid only in authenticated and selected states | MUST | rev1 | pass |  |
| RFC5255-4.7-1 — COMPARATOR command is valid only in authenticated and selected states | MUST | rev2 | pass |  |
| RFC5255-4.7-2 — COMPARATOR with no arguments queries the active comparator; with arguments it changes it | MUST | rev1 | pass |  |
| RFC5255-4.7-2 — COMPARATOR with no arguments queries the active comparator; with arguments it changes it | MUST | rev2 | pass |  |
| RFC5255-4.7-3 — First-match-wins when a COMPARATOR argument matches more than one installed comparator | MUST | rev1 | pass |  |
| RFC5255-4.7-3 — First-match-wins when a COMPARATOR argument matches more than one installed comparator | MUST | rev2 | pass |  |
| RFC5255-4.7-4 — "default" argument refers to the server's default comparator; other arguments are RFC 4790 collation specifications | MUST | rev1 | pass |  |
| RFC5255-4.7-4 — "default" argument refers to the server's default comparator; other arguments are RFC 4790 collation specifications | MUST | rev2 | pass |  |
| RFC5255-4.8-1 — Client MUST accept the COMPARATOR response as a result of a COMPARATOR command | MUST | rev1 | pass |  |
| RFC5255-4.8-1 — Client MUST accept the COMPARATOR response as a result of a COMPARATOR command | MUST | rev2 | pass |  |
| RFC5255-4.8-2 — Client MUST parse the first COMPARATOR-response argument as the active comparator's name | MUST | rev1 | pass |  |
| RFC5255-4.8-2 — Client MUST parse the first COMPARATOR-response argument as the active comparator's name | MUST | rev2 | pass |  |
| RFC5255-4.8-3 — Client MUST accept an optional second COMPARATOR-response argument listing matched comparators | MUST | rev1 | pass |  |
| RFC5255-4.8-3 — Client MUST accept an optional second COMPARATOR-response argument listing matched comparators | MUST | rev2 | pass |  |
| RFC5255-4.9-1 — Client MUST accept the [BADCOMPARATOR] response code on a failed COMPARATOR command | SHOULD | rev1 | pass |  |
| RFC5255-4.9-1 — Client MUST accept the [BADCOMPARATOR] response code on a failed COMPARATOR command | SHOULD | rev2 | pass |  |
| RFC5259-3.1-1 — Client MUST NOT issue CONVERT without the CONVERT capability | MUST | rev1 | fail | unimplemented: A server that supports the CONVERT extension MUST return "CONVERT" and "BINARY" in the CAPABILITY response or response code. |
| RFC5259-3.1-1 — Client MUST NOT issue CONVERT without the CONVERT capability | MUST | rev2 | fail | unimplemented: A server that supports the CONVERT extension MUST return "CONVERT" and "BINARY" in the CAPABILITY response or response code. |
| RFC5259-5.1-1 — CONVERSIONS command form: source and target MIME type, wildcardable | MUST | rev1 | fail | unimplemented: The first parameter to the CONVERSIONS command is a source MIME type, the second parameter is the target MIME type. Both parameters are partially (e.g., "text/*… |
| RFC5259-5.1-1 — CONVERSIONS command form: source and target MIME type, wildcardable | MUST | rev2 | fail | unimplemented: The first parameter to the CONVERSIONS command is a source MIME type, the second parameter is the target MIME type. Both parameters are partially (e.g., "text/*… |
| RFC5259-5.1-2 — Client MUST accept the untagged CONVERSION response (zero-or-more) to CONVERSIONS | MUST | rev1 | pass |  |
| RFC5259-5.1-2 — Client MUST accept the untagged CONVERSION response (zero-or-more) to CONVERSIONS | MUST | rev2 | pass |  |
| RFC5259-6-1 — A single CONVERT/UID CONVERT command performs only one type of conversion | MAY | rev1 | fail | unimplemented: Note that a single CONVERT/ UID CONVERT command can only perform a single type of conversion as defined by the conversion parameters. A client that needs to per… |
| RFC5259-6-1 — A single CONVERT/UID CONVERT command performs only one type of conversion | MAY | rev2 | fail | unimplemented: Note that a single CONVERT/ UID CONVERT command can only perform a single type of conversion as defined by the conversion parameters. A client that needs to per… |
| RFC5259-6-2 — Client MAY use NIL as a "default conversion" marker instead of a specific target MIME type | MAY | rev1 | fail | unimplemented: Instead of specifying the exact target MIME media type the client wants to convert to, the client MAY use a special marker NIL (also known as "default conversio… |
| RFC5259-6-2 — Client MAY use NIL as a "default conversion" marker instead of a specific target MIME type | MAY | rev2 | fail | unimplemented: Instead of specifying the exact target MIME media type the client wants to convert to, the client MAY use a special marker NIL (also known as "default conversio… |
| RFC5259-6-3 — Clients SHOULD avoid default conversion unless they signal capabilities to the server | SHOULD | rev1 | untestable | Whether a client 'provided a way (in-band or out-band) to signal capabilities' is itself an open-ended, out-of-band fact (device-characteristics signaling can happen entirely outside the IMAP session, e.g. via a separate provisioning channel — 'out-of-scope for CONVERT' per §1). A black-box observer sees only whether a given CONVERT used NIL or a concrete MIME type; it cannot determine whether the client's choice to use (or avoid) NIL was preceded by adequate capability signaling through some unobservable side channel, so a matcher cannot distinguish a compliant client that signaled capabilities before using default conversion from a non-compliant one that guessed. This is a client policy/judgment call about when to trust server guessing, not a wire-observable protocol action. |
| RFC5259-6-3 — Clients SHOULD avoid default conversion unless they signal capabilities to the server | SHOULD | rev2 | untestable | Whether a client 'provided a way (in-band or out-band) to signal capabilities' is itself an open-ended, out-of-band fact (device-characteristics signaling can happen entirely outside the IMAP session, e.g. via a separate provisioning channel — 'out-of-scope for CONVERT' per §1). A black-box observer sees only whether a given CONVERT used NIL or a concrete MIME type; it cannot determine whether the client's choice to use (or avoid) NIL was preceded by adequate capability signaling through some unobservable side channel, so a matcher cannot distinguish a compliant client that signaled capabilities before using default conversion from a non-compliant one that guessed. This is a client policy/judgment call about when to trust server guessing, not a wire-observable protocol action. |
| RFC5259-6-4 — Client must issue a separate STORE to set \Seen, since CONVERT does not set it | MUST | rev1 | fail | unimplemented: Note that unlike the FETCH command, the CONVERT command never sets the \Seen flag on converted messages. A client wishing to mark a message with the \Seen flag … |
| RFC5259-6-4 — Client must issue a separate STORE to set \Seen, since CONVERT does not set it | MUST | rev2 | fail | unimplemented: Note that unlike the FETCH command, the CONVERT command never sets the \Seen flag on converted messages. A client wishing to mark a message with the \Seen flag … |
| RFC5259-6-5 — UID CONVERT takes a UID sequence set and MUST include the UID data item in its response | MUST | rev1 | fail | unimplemented: UID CONVERT takes as a parameter a sequence of UIDs instead of a sequence of message numbers. o UID CONVERT command MUST result in the UID data item in a corres… |
| RFC5259-6-5 — UID CONVERT takes a UID sequence set and MUST include the UID data item in its response | MUST | rev2 | fail | unimplemented: UID CONVERT takes as a parameter a sequence of UIDs instead of a sequence of message numbers. o UID CONVERT command MUST result in the UID data item in a corres… |
| RFC5259-6-6 — CHARSET parameter is REQUIRED for BODY[...HEADER]/BODY[...MIME] encoded-word conversions | MUST | rev1 | fail | unimplemented: BODY[...HEADER] encoded words in the requested headers are converted to the specified charset. The CHARSET parameter is REQUIRED for this conversion. |
| RFC5259-6-6 — CHARSET parameter is REQUIRED for BODY[...HEADER]/BODY[...MIME] encoded-word conversions | MUST | rev2 | fail | unimplemented: BODY[...HEADER] encoded words in the requested headers are converted to the specified charset. The CHARSET parameter is REQUIRED for this conversion. |
| RFC5259-6-7 — No destination MIME type may be specified with BODY[HEADER]/BODY[...HEADER]/BODY[...MIME] | MUST NOT | rev1 | fail | unimplemented: No destination MIME type MUST be specified with BODY[HEADER], BODY[section.HEADER], or BODY[section.MIME]. That is, BODY[HEADER], BODY[section.HEADER], or BODY[… |
| RFC5259-6-7 — No destination MIME type may be specified with BODY[HEADER]/BODY[...HEADER]/BODY[...MIME] | MUST NOT | rev2 | fail | unimplemented: No destination MIME type MUST be specified with BODY[HEADER], BODY[section.HEADER], or BODY[section.MIME]. That is, BODY[HEADER], BODY[section.HEADER], or BODY[… |
| RFC5259-6-8 — Clients MUST gracefully handle comments removed during header-parameter conversion | MUST | rev1 | untestable | 'Gracefully handle' describes the client's internal parsing/error-tolerance behavior when consuming a converted header whose RFC 2231 parameter comments were dropped by the server — there is no distinguishing wire artifact between a client that 'gracefully handles' the comment-free header and one that mishandles it (e.g. crashes or corrupts its internal model): both receive the identical BODY[...MIME]/[...HEADER] response bytes. Grace of handling is an internal robustness property observable only through the client's own downstream behavior (e.g. UI presentation, later requests unaffected), not through any CONVERT protocol exchange a black-box harness can script. |
| RFC5259-6-8 — Clients MUST gracefully handle comments removed during header-parameter conversion | MUST | rev2 | untestable | 'Gracefully handle' describes the client's internal parsing/error-tolerance behavior when consuming a converted header whose RFC 2231 parameter comments were dropped by the server — there is no distinguishing wire artifact between a client that 'gracefully handles' the comment-free header and one that mishandles it (e.g. crashes or corrupts its internal model): both receive the identical BODY[...MIME]/[...HEADER] response bytes. Grace of handling is an internal robustness property observable only through the client's own downstream behavior (e.g. UI presentation, later requests unaffected), not through any CONVERT protocol exchange a black-box harness can script. |
| RFC5259-7-1 — Conversion parameter names are case-insensitive | MUST | rev1 | fail | unimplemented: According to [MEDIAFEAT-REG], conversion parameter names are case- insensitive. |
| RFC5259-7-1 — Conversion parameter names are case-insensitive | MUST | rev2 | fail | unimplemented: According to [MEDIAFEAT-REG], conversion parameter names are case- insensitive. |
| RFC5259-8.1-1 — Client MUST accept the untagged CONVERTED response (successful, partial, or failed) | MUST | rev1 | pass |  |
| RFC5259-8.1-1 — Client MUST accept the untagged CONVERTED response (successful, partial, or failed) | MUST | rev2 | pass |  |
| RFC5259-8.1-2 — Client uses the convert correlator TAG to match a CONVERTED response to its command | MUST | rev1 | fail | unimplemented: The label is followed by a convert correlator, which contains the tag of the command that caused the response to be returned. This can be used by a client to ma… |
| RFC5259-8.1-2 — Client uses the convert correlator TAG to match a CONVERTED response to its command | MUST | rev2 | fail | unimplemented: The label is followed by a convert correlator, which contains the tag of the command that caused the response to be returned. This can be used by a client to ma… |
| RFC5259-8.2-1 — Client can expect the returned MIME type to match the one requested and can treat a mismatch as an error | MUST | rev1 | fail | unimplemented: Note that the client can expect the returned MIME type to match the one it requested (as the server is required to obey the requested MIME type) and can treat m… |
| RFC5259-8.2-1 — Client can expect the returned MIME type to match the one requested and can treat a mismatch as an error | MUST | rev2 | fail | unimplemented: Note that the client can expect the returned MIME type to match the one it requested (as the server is required to obey the requested MIME type) and can treat m… |
| RFC5259-8.3-1 — Client MUST NOT assume repeated conversions across sessions yield identical results | MUST NOT | rev1 | untestable | 'MUST NOT assume' governs the client's internal belief/expectation about cross-session result stability, not an action it performs on the wire. A client that (wrongly) assumes stability and one that (rightly) treats each session's conversion as independent both simply issue a fresh BINARY/BINARY.SIZE/BODYPARTSTRUCTURE request in the new session and receive whatever the server currently returns; the forbidden assumption would only matter if the client tried to skip re-requesting and reuse a prior result, which is exactly the caching prohibition captured separately and testably in RFC5259-8.3-2. This entry's own text targets the belief itself, which has no independent wire signature beyond that caching duty. |
| RFC5259-8.3-1 — Client MUST NOT assume repeated conversions across sessions yield identical results | MUST NOT | rev2 | untestable | 'MUST NOT assume' governs the client's internal belief/expectation about cross-session result stability, not an action it performs on the wire. A client that (wrongly) assumes stability and one that (rightly) treats each session's conversion as independent both simply issue a fresh BINARY/BINARY.SIZE/BODYPARTSTRUCTURE request in the new session and receive whatever the server currently returns; the forbidden assumption would only matter if the client tried to skip re-requesting and reuse a prior result, which is exactly the caching prohibition captured separately and testably in RFC5259-8.3-2. This entry's own text targets the belief itself, which has no independent wire signature beyond that caching duty. |
| RFC5259-8.3-2 — Client MUST NOT cache converted sizes across sessions or reuse them on another connection | MUST NOT | rev1 | fail | unimplemented: In particular, clients MUST NOT cache sizes of converted messages/ body parts beyond duration of any IMAP "session", or use sizes obtained in one connection in … |
| RFC5259-8.3-2 — Client MUST NOT cache converted sizes across sessions or reuse them on another connection | MUST NOT | rev2 | fail | unimplemented: In particular, clients MUST NOT cache sizes of converted messages/ body parts beyond duration of any IMAP "session", or use sizes obtained in one connection in … |
| RFC5259-8.4-1 — Client MUST accept the AVAILABLECONVERSIONS response item (target-MIME-type list or ERROR) | MUST | rev1 | fail | unimplemented: AVAILABLECONVERSIONS[section-part] allows the client to request the list of target MIME types the specified body part of a message or the whole message can be c… |
| RFC5259-8.4-1 — Client MUST accept the AVAILABLECONVERSIONS response item (target-MIME-type list or ERROR) | MUST | rev2 | fail | unimplemented: AVAILABLECONVERSIONS[section-part] allows the client to request the list of target MIME types the specified body part of a message or the whole message can be c… |
| RFC5259-9-1 — Client MUST parse the TEMPFAIL tagged-NO response code and MAY retry | MAY | rev1 | pass |  |
| RFC5259-9-1 — Client MUST parse the TEMPFAIL tagged-NO response code and MAY retry | MAY | rev2 | pass |  |
| RFC5259-9-2 — Client MUST parse the MAXCONVERTMESSAGES <number> tagged-NO response code | MUST | rev1 | pass |  |
| RFC5259-9-2 — Client MUST parse the MAXCONVERTMESSAGES <number> tagged-NO response code | MUST | rev2 | pass |  |
| RFC5259-9-3 — Client MUST parse the MAXCONVERTPARTS <number> tagged-NO response code | MUST | rev1 | pass |  |
| RFC5259-9-3 — Client MUST parse the MAXCONVERTPARTS <number> tagged-NO response code | MUST | rev2 | pass |  |
| RFC5259-9-4 — Client MUST parse the ERROR phrase and its convert-error-code (TEMPFAIL/BADPARAMETERS/MISSINGPARAMETERS) | MUST | rev1 | fail | unimplemented: The word ERROR is always followed by an informal human-readable descriptive text, which is followed by the convert-error-code. The convert-error-code MUST be on… |
| RFC5259-9-4 — Client MUST parse the ERROR phrase and its convert-error-code (TEMPFAIL/BADPARAMETERS/MISSINGPARAMETERS) | MUST | rev2 | fail | unimplemented: The word ERROR is always followed by an informal human-readable descriptive text, which is followed by the convert-error-code. The convert-error-code MUST be on… |
| RFC5259-9-5 — BADPARAMETERS ERROR carries the concrete source/destination MIME types and rejected parameters | MUST | rev1 | fail | unimplemented: BADPARAMETERS from-concrete-mime-type to-mime-type "(" transcoding-params ")" - The listed parameters were not understood, not valid for the source/destination … |
| RFC5259-9-5 — BADPARAMETERS ERROR carries the concrete source/destination MIME types and rejected parameters | MUST | rev2 | fail | unimplemented: BADPARAMETERS from-concrete-mime-type to-mime-type "(" transcoding-params ")" - The listed parameters were not understood, not valid for the source/destination … |
| RFC5259-9-6 — MISSINGPARAMETERS ERROR carries the source/destination MIME types and required parameter names | MUST | rev1 | fail | unimplemented: MISSINGPARAMETERS from-concrete-mime-type to-mime-type "(" transcoding-params ")" - The listed parameters are required for conversion of the specified source MI… |
| RFC5259-9-6 — MISSINGPARAMETERS ERROR carries the source/destination MIME types and required parameter names | MUST | rev2 | fail | unimplemented: MISSINGPARAMETERS from-concrete-mime-type to-mime-type "(" transcoding-params ")" - The listed parameters are required for conversion of the specified source MI… |
| RFC5259-9-7 — Client MUST treat an OK completion as meaning at least one requested conversion succeeded | MUST | rev1 | fail | unimplemented: If at least one conversion succeeds, the server MUST return an OK response. If all conversions fail, the server MAY return OK or NO. |
| RFC5259-9-7 — Client MUST treat an OK completion as meaning at least one requested conversion succeeded | MUST | rev2 | fail | unimplemented: If at least one conversion succeeds, the server MUST return an OK response. If all conversions fail, the server MAY return OK or NO. |
| RFC5259-9-8 — Client SHOULD wait at least mm minutes before retrying after an ERROR TEMPFAIL mm | SHOULD | rev1 | fail | unimplemented: The client SHOULD wait for at least mm minutes before retrying. |
| RFC5259-9-8 — Client SHOULD wait at least mm minutes before retrying after an ERROR TEMPFAIL mm | SHOULD | rev2 | fail | unimplemented: The client SHOULD wait for at least mm minutes before retrying. |
| RFC5259-13-1 — Clients should be careful when requesting conversions or processing transformed attachments | SHOULD | rev1 | untestable | 'Should be careful' is an open-ended security posture, not a specific protocol action — it covers an unenumerated range of client-side precautions (e.g. sandboxing conversion of untrusted attachments, being wary of converted executables per the preceding threat-model sentence about 'converting a document to a damaging executable'). There is no wire-observable difference between a 'careful' client and a careless one issuing the identical CONVERT command and consuming the identical CONVERTED response; the duty is discharged (or not) entirely in the client's internal handling of the converted payload after receipt, which a black-box IMAP harness cannot observe. |
| RFC5259-13-1 — Clients should be careful when requesting conversions or processing transformed attachments | SHOULD | rev2 | untestable | 'Should be careful' is an open-ended security posture, not a specific protocol action — it covers an unenumerated range of client-side precautions (e.g. sandboxing conversion of untrusted attachments, being wary of converted executables per the preceding threat-model sentence about 'converting a document to a damaging executable'). There is no wire-observable difference between a 'careful' client and a careless one issuing the identical CONVERT command and consuming the identical CONVERTED response; the duty is discharged (or not) entirely in the client's internal handling of the converted payload after receipt, which a black-box IMAP harness cannot observe. |
| RFC5259-13-2 — Clients SHOULD use mutual SASL authentication and the SASL/TLS integrity layer to trust servers | SHOULD | rev1 | untestable | Whether a client established the connection using mutual SASL authentication plus a SASL/TLS integrity layer is a fact about connection-establishment and mechanism selection that belongs to the general SASL/TLS security posture of the client, not to the CONVERT extension's own wire exchange — it would be re-litigating the SASL mechanism and STARTTLS/TLS catalogs already covered elsewhere in this suite (RFC 4422 and the TLS-related catalogs), and CONVERT's own commands/responses look identical regardless of which authentication/security layer preceded them in the session. No CONVERT-specific wire artifact distinguishes a client that heeded this SHOULD from one that didn't. |
| RFC5259-13-2 — Clients SHOULD use mutual SASL authentication and the SASL/TLS integrity layer to trust servers | SHOULD | rev2 | untestable | Whether a client established the connection using mutual SASL authentication plus a SASL/TLS integrity layer is a fact about connection-establishment and mechanism selection that belongs to the general SASL/TLS security posture of the client, not to the CONVERT extension's own wire exchange — it would be re-litigating the SASL mechanism and STARTTLS/TLS catalogs already covered elsewhere in this suite (RFC 4422 and the TLS-related catalogs), and CONVERT's own commands/responses look identical regardless of which authentication/security layer preceded them in the session. No CONVERT-specific wire artifact distinguishes a client that heeded this SHOULD from one that didn't. |
| RFC4467-1-1 — Client MUST NOT use URLAUTH commands/URL construction without the URLAUTH capability | MUST | rev1 | pass |  |
| RFC4467-1-1 — Client MUST NOT use URLAUTH commands/URL construction without the URLAUTH capability | MUST | rev2 | pass |  |
| RFC4467-3-1 — Client selects one of four access-identifier forms when requesting a URLAUTH-authorized URL | MAY | rev1 | untestable | Which access identifier a client picks when it builds the ';URLAUTH=<access>' component to hand to GENURLAUTH — 'submit+<userid>' (this entry), 'user+<userid>' (RFC4467-3-3), 'authuser' (RFC4467-3-4), or 'anonymous' (RFC4467-3-5) — is a policy decision about the intended audience/redeemer of the resulting URL (message-submission-on-behalf-of, a specific user, any authenticated user, or anyone). All four are equally well-formed GENURLAUTH requests; a black-box observer sees only which literal string the client sent, not whether that choice matches the client's actual intended-use policy for the URL it is generating (e.g. a BURL-forward scenario legitimately wants 'submit+', while a public-export scenario legitimately wants 'anonymous') — there is no protocol-observable 'correct' choice to check against. |
| RFC4467-3-1 — Client selects one of four access-identifier forms when requesting a URLAUTH-authorized URL | MAY | rev2 | untestable | Which access identifier a client picks when it builds the ';URLAUTH=<access>' component to hand to GENURLAUTH — 'submit+<userid>' (this entry), 'user+<userid>' (RFC4467-3-3), 'authuser' (RFC4467-3-4), or 'anonymous' (RFC4467-3-5) — is a policy decision about the intended audience/redeemer of the resulting URL (message-submission-on-behalf-of, a specific user, any authenticated user, or anyone). All four are equally well-formed GENURLAUTH requests; a black-box observer sees only which literal string the client sent, not whether that choice matches the client's actual intended-use policy for the URL it is generating (e.g. a BURL-forward scenario legitimately wants 'submit+', while a public-export scenario legitimately wants 'anonymous') — there is no protocol-observable 'correct' choice to check against. |
| RFC4467-3-2 — Client MUST NOT construct a URLAUTH for a whole-server/mailbox-list/whole-mailbox/search-result URL | MUST NOT | rev1 | pass |  |
| RFC4467-3-2 — Client MUST NOT construct a URLAUTH for a whole-server/mailbox-list/whole-mailbox/search-result URL | MUST NOT | rev2 | pass |  |
| RFC4467-3-3 — Client constructs the user+<userid> access identifier verbatim when limiting a URL to one user | MAY | rev1 | fail | unimplemented: The "user+" access identifier prefix, followed by a userid, indicates that use of this URL is limited to IMAP sessions that are logged in as the specified useri… |
| RFC4467-3-3 — Client constructs the user+<userid> access identifier verbatim when limiting a URL to one user | MAY | rev2 | fail | unimplemented: The "user+" access identifier prefix, followed by a userid, indicates that use of this URL is limited to IMAP sessions that are logged in as the specified useri… |
| RFC4467-3-4 — Client constructs the authuser access identifier verbatim to permit any authorized user | MAY | rev1 | fail | unimplemented: The "authuser" access identifier indicates that use of this URL is limited to IMAP sessions that are logged in as an authorized user (that is, have authorizatio… |
| RFC4467-3-4 — Client constructs the authuser access identifier verbatim to permit any authorized user | MAY | rev2 | fail | unimplemented: The "authuser" access identifier indicates that use of this URL is limited to IMAP sessions that are logged in as an authorized user (that is, have authorizatio… |
| RFC4467-3-5 — Client constructs the anonymous access identifier verbatim to permit unrestricted use | MAY | rev1 | fail | unimplemented: The "anonymous" access identifier indicates that use of this URL is not restricted by session authorization identity; that is, any IMAP session in authenticated… |
| RFC4467-3-5 — Client constructs the anonymous access identifier verbatim to permit unrestricted use | MAY | rev2 | fail | unimplemented: The "anonymous" access identifier indicates that use of this URL is not restricted by session authorization identity; that is, any IMAP session in authenticated… |
| RFC4467-3-6 — Client's GENURLAUTH url-rump argument MUST include a valid access identifier | MUST | rev1 | fail | unimplemented: There is a valid access identifier that, in the case of "submit+" and "user+", will contain a valid userid. |
| RFC4467-3-6 — Client's GENURLAUTH url-rump argument MUST include a valid access identifier | MUST | rev2 | fail | unimplemented: There is a valid access identifier that, in the case of "submit+" and "user+", will contain a valid userid. |
| RFC4467-7-1 — RESETKEY command form: optional mailbox name, optional mechanism name(s) | MUST | rev1 | fail | unimplemented: Arguments: optional mailbox name optional mechanism name(s) |
| RFC4467-7-1 — RESETKEY command form: optional mailbox name, optional mechanism name(s) | MUST | rev2 | fail | unimplemented: Arguments: optional mailbox name optional mechanism name(s) |
| RFC4467-7-2 — GENURLAUTH command form: one or more URL/mechanism pairs | MUST | rev1 | fail | unimplemented: Argument: one or more URL/mechanism pairs |
| RFC4467-7-2 — GENURLAUTH command form: one or more URL/mechanism pairs | MUST | rev2 | fail | unimplemented: Argument: one or more URL/mechanism pairs |
| RFC4467-7-3 — URLFETCH command form: one or more URLs | MUST | rev1 | fail | unimplemented: Argument: one or more URLs |
| RFC4467-7-3 — URLFETCH command form: one or more URLs | MUST | rev2 | fail | unimplemented: Argument: one or more URLs |
| RFC4467-8-1 — Client MUST parse the URLMECH status response code on RESETKEY/SELECT/EXAMINE | MUST | rev1 | pass |  |
| RFC4467-8-1 — Client MUST parse the URLMECH status response code on RESETKEY/SELECT/EXAMINE | MUST | rev2 | pass |  |
| RFC4467-8-2 — Client MUST accept the untagged * GENURLAUTH response carrying the authorized URL(s) | MUST | rev1 | pass |  |
| RFC4467-8-2 — Client MUST accept the untagged * GENURLAUTH response carrying the authorized URL(s) | MUST | rev2 | pass |  |
| RFC4467-8-3 — Client MUST accept the untagged * URLFETCH response with an nstring body per URL | MUST | rev1 | pass |  |
| RFC4467-8-3 — Client MUST accept the untagged * URLFETCH response with an nstring body per URL | MUST | rev2 | pass |  |
| RFC4467-8-4 — Client MUST NOT require a selected mailbox to accept a URLFETCH exchange | MUST | rev1 | fail | unimplemented: Note: This command does not require that the URL refer to the selected mailbox; nor does it require that any mailbox be selected. It also does not in any way in… |
| RFC4467-8-4 — Client MUST NOT require a selected mailbox to accept a URLFETCH exchange | MUST | rev2 | fail | unimplemented: Note: This command does not require that the URL refer to the selected mailbox; nor does it require that any mailbox be selected. It also does not in any way in… |
| RFC4467-9-1 — RESETKEY ABNF: mailbox argument required before any mechanism argument(s) | MUST | rev1 | fail | unimplemented: resetkey = "RESETKEY" [SP mailbox *(SP mechanism)] |
| RFC4467-9-1 — RESETKEY ABNF: mailbox argument required before any mechanism argument(s) | MUST | rev2 | fail | unimplemented: resetkey = "RESETKEY" [SP mailbox *(SP mechanism)] |
| RFC4467-9-2 — GENURLAUTH ABNF: repeatable url-rump/mechanism pairs, at least one required | MUST | rev1 | fail | unimplemented: genurlauth = "GENURLAUTH" 1*(SP url-rump SP mechanism) |
| RFC4467-9-2 — GENURLAUTH ABNF: repeatable url-rump/mechanism pairs, at least one required | MUST | rev2 | fail | unimplemented: genurlauth = "GENURLAUTH" 1*(SP url-rump SP mechanism) |
| RFC4467-9-3 — URLFETCH ABNF: repeatable url-full arguments, at least one required | MUST | rev1 | fail | unimplemented: urlfetch = "URLFETCH" 1*(SP url-full) |
| RFC4467-9-3 — URLFETCH ABNF: repeatable url-full arguments, at least one required | MUST | rev2 | fail | unimplemented: urlfetch = "URLFETCH" 1*(SP url-full) |
| RFC5524-3-1 — Client MUST NOT use extended URLFETCH parameters without the URLAUTH=BINARY capability | MUST | rev1 | pass |  |
| RFC5524-3-1 — Client MUST NOT use extended URLFETCH parameters without the URLAUTH=BINARY capability | MUST | rev2 | pass |  |
| RFC5524-3.1-1 — Extended URLFETCH form parenthesizes each URL together with its requested parameters | MUST | rev1 | fail | unimplemented: The extended URLFETCH command is distinct by enclosing each URL and associated parameters in a parenthesized list. |
| RFC5524-3.1-1 — Extended URLFETCH form parenthesizes each URL together with its requested parameters | MUST | rev2 | fail | unimplemented: The extended URLFETCH command is distinct by enclosing each URL and associated parameters in a parenthesized list. |
| RFC5524-3.1-2 — Client MUST NOT request both BINARY and BODY for the same URL | MUST NOT | rev1 | pass |  |
| RFC5524-3.1-2 — Client MUST NOT request both BINARY and BODY for the same URL | MUST NOT | rev2 | pass |  |
| RFC5524-3.1-3 — Client MUST NOT repeat the same metadata parameter for one URL | MUST NOT | rev1 | pass |  |
| RFC5524-3.1-3 — Client MUST NOT repeat the same metadata parameter for one URL | MUST NOT | rev2 | pass |  |
| RFC5524-3.1-4 — Client requests one or more of the three named parameters: BODYPARTSTRUCTURE, BINARY, BODY | MAY | rev1 | fail | unimplemented: Available parameters are: BODYPARTSTRUCTURE |
| RFC5524-3.1-4 — Client requests one or more of the three named parameters: BODYPARTSTRUCTURE, BINARY, BODY | MAY | rev2 | fail | unimplemented: Available parameters are: BODYPARTSTRUCTURE |
| RFC5524-3.2-1 — Client MUST accept the extended * URLFETCH response's parenthesized per-URL metadata elements | MUST | rev1 | pass |  |
| RFC5524-3.2-1 — Client MUST accept the extended * URLFETCH response's parenthesized per-URL metadata elements | MUST | rev2 | pass |  |
| RFC5524-3.2-2 — Client MUST accept literal8-framed BINARY metadata and an NIL BINARY item on decode failure | MUST | rev1 | pass |  |
| RFC5524-3.2-2 — Client MUST accept literal8-framed BINARY metadata and an NIL BINARY item on decode failure | MUST | rev2 | pass |  |
| RFC5802-5-1 — Client-first-message MUST start with the gs2-cbind-flag byte n, y, or p | MUST | rev1 | fail | unimplemented: Note that the client's first message will always start with "n", "y", or "p"; otherwise, the message is invalid and authentication MUST fail. |
| RFC5802-5-1 — Client-first-message MUST start with the gs2-cbind-flag byte n, y, or p | MUST | rev2 | fail | unimplemented: Note that the client's first message will always start with "n", "y", or "p"; otherwise, the message is invalid and authentication MUST fail. |
| RFC5802-5-2 — Attribute order in client (and server) messages is fixed except for extension attributes | MUST | rev1 | fail | unimplemented: Note that the order of attributes in client or server messages is fixed, with the exception of extension attributes (described by the "extensions" ABNF producti… |
| RFC5802-5-2 — Attribute order in client (and server) messages is fixed except for extension attributes | MUST | rev2 | fail | unimplemented: Note that the order of attributes in client or server messages is fixed, with the exception of extension attributes (described by the "extensions" ABNF producti… |
| RFC5802-5-3 — Client MUST treat a mismatched ServerSignature as authentication failure | MUST | rev1 | fail | unimplemented: The client then authenticates the server by computing the ServerSignature and comparing it to the value sent by the server. If the two are different, the client… |
| RFC5802-5-3 — Client MUST treat a mismatched ServerSignature as authentication failure | MUST | rev2 | fail | unimplemented: The client then authenticates the server by computing the ServerSignature and comparing it to the value sent by the server. If the two are different, the client… |
| RFC5802-5.1-1 — 'a=' authzid syntax matches 'n='s comma/equals quoting rule | MUST | rev1 | fail | unimplemented: The syntax of this field is the same as that of the "n" field with respect to quoting of '=' and ','. |
| RFC5802-5.1-1 — 'a=' authzid syntax matches 'n='s comma/equals quoting rule | MUST | rev2 | fail | unimplemented: The syntax of this field is the same as that of the "n" field with respect to quoting of '=' and ','. |
| RFC5802-5.1-2 — Client MUST include the 'n=' username attribute in its first message | MUST | rev1 | fail | unimplemented: A client MUST include it in its first message to the server. |
| RFC5802-5.1-2 — Client MUST include the 'n=' username attribute in its first message | MUST | rev2 | fail | unimplemented: A client MUST include it in its first message to the server. |
| RFC5802-5.1-3 — Client SHOULD SASLprep the username before sending, and SHOULD abort on preparation failure/empty result | SHOULD | rev1 | fail | unimplemented: Before sending the username to the server, the client SHOULD prepare the username using the "SASLprep" profile [RFC4013] of the "stringprep" algorithm [RFC3454]… |
| RFC5802-5.1-3 — Client SHOULD SASLprep the username before sending, and SHOULD abort on preparation failure/empty result | SHOULD | rev2 | fail | unimplemented: Before sending the username to the server, the client SHOULD prepare the username using the "SASLprep" profile [RFC4013] of the "stringprep" algorithm [RFC3454]… |
| RFC5802-5.1-4 — Comma and equals in the username are escaped as '=2C' and '=3D' | MUST | rev1 | fail | unimplemented: The characters ',' or '=' in usernames are sent as '=2C' and '=3D' respectively. |
| RFC5802-5.1-4 — Comma and equals in the username are escaped as '=2C' and '=3D' | MUST | rev2 | fail | unimplemented: The characters ',' or '=' in usernames are sent as '=2C' and '=3D' respectively. |
| RFC5802-5.1-5 — Presence of the reserved 'm=' attribute in a client message MUST cause authentication failure | MUST | rev1 | fail | unimplemented: This attribute is reserved for future extensibility. In this version of SCRAM, its presence in a client or a server message MUST cause authentication failure wh… |
| RFC5802-5.1-5 — Presence of the reserved 'm=' attribute in a client message MUST cause authentication failure | MUST | rev2 | fail | unimplemented: This attribute is reserved for future extensibility. In this version of SCRAM, its presence in a client or a server message MUST cause authentication failure wh… |
| RFC5802-5.1-6 — 'r=' nonce is a sequence of random printable ASCII characters excluding comma, unquoted | MUST | rev1 | fail | unimplemented: This attribute specifies a sequence of random printable ASCII characters excluding ',' (which forms the nonce used as input to the hash function). No quoting is… |
| RFC5802-5.1-6 — 'r=' nonce is a sequence of random printable ASCII characters excluding comma, unquoted | MUST | rev2 | fail | unimplemented: This attribute specifies a sequence of random printable ASCII characters excluding ',' (which forms the nonce used as input to the hash function). No quoting is… |
| RFC5802-5.1-7 — Client's nonce MUST be different for each authentication | MUST | rev1 | fail | unimplemented: It is important that this value be different for each authentication (see [RFC4086] for more details on how to achieve this). |
| RFC5802-5.1-7 — Client's nonce MUST be different for each authentication | MUST | rev2 | fail | unimplemented: It is important that this value be different for each authentication (see [RFC4086] for more details on how to achieve this). |
| RFC5802-5.1-8 — Client MUST verify the server echoed its nonce as the initial part of the combined nonce | MUST | rev1 | fail | unimplemented: The client MUST verify that the initial part of the nonce used in subsequent messages is the same as the nonce it initially specified. |
| RFC5802-5.1-8 — Client MUST verify the server echoed its nonce as the initial part of the combined nonce | MUST | rev2 | fail | unimplemented: The client MUST verify that the initial part of the nonce used in subsequent messages is the same as the nonce it initially specified. |
| RFC5802-5.1-9 — 'c=' channel-binding attribute is REQUIRED in the client-final-message | MUST | rev1 | fail | unimplemented: This REQUIRED attribute specifies the base64-encoded GS2 header and channel binding data. It is sent by the client in its second authentication message. The att… |
| RFC5802-5.1-9 — 'c=' channel-binding attribute is REQUIRED in the client-final-message | MUST | rev2 | fail | unimplemented: This REQUIRED attribute specifies the base64-encoded GS2 header and channel binding data. It is sent by the client in its second authentication message. The att… |
| RFC5802-5.1-10 — 'c=' value's first component is the client-first-message's GS2 header, including channel-binding-type prefix iff channel binding is used | MUST | rev1 | fail | unimplemented: the GS2 header from the client's first message (recall that the GS2 header contains a channel binding flag and an optional authzid). This header is going to inc… |
| RFC5802-5.1-10 — 'c=' value's first component is the client-first-message's GS2 header, including channel-binding-type prefix iff channel binding is used | MUST | rev2 | fail | unimplemented: the GS2 header from the client's first message (recall that the GS2 header contains a channel binding flag and an optional authzid). This header is going to inc… |
| RFC5802-5.1-11 — 'c=' value's second component is the external channel's binding data, present iff channel binding is used | MUST | rev1 | fail | unimplemented: followed by the external channel's channel binding data, if and only if the client is using channel binding. |
| RFC5802-5.1-11 — 'c=' value's second component is the external channel's binding data, present iff channel binding is used | MUST | rev2 | fail | unimplemented: followed by the external channel's channel binding data, if and only if the client is using channel binding. |
| RFC5802-5.1-12 — 'p=' proof is the client-computed, base64-encoded ClientProof | MUST | rev1 | fail | unimplemented: This attribute specifies a base64-encoded ClientProof. The client computes this value as described in the overview and sends it to the server. |
| RFC5802-5.1-12 — 'p=' proof is the client-computed, base64-encoded ClientProof | MUST | rev2 | fail | unimplemented: This attribute specifies a base64-encoded ClientProof. The client computes this value as described in the overview and sends it to the server. |
| RFC5802-5.1-13 — 'v=' verifier is the base64-encoded ServerSignature the client uses to verify the server | MUST | rev1 | fail | unimplemented: This attribute specifies a base64-encoded ServerSignature. It is sent by the server in its final message, and is used by the client to verify that the server ha… |
| RFC5802-5.1-13 — 'v=' verifier is the base64-encoded ServerSignature the client uses to verify the server | MUST | rev2 | fail | unimplemented: This attribute specifies a base64-encoded ServerSignature. It is sent by the server in its final message, and is used by the client to verify that the server ha… |
| RFC5802-5.1-14 — Client MAY receive no server-final-message at all on authentication failure | MAY | rev1 | fail | unimplemented: On failed authentication, the entire server-final-message is OPTIONAL; specifically, a server implementation MAY conclude the SASL exchange with a failure witho… |
| RFC5802-5.1-14 — Client MAY receive no server-final-message at all on authentication failure | MAY | rev2 | fail | unimplemented: On failed authentication, the entire server-final-message is OPTIONAL; specifically, a server implementation MAY conclude the SASL exchange with a failure witho… |
| RFC5802-5.1-15 — Client MUST fail authentication on an unsupported mandatory extension | MUST | rev1 | fail | unimplemented: Mandatory extensions sent by one peer but not understood by the other MUST cause authentication failure (the server SHOULD send the "extensions-not-supported" s… |
| RFC5802-5.1-15 — Client MUST fail authentication on an unsupported mandatory extension | MUST | rev2 | fail | unimplemented: Mandatory extensions sent by one peer but not understood by the other MUST cause authentication failure (the server SHOULD send the "extensions-not-supported" s… |
| RFC5802-5.1-16 — Client MUST ignore unknown optional extension attributes | MUST | rev1 | fail | unimplemented: Unknown optional extensions MUST be ignored upon receipt. |
| RFC5802-5.1-16 — Client MUST ignore unknown optional extension attributes | MUST | rev2 | fail | unimplemented: Unknown optional extensions MUST be ignored upon receipt. |
| RFC5802-6-1 — Client supporting channel binding MUST NOT use the 'n' gs2-cbind-flag if the server did not advertise a -PLUS mechanism | MUST NOT | rev1 | fail | unimplemented: If the client supports channel binding and the server does not appear to (i.e., the client did not see the -PLUS name advertised by the server), then the client… |
| RFC5802-6-1 — Client supporting channel binding MUST NOT use the 'n' gs2-cbind-flag if the server did not advertise a -PLUS mechanism | MUST NOT | rev2 | fail | unimplemented: If the client supports channel binding and the server does not appear to (i.e., the client did not see the -PLUS name advertised by the server), then the client… |
| RFC5802-6-2 — Client supporting mechanism negotiation and channel binding MUST use the 'p' gs2-cbind-flag when the server offers the PLUS variant | MUST | rev1 | fail | unimplemented: Clients that support mechanism negotiation and channel binding MUST use a "p" gs2-cbind-flag when the server offers the PLUS-variant of the desired GS2 mechanis… |
| RFC5802-6-2 — Client supporting mechanism negotiation and channel binding MUST use the 'p' gs2-cbind-flag when the server offers the PLUS variant | MUST | rev2 | fail | unimplemented: Clients that support mechanism negotiation and channel binding MUST use a "p" gs2-cbind-flag when the server offers the PLUS-variant of the desired GS2 mechanis… |
| RFC5802-6-3 — Client without channel-binding support MUST use 'n'; a client requiring it MUST use 'p'; 'y' is never used absent mechanism negotiation | MUST | rev1 | fail | unimplemented: If the client does not support channel binding, then it MUST use an "n" gs2-cbind-flag. Conversely, if the client requires the use of channel binding then it MU… |
| RFC5802-6-3 — Client without channel-binding support MUST use 'n'; a client requiring it MUST use 'p'; 'y' is never used absent mechanism negotiation | MUST | rev2 | fail | unimplemented: If the client does not support channel binding, then it MUST use an "n" gs2-cbind-flag. Conversely, if the client requires the use of channel binding then it MU… |
| RFC5802-6.1-1 — 'tls-unique' is the default channel binding type | MUST | rev1 | fail | unimplemented: 'tls-unique' is the default channel binding type for any application that doesn't specify one. |
| RFC5802-6.1-1 — 'tls-unique' is the default channel binding type | MUST | rev2 | fail | unimplemented: 'tls-unique' is the default channel binding type for any application that doesn't specify one. |
| RFC5802-6.1-2 — Client SHOULD implement the 'tls-unique' channel binding type if it implements any channel binding | SHOULD | rev1 | fail | unimplemented: Clients SHOULD implement the "tls-unique" [RFC5929] channel binding type, if they implement any channel binding. Clients and servers SHOULD choose the highest-l… |
| RFC5802-6.1-2 — Client SHOULD implement the 'tls-unique' channel binding type if it implements any channel binding | SHOULD | rev2 | fail | unimplemented: Clients SHOULD implement the "tls-unique" [RFC5929] channel binding type, if they implement any channel binding. Clients and servers SHOULD choose the highest-l… |
| RFC5802-7-1 — gs2-cbind-flag / gs2-header ABNF the client MUST construct | MUST | rev1 | fail | unimplemented: gs2-cbind-flag = ("p=" cb-name) / "n" / "y" ... gs2-header = gs2-cbind-flag "," [ authzid ] "," |
| RFC5802-7-1 — gs2-cbind-flag / gs2-header ABNF the client MUST construct | MUST | rev2 | fail | unimplemented: gs2-cbind-flag = ("p=" cb-name) / "n" / "y" ... gs2-header = gs2-cbind-flag "," [ authzid ] "," |
| RFC5802-7-2 — client-first-message / client-final-message(-without-proof) ABNF the client MUST construct | MUST | rev1 | fail | unimplemented: client-first-message-bare = [reserved-mext ","] username "," nonce ["," extensions] ... client-first-message = gs2-header client-first-message-bare ... client-f… |
| RFC5802-7-2 — client-first-message / client-final-message(-without-proof) ABNF the client MUST construct | MUST | rev2 | fail | unimplemented: client-first-message-bare = [reserved-mext ","] username "," nonce ["," extensions] ... client-first-message = gs2-header client-first-message-bare ... client-f… |
| RFC5802-7-3 — cbind-data MUST be present in cbind-input iff the gs2-cbind-flag is 'p', and MUST be absent for 'y' or 'n' | MUST | rev1 | fail | unimplemented: cbind-input = gs2-header [ cbind-data ] ... cbind-data MUST be present for gs2-cbind-flag of "p" and MUST be absent for "y" or "n". |
| RFC5802-7-3 — cbind-data MUST be present in cbind-input iff the gs2-cbind-flag is 'p', and MUST be absent for 'y' or 'n' | MUST | rev2 | fail | unimplemented: cbind-input = gs2-header [ cbind-data ] ... cbind-data MUST be present for gs2-cbind-flag of "p" and MUST be absent for "y" or "n". |
| RFC5802-3-1 — Username and password MUST be encoded in UTF-8 | MUST | rev1 | fail | unimplemented: Note that both the username and the password MUST be encoded in UTF-8 [RFC3629]. |
| RFC5802-3-1 — Username and password MUST be encoded in UTF-8 | MUST | rev2 | fail | unimplemented: Note that both the username and the password MUST be encoded in UTF-8 [RFC3629]. |
| RFC5802-3-2 — Client MUST either implement SASLprep or disallow non-US-ASCII Unicode codepoints in prepared strings | MUST | rev1 | fail | unimplemented: implementations MUST either implement SASLprep or disallow use of non US-ASCII Unicode codepoints in "str". |
| RFC5802-3-2 — Client MUST either implement SASLprep or disallow non-US-ASCII Unicode codepoints in prepared strings | MUST | rev2 | fail | unimplemented: implementations MUST either implement SASLprep or disallow use of non US-ASCII Unicode codepoints in "str". |
| RFC7677-3-1 — SCRAM-SHA-256(-PLUS) is SCRAM-SHA-1(-PLUS) with SHA-256 substituted for HMAC()/H(), servers SHOULD announce iteration-count >= 4096 | MUST | rev1 | fail | unimplemented: The SCRAM-SHA-256 and SCRAM-SHA-256-PLUS SASL mechanisms are defined in the same way that SCRAM-SHA-1 and SCRAM-SHA-1-PLUS are defined in [RFC5802], except that… |
| RFC7677-3-1 — SCRAM-SHA-256(-PLUS) is SCRAM-SHA-1(-PLUS) with SHA-256 substituted for HMAC()/H(), servers SHOULD announce iteration-count >= 4096 | MUST | rev2 | fail | unimplemented: The SCRAM-SHA-256 and SCRAM-SHA-256-PLUS SASL mechanisms are defined in the same way that SCRAM-SHA-1 and SCRAM-SHA-1-PLUS are defined in [RFC5802], except that… |
| RFC7677-4-1 — -PLUS variant MUST be used over a TLS channel with session-hash negotiated, or session resumption MUST NOT have been used | MUST | rev1 | fail | unimplemented: To be secure, either SCRAM-SHA-256-PLUS and SCRAM-SHA-1-PLUS MUST be used over a TLS channel that has had the session hash extension [RFC7627] negotiated, or se… |
| RFC7677-4-1 — -PLUS variant MUST be used over a TLS channel with session-hash negotiated, or session resumption MUST NOT have been used | MUST | rev2 | fail | unimplemented: To be secure, either SCRAM-SHA-256-PLUS and SCRAM-SHA-1-PLUS MUST be used over a TLS channel that has had the session hash extension [RFC7627] negotiated, or se… |
| RFC4505-2-1 — ANONYMOUS exchange is a single client-to-server message | MUST | rev1 | fail | unimplemented: The mechanism consists of a single message from the client to the server. |
| RFC4505-2-1 — ANONYMOUS exchange is a single client-to-server message | MUST | rev2 | fail | unimplemented: The mechanism consists of a single message from the client to the server. |
| RFC4505-2-2 — Trace information, if included, is UTF-8/Unicode prepared per the trace stringprep profile | MAY | rev1 | fail | unimplemented: The client may include in this message trace information in the form of a string of [UTF-8]-encoded [Unicode] characters prepared in accordance with [StringPrep… |
| RFC4505-2-2 — Trace information, if included, is UTF-8/Unicode prepared per the trace stringprep profile | MAY | rev2 | fail | unimplemented: The client may include in this message trace information in the form of a string of [UTF-8]-encoded [Unicode] characters prepared in accordance with [StringPrep… |
| RFC4505-2-3 — Trace information should be an email address or an opaque non-'@' token | SHOULD | rev1 | fail | unimplemented: The trace information, which has no semantical value, should take one of two forms: an Internet email address, or an opaque string that does not contain the '@'… |
| RFC4505-2-3 — Trace information should be an email address or an opaque non-'@' token | SHOULD | rev2 | fail | unimplemented: The trace information, which has no semantical value, should take one of two forms: an Internet email address, or an opaque string that does not contain the '@'… |
| RFC4505-2-4 — Use identifying trace information only with the user's permission | SHOULD | rev1 | untestable | Whether the human user granted permission for the client to send identifying trace information is an out-of-band fact about user consent, not something observable from the wire exchange itself — the harness can see which trace string was sent but cannot determine whether the user authorized sending it. Sharpened to an explicit, still-untestable-for-the-same-reason operative duty in Section 5 (entry RFC4505-5-1), which additionally requires an affirmative opt-out affordance. |
| RFC4505-2-4 — Use identifying trace information only with the user's permission | SHOULD | rev2 | untestable | Whether the human user granted permission for the client to send identifying trace information is an out-of-band fact about user consent, not something observable from the wire exchange itself — the harness can see which trace string was sent but cannot determine whether the user authorized sending it. Sharpened to an explicit, still-untestable-for-the-same-reason operative duty in Section 5 (entry RFC4505-5-1), which additionally requires an affirmative opt-out affordance. |
| RFC4505-2-5 — Token trace information is capped at 255 UTF-8-encoded Unicode characters | MUST | rev1 | fail | unimplemented: The <token> production is restricted to 255 UTF-8-encoded Unicode characters. |
| RFC4505-2-5 — Token trace information is capped at 255 UTF-8-encoded Unicode characters | MUST | rev2 | fail | unimplemented: The <token> production is restricted to 255 UTF-8-encoded Unicode characters. |
| RFC4505-2-6 — Token trace information may be as long as 1020 octets when encoded | MAY | rev1 | fail | unimplemented: As the encoding of a characters uses a sequence of 1 to 4 octets, a token may be as long as 1020 octets. |
| RFC4505-2-6 — Token trace information may be as long as 1020 octets when encoded | MAY | rev2 | fail | unimplemented: As the encoding of a characters uses a sequence of 1 to 4 octets, a token may be as long as 1020 octets. |
| RFC4505-3-1 — Client prepares the message production per the trace stringprep profile | MUST | rev1 | fail | unimplemented: Specifically, the client is to prepare the <message> production in accordance with this profile. |
| RFC4505-3-1 — Client prepares the message production per the trace stringprep profile | MUST | rev2 | fail | unimplemented: Specifically, the client is to prepare the <message> production in accordance with this profile. |
| RFC4505-3-2 — No mapping step in trace-profile preparation | MUST NOT | rev1 | untestable | A stringprep 'no mapping required' rule governs the internal character-transformation algorithm the client runs while preparing the message, not a property of the transmitted bytes: many distinct client-side code paths (including one that mapped characters but happened to produce an unchanged result for a given input) are indistinguishable on the wire from one that correctly performs no mapping. Only inputs deliberately chosen to expose a mapping step (e.g. characters with a stringprep case-fold or width-mapping in other profiles) would reveal a violation, and RFC 4505's trace profile defines none to test against — the absence-of-a-step cannot be black-box confirmed in general. |
| RFC4505-3-2 — No mapping step in trace-profile preparation | MUST NOT | rev2 | untestable | A stringprep 'no mapping required' rule governs the internal character-transformation algorithm the client runs while preparing the message, not a property of the transmitted bytes: many distinct client-side code paths (including one that mapped characters but happened to produce an unchanged result for a given input) are indistinguishable on the wire from one that correctly performs no mapping. Only inputs deliberately chosen to expose a mapping step (e.g. characters with a stringprep case-fold or width-mapping in other profiles) would reveal a violation, and RFC 4505's trace profile defines none to test against — the absence-of-a-step cannot be black-box confirmed in general. |
| RFC4505-3-3 — No Unicode normalization step in trace-profile preparation | MUST NOT | rev1 | untestable | Same reasoning as entry 3-2: whether the client's internal preparation pipeline applies a normalization step is not observable purely from the output bytes for well-formed input, since already-normalized input round-trips identically whether or not a (no-op) normalization pass ran. Distinguishing 'no normalization performed' from 'normalization performed but input was already normal' requires knowledge of the client's internal processing, not just the wire artifact. |
| RFC4505-3-3 — No Unicode normalization step in trace-profile preparation | MUST NOT | rev2 | untestable | Same reasoning as entry 3-2: whether the client's internal preparation pipeline applies a normalization step is not observable purely from the output bytes for well-formed input, since already-normalized input round-trips identically whether or not a (no-op) normalization pass ran. Distinguishing 'no normalization performed' from 'normalization performed but input was already normal' requires knowledge of the client's internal processing, not just the wire artifact. |
| RFC4505-3-4 — Trace-profile message excludes prohibited StringPrep character tables | MUST NOT | rev1 | fail | unimplemented: Characters from the following tables of [StringPrep] are prohibited: |
| RFC4505-3-4 — Trace-profile message excludes prohibited StringPrep character tables | MUST NOT | rev2 | fail | unimplemented: Characters from the following tables of [StringPrep] are prohibited: |
| RFC4505-3-5 — Trace-profile preparation requires bidirectional character checking | MUST | rev1 | untestable | StringPrep Section 6 bidirectional checking (the 'Bidi' rule restricting mixing of RandALCat and LCat characters, and requiring a RandALCat string to start and end with RandALCat characters) is an internal validation algorithm the client runs before deciding whether to send a trace string at all; a black-box observer sees only whether a given string was sent or withheld, which is equally consistent with the client running the check correctly, running a different check, or running no check at all when the supplied trace value happens to already satisfy (or violate and get rejected upstream for unrelated reasons) the Bidi rule. No wire-observable artifact distinguishes 'bidirectional check performed' from 'no check performed, input happened to pass'. |
| RFC4505-3-5 — Trace-profile preparation requires bidirectional character checking | MUST | rev2 | untestable | StringPrep Section 6 bidirectional checking (the 'Bidi' rule restricting mixing of RandALCat and LCat characters, and requiring a RandALCat string to start and end with RandALCat characters) is an internal validation algorithm the client runs before deciding whether to send a trace string at all; a black-box observer sees only whether a given string was sent or withheld, which is equally consistent with the client running the check correctly, running a different check, or running no check at all when the supplied trace value happens to already satisfy (or violate and get rejected upstream for unrelated reasons) the Bidi rule. No wire-observable artifact distinguishes 'bidirectional check performed' from 'no check performed, input happened to pass'. |
| RFC4505-5-1 — Do not send email trace info without permission; offer no-trace-information option | SHOULD NOT | rev1 | untestable | Two duties: (1) not sending the user's email address as trace information absent explicit user permission — like entry 2-4, whether permission was obtained is an out-of-band fact about user consent invisible to a black-box wire observer, who can see only which string (if any) was sent; and (2) that the client offers a UI/API affordance to supply no trace information — an affordance-existence claim about the client's configuration surface, not a single-exchange wire observation (a session sending no trace information is consistent with the option existing and being exercised, or with the client simply defaulting to empty with no user-facing option at all). Sharpens entry 2-4's framing into an explicit SHOULD NOT plus an additional MAY-strength affordance duty; both halves share the same out-of-band untestability. |
| RFC4505-5-1 — Do not send email trace info without permission; offer no-trace-information option | SHOULD NOT | rev2 | untestable | Two duties: (1) not sending the user's email address as trace information absent explicit user permission — like entry 2-4, whether permission was obtained is an out-of-band fact about user consent invisible to a black-box wire observer, who can see only which string (if any) was sent; and (2) that the client offers a UI/API affordance to supply no trace information — an affordance-existence claim about the client's configuration surface, not a single-exchange wire observation (a session sending no trace information is consistent with the option existing and being exercised, or with the client simply defaulting to empty with no user-facing option at all). Sharpens entry 2-4's framing into an explicit SHOULD NOT plus an additional MAY-strength affordance duty; both halves share the same out-of-band untestability. |
| RFC2221-3-1 — Client MUST NOT follow more than 10 levels of referral without consulting the user | MUST NOT | rev1 | pass |  |
| RFC2221-3-1 — Client MUST NOT follow more than 10 levels of referral without consulting the user | MUST NOT | rev2 | pass |  |
| RFC2221-4.1-1 — Client (implicit) MUST parse/accept a LOGIN-REFERRALS response code on a tagged NO to a LOGIN/AUTHENTICATE | MUST | rev1 | pass |  |
| RFC2221-4.1-1 — Client (implicit) MUST parse/accept a LOGIN-REFERRALS response code on a tagged NO to a LOGIN/AUTHENTICATE | MUST | rev2 | pass |  |
| RFC2221-4.1-2 — Client (implicit) MUST recover the referral URL from a tagged NO, not just notice the command failed | MUST | rev1 | pass |  |
| RFC2221-4.1-2 — Client (implicit) MUST recover the referral URL from a tagged NO, not just notice the command failed | MUST | rev2 | pass |  |
| RFC2221-4.1-3 — Client (implicit) MUST NOT treat a tagged OK carrying [REFERRAL ...] as an unqualified plain success | MUST | rev1 | pass |  |
| RFC2221-4.1-3 — Client (implicit) MUST NOT treat a tagged OK carrying [REFERRAL ...] as an unqualified plain success | MUST | rev2 | pass |  |
| RFC2221-4.2-1 — Client (implicit) MUST treat an untagged BYE carrying [REFERRAL ...] as a startup redirection, not a bare refusal | MUST | rev1 | pass |  |
| RFC2221-4.2-1 — Client (implicit) MUST treat an untagged BYE carrying [REFERRAL ...] as a startup redirection, not a bare refusal | MUST | rev2 | pass |  |
| RFC2221-4-1 — Client (implicit) MUST NOT assume a home server referral is permanent or temporary | MUST | rev1 | untestable | There is no wire signal at all — the referral URL and its surrounding text carry no permanent-vs-temporary flag — so 'not assuming' is a purely internal abstention with zero observable protocol consequence. A client that (incorrectly) caches the referral as permanent and one that treats every referral as freshly re-checked on each connection are wire-indistinguishable in any single scripted exchange; divergence would only surface as a client-side caching-policy choice across sessions, which is exactly the cross-session/internal-decision territory this suite's single-session harness cannot observe. |
| RFC2221-4-1 — Client (implicit) MUST NOT assume a home server referral is permanent or temporary | MUST | rev2 | untestable | There is no wire signal at all — the referral URL and its surrounding text carry no permanent-vs-temporary flag — so 'not assuming' is a purely internal abstention with zero observable protocol consequence. A client that (incorrectly) caches the referral as permanent and one that treats every referral as freshly re-checked on each connection are wire-indistinguishable in any single scripted exchange; divergence would only surface as a client-side caching-policy choice across sessions, which is exactly the cross-session/internal-decision territory this suite's single-session harness cannot observe. |
| RFC2221-4-2 — Client (implicit) MUST connect to the referral URL's host and use its embedded auth parameter when following a referral | MUST | rev1 | untestable | Exercising this duty requires the client to actually open a second TCP connection to the referred host and carry the URL's userid/AUTH-mechanism-restriction segment into a follow-up AUTHENTICATE/LOGIN exchange there — a full two-connection, two-handshake integration flow. The suite's harness scripts a single connection per test; no driver verb exists that follows a parsed referral URL into a new connection at all today, so there is nothing to probe short of building an out-of-band multi-server integration harness outside this suite's black-box single-session design. |
| RFC2221-4-2 — Client (implicit) MUST connect to the referral URL's host and use its embedded auth parameter when following a referral | MUST | rev2 | untestable | Exercising this duty requires the client to actually open a second TCP connection to the referred host and carry the URL's userid/AUTH-mechanism-restriction segment into a follow-up AUTHENTICATE/LOGIN exchange there — a full two-connection, two-handshake integration flow. The suite's harness scripts a single connection per test; no driver verb exists that follows a parsed referral URL into a new connection at all today, so there is nothing to probe short of building an out-of-band multi-server integration harness outside this suite's black-box single-session design. |
| RFC2221-6-1 — Client (implicit) MUST carry the URL's AUTH-mechanism restriction forward rather than silently falling back to anonymous login | MUST | rev1 | untestable | Same out-of-band reasoning as RFC2221-4-2: detecting whether a client silently degrades to anonymous login on a followed referral requires actually following the referral to a second connection and observing the authentication mechanism it chooses there — a two-connection integration scenario the single-session harness does not support, and for which no driver verb exists. |
| RFC2221-6-1 — Client (implicit) MUST carry the URL's AUTH-mechanism restriction forward rather than silently falling back to anonymous login | MUST | rev2 | untestable | Same out-of-band reasoning as RFC2221-4-2: detecting whether a client silently degrades to anonymous login on a followed referral requires actually following the referral to a second connection and observing the authentication mechanism it chooses there — a two-connection integration scenario the single-session harness does not support, and for which no driver verb exists. |
| RFC2193-3-1 — Client (implicit) MUST be prepared for a URL of any type, though it need only process IMAP URLs | MUST | rev1 | pass |  |
| RFC2193-3-1 — Client (implicit) MUST be prepared for a URL of any type, though it need only process IMAP URLs | MUST | rev2 | pass |  |
| RFC2193-3-2 — Client (implicit) MUST parse/accept a REFERRAL response code on a tagged NO to SELECT/EXAMINE/DELETE/SUBSCRIBE/UNSUBSCRIBE/STATUS/APPEND | MUST | rev1 | pass |  |
| RFC2193-3-2 — Client (implicit) MUST parse/accept a REFERRAL response code on a tagged NO to SELECT/EXAMINE/DELETE/SUBSCRIBE/UNSUBSCRIBE/STATUS/APPEND | MUST | rev2 | pass |  |
| RFC2193-3-3 — Client (implicit) MUST recover every URL when a REFERRAL response code carries multiple | MUST | rev1 | pass |  |
| RFC2193-3-3 — Client (implicit) MUST recover every URL when a REFERRAL response code carries multiple | MUST | rev2 | pass |  |
| RFC2193-4.1-1 — Client (implicit) MUST accept a REFERRAL response code on SELECT/EXAMINE/DELETE/SUBSCRIBE/UNSUBSCRIBE/STATUS/APPEND specifically | MUST | rev1 | pass |  |
| RFC2193-4.1-1 — Client (implicit) MUST accept a REFERRAL response code on SELECT/EXAMINE/DELETE/SUBSCRIBE/UNSUBSCRIBE/STATUS/APPEND specifically | MUST | rev2 | pass |  |
| RFC2193-4.2-1 — Client (implicit) MUST accept a REFERRAL response code on CREATE | MUST | rev1 | pass |  |
| RFC2193-4.2-1 — Client (implicit) MUST accept a REFERRAL response code on CREATE | MUST | rev2 | pass |  |
| RFC2193-4.3-1 — Client (implicit) MUST accept a REFERRAL response-code PAIR on RENAME meaning old-name/new-name URLs | MUST | rev1 | pass |  |
| RFC2193-4.3-1 — Client (implicit) MUST accept a REFERRAL response-code PAIR on RENAME meaning old-name/new-name URLs | MUST | rev2 | pass |  |
| RFC2193-4.3-2 — Client (implicit) MUST decide same-server-reissue vs. constituent-command-sequence based on comparing the RENAME referral pair's hosts | MAY | rev1 | untestable | Both the same-server and different-server cases begin from an identical two-URL wire shape (a tagged NO with a [REFERRAL url1 url2] code); the RFC itself offers the different-server follow-up only as a MAY ('the client MAY issue the constituent CREATE, FETCH, APPEND, and DELETE commands against both servers'). Which strategy — simple reissue vs. constituent-command decomposition — the client picks after comparing the two URLs' host components is an internal decision-procedure choice with no distinguishing wire consequence visible from the initial RENAME exchange alone; both a client that always decomposes and one that compares hosts first are indistinguishable in a single scripted probe of that exchange. |
| RFC2193-4.3-2 — Client (implicit) MUST decide same-server-reissue vs. constituent-command-sequence based on comparing the RENAME referral pair's hosts | MAY | rev2 | untestable | Both the same-server and different-server cases begin from an identical two-URL wire shape (a tagged NO with a [REFERRAL url1 url2] code); the RFC itself offers the different-server follow-up only as a MAY ('the client MAY issue the constituent CREATE, FETCH, APPEND, and DELETE commands against both servers'). Which strategy — simple reissue vs. constituent-command decomposition — the client picks after comparing the two URLs' host components is an internal decision-procedure choice with no distinguishing wire consequence visible from the initial RENAME exchange alone; both a client that always decomposes and one that compares hosts first are indistinguishable in a single scripted probe of that exchange. |
| RFC2193-4.4-1 — Client (implicit) MUST accept a REFERRAL response code on COPY | MUST | rev1 | pass |  |
| RFC2193-4.4-1 — Client (implicit) MUST accept a REFERRAL response code on COPY | MUST | rev2 | pass |  |
| RFC2193-5.1-1 — Client (implicit) MUST issue RLIST rather than LIST to see remote mailboxes when using MAILBOX-REFERRALS | MUST | rev1 | fail | unimplemented: A MAILBOX-REFERRALS capable client will issue the RLIST and RLSUB commands in lieu of LIST and LSUB. |
| RFC2193-5.1-1 — Client (implicit) MUST issue RLIST rather than LIST to see remote mailboxes when using MAILBOX-REFERRALS | MUST | rev2 | fail | unimplemented: A MAILBOX-REFERRALS capable client will issue the RLIST and RLSUB commands in lieu of LIST and LSUB. |
| RFC2193-5.2-1 — Client (implicit) MUST issue RLSUB rather than LSUB to see remote mailboxes when using MAILBOX-REFERRALS | MUST | rev1 | fail | unimplemented: The RLIST and RLSUB commands behave identically to their LIST and LSUB counterparts, except remote mailboxes are returned in addition to local mailboxes in the … |
| RFC2193-5.2-1 — Client (implicit) MUST issue RLSUB rather than LSUB to see remote mailboxes when using MAILBOX-REFERRALS | MUST | rev2 | fail | unimplemented: The RLIST and RLSUB commands behave identically to their LIST and LSUB counterparts, except remote mailboxes are returned in addition to local mailboxes in the … |
| RFC2193-3-4 — Client (implicit) MUST connect to the referral URL's host and honor its embedded auth restriction when processing a referred mailbox | MUST | rev1 | untestable | Identical reasoning to RFC2221-4-2: fully exercising this duty requires the client to open a second connection to the referral URL's host and carry the user;AUTH=* restriction into a follow-up AUTHENTICATE/LOGIN there. This suite's harness scripts a single connection per test and has no driver verb that follows a parsed referral URL into a new connection, making this a genuine out-of-band, multi-connection integration behavior rather than something a black-box single-session probe can observe. |
| RFC2193-3-4 — Client (implicit) MUST connect to the referral URL's host and honor its embedded auth restriction when processing a referred mailbox | MUST | rev2 | untestable | Identical reasoning to RFC2221-4-2: fully exercising this duty requires the client to open a second connection to the referral URL's host and carry the user;AUTH=* restriction into a follow-up AUTHENTICATE/LOGIN there. This suite's harness scripts a single connection per test and has no driver verb that follows a parsed referral URL into a new connection, making this a genuine out-of-band, multi-connection integration behavior rather than something a black-box single-session probe can observe. |
| RFC3348-3-1 — Client MUST be prepared to accept \HasChildren as a hint that may not hold in the subsequent LIST response | MUST | rev1 | untestable | 'Being prepared to accept' a hint that may not hold is a robustness property of client-side logic, not a discrete wire action: the server sends an ordinary LIST response either way, and any client behavior after receiving \HasChildren followed by zero matching children is compliant provided the client does not crash or assert a contradiction, which is not a protocol-level pass/fail boundary this catalog otherwise encodes. Identical reasoning to the dual-scored counterparts RFC9051-6.3.9.5-1, RFC9051-7.3.1-2, and RFC5258-4-1. |
| RFC3348-3-2 — Client MUST NOT assume a mailbox lacks children merely because both \HasChildren and \HasNoChildren are absent | MUST NOT | rev1 | untestable | 'Not making an assumption' is a negative internal-state property of client logic — there is no wire action the client takes or omits that directly manifests whether it assumed a false negative from an absent attribute. Any client behavior following a LIST response that omits both attributes is compliant provided the client does not assert a false 'mailbox has no children' conclusion, and that assertion (or its absence) has no independent wire signature distinct from the client's own internal bookkeeping — the same class of duty as the CHILDINFO/\HasChildren race-tolerance entries (RFC9051-6.3.9.1-1, RFC9051-6.3.9.5-1) already in this catalog family. |
| RFC3348-3-3 — Client MUST NOT assume LSUB responses carry authoritative CHILDREN hierarchy information | MUST NOT | rev1 | pass |  |
| X-GM-EXT-1-cap-1 — Extension support advertised via CAPABILITY | MUST | rev1 | pass |  |
| X-GM-EXT-1-cap-1 — Extension support advertised via CAPABILITY | MUST | rev2 | pass |  |
| X-GM-EXT-1-cap-2 — X-GM-EXT-1 token gates all extensions in this document | MUST | rev1 | pass |  |
| X-GM-EXT-1-cap-2 — X-GM-EXT-1 token gates all extensions in this document | MUST | rev2 | pass |  |
| X-GM-EXT-1-msgid-1 — X-GM-MSGID identifies a message across folders | MUST | rev1 | untestable | This sentence describes a server-side identity guarantee (the same message keeps the same X-GM-MSGID as it is copied/moved across Gmail folders/labels) — there is no client duty to verify or enforce this; it is a property of the server's ID assignment scheme that a client can only ever observe by reading whatever value the server sends back. A client cannot be non-compliant with a fact about server-side ID stability; it can only consume whatever value is returned. The client-observable duties this fact underlies (retrieve via FETCH, use in SEARCH) are separately cataloged as testable entries below. |
| X-GM-EXT-1-msgid-1 — X-GM-MSGID identifies a message across folders | MUST | rev2 | untestable | This sentence describes a server-side identity guarantee (the same message keeps the same X-GM-MSGID as it is copied/moved across Gmail folders/labels) — there is no client duty to verify or enforce this; it is a property of the server's ID assignment scheme that a client can only ever observe by reading whatever value the server sends back. A client cannot be non-compliant with a fact about server-side ID stability; it can only consume whatever value is returned. The client-observable duties this fact underlies (retrieve via FETCH, use in SEARCH) are separately cataloged as testable entries below. |
| X-GM-EXT-1-msgid-2 — X-GM-MSGID retrieved via FETCH attribute | MUST | rev1 | fail | unimplemented: Retrieval of this message ID is supported via the `X-GM-MSGID` attribute on the `FETCH` command. |
| X-GM-EXT-1-msgid-2 — X-GM-MSGID retrieved via FETCH attribute | MUST | rev2 | fail | unimplemented: Retrieval of this message ID is supported via the `X-GM-MSGID` attribute on the `FETCH` command. |
| X-GM-EXT-1-msgid-3 — X-GM-MSGID is a 64-bit unsigned integer, decimal form of the web/API hex ID | MUST | rev1 | fail | unimplemented: The message ID is a 64-bit unsigned integer and is the decimal equivalent for the ID hex string used in the web interface and the Gmail API. |
| X-GM-EXT-1-msgid-3 — X-GM-MSGID is a 64-bit unsigned integer, decimal form of the web/API hex ID | MUST | rev2 | fail | unimplemented: The message ID is a 64-bit unsigned integer and is the decimal equivalent for the ID hex string used in the web interface and the Gmail API. |
| X-GM-EXT-1-msgid-4 — X-GM-MSGID usable as a SEARCH/UID SEARCH key | MAY | rev1 | fail | unimplemented: The `X-GM-MSGID` attribute may also be used in the `SEARCH` or `UID SEARCH` commands to find the sequence numbers or `UID` of a message given Gmail's message ID… |
| X-GM-EXT-1-msgid-4 — X-GM-MSGID usable as a SEARCH/UID SEARCH key | MAY | rev2 | fail | unimplemented: The `X-GM-MSGID` attribute may also be used in the `SEARCH` or `UID SEARCH` commands to find the sequence numbers or `UID` of a message given Gmail's message ID… |
| X-GM-EXT-1-msgid-5 — X-GM-MSGID valid only under the X-GM-EXT-1 capability | MUST | rev1 | untestable | This entry cross-references the capability gate (X-GM-EXT-1-cap-2, same quote) as it specifically applies to X-GM-MSGID. A black-box harness can observe whether the client sends a FETCH/SEARCH with the X-GM-MSGID attribute in a given session, and whether the server advertised X-GM-EXT-1 in that session's CAPABILITY response — but 'the client must not assume X-GM-MSGID is usable without first checking CAPABILITY' is a duty about the client's internal decision process (whether it consulted the capability list before acting), not a distinguishing wire signature: a client that checks and one that guesses-and-happens-to-be-right against a real Gmail server emit identical FETCH/SEARCH bytes. The observable half (does the server accept X-GM-MSGID only when it advertised X-GM-EXT-1) is a server-side fact, not a client duty; the client-side gating discipline itself has no separate wire trace from correct usage. |
| X-GM-EXT-1-msgid-5 — X-GM-MSGID valid only under the X-GM-EXT-1 capability | MUST | rev2 | untestable | This entry cross-references the capability gate (X-GM-EXT-1-cap-2, same quote) as it specifically applies to X-GM-MSGID. A black-box harness can observe whether the client sends a FETCH/SEARCH with the X-GM-MSGID attribute in a given session, and whether the server advertised X-GM-EXT-1 in that session's CAPABILITY response — but 'the client must not assume X-GM-MSGID is usable without first checking CAPABILITY' is a duty about the client's internal decision process (whether it consulted the capability list before acting), not a distinguishing wire signature: a client that checks and one that guesses-and-happens-to-be-right against a real Gmail server emit identical FETCH/SEARCH bytes. The observable half (does the server accept X-GM-MSGID only when it advertised X-GM-EXT-1) is a server-side fact, not a client duty; the client-side gating discipline itself has no separate wire trace from correct usage. |
| X-GM-EXT-1-thrid-1 — X-GM-THRID associates messages into Gmail threads | MUST | rev1 | untestable | Describes a server-side grouping guarantee (messages sharing a thread ID are the same conversation as Gmail's web UI would group them) — this is a fact about server-side threading logic, not a client behavior. A client cannot violate or fulfill 'associates groups of messages the same way the web interface does'; it can only read back whatever thread ID value the server assigns. The client-observable duties (retrieve via FETCH, use in SEARCH) are cataloged separately as testable entries below. |
| X-GM-EXT-1-thrid-1 — X-GM-THRID associates messages into Gmail threads | MUST | rev2 | untestable | Describes a server-side grouping guarantee (messages sharing a thread ID are the same conversation as Gmail's web UI would group them) — this is a fact about server-side threading logic, not a client behavior. A client cannot violate or fulfill 'associates groups of messages the same way the web interface does'; it can only read back whatever thread ID value the server assigns. The client-observable duties (retrieve via FETCH, use in SEARCH) are cataloged separately as testable entries below. |
| X-GM-EXT-1-thrid-2 — X-GM-THRID retrieved via FETCH attribute | MUST | rev1 | fail | unimplemented: Retrieval of this thread ID is supported via the `X-GM-THRID` attribute on the `FETCH` command. |
| X-GM-EXT-1-thrid-2 — X-GM-THRID retrieved via FETCH attribute | MUST | rev2 | fail | unimplemented: Retrieval of this thread ID is supported via the `X-GM-THRID` attribute on the `FETCH` command. |
| X-GM-EXT-1-thrid-3 — X-GM-THRID is a 64-bit unsigned integer, decimal form of the web/API hex ID | MUST | rev1 | fail | unimplemented: The thread ID is a 64-bit unsigned integer and is the decimal equivalent for the ID hex string used in the web interface and the Gmail API. |
| X-GM-EXT-1-thrid-3 — X-GM-THRID is a 64-bit unsigned integer, decimal form of the web/API hex ID | MUST | rev2 | fail | unimplemented: The thread ID is a 64-bit unsigned integer and is the decimal equivalent for the ID hex string used in the web interface and the Gmail API. |
| X-GM-EXT-1-thrid-4 — X-GM-THRID usable as a SEARCH/UID SEARCH key | MAY | rev1 | fail | unimplemented: The `X-GM-THRID` attribute may also be used in the `SEARCH` or `UID SEARCH` commands to find the sequence numbers or `UID`s of messages in a given thread. |
| X-GM-EXT-1-thrid-4 — X-GM-THRID usable as a SEARCH/UID SEARCH key | MAY | rev2 | fail | unimplemented: The `X-GM-THRID` attribute may also be used in the `SEARCH` or `UID SEARCH` commands to find the sequence numbers or `UID`s of messages in a given thread. |
| X-GM-EXT-1-thrid-5 — X-GM-THRID valid only under the X-GM-EXT-1 capability | MUST | rev1 | untestable | Same reasoning as X-GM-EXT-1-msgid-5, restated for X-GM-THRID: the internal decision of whether the client consulted CAPABILITY before using X-GM-THRID has no wire signature distinct from correct usage; only the server-side acceptance/rejection is observable, and that is a server behavior, not a client duty this harness scores. |
| X-GM-EXT-1-thrid-5 — X-GM-THRID valid only under the X-GM-EXT-1 capability | MUST | rev2 | untestable | Same reasoning as X-GM-EXT-1-msgid-5, restated for X-GM-THRID: the internal decision of whether the client consulted CAPABILITY before using X-GM-THRID has no wire signature distinct from correct usage; only the server-side acceptance/rejection is observable, and that is a server behavior, not a client duty this harness scores. |
| X-GM-EXT-1-labels-1 — Gmail labels are IMAP folders | MUST | rev1 | untestable | A description of Gmail's server-side mapping of its label model onto IMAP's mailbox/ folder model — this is server architecture, not a client action. No client behavior is mandated by this sentence in isolation; it is background context motivating the following testable duties (label modification via CREATE/RENAME/DELETE, retrieval via X-GM-LABELS). |
| X-GM-EXT-1-labels-1 — Gmail labels are IMAP folders | MUST | rev2 | untestable | A description of Gmail's server-side mapping of its label model onto IMAP's mailbox/ folder model — this is server architecture, not a client action. No client behavior is mandated by this sentence in isolation; it is background context motivating the following testable duties (label modification via CREATE/RENAME/DELETE, retrieval via X-GM-LABELS). |
| X-GM-EXT-1-labels-2 — Labels modified via standard CREATE/RENAME/DELETE | MUST | rev1 | fail | unimplemented: Labels can be modified using the standard IMAP commands, `CREATE`, `RENAME`, and `DELETE`, that act on folders. |
| X-GM-EXT-1-labels-2 — Labels modified via standard CREATE/RENAME/DELETE | MUST | rev2 | fail | unimplemented: Labels can be modified using the standard IMAP commands, `CREATE`, `RENAME`, and `DELETE`, that act on folders. |
| X-GM-EXT-1-labels-3 — System labels are reserved and bracket-prefixed | MUST | rev1 | untestable | This describes a server-side naming convention/reservation applied to Gmail's own system-created labels (e.g. how they appear in the mailbox-listing hierarchy under '[Gmail]'/'[GoogleMail]'). There is no client duty here beyond passively observing whatever names the server sends — a client cannot violate a reservation rule that constrains what the server is allowed to name things, and any client action on these names (LIST/SELECT) is already covered by the general, already-cataloged mailbox-naming and LIST-response duties under RFC 3501/RFC 9051. Nothing X-GM-EXT-1-specific is asked of the client beyond that. |
| X-GM-EXT-1-labels-3 — System labels are reserved and bracket-prefixed | MUST | rev2 | untestable | This describes a server-side naming convention/reservation applied to Gmail's own system-created labels (e.g. how they appear in the mailbox-listing hierarchy under '[Gmail]'/'[GoogleMail]'). There is no client duty here beyond passively observing whatever names the server sends — a client cannot violate a reservation rule that constrains what the server is allowed to name things, and any client action on these names (LIST/SELECT) is already covered by the general, already-cataloged mailbox-naming and LIST-response duties under RFC 3501/RFC 9051. Nothing X-GM-EXT-1-specific is asked of the client beyond that. |
| X-GM-EXT-1-labels-4 — X-GM-LABELS retrieved via FETCH attribute | MAY | rev1 | fail | unimplemented: The labels for a given message may be retrieved by using the `X-GM-LABELS` attribute with the `FETCH` command. |
| X-GM-EXT-1-labels-4 — X-GM-LABELS retrieved via FETCH attribute | MAY | rev2 | fail | unimplemented: The labels for a given message may be retrieved by using the `X-GM-LABELS` attribute with the `FETCH` command. |
| X-GM-EXT-1-labels-5 — X-GM-LABELS response is a list of ASTRINGs, UTF-7 encoded as appropriate | MUST | rev1 | fail | unimplemented: The attribute is returned as a list of `ASTRING`s, encoded in UTF-7 as appropriate. |
| X-GM-EXT-1-labels-5 — X-GM-LABELS response is a list of ASTRINGs, UTF-7 encoded as appropriate | MUST | rev2 | fail | unimplemented: The attribute is returned as a list of `ASTRING`s, encoded in UTF-7 as appropriate. |
| X-GM-EXT-1-labels-6 — Labels added to a message via STORE +X-GM-LABELS | MAY | rev1 | fail | unimplemented: Labels may be added to a message using the `STORE` command in conjunction with the `X-GM-LABELS` attribute. |
| X-GM-EXT-1-labels-6 — Labels added to a message via STORE +X-GM-LABELS | MAY | rev2 | fail | unimplemented: Labels may be added to a message using the `STORE` command in conjunction with the `X-GM-LABELS` attribute. |
| X-GM-EXT-1-labels-7 — -X-GM-LABELS (remove) and X-GM-LABELS (replace) STORE forms are not documented with a worked example | MAY | rev1 | untestable | None of five independent WebFetch passes of the page (2026-07-04) surfaced a worked example, or even a sentence, documenting `-X-GM-LABELS` (remove) or bare `X-GM-LABELS` (replace) STORE forms, or a `.SILENT` variant — only the `+X-GM-LABELS` (add) form has a quoted sentence and worked example (X-GM-EXT-1-labels-6). Per this task's Step 2 rule ('do NOT fabricate a quote... use a documented paraphrase in notes'), no `text` is asserted for the remove/replace/SILENT forms beyond the same general 'STORE command in conjunction with the X-GM-LABELS attribute' sentence already used for the add form — this entry exists to record the gap honestly rather than to assert an unverified normative claim about remove/replace syntax. In practice Gmail's servers are widely known to accept the standard `-X-GM-LABELS`/bare-`X-GM-LABELS`/`.SILENT` STORE conventions (mirroring FLAGS), but that is outside what this specific page verifiably documents, so it is not asserted as a catalog fact with page-sourced text. This is flagged as an ambiguity in the final task report. |
| X-GM-EXT-1-labels-7 — -X-GM-LABELS (remove) and X-GM-LABELS (replace) STORE forms are not documented with a worked example | MAY | rev2 | untestable | None of five independent WebFetch passes of the page (2026-07-04) surfaced a worked example, or even a sentence, documenting `-X-GM-LABELS` (remove) or bare `X-GM-LABELS` (replace) STORE forms, or a `.SILENT` variant — only the `+X-GM-LABELS` (add) form has a quoted sentence and worked example (X-GM-EXT-1-labels-6). Per this task's Step 2 rule ('do NOT fabricate a quote... use a documented paraphrase in notes'), no `text` is asserted for the remove/replace/SILENT forms beyond the same general 'STORE command in conjunction with the X-GM-LABELS attribute' sentence already used for the add form — this entry exists to record the gap honestly rather than to assert an unverified normative claim about remove/replace syntax. In practice Gmail's servers are widely known to accept the standard `-X-GM-LABELS`/bare-`X-GM-LABELS`/`.SILENT` STORE conventions (mirroring FLAGS), but that is outside what this specific page verifiably documents, so it is not asserted as a catalog fact with page-sourced text. This is flagged as an ambiguity in the final task report. |
| X-GM-EXT-1-labels-8 — X-GM-LABELS usable as a SEARCH/UID SEARCH key | MAY | rev1 | fail | unimplemented: The `X-GM-LABELS` attribute may also be used in the `SEARCH` or `UID SEARCH` commands to find the sequence numbers or `UID`s of all messages in the folder with … |
| X-GM-EXT-1-labels-8 — X-GM-LABELS usable as a SEARCH/UID SEARCH key | MAY | rev2 | fail | unimplemented: The `X-GM-LABELS` attribute may also be used in the `SEARCH` or `UID SEARCH` commands to find the sequence numbers or `UID`s of all messages in the folder with … |
| X-GM-EXT-1-labels-9 — X-GM-LABELS valid only under the X-GM-EXT-1 capability | MUST | rev1 | untestable | Same reasoning as X-GM-EXT-1-msgid-5/thrid-5, restated for X-GM-LABELS: whether the client internally consulted CAPABILITY before using the X-GM-LABELS FETCH/STORE/SEARCH forms has no wire signature distinct from a client that simply happens to use it correctly against a real Gmail server; only server-side acceptance is observable, and that is not this client's duty to score. |
| X-GM-EXT-1-labels-9 — X-GM-LABELS valid only under the X-GM-EXT-1 capability | MUST | rev2 | untestable | Same reasoning as X-GM-EXT-1-msgid-5/thrid-5, restated for X-GM-LABELS: whether the client internally consulted CAPABILITY before using the X-GM-LABELS FETCH/STORE/SEARCH forms has no wire signature distinct from a client that simply happens to use it correctly against a real Gmail server; only server-side acceptance is observable, and that is not this client's duty to score. |
| X-GM-EXT-1-labels-10 — XLIST retrieves the entire list of labels for a mailbox | MAY | rev1 | untestable | XLIST is Gmail's own (non-standard, pre-dating and now superseded by RFC 6154 SPECIAL-USE) command for enumerating labels/mailboxes with special-use attributes attached; the page elsewhere documents XLIST is deprecated in favor of the standard LIST command combined with RFC 6154's SPECIAL-USE attributes (already covered by the existing RFC 6154 catalog, per this module's extractionNote's exclusion of the page's 'Special-Use Extension of the LIST command'/'XLIST is deprecated' sections). Whether a client chooses to call the deprecated XLIST command at all — versus the recommended LIST-based replacement — is now purely an internal client-implementation-strategy choice with no remaining spec-compliance consequence: both a client that still issues XLIST and one that has fully migrated to LIST-with-SPECIAL-USE can correctly retrieve the complete label set, and a black-box harness has no normative basis (post-deprecation) to prefer one over the other. This is why the entry is graded MAY/untestable/internal-decision rather than folded into the RFC 6154 catalog: the RFC 6154 SPECIAL-USE mechanism this sentence's XLIST is described elsewhere as superseded by is a distinct command surface (LIST with an attribute-matching extension) from XLIST itself, so RFC 6154 does not directly cover 'use XLIST' as a client action — only 'use LIST/SPECIAL-USE instead' — leaving this sentence without a scoring home until now. |
| X-GM-EXT-1-labels-10 — XLIST retrieves the entire list of labels for a mailbox | MAY | rev2 | untestable | XLIST is Gmail's own (non-standard, pre-dating and now superseded by RFC 6154 SPECIAL-USE) command for enumerating labels/mailboxes with special-use attributes attached; the page elsewhere documents XLIST is deprecated in favor of the standard LIST command combined with RFC 6154's SPECIAL-USE attributes (already covered by the existing RFC 6154 catalog, per this module's extractionNote's exclusion of the page's 'Special-Use Extension of the LIST command'/'XLIST is deprecated' sections). Whether a client chooses to call the deprecated XLIST command at all — versus the recommended LIST-based replacement — is now purely an internal client-implementation-strategy choice with no remaining spec-compliance consequence: both a client that still issues XLIST and one that has fully migrated to LIST-with-SPECIAL-USE can correctly retrieve the complete label set, and a black-box harness has no normative basis (post-deprecation) to prefer one over the other. This is why the entry is graded MAY/untestable/internal-decision rather than folded into the RFC 6154 catalog: the RFC 6154 SPECIAL-USE mechanism this sentence's XLIST is described elsewhere as superseded by is a distinct command surface (LIST with an attribute-matching extension) from XLIST itself, so RFC 6154 does not directly cover 'use XLIST' as a client action — only 'use LIST/SPECIAL-USE instead' — leaving this sentence without a scoring home until now. |
| X-GM-EXT-1-raw-1 — X-GM-RAW provides full Gmail search syntax passthrough | MUST | rev1 | fail | unimplemented: To provide access to the full Gmail search syntax, Gmail provides the `X-GM-RAW` search attribute. |
| X-GM-EXT-1-raw-1 — X-GM-RAW provides full Gmail search syntax passthrough | MUST | rev2 | fail | unimplemented: To provide access to the full Gmail search syntax, Gmail provides the `X-GM-RAW` search attribute. |
| X-GM-EXT-1-raw-2 — X-GM-RAW arguments interpreted as Gmail web-interface search syntax | MUST | rev1 | fail | unimplemented: Arguments passed along with the `X-GM-RAW` attribute when executing the `SEARCH` or `UID SEARCH` commands will be interpreted in the same manner as in the Gmail… |
| X-GM-EXT-1-raw-2 — X-GM-RAW arguments interpreted as Gmail web-interface search syntax | MUST | rev2 | fail | unimplemented: Arguments passed along with the `X-GM-RAW` attribute when executing the `SEARCH` or `UID SEARCH` commands will be interpreted in the same manner as in the Gmail… |
| RFC7889-2-1 — Client MUST distinguish the bare APPENDLIMIT capability form from the valued APPENDLIMIT=<number> form | MUST | rev1 | pass |  |
| RFC7889-2-1 — Client MUST distinguish the bare APPENDLIMIT capability form from the valued APPENDLIMIT=<number> form | MUST | rev2 | pass |  |
| RFC7889-3.1-1 — Client (implicit) MUST emit the APPENDLIMIT STATUS item to query a mailbox's upload limit | MUST | rev1 | fail | unimplemented: A new attribute APPENDLIMIT is added to get the limit set by the server for a mailbox as part of a STATUS command. An IMAP client should issue a STATUS command … |
| RFC7889-3.1-1 — Client (implicit) MUST emit the APPENDLIMIT STATUS item to query a mailbox's upload limit | MUST | rev2 | fail | unimplemented: A new attribute APPENDLIMIT is added to get the limit set by the server for a mailbox as part of a STATUS command. An IMAP client should issue a STATUS command … |
| RFC7889-3.2-1 — Client (implicit) MUST emit LIST ... RETURN (STATUS (APPENDLIMIT)) to batch-query mailbox limits | MUST | rev1 | fail | unimplemented: If the server advertises the LIST-STATUS capability [RFC5819], the client can issue a LIST command in combination with the STATUS return option to get the mailb… |
| RFC7889-3.2-1 — Client (implicit) MUST emit LIST ... RETURN (STATUS (APPENDLIMIT)) to batch-query mailbox limits | MUST | rev2 | fail | unimplemented: If the server advertises the LIST-STATUS capability [RFC5819], the client can issue a LIST command in combination with the STATUS return option to get the mailb… |
| RFC7889-3.2-2 — Client SHOULD fall back to plain STATUS when the server lacks the LIST STATUS return option | SHOULD | rev1 | fail | unimplemented: If the server does not support the STATUS return option on the LIST command, then the client should use the STATUS command instead. |
| RFC7889-3.2-2 — Client SHOULD fall back to plain STATUS when the server lacks the LIST STATUS return option | SHOULD | rev2 | fail | unimplemented: If the server does not support the STATUS return option on the LIST command, then the client should use the STATUS command instead. |
| RFC7889-4-1 — Client MUST accept a tagged NO [TOOBIG] as the well-formed rejection of an over-limit APPEND | MUST | rev1 | fail | unimplemented: If a client uploads a message that exceeds the maximum upload size set for that mailbox, then the server SHALL reject the APPEND command with a tagged TOOBIG re… |
| RFC7889-4-1 — Client MUST accept a tagged NO [TOOBIG] as the well-formed rejection of an over-limit APPEND | MUST | rev2 | fail | unimplemented: If a client uploads a message that exceeds the maximum upload size set for that mailbox, then the server SHALL reject the APPEND command with a tagged TOOBIG re… |
| RFC7889-4-2 — Client SHOULD avoid non-synchronizing literals when the maximum upload size is unknown | SHOULD | rev1 | fail | unimplemented: A client SHOULD avoid use of non-synchronizing literals [RFC7888] when the maximum upload size supported by the IMAP server is unknown. |
| RFC7889-4-2 — Client SHOULD avoid non-synchronizing literals when the maximum upload size is unknown | SHOULD | rev2 | fail | unimplemented: A client SHOULD avoid use of non-synchronizing literals [RFC7888] when the maximum upload size supported by the IMAP server is unknown. |
| RFC8438-3-1 — Client (implicit) MUST emit the SIZE STATUS item to request the mailbox's total octet size | MUST | rev1 | fail | unimplemented: This extension defines one new status data item for the STATUS command and response: SIZE The total size of the mailbox in octets. |
| RFC8438-3-1 — Client (implicit) MUST emit the SIZE STATUS item to request the mailbox's total octet size | MUST | rev2 | fail | unimplemented: This extension defines one new status data item for the STATUS command and response: SIZE The total size of the mailbox in octets. |
| RFC8438-3-2 — Client MUST be capable of receiving 63-bit SIZE data item values | MUST | rev1 | fail | unimplemented: Since the total storage size of a mailbox can easily exceed 4 GB, clients MUST be capable of receiving 63-bit SIZE data item values. The message size is chosen … |
| RFC8438-3-2 — Client MUST be capable of receiving 63-bit SIZE data item values | MUST | rev2 | fail | unimplemented: Since the total storage size of a mailbox can easily exceed 4 GB, clients MUST be capable of receiving 63-bit SIZE data item values. The message size is chosen … |
| RFC8438-3-3 — Client (implicit) MAY combine SIZE with the LIST-STATUS return option to batch-query mailbox sizes | MAY | rev1 | fail | unimplemented: When the LIST-STATUS IMAP capability [LIST-STATUS] is also available, the STATUS command can be combined with the LIST command to further improve efficiency. Th… |
| RFC8438-3-3 — Client (implicit) MAY combine SIZE with the LIST-STATUS return option to batch-query mailbox sizes | MAY | rev2 | fail | unimplemented: When the LIST-STATUS IMAP capability [LIST-STATUS] is also available, the STATUS command can be combined with the LIST command to further improve efficiency. Th… |
| RFC8440-3-1 — Client (implicit) MUST emit LIST ... RETURN (MYRIGHTS) to request interleaved rights information | MUST | rev1 | fail | unimplemented: This document extends the LIST command with a new "MYRIGHTS" return option [RFC5258] that allows the client to request all of the desired information in a singl… |
| RFC8440-3-1 — Client (implicit) MUST emit LIST ... RETURN (MYRIGHTS) to request interleaved rights information | MUST | rev2 | fail | unimplemented: This document extends the LIST command with a new "MYRIGHTS" return option [RFC5258] that allows the client to request all of the desired information in a singl… |
| RFC8440-3-2 — Client MUST NOT expect/require a MYRIGHTS response before the LIST response for the same mailbox | MUST NOT | rev1 | fail | unimplemented: The ordering of the responses is significant only in that the server MUST NOT send a MYRIGHTS response for a given mailbox before it sends the LIST response for… |
| RFC8440-3-2 — Client MUST NOT expect/require a MYRIGHTS response before the LIST response for the same mailbox | MUST NOT | rev2 | fail | unimplemented: The ordering of the responses is significant only in that the server MUST NOT send a MYRIGHTS response for a given mailbox before it sends the LIST response for… |
| RFC8440-3-3 — Client (implicit) MUST accept a LIST response with no accompanying MYRIGHTS response as well-formed | MUST | rev1 | fail | unimplemented: If the server is unable to look up the set of rights for a given mailbox, it does not send the MYRIGHTS reply for that mailbox. |
| RFC8440-3-3 — Client (implicit) MUST accept a LIST response with no accompanying MYRIGHTS response as well-formed | MUST | rev2 | fail | unimplemented: If the server is unable to look up the set of rights for a given mailbox, it does not send the MYRIGHTS reply for that mailbox. |
| RFC8440-6-1 — Client SHOULD scope LIST-MYRIGHTS requests with a narrow match pattern/selection option | SHOULD | rev1 | fail | unimplemented: Clients SHOULD use a suitable match pattern and/or selection option to limit the set of mailboxes returned to only those in whose rights they are interested. |
| RFC8440-6-1 — Client SHOULD scope LIST-MYRIGHTS requests with a narrow match pattern/selection option | SHOULD | rev2 | fail | unimplemented: Clients SHOULD use a suitable match pattern and/or selection option to limit the set of mailboxes returned to only those in whose rights they are interested. |
| RFC8970-3.1-1 — Client (implicit) MUST emit the PREVIEW FETCH attribute to request a preview | MUST | rev1 | fail | unimplemented: To retrieve a preview for a message, the PREVIEW FETCH attribute is used when issuing a FETCH command. |
| RFC8970-3.1-1 — Client (implicit) MUST emit the PREVIEW FETCH attribute to request a preview | MUST | rev2 | fail | unimplemented: To retrieve a preview for a message, the PREVIEW FETCH attribute is used when issuing a FETCH command. |
| RFC8970-3.2-1 — Client MUST accept the PREVIEW FETCH response as a string, and (when LAZY was used) MUST accept NIL as a valid, non-error response | MUST | rev1 | fail | unimplemented: If the LAZY modifier (Section 4.1) is used, the server MAY return NIL for the preview response, indicating that preview generation could not be completed withou… |
| RFC8970-3.2-1 — Client MUST accept the PREVIEW FETCH response as a string, and (when LAZY was used) MUST accept NIL as a valid, non-error response | MUST | rev2 | fail | unimplemented: If the LAZY modifier (Section 4.1) is used, the server MAY return NIL for the preview response, indicating that preview generation could not be completed withou… |
| RFC8970-3.2-2 — Client MUST treat a zero-length PREVIEW string as 'no preview available' and SHOULD NOT re-request it | MUST | rev1 | fail | unimplemented: In such cases, the server MUST return a zero-length string. Clients SHOULD NOT send another FETCH for a preview for such messages. |
| RFC8970-3.2-2 — Client MUST treat a zero-length PREVIEW string as 'no preview available' and SHOULD NOT re-request it | MUST | rev2 | fail | unimplemented: In such cases, the server MUST return a zero-length string. Clients SHOULD NOT send another FETCH for a preview for such messages. |
| RFC8970-3.3-1 — Client MUST treat PREVIEW text as unencoded UTF-8 text/plain data (not content-transfer-decoded) | MUST | rev1 | fail | unimplemented: The generated preview text MUST be treated as text/plain [RFC2046] media type data by the client. The generated string MUST NOT be content transfer encoded and … |
| RFC8970-3.3-1 — Client MUST treat PREVIEW text as unencoded UTF-8 text/plain data (not content-transfer-decoded) | MUST | rev2 | fail | unimplemented: The generated preview text MUST be treated as text/plain [RFC2046] media type data by the client. The generated string MUST NOT be content transfer encoded and … |
| RFC8970-4.1-1 — Client (implicit) MUST emit the LAZY priority modifier as 'PREVIEW (LAZY)' | MUST | rev1 | fail | unimplemented: The LAZY modifier directs the server to return the preview representation only if that data can be returned without undue delay to the client. |
| RFC8970-4.1-1 — Client (implicit) MUST emit the LAZY priority modifier as 'PREVIEW (LAZY)' | MUST | rev2 | fail | unimplemented: The LAZY modifier directs the server to return the preview representation only if that data can be returned without undue delay to the client. |
| RFC8970-4.2-1 — Client SHOULD NOT continually re-issue FETCH PREVIEW (LAZY) requests in a selected mailbox | SHOULD NOT | rev1 | fail | unimplemented: A client SHOULD NOT continually issue FETCH PREVIEW requests with the LAZY modifier in a selected mailbox as the server is under no requirement to return previe… |
| RFC8970-4.2-1 — Client SHOULD NOT continually re-issue FETCH PREVIEW (LAZY) requests in a selected mailbox | SHOULD NOT | rev2 | fail | unimplemented: A client SHOULD NOT continually issue FETCH PREVIEW requests with the LAZY modifier in a selected mailbox as the server is under no requirement to return previe… |
| RFC9585-4-1 — Client MUST accept the INPROGRESS resp-text-code in an untagged OK in every ABNF-permitted form (bare, and the tag/progress/goal tuple with independent NILs) | MUST | rev1 | pass |  |
| RFC9585-4-1 — Client MUST accept the INPROGRESS resp-text-code in an untagged OK in every ABNF-permitted form (bare, and the tag/progress/goal tuple with independent NILs) | MUST | rev2 | pass |  |
| RFC9585-4-2 — Client MUST assume PROGRESS = 0 and GOAL = unknown before the server's first notification for a command | MUST | rev1 | untestable | Explicit MUST, but it binds an internal default assumption the client holds before any INPROGRESS notification has arrived for a given command — there is no wire event to observe compliance against in that window, and the client today attaches no consumer logic to INPROGRESS values at all (they are inert parsed event data), so there is no internal 'assumed PROGRESS/GOAL' state to probe for correctness even in principle at this library's current architecture. A compliant and a non-compliant implementation (one that assumed something else, or nothing) are wire-indistinguishable: nothing the client sends or how it behaves before the first notification differs based on this internal assumption. Consistent with the catalog's established `internal-decision` verdict for 'do not depend on'/'assume X' duties with no separately-testable downstream wire effect (RFC3501-5.1.3-1 precedent). |
| RFC9585-4-2 — Client MUST assume PROGRESS = 0 and GOAL = unknown before the server's first notification for a command | MUST | rev2 | untestable | Explicit MUST, but it binds an internal default assumption the client holds before any INPROGRESS notification has arrived for a given command — there is no wire event to observe compliance against in that window, and the client today attaches no consumer logic to INPROGRESS values at all (they are inert parsed event data), so there is no internal 'assumed PROGRESS/GOAL' state to probe for correctness even in principle at this library's current architecture. A compliant and a non-compliant implementation (one that assumed something else, or nothing) are wire-indistinguishable: nothing the client sends or how it behaves before the first notification differs based on this internal assumption. Consistent with the catalog's established `internal-decision` verdict for 'do not depend on'/'assume X' duties with no separately-testable downstream wire effect (RFC3501-5.1.3-1 precedent). |
| RFC9585-4-3 — Client MUST be prepared to handle a server that cannot keep GOAL constant or PROGRESS monotonically increasing | MUST | rev1 | untestable | Explicit MUST, but 'be prepared to handle' has no wire-observable pass/fail boundary at this library's current architecture: a live probe (scripted connectLow exchange arming a PROGRESS regression, a mid-stream GOAL change, and a PROGRESS > GOAL notification, followed by a trailing EXISTS sentinel) confirmed the client's response stream survives all three without incident — but this is not evidence of spec-compliant 'graceful handling'; it is because the client attaches literally no consumer logic to a parsed INPROGRESS code today (it is inert data on a serverStatus event, per RFC9585-4-1's parse-acceptance duty), so nothing could throw or misbehave regardless of the values' internal consistency. Asserting 'the client doesn't crash' here would be a vacuous/confounded pass — identical between a hypothetical implementation that actually tracks progress and reacts badly to regressions and one (like today's) that does nothing with the values at all — which is exactly the known confounded-pass defect class this phase's extraction rules flag for avoidance. Recorded `internal-decision`: only once the client grows real progress-tracking/UI-update logic would this duty gain an observable divergent behavior to test. |
| RFC9585-4-3 — Client MUST be prepared to handle a server that cannot keep GOAL constant or PROGRESS monotonically increasing | MUST | rev2 | untestable | Explicit MUST, but 'be prepared to handle' has no wire-observable pass/fail boundary at this library's current architecture: a live probe (scripted connectLow exchange arming a PROGRESS regression, a mid-stream GOAL change, and a PROGRESS > GOAL notification, followed by a trailing EXISTS sentinel) confirmed the client's response stream survives all three without incident — but this is not evidence of spec-compliant 'graceful handling'; it is because the client attaches literally no consumer logic to a parsed INPROGRESS code today (it is inert data on a serverStatus event, per RFC9585-4-1's parse-acceptance duty), so nothing could throw or misbehave regardless of the values' internal consistency. Asserting 'the client doesn't crash' here would be a vacuous/confounded pass — identical between a hypothetical implementation that actually tracks progress and reacts badly to regressions and one (like today's) that does nothing with the values at all — which is exactly the known confounded-pass defect class this phase's extraction rules flag for avoidance. Recorded `internal-decision`: only once the client grows real progress-tracking/UI-update logic would this duty gain an observable divergent behavior to test. |
| RFC9585-4-4 — Client MUST NOT treat PROGRESS/GOAL as authoritative beyond progress evaluation, and MUST disregard notifications with exception-inducing values | MUST | rev1 | untestable | Two explicit MUST/MUST NOT sentences (§4's use-boundary MUST NOT and §6's disregard-on-exception-risk MUST) folded into one entry because both constrain the same internal-use boundary of the same parsed values, with no separate wire-observable half for either: 'not considering values authoritative for another use' and 'disregarding a notification' are both internal decisions about what the client does AFTER parsing a value that RFC9585-4-1 already scores as accepted — the client has no consumer logic today that uses GOAL/PROGRESS for anything (e.g. no code path derives a folder message count from GOAL, so there is nothing to catch violating this duty), and 'disregarding' vs. 'acting on' a pathological notification is not distinguishable on the wire either way when nothing consumes the value. A compliant and a non-compliant implementation produce identical wire traces (both simply continue processing the underlying long-running command normally) unless the non-compliant one also corrupts some separately-catalogued, testable behavior (e.g. reporting a wrong SEARCH result count) — which would be scored against that duty, not this one. The security consideration's own parenthetical confirms the boundary case (GOAL=0 etc.) is 'not possible within a correct implementation of the ABNF syntax above', i.e. this is a defensive guard against a misbehaving/malicious server sending non-ABNF-conforming octets — the client's disregard action itself has no wire footprint to assert against black-box. |
| RFC9585-4-4 — Client MUST NOT treat PROGRESS/GOAL as authoritative beyond progress evaluation, and MUST disregard notifications with exception-inducing values | MUST | rev2 | untestable | Two explicit MUST/MUST NOT sentences (§4's use-boundary MUST NOT and §6's disregard-on-exception-risk MUST) folded into one entry because both constrain the same internal-use boundary of the same parsed values, with no separate wire-observable half for either: 'not considering values authoritative for another use' and 'disregarding a notification' are both internal decisions about what the client does AFTER parsing a value that RFC9585-4-1 already scores as accepted — the client has no consumer logic today that uses GOAL/PROGRESS for anything (e.g. no code path derives a folder message count from GOAL, so there is nothing to catch violating this duty), and 'disregarding' vs. 'acting on' a pathological notification is not distinguishable on the wire either way when nothing consumes the value. A compliant and a non-compliant implementation produce identical wire traces (both simply continue processing the underlying long-running command normally) unless the non-compliant one also corrupts some separately-catalogued, testable behavior (e.g. reporting a wrong SEARCH result count) — which would be scored against that duty, not this one. The security consideration's own parenthetical confirms the boundary case (GOAL=0 etc.) is 'not possible within a correct implementation of the ABNF syntax above', i.e. this is a defensive guard against a misbehaving/malicious server sending non-ABNF-conforming octets — the client's disregard action itself has no wire footprint to assert against black-box. |
| RFC9585-4-5 — Client MAY disregard progress notifications entirely; deciding UI treatment is the client's own policy | MAY | rev1 | untestable | MAY-level permission with an explicit UI-policy delegation ('it is the client's duty to decide') — grants pure discretion with no constraining envelope, so disregarding, surfacing to a UI, or ignoring entirely are all equally compliant. This headless library has no UI of its own; even applying the catalog's instrumental logger-capture mechanism (used elsewhere to flip MUST-level 'present to the user' duties, e.g. RFC3501-7.1-1's ALERT text) would not help here, because there is no pass/fail boundary to observe even with a capture channel — MAY-level 'the client decides' duties are vacuous by requirement level, not by instrumentation, matching the established RFC3501-7.1.1-2/RFC9051-7.1.1-2 no-flip precedent for MAY-level presentation permissions. |
| RFC9585-4-5 — Client MAY disregard progress notifications entirely; deciding UI treatment is the client's own policy | MAY | rev2 | untestable | MAY-level permission with an explicit UI-policy delegation ('it is the client's duty to decide') — grants pure discretion with no constraining envelope, so disregarding, surfacing to a UI, or ignoring entirely are all equally compliant. This headless library has no UI of its own; even applying the catalog's instrumental logger-capture mechanism (used elsewhere to flip MUST-level 'present to the user' duties, e.g. RFC3501-7.1-1's ALERT text) would not help here, because there is no pass/fail boundary to observe even with a capture channel — MAY-level 'the client decides' duties are vacuous by requirement level, not by instrumentation, matching the established RFC3501-7.1.1-2/RFC9051-7.1.1-2 no-flip precedent for MAY-level presentation permissions. |
| RFC9585-5-1 — Client (implicit) MUST accept INPROGRESS and its embedded literals case-insensitively | MUST | rev1 | pass |  |
| RFC9585-5-1 — Client (implicit) MUST accept INPROGRESS and its embedded literals case-insensitively | MUST | rev2 | pass |  |
